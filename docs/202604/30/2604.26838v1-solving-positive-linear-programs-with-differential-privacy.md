---
title: Solving Positive Linear Programs with Differential Privacy
title_zh: 求解具有差分隐私的正线性规划
authors: "Alina Ene, Huy Le Nguyen, Ta Duy Nguyen, Adrian Vladu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26838v1"
tags: ["query:daily"]
score: 9.0
evidence: 线性规划的差分隐私近似算法
tldr: 本研究探讨了在差分隐私保护下求解正线性规划（包括装箱、覆盖及混合问题）的近似算法。针对约束隐私的高敏感度场景，即相邻实例可能存在任意约束差异的情况，本文提出了一套新型隐私求解器。该算法通过允许受控数量的约束违反来获得近似解，不仅改进了现有的实例相关保证，还引入了仅依赖于维度的全新数据独立界限。其核心技术是基于正则化对偶视角的密集乘法权重更新法，并针对正线性规划的结构进行了优化。
source: arxiv
selection_source: fresh_fetch
motivation: 在约束隐私的高敏感度场景下，如何在保护隐私的同时为正线性规划寻找近似最优解并有效控制约束违反数量。
method: 采用基于正则化对偶视角的密集乘法权重更新（MWU）方法，并利用正线性规划的特定结构进行算法分析。
result: 改进了现有的实例相关性能保证，并首次提出了仅取决于问题维度的全新数据独立界限。
conclusion: 该研究为处理具有隐私约束的大规模线性规划问题提供了更高效且理论完备的近似求解方案。
---

## 摘要
我们研究了针对正线性规划（具有非负系数和变量的线性规划）的差分隐私近似算法，重点关注装箱（packing）、覆盖（covering）以及混合装箱-覆盖（mixed packing-covering）公式等基础类别。我们关注 Hsu-Roth-Roughgarden-Ullman (ICALP 2014) 提出的高敏感度、约束隐私（constraint-private）机制，在这种机制下，相邻实例可能在任意单个约束上有所不同，因此无法指望在隐私保护下近似满足每一个约束。我们提供了隐私求解器，在返回近似解的同时，仅违反受控数量的约束。我们的算法改进了先前的实例依赖型（instance-dependent）保证，并产生了仅依赖于维度的全新数据独立型（data-independent）界限。我们的技术涉及一种从正则化对偶视角开发的密集乘法权重更新方法，并利用正线性规划特有的结构对其进行了分析。

## Abstract
We study differentially private approximation algorithms for positive linear programs (LPs with nonnegative coefficients and variables), focusing on the fundamental families of packing, covering, and mixed packing-covering formulations. We focus on the high-sensitivity, constraint-private regime of Hsu-Roth-Roughgarden-Ullman (ICALP 2014), where neighboring instances may differ by an arbitrary single constraint, so one cannot hope to approximately satisfy every constraint under privacy. We give private solvers that return approximate solutions while violating only a controlled number of constraints. Our algorithms improve the prior instance-dependent guarantees, and also yield new data-independent bounds that depend only on the dimension. Our techniques involve a dense multiplicative weights update method developed from a regularized dual viewpoint, which we analyze in a way that exploits structure specific to positive LPs.

---

## 论文详细总结（自动生成）

以下是对论文《Solving Positive Linear Programs with Differential Privacy》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：正线性规划（Positive LPs，如装箱、覆盖和混合装箱-覆盖问题）在机器学习和运筹学中至关重要，常用于资源分配、设施选址和公平分配等场景。这些场景往往涉及用户的敏感数据（如财务或健康信息）。
*   **核心挑战**：在差分隐私（DP）框架下，满足所有约束与保护隐私是互斥的（增加或删除一个约束可能导致可行解变为不可行）。因此，必须允许违反或删除少量约束。
*   **研究目标**：针对“约束隐私”（Constraint-private）这一最具挑战性的场景（相邻实例差异为一个约束），开发能够最小化违反约束数量 $s$ 的隐私近似算法，并提供更优的理论界限。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：采用**密集乘法权重更新（Dense MWU）**方法，并结合**正则化对偶视角**。通过引入**截断 Softmax（Truncated Softmax）**函数来控制对偶变量的权重上限，从而降低算法对单个约束变化的敏感度。
*   **关键技术细节**：
    *   **截断 Softmax 函数**：定义在单位单纯形上，且限制每个坐标的最大值不超过 $U$。这确保了任何单个约束对整体权重的影响受到严格限制。
    *   **敏感度控制**：通过调整截断阈值 $U$，算法能够平衡隐私保护（低敏感度）与优化精度（低约束违反率）。
*   **算法流程**（以装箱问题为例）：
    1.  **初始化**：解 $x=0$，设置迭代次数 $T$ 和步长 $\eta$。
    2.  **预处理（可选）**：对输入矩阵 $A$ 的大数值进行截断，以获得数据独立的界限。
    3.  **迭代更新**：在每一步中，调用隐私 Oracle（利用**指数机制**）选择一个坐标进行更新，使得加权约束违反最小。
    4.  **后处理（可选）**：对解进行微调（如截断极小值），以确保满足原始约束。

### 3. 实验设计
*   **研究性质**：本论文属于**纯理论算法研究**，侧重于数学证明和渐近复杂度分析。
*   **Benchmark（基准）**：
    *   **Hsu et al. (2014)**：早期的隐私 LP 框架，其约束违反界限与实例相关（如依赖于 $OPT^2$）。
    *   **Kaplan et al. (2024) & Ene et al. (2025)**：针对通用 LP 的高精度隐私求解器，但对维度 $d$ 的依赖度极高（如 $d^4$ 或 $d^9$）。
*   **对比方式**：通过数学推导对比在相同隐私预算 $(\epsilon, \delta)$ 下，违反约束数量 $s$ 的量级。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何 GPU/CPU 硬件型号、数量或训练时长。
*   **原因**：该研究不涉及大规模数值仿真或深度学习训练，其贡献在于算法的时间复杂度和隐私/效用权衡的理论界限证明。

### 5. 实验数量与充分性
*   **实验充分性**：由于缺乏实证实验（Empirical experiments），无法从数据分布的角度评价实验充分性。
*   **理论完备性**：论文对装箱（Packing）、覆盖（Covering）和混合（Mixed）三种主要的正 LP 类型均给出了详尽的引理证明和定理推导，涵盖了从隐私性证明到效用界限分析的全过程，在理论层面是充分且严谨的。

### 6. 主要结论与发现
*   **界限改进**：对于装箱/覆盖问题，违反约束的数量 $s$ 从先前的 $\tilde{O}(OPT^2)$ 改进为 $\tilde{O}(OPT^{1.5})$。
*   **数据独立界限**：首次为正 LP 提供了仅依赖于维度的界限（如装箱问题为 $\tilde{O}(d^{1.5})$），在 $OPT$ 较大时显著优于实例相关界限。
*   **混合 LP 突破**：为复杂的混合装箱-覆盖问题提供了首个具有多项式维度依赖的隐私算法界限（$\tilde{O}(d^3)$）。

### 7. 优点
*   **结构利用**：不同于通用 LP 求解器，该方法充分利用了正 LP 的非负性结构，显著降低了对维度 $d$ 的依赖。
*   **技术创新**：引入截断 Softmax 作为正则化项，为处理 DP 中的高敏感度问题提供了一种通用的分析工具。
*   **灵活性**：算法提供了“数据相关”和“数据独立”两种保证，适应不同的应用场景。

### 8. 不足与局限
*   **适用范围限制**：算法严格限定在“正线性规划”（系数非负），无法直接处理包含负系数的通用线性规划。
*   **缺乏实证验证**：理论界限中的常数项和对数项在实际小规模数据集上的表现尚不明确，缺乏真实场景下的性能评估。
*   **混合问题的复杂性**：对于混合 LP，算法对维度和精度的依赖（如 $1/\alpha^6$）仍然较高，在大规模高维场景下可能仍面临挑战。

（完）
