---
title: Estimating Power-Law Exponent with Edge Differential Privacy
title_zh: 基于边差分隐私的幂律指数估计
authors: "Adam Tan, Mohamed Hefny, Keval Vora"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20274v1"
tags: ["query:daily"]
score: 8.0
evidence: 用于图结构估计的边差分隐私
tldr: 本研究探讨了在边缘差分隐私（Edge-DP）保护下估计图数据幂律指数α的方法。针对传统方法发布噪声度分布导致的高失真问题，本文提出仅对估计α所需的低维充分统计量进行私有化处理。该方法支持离散近似和基于似然的数值优化，涵盖了集中式和本地差分隐私模型。实验表明，该方法在多种隐私预算和数据集下均能实现高效且准确的参数估计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20274v1/fig-001.webp\", \"caption\": \"Table 2: Graph datasets & their power-law scaling parameter 𝛼 .\", \"page\": 6, \"index\": 1, \"width\": 483, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.20274v1/fig-002.webp\", \"caption\": \"Table 7: Mean 𝑙1 (%) in local model, averaged over 20 runs. Bold marks the lowest mean within each dataset and 𝑑min block.\", \"page\": 10, \"index\": 2, \"width\": 410, \"height\": 435}, {\"url\": \"assets/figures/arxiv/2604.20274v1/fig-003.webp\", \"caption\": \"Table 8: Mean 𝑙1 error for 𝜀 with 𝑑min = 1 for Syn-power-1, Brightkite, and Ego-twitter. Bold marks the best method within the Central and Local blocks for each 𝜀 within each dataset. The Ego-twitter NO/LR entry at 𝜀 = 0.1 is marked – because all runs optimized to 𝛼 < 0; with the minimum 𝛼 clamped to 0, no valid estimates remained.\", \"page\": 10, \"index\": 3, \"width\": 489, \"height\": 465}]"
motivation: 传统的差分隐私图分析方法通过发布完整的噪声度分布来估计幂律指数，这会引入巨大的噪声和失真。
method: 提出了一种仅对估计幂律指数所需的低维充分统计量进行私有化的算法，并支持集中式和本地差分隐私模型。
result: 实验结果显示，与直接发布度分布的方法相比，基于统计量发布的方法在不同隐私预算下均表现出更高的估计精度。
conclusion: 通过私有化低维充分统计量而非完整分布，可以更有效地在保护边缘隐私的同时实现对图幂律特性的准确刻画。
---

## 摘要
许多现实世界的图具有可以用幂律很好地近似的度分布，相应的缩放参数 $α$ 提供了该结构的紧凑总结，对图分析和系统优化非常有用。当图中包含敏感的关系数据时，必须在不泄露单个边信息的情况下估计 $α$。本文研究了边差分隐私下的幂律指数估计。我们没有采用先发布带噪声的度分布再拟合幂律模型的方法，而是提出仅对估计 $α$ 所需的低维充分统计量进行隐私化处理，从而避免了传统方法引入的高失真。利用这些发布的统计量，我们支持离散近似和基于似然的数值优化，以实现高效的参数估计。我们为中心化和本地差分隐私模型开发了边差分隐私算法，在本地设置下比较了度发布和对数统计量发布，并在多种隐私预算和尾部截断设置下，在各种图数据集上评估了所得方法。

## Abstract
Many real-world graphs have degree distributions that are well approximated by a power-law, and the corresponding scaling parameter $α$ provides a compact summary of that structure which is useful for graph analysis and system optimization. When graphs contain sensitive relationship data, $α$ must be estimated without revealing information about individual edges. This paper studies power-law exponent estimation under edge differential privacy. Instead of first releasing a noisy degree distribution and then fitting a power-law model, we propose privatizing only the low-dimensional sufficient statistics needed to estimate $α$, thereby avoiding the high distortion introduced by traditional approaches. Using these released statistics, we support both discrete approximation and likelihood-based numerical optimization for efficient parameter estimation. We develop edge-DP algorithms for both centralized and local DP models, compare degree release and log-statistic release in the local setting, and evaluate the resulting methods on various graph datasets across multiple privacy budgets and tail-cutoff settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Estimating Power-Law Exponent with Edge Differential Privacy》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
现实世界中的许多图（如社交网络、万维网）呈现“无标度”特性，其节点度分布遵循**幂律分布**（$P(d) \propto d^{-\alpha}$）。缩放参数 $\alpha$（幂律指数）是理解图结构、优化系统性能的关键指标。然而，图数据往往包含敏感的边关系。
*   **核心挑战**：如何在满足**边差分隐私（Edge-DP）**的前提下准确估计 $\alpha$。
*   **研究动机**：传统方法通常先发布一个带噪声的完整度分布直方图，再进行拟合。这种做法在估计单一标量参数时效率极低，且由于直方图每个分桶都需加噪，会导致尾部数据严重失真，进而使 $\alpha$ 的估计产生巨大偏差。

### 2. 方法论：核心思想与关键技术
本文的核心思想是**直接私有化估计 $\alpha$ 所需的低维充分统计量**，而非整个分布。

*   **核心统计量**：基于最大似然估计（MLE），估计 $\alpha$ 所需的关键统计量为 $T_{disc} = \sum \ln(d_i / (d_{min} - 0.5))$ 和样本量 $N$。
*   **隐私化策略**：
    *   **敏感度分析**：证明了 $T_{disc}$ 的全局敏感度受限于 $O(\ln(\frac{d_{min}+1}{d_{min}}))$，且 $N$ 的敏感度为 2。这意味着加噪量相对较小。
    *   **拉普拉斯机制**：在统计量上添加拉普拉斯噪声，得到私有化的 $\tilde{T}_{disc}$ 和 $\tilde{N}$。
*   **估计模型**：
    *   **集中式模型（Centralized DP）**：受信任的服务器计算统计量并加噪发布。支持“离散近似”和“数值优化”两种估计器。
    *   **本地模型（Local DP, LEDP）**：节点不信任服务器。提出两种策略：**度发布（DR）**（节点发布加噪后的度）和**对数统计量发布（LR）**（节点发布加噪后的对数贡献值）。
*   **算法流程**：计算节点度 -> 提取尾部节点 -> 计算/加噪充分统计量 -> 通过闭式解或数值优化求解 $\alpha$。

### 3. 实验设计
*   **数据集**：使用了 6 个真实数据集（来自 SNAP，如 wiki, enron, twitter, gplus 等）和 3 个合成幂律图数据集（syn-power-0/1/2）。
*   **基准方法（Benchmark）**：对比了 **BASE** 方法（即 Hay 等人提出的基于私有化度分布直方图进行拟合的传统方法）。
*   **对比维度**：
    *   集中式 vs 本地化模型。
    *   离散近似 (DA) vs 数值优化 (NO)。
    *   本地模型下的度发布 (DR) vs 对数统计量发布 (LR)。
    *   不同的隐私预算 $\epsilon$（0.1 到 5.0）和不同的尾部阈值 $d_{min}$。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU/CPU 型号、内存大小）或具体的训练/运行耗时。
*   由于该算法主要涉及统计量计算和简单的数值优化（非深度学习），其计算开销通常较低，普通的商用 CPU 即可胜任。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个不同规模和特性的数据集，每组实验重复运行 20 次以获取均值和标准差。
*   **充分性**：实验设计较为充分。作者不仅验证了不同隐私模型下的表现，还深入探讨了隐私预算 $\epsilon$ 和超参数 $d_{min}$ 对结果的影响。
*   **公平性**：对比实验在相同的隐私预算下进行，且涵盖了真实与合成数据，评估指标（$l_1$ 误差）客观。

### 6. 主要结论与发现
*   **直接私有化统计量优于直方图拟合**：在集中式模型中，新方法（NO）的误差比基准方法（BASE）低 2-3 个数量级。
*   **数值优化更精准**：在所有设置下，基于数值优化的估计器（NO）普遍优于闭式离散近似（DA）。
*   **本地模型表现**：在 LEDP 模式下，**NO/DR（数值优化+度发布）** 通常是表现最强的变体。
*   **隐私预算影响**：集中式算法对 $\epsilon$ 的变化相对鲁棒，而本地化算法的精度随 $\epsilon$ 增加有显著提升。

### 7. 优点（亮点）
*   **效率与精度双赢**：通过降维（只处理充分统计量），极大地减少了注入的噪声总量。
*   **理论支撑扎实**：对关键统计量的全局敏感度进行了严谨的数学证明。
*   **灵活性**：同时提供了集中式和本地化两种隐私方案，适应不同的应用场景（如联邦学习或中心化数据库）。

### 8. 不足与局限
*   **对 $d_{max}$ 的依赖**：数值优化需要知道最大度 $d_{max}$，虽然文中讨论了其影响较小，但在极端隐私要求下，如何完全私有化 $d_{max}$ 仍需更多探讨。
*   **$d_{min}$ 的选择**：实验发现 $d_{min}$ 的选择对不同方法的影响不一致，缺乏一个自动选择最优 $d_{min}$ 的私有化准则。
*   **应用限制**：该方法假设度分布确实遵循幂律。如果图结构偏离幂律分布（如对数正态分布），该方法的鲁棒性尚未得到验证。

（完）
