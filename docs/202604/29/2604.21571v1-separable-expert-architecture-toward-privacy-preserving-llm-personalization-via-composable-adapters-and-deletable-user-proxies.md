---
title: "Separable Expert Architecture: Toward Privacy-Preserving LLM Personalization via Composable Adapters and Deletable User Proxies"
title_zh: 可分离专家架构：通过可组合适配器和可删除用户代理实现保护隐私的大语言模型个性化
authors: "Chris Schneider, Philipp Schoenegger, Ben Bariach"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21571v1"
tags: ["query:daily"]
score: 8.5
evidence: 通过将个人数据与共享权重解耦来实现保护隐私的大模型个性化
tldr: 针对大模型个性化中用户数据难以彻底删除的问题，本文提出一种可分离专家架构。该架构通过静态基础模型、领域专家LoRA适配器和可删除的用户代理实现三层解耦。用户数据仅存储在代理中，删除代理即可实现确定性遗忘，在提供个性化体验的同时，从根本上解决了隐私泄露和机器遗忘的难题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21571v1/fig-001.webp\", \"caption\": \"Figure 2: Distribution of unpersonalized-to-baseline KL-divergence scores across all prompt-user combinations for both base models (476 observations for Phi-3.5-mini, 490 for Llama-3.1-8B). Dashed lines mark the per-model mean. Verification uses a noise-calibrated per-query threshold (Equation 4) rather than a fixed cutoff, so no single threshold line is shown. The KL distribution is bimodal rather than gradual: verified observations cluster in [0.00, 0.30] and failures in [0.30, 0.94], with no ambiguous intermediate population. This sharp boundary is consistent with the structural guarantee, as proxy removal either fully eliminates user influence (the common case) or generation variance produces an outlier sample (the failure case), with no evidence of partial leakage.\", \"page\": 5, \"index\": 1, \"width\": 571, \"height\": 454}]"
motivation: 当前模型训练将用户信息直接融入共享权重，导致在不重新训练的情况下难以实现个人数据的彻底删除。
method: 采用三层架构，将静态基座模型、不含私有数据的领域适配器与存储个人信息的独立用户代理进行解耦。
result: 实验表明删除用户代理后模型可恢复至基准状态，且能有效防御模型逆向和成员推理等隐私攻击。
conclusion: 该方案将复杂的机器遗忘问题转化为简单的确定性删除操作，实现了兼顾隐私保护与高效个性化的模型架构。
---

## 摘要
现有的模型训练方法将用户信息直接整合到共享权重中，导致在不重新训练的情况下，从计算上无法移除个人数据。本文提出了一种三层架构，通过结合静态基础模型、在不传递用户数据的情况下塑造行为的可组合领域专家 LoRA 适配器，以及其删除即构成确定性遗忘的每用户代理伪影，实现了个人数据与共享权重的解耦。在 Phi-3.5-mini 和 Llama-3.1-8B 上的评估证实了每用户的差异化，即个人数据在保持隔离的同时影响输出，这通过代理移除后回归基准（KL 散度约为 0.21 nats，验证通过率为 82-89%）以及接近零的跨用户污染得到了验证。由于用户特定信息从未进入共享权重，该架构在设计上缓解了针对共享模型组件的模型反演、成员推理和训练数据提取攻击。该方法将机器遗忘从棘手的权重编辑问题转变为确定性的删除操作，在保留个性化的同时提供了增强隐私的保证，并与差分隐私随机梯度下降（DP-SGD）兼容，以实现保护隐私的共享模型改进。

## Abstract
Current model training approaches incorporate user information directly into shared weights, making individual data removal computationally infeasible without retraining. This paper presents a three-layer architecture that decouples personal data from shared weights by combining a static base model, composable domain-expert LoRA adapters that shape behavior without imparting user data, and per-user proxy artefacts whose deletion constitutes deterministic unlearning. Evaluation on Phi-3.5-mini and Llama-3.1-8B confirms per-user differentiation in which personal data influences outputs while remaining isolated, verified by a return to baseline after proxy removal (KL divergence of approximately 0.21 nats, 82-89% verification pass rate) and near-zero cross-user contamination. Because user-specific information never enters shared weights, the architecture mitigates model inversion, membership inference, and training-data extraction against shared model components by construction. The approach converts machine unlearning from an intractable weight-editing problem into a deterministic deletion operation that preserves personalization alongside privacy-enhancing guarantees and is compatible with differentially private stochastic gradient descent (DP-SGD) for privacy-preserving shared model improvement.

---

## 论文详细总结（自动生成）

### 论文总结：可分离专家架构 (SEA) —— 实现保护隐私的 LLM 个性化

#### 1. 核心问题与研究背景
*   **研究动机**：随着大语言模型（LLM）个性化需求的增长，如何平衡“个性化体验”与“数据删除权（遗忘权）”成为难题。
*   **核心矛盾**：传统的微调方法将用户信息直接融入模型共享权重中。一旦用户请求删除数据，由于权重耦合，除非重新训练整个模型，否则无法实现彻底的“机器遗忘”。此外，共享权重容易受到模型反演和成员推理等隐私攻击。
*   **整体含义**：本文旨在设计一种新架构，使个性化信息与共享模型在结构上完全解耦，将复杂的“权重编辑”问题转化为简单的“文件删除”操作。

#### 2. 方法论：可分离专家架构 (SEA)
*   **核心思想**：采用“组合式”而非“吸收式”的个性化方案。用户信息被隔离在独立的用户代理（User Proxy）中，推理时动态加载，删除时直接物理抹除。
*   **关键技术细节**：
    *   **基础层 (Base Layer)**：冻结的、共享的基座模型（如 Phi-3.5 或 Llama-3.1），不存储任何用户信息。
    *   **专家层 (Expert Layer)**：一组共享的领域专家 LoRA 适配器（如 Security, Code 等），通过轻量级路由器根据查询内容动态分配权重。
    *   **用户代理层 (User Proxy)**：每个用户独有的、可删除的目录（约 2-5 MB），包含三个组件：
        1.  **路由偏置 ($b_u$)**：根据用户历史偏好调整专家选择的概率分布。
        2.  **对比引导向量 ($s_u$)**：通过对比激活叠加（CAA）技术，在推理时注入残差流以调整输出风格（如简洁、专业）。
        3.  **个人 LoRA ($L_u$)**：极低秩（Rank-4）的适配器，通过 DPO 训练捕捉用户特有的知识模式。
*   **删除协议**：删除操作即为文件系统中的目录删除。通过计算“删除后模型”与“原始基准模型”之间的 **KL 散度**，并结合噪声校准阈值进行统计验证，确保模型行为已回归非个性化状态。

#### 3. 实验设计
*   **模型与场景**：在 Phi-3.5-mini (3.8B) 和 Llama-3.1-8B 上进行验证。
*   **数据集**：
    *   专家训练：使用 Security (OWASP-NVD)、Code (CodeAlpaca)、Data (Text-to-SQL) 等专业数据集。
    *   用户画像：构建了 4 个合成画像（安全专家、业余程序员、数据分析师、普通用户）。
*   **Benchmark**：以“基座模型 + 专家适配器（无用户代理）”作为非个性化基准。
*   **对比指标**：Jaccard 相似度（衡量输出差异）、风格匹配度、KL 散度（验证删除）、跨用户污染度。

#### 4. 资源与算力
*   **算力说明**：论文提到使用了 **QLoRA (4-bit NF4 量化)** 技术来降低显存占用并支持多适配器运行。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）、GPU 数量以及训练这些专家适配器和个人代理所需的总时长。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 **140 次评估运行**（每个模型 70 次），涵盖了 20 个评估提示词。
*   **充分性**：实验跨越了两个不同规模的模型（3.8B 和 8B），并对每个用户画像进行了多次重复实验以获取 95% 置信区间。
*   **客观性**：引入了噪声校准机制来设定 KL 验证阈值，考虑了随机采样带来的自然波动，实验设计较为严谨客观。

#### 6. 主要结论与发现
*   **个性化有效**：用户代理能显著改变模型行为，Phi-3.5 的风格匹配度（1.71）高于 Llama-3.1（0.63）。
*   **删除可验证**：删除代理后，模型输出与基准模型的 KL 散度保持在较低水平（~0.21 nats），验证通过率在 82% 到 89% 之间。
*   **零污染隔离**：跨用户污染极低（≤ 0.05），证明一个用户的偏好不会泄露给另一个用户。
*   **确定性遗忘**：该架构成功将机器遗忘从概率性的权重修改转变为确定性的物理删除。

#### 7. 优点与亮点
*   **结构化解耦**：从根本上解决了 LLM 个性化中的隐私合规问题，符合 GDPR 等数据删除权要求。
*   **低开销**：用户代理体积极小（几兆字节），便于大规模多租户部署。
*   **安全性**：由于用户信息从未进入共享权重，天然免疫针对共享模型的成员推理和数据提取攻击。

#### 8. 不足与局限
*   **个性化深度受限**：为了保证可分离性，个人 LoRA 采用了极低秩（Rank-4），其表达能力弱于全参数微调。
*   **合成数据偏差**：实验主要基于合成的用户画像，在真实复杂用户场景下的表现尚待验证。
*   **Proxy 安全风险**：虽然解决了权重泄露问题，但独立的 Proxy 文件成为了新的攻击目标，若 Proxy 被窃取，用户的个性化特征将完全暴露。
*   **验证通过率非 100%**：受随机解码影响，仍有部分案例无法通过 KL 验证，这可能在合规审计中引发争议。

（完）
