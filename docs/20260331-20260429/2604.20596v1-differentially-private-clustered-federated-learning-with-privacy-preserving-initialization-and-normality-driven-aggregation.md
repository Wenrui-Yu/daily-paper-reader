---
title: Differentially Private Clustered Federated Learning with Privacy-Preserving Initialization and Normality-Driven Aggregation
title_zh: 具有隐私保护初始化和正态性驱动聚合的差分隐私聚类联邦学习
authors: "Jie Xu, Haaris Mehmood, Rogier Van Dalen, Karthikeyan Saravanan, Mete Ozay"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20596v1"
tags: ["query:daily"]
score: 10.0
evidence: 用于隐私保护分布式训练的差分隐私聚类联邦学习
tldr: 针对联邦学习中数据异构性与隐私泄露的双重挑战，本文提出PINA框架。该框架结合了差分隐私（DP）与聚类联邦学习（CFL），通过两阶段策略解决DP噪声干扰聚类初始化的问题。第一阶段利用LoRA适配器和压缩草图构建鲁棒的聚类中心，第二阶段采用正态驱动聚合机制提升收敛性。实验表明，在保证隐私的前提下，该方法显著提升了异构数据的分类精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20596v1/fig-001.webp\", \"caption\": \"Fig. 2. Stage 2 of PINA with normality-based update scaling.\", \"page\": 3, \"index\": 1, \"width\": 472, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.20596v1/fig-002.webp\", \"caption\": \"Table 1. Results and comparisons on rotated CIFAR-10, rotated FMNIST and FEMNIST. We boldface the best accuracy.\", \"page\": 4, \"index\": 2, \"width\": 1042, \"height\": 219}]"
motivation: 现有的聚类联邦学习在引入差分隐私时，由于噪声干扰导致聚类中心初始化困难且模型收敛缓慢。
method: 提出PINA框架，通过轻量级LoRA适配器、压缩草图共享以及正态驱动的聚合机制来实现隐私保护下的高效聚类。
result: "在隐私预算为2到8的设置下，该方法比现有最先进的差分隐私联邦学习算法平均准确率提高了2.9%。"
conclusion: PINA在保护用户隐私的同时，有效缓解了跨设备联邦学习中的数据异构性问题，实现了更高的模型性能。
---

## 摘要
联邦学习 (FL) 能够在将原始数据保留在终端设备上的同时训练全局模型。尽管如此，FL 已被证明会泄露用户隐私信息，因此在实践中，它通常与差分隐私 (DP) 和安全向量求和等方法结合使用，以为参与者提供正式的隐私保证。在现实的跨设备部署中，数据具有高度异构性，因此原始联邦学习收敛缓慢且泛化能力差。聚类联邦学习 (CFL) 通过将用户划分为不同的簇来缓解这一问题，从而降低了簇内数据的异构性。然而，将 CFL 与 DP 结合仍然具有挑战性：注入的 DP 噪声使得单个客户端的更新噪声过大，且服务器无法利用噪声较小的聚合更新来初始化簇中心。为了应对这一挑战，我们提出了 PINA，这是一个两阶段框架，首先让每个客户端微调一个轻量级的低秩自适应 (LoRA) 适配器，并私密地共享更新的压缩草图 (sketch)。服务器利用这些草图构建鲁棒的簇中心。在第二阶段，PINA 引入了一种正态性驱动的聚合机制，提高了收敛性和鲁棒性。我们的方法在保留聚类 FL 优势的同时，针对不可信服务器提供了正式的隐私保证。广泛的评估表明，在隐私预算 (epsilon 为 {2, 8}) 下，我们提出的方法在准确率上平均优于最先进的 DP-FL 算法 2.9%。

## Abstract
Federated learning (FL) enables training of a global model while keeping raw data on end-devices. Despite this, FL has shown to leak private user information and thus in practice, it is often coupled with methods such as differential privacy (DP) and secure vector sum to provide formal privacy guarantees to its participants. In realistic cross-device deployments, the data are highly heterogeneous, so vanilla federated learning converges slowly and generalizes poorly. Clustered federated learning (CFL) mitigates this by segregating users into clusters, leading to lower intra-cluster data heterogeneity. Nevertheless, coupling CFL with DP remains challenging: the injected DP noise makes individual client updates excessively noisy, and the server is unable to initialize cluster centroids with the less noisy aggregated updates. To address this challenge, we propose PINA, a two-stage framework that first lets each client fine-tune a lightweight low-rank adaptation (LoRA) adapter and privately share a compressed sketch of the update. The server leverages these sketches to construct robust cluster centroids. In the second stage, PINA introduces a normality-driven aggregation mechanism that improves convergence and robustness. Our method retains the benefits of clustered FL while providing formal privacy guarantees against an untrusted server. Extensive evaluations show that our proposed method outperforms state-of-the-art DP-FL algorithms by an average of 2.9% in accuracy for privacy budgets (epsilon in {2, 8}).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PINA** 的新型聚类联邦学习框架，旨在同时解决联邦学习中的**数据异构性（Non-IID）**与**隐私保护（差分隐私）**之间的冲突。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：在跨设备联邦学习（FL）中，用户数据通常是高度异构的（Non-IID），导致单一全局模型收敛慢且性能差。聚类联邦学习（CFL）通过为不同数据分布的用户群体训练多个模型来解决此问题。
*   **核心挑战**：当引入差分隐私（DP）保护隐私时，CFL 面临两大难题：
    1.  **初始化困境**：服务器需要根据客户端更新来划分聚类，但在不可信服务器场景下，单个客户端的 DP 噪声极大，导致聚类识别失败。
    2.  **聚合效率**：在加密聚合（Secure Aggregation）环境下，服务器无法看到单个客户端的贡献，难以在噪声干扰下稳定地更新多个聚类模型。

### 2. 核心方法论
PINA 框架分为两个阶段，结合了参数高效微调（LoRA）和创新的聚合策略：

*   **第一阶段：隐私保护初始化（Privacy-Preserving Initialization）**
    *   **轻量化训练**：客户端仅微调 Transformer 模型最后一层注意力机制的 LoRA 适配器（秩 $r=1$），极大地减少了参数量。
    *   **压缩草图（Sketching）**：客户端仅选取更新中绝对值最大的 4 个值（2 正 2 负），应用本地差分隐私（LDP）后发送给服务器。
    *   **聚类构建**：服务器利用这些极度稀疏但保留了关键特征的“草图”进行 K-means 聚类，初始化各簇的中心模型。
*   **第二阶段：正态性驱动聚合（Normality-Driven Aggregation）**
    *   **簇识别**：客户端根据本地损失函数选择最匹配的簇模型进行训练。
    *   **正态性缩放（Normality-based Scaling）**：服务器利用 **Shapiro-Wilk 检验**统计量来评估聚合更新的“正态性”。由于未被选中的簇模型在聚合后主要包含高斯噪声，其正态性较高；而包含真实更新的簇模型正态性较低。
    *   **自适应调整**：根据正态性得分动态缩放更新步长，恢复被稀释的更新幅度，从而在不破坏隐私的前提下提高收敛速度。

### 3. 实验设计
*   **数据集**：
    *   **Rotated CIFAR-10**（2 个簇，通过旋转 0° 和 180° 构造）。
    *   **Rotated FMNIST**（4 个簇，通过旋转 0°/90°/180°/270° 构造）。
    *   **FEMNIST**（自然非独立同分布数据集，2 个簇）。
*   **基准方法（Benchmarks）**：
    *   基础 FL 算法：FedAvg, FedProx, FedNova。
    *   聚类 FL 算法：IFCA（最先进的聚类 FL 框架）。
*   **隐私设置**：对比了无隐私（$\epsilon = \infty$）以及严格隐私预算（$\epsilon \in \{2, 8\}$）下的表现。

### 4. 资源与算力
*   **模型规模**：使用了拥有 2200 万参数的 **ViT-Small** 预训练模型（ImageNet-21k 预训练）。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。但提到模拟了 10,000 个客户端的队列规模，并使用了 Hugging Face 的公开模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集，每组实验均在不同的隐私预算下进行，并提供了标准差（平均 3 次运行）。
*   **消融实验**：专门对比了 PINA 与随机初始化（Random Initialization）以及 LDP-IFCA 的聚类准确率随回合数的变化。
*   **评价**：实验设计较为客观，特别是在自然 Non-IID 数据集（FEMNIST）上的验证增强了说服力。但在簇数量较多（如 $C > 4$）或簇分布极度不均的情况下的表现尚待进一步验证。

### 6. 主要结论与发现
*   **性能提升**：在隐私保护设置下，PINA 的准确率比现有最先进的 DP-FL 算法平均高出 **2.9%**。
*   **聚类鲁棒性**：即使在 $\epsilon=2$ 的高噪声环境下，PINA 也能快速且准确地识别出潜在的聚类结构，而传统的 IFCA 在此环境下几乎失效。
*   **收敛速度**：通过正态性驱动的缩放机制，PINA 显著加快了模型在训练初期的收敛速度。

### 7. 优点
*   **无需辅助数据**：与需要服务器侧代理数据的 CFL 方法不同，PINA 完全依赖客户端的隐私更新。
*   **隐私与效能平衡**：巧妙利用 LoRA 降低参数维度，并结合 LDP 草图解决了初始化难题。
*   **抗干扰能力强**：正态性驱动聚合有效地从噪声背景中提取了有效的模型更新信号。

### 8. 不足与局限
*   **簇数量预设**：算法假设聚类数量 $C$ 是预先已知的，在实际应用中，确定最优的簇数量本身就是一个难题。
*   **计算开销**：虽然 LoRA 减少了通信量，但客户端在第二阶段需要下载所有簇的模型进行损失计算（簇识别），这会增加下行链路的带宽压力。
*   **算力细节缺失**：缺乏训练资源消耗的具体数据，难以评估其在极低功耗边缘设备上的可行性。

（完）
