---
title: "Fed-DLoRA: Efficient Wireless Federated Learning with Dynamic Low-Rank Adaptation"
title_zh: Fed-DLoRA：基于动态低秩自适应的高效无线联邦学习
authors: "Huaicheng Li, Junhui Zhao, Haoyu Quan, Xiaoming Wang"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24103v1"
tags: ["query:daily"]
score: 9.0
evidence: 具有动态低秩自适应的高效无线联邦学习
tldr: 针对车联网中联邦学习面临的通信开销大和环境动态变化等挑战，本文提出了一种名为Fed-DLoRA的轻量级算法。该方法结合低秩自适应（LoRA）技术减少参数量，并通过奇异值分解进行收敛性分析。此外，提出ARBVS算法动态优化秩选择、带宽分配和车辆筛选，在保证模型精度的同时显著提升了通信效率和收敛速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24103v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 816, \"height\": 667}]"
motivation: 旨在解决车联网环境下联邦学习的高通信开销以及模型压缩率与车辆参与策略之间的权衡问题。
method: 提出结合LoRA的Fed-DLoRA算法，并利用ARBVS算法在每轮通信中自适应调整秩大小、带宽和车辆选择。
result: 实验结果显示，Fed-DLoRA在模型准确率、收敛速度和通信效率方面均优于传统的联邦学习方法。
conclusion: 该研究证明了动态低秩自适应在提升无线联邦学习系统性能方面的有效性，为资源受限的分布式学习提供了新思路。
---

## 摘要
联邦学习（FL）为车联网（IoV）应用提供了一种极具前景的分布式学习范式。然而，它面临着通信开销和动态环境带来的挑战。模型压缩技术虽然减轻了计算和通信负担，但在压缩率与车辆参与策略之间产生了权衡。在本文中，我们提出了一种名为动态低秩自适应联邦学习（Fed-DLoRA）的轻量级联邦学习算法，该算法结合了低秩自适应（LoRA），在提高训练效率的同时，有效减少了参数量和通信成本。通过结合奇异值分解的随机梯度下降优化，对 Fed-DLoRA 进行了收敛性分析。该分析建立了 LoRA 秩、车辆调度策略与模型收敛特性之间的理论关系。基于这些见解，我们制定了一个旨在最大化系统性能的联合优化问题。为了解决这一问题，我们提出了一种自适应秩、带宽和车辆选择（ARBVS）算法，该算法集成了枚举与贪婪优化策略。该算法为每个联邦学习通信轮次提供高效的秩选择和资源调度策略，从而实现了联邦学习系统性能的有效提升。实验结果表明，与传统的联邦学习方法相比，Fed-DLoRA 具有更优越的性能，表现出更高的准确性、更快的收敛速度以及更高的通信效率。

## Abstract
Federated learning (FL) offers a promising distributed learning paradigm for internet of vehicles (IoV) applications. However, it faces challenges from communication overhead and dynamic environments. Model compression techniques reduce computing and communication burden yet create trade-offs between compression ratios and vehicle participation strategies. In this paper, we propose a lightweight FL algorithm named federated learning with dynamic low-rank adaptation (Fed-DLoRA), which is combined with low-rank adaptation (LoRA) to effectively reduce parameters and communication costs while enhancing training efficiency. The convergence analysis of Fed-DLoRA is conducted through stochastic gradient descent optimization coupled with singular value decomposition. This analysis establishes the theoretical relationships among LoRA rank, vehicular scheduling strategies and the model's convergence characteristics. Building on these insights, we formulate a joint optimization problem aimed at maximizing system performance. To address this problem, we propose an adaptive rank, bandwidth and vehicle selection (ARBVS) algorithm that integrates enumeration with greedy optimization strategies. The algorithm provides efficient rank selection and resource scheduling strategies for each FL communication round, thereby achieving effective performance improvements for the FL system. Experimental results demonstrate that Fed-DLoRA achieves superior performance compared to conventional federated learning approaches, exhibiting enhanced accuracy, faster convergence, and improved communication efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fed-DLoRA: Efficient Wireless Federated Learning with Dynamic Low-Rank Adaptation》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**车联网（IoV）环境下的联邦学习（FL）优化问题**。
*   **研究动机**：在智能交通系统中，车辆需要通过联邦学习协同训练模型以保护隐私。然而，车联网面临两大挑战：一是**通信开销巨大**，频繁交换大规模模型参数会耗尽带宽；二是**环境高度动态**，车辆的高速移动导致其在基站覆盖范围内的停留时间有限，且计算/通信资源随时间剧烈波动。
*   **核心问题**：如何在资源受限且拓扑动态变化的车联网中，通过模型压缩技术（如 LoRA）降低开销，并动态地协调“模型压缩率（秩）”、“带宽分配”与“车辆参与策略”之间的权衡，以实现最优的收敛性能。

### 2. 方法论
论文提出了 **Fed-DLoRA** 框架及其配套的调度算法：
*   **核心思想**：将低秩自适应（LoRA）集成到联邦学习中。在本地训练时，冻结原始模型权重 $W_0$，仅训练并上传两个低秩矩阵 $A$ 和 $B$。这极大地减少了需要传输的参数量。
*   **收敛性分析（关键技术细节）**：
    *   利用**奇异值分解（SVD）**来量化 LoRA 引入的低秩梯度与原始全秩梯度之间的差距。
    *   推导出全局梯度的上界，证明了收敛性能与 LoRA 的秩（Rank）、参与车辆的数量以及调度策略直接相关。
*   **ARBVS 算法（自适应秩、带宽与车辆选择）**：
    *   **问题建模**：构建了一个非凸混合整数非线性规划问题，目标是在满足车辆停留时间约束（由车速和位置决定）的前提下，最小化收敛误差。
    *   **求解策略**：采用“枚举+贪婪”的混合策略。首先枚举可能的 LoRA 秩 $r$，在固定 $r$ 的情况下，通过二分法计算每辆车所需的最小带宽，最后利用贪婪策略优先选择带宽需求低的车辆，以最大化参与训练的车辆总数。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **CIFAR-10** 和 **CIFAR-100** 数据集。
    *   设置了 **IID（独立同分布）** 和 **Non-IID（非独立同分布）** 两种数据分布。
    *   模拟单基站 C-V2X 场景，车辆速度在 12-22 m/s 之间随机分布，覆盖半径 500 米。
*   **Benchmark（对比方法）**：
    *   **FedAvg**：传统的全参数聚合算法。
    *   **FedPT**：部分参数冻结（冻结 ResNet 的部分残差块）。
    *   **FedRA**：一种随机分配 LoRA 掩码的联邦微调策略。
*   **基础模型**：采用缩减版的 **ResNet18**（通道基宽为 32，约 275 万参数）。

### 4. 资源与算力
论文明确列出了实验环境：
*   **硬件**：NVIDIA RTX 4090 GPU (24GB), Intel 14900KF CPU。
*   **软件**：Ubuntu 20.04, Python 3.10, PyTorch 2.3.0。
*   **训练细节**：每轮通信包含 4 个本地 Epoch，Batch Size 为 32，学习率为 0.01，总计运行 100 轮通信。

### 5. 实验数量与充分性
*   **实验组数**：论文进行了多维度的对比，包括准确率随通信轮次的变化、达到目标准确率所需的时间、累计通信成本对比，以及针对 ARBVS 算法的消融实验。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅对比了最终的准确率，还深入分析了通信效率（节省了高达 77% 的上行流量）和时延。通过 Non-IID 实验验证了算法在极端数据异构下的鲁棒性。消融实验部分清晰地展示了“秩”和“车辆数”对结果的独立影响，逻辑严密。

### 6. 主要结论与发现
*   **性能提升**：Fed-DLoRA 在所有设置下均优于基准方法。在 CIFAR-10 IID 场景下，20 轮时的准确率比 FedAvg 高出约 15%。
*   **通信与时延优势**：相比 FedAvg，达到 50% 准确率时，Fed-DLoRA 节省了约 77.5% 的通信开销，并缩短了约 39.4% 的总训练时间。
*   **动态调度的必要性**：实验证明，自适应地选择秩和车辆（ARBVS）比固定参数或随机选择更能适应车联网的带宽限制，从而通过增加参与车辆数来弥补低秩表示带来的精度损失。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了算法，还通过 SVD 提供了严谨的收敛性证明，为 LoRA 在 FL 中的应用提供了理论支撑。
*   **针对性强**：充分考虑了车联网的物理特性（如车辆停留时间 $T_{st}$、多普勒效应导致的信道变化等），而非通用的分布式学习。
*   **高效性**：ARBVS 算法复杂度较低（接近线性），适合在基站侧进行实时调度决策。

### 8. 不足与局限
*   **模型规模有限**：实验主要基于 ResNet18 和 CIFAR 数据集。虽然论文提到未来将扩展到大语言模型（LLM），但目前尚未在大规模参数模型上验证其动态秩调整的稳定性。
*   **预训练依赖性**：LoRA 通常用于微调预训练模型，而本实验是从头训练（Random Initialization）。虽然结果良好，但在实际应用中，从头训练低秩模块可能会限制模型的表达上限。
*   **场景单一**：仅考虑了单基站场景，未涉及多基站切换（Handover）或 V2V 协作对联邦学习的影响。

（完）
