---
title: Accelerating Optimization and Machine Learning through Decentralization
title_zh: 通过去中心化加速优化和机器学习
authors: "Ziqin Chen, Zuang Wang, Yongqiang Wang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19518v1"
tags: ["query:daily"]
score: 10.0
evidence: 去中心化优化通过将数据保留在本地设备上来增强隐私
tldr: 本研究探讨了去中心化优化在机器学习中的潜力，挑战了其仅作为隐私保护妥协方案的传统观点。通过逻辑回归和神经网络实验，研究发现去中心化不仅能保护数据隐私，还能在迭代次数上超越中心化方法，实现更快的收敛速度。这一发现证明了去中心化在提升优化效率方面的战略优势，为高效机器学习提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19518v1/fig-001.webp\", \"caption\": \"Figure 2: Comparison of Algorithm 1 with its centralized counterpart (labeled GD) using the W8A dataset. (a) and (c) compare the convergence performance between the centralized approach (labeled GD) and our proposed decentralized method under three partitioning schemes for the entire data, labeled Algorithm 1 (by labels), Algorithm 1 (by norms), and Algorithm 1 (by eigenvalues), respectively. The results show that in all data partitioning schemes, using decentralization substantially reduces the number of iterations required to reach a certain accuracy level. (b) plots the resulting smoothness constants under the three partitioning schemes, respectively, where on the x-axis, “C” represents the smoothness constant of the entire dataset (used in the centralized case), and “D1” and “D2” represent the smoothness constants for device 1 and device 2, respectively.\", \"page\": 7, \"index\": 1, \"width\": 766, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-002.webp\", \"caption\": \"Figure 9: Comparison of Algorithm 1 with FedOpt variants including FedAdam, FedYogi, and FedAdagrad from [52], as well as FedAvgM from [53]. In this experiment, all algorithms use fullbatch gradient computation, meaning that each device computes gradients using all the data allocated to it.\", \"page\": 18, \"index\": 2, \"width\": 803, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-003.webp\", \"caption\": \"Figure 10: Algorithm 1 with each agent having a participation probability of p ∈ {0.7, 0.8, 0.9, 1.0} at each communication round on the MNIST dataset. In this experiment, all algorithms employ full-batch gradient computation, meaning that each device computes gradients using all locally allocated data.\", \"page\": 18, \"index\": 3, \"width\": 803, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-004.webp\", \"caption\": \"Figure 3: Comparison of Algorithm 1 with its centralized counterpart (labeled GD) using the CIFAR-10 dataset. In this experiment, both algorithms use mini-batch gradient computation: in each iteration, the centralized approach processes ten randomly selected samples, while each device in the decentralized approach computes gradients using one sample from the data allocated to it (so both approaches process the same number of ten samples in each iteration). (a) shows that the decentralized approach achieves faster convergence than the centralized counterpart in terms of training loss, training accuracy, and test accuracy. (b) compares the training loss over eight runs with different estimated smoothness constants and random initializations. (c) illustrates the estimated smoothness constants alongside the training loss trajectories of three selected runs from the eight. On the x-axis representing the device index, the label “C” denotes the centralized case, and “D1” through “D10” represent the devices in the decentralized case.\", \"page\": 8, \"index\": 4, \"width\": 813, \"height\": 585}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-005.webp\", \"caption\": \"Figure 1: Performance comparison of Algorithm 1 with its centralized counterpart (GD) using PEP based theoretical analysis. (a) shows the acceleration of Algorithm 1 (results under three switch time instants are given, represented by three red curves with varying shades) over its centralized counterpart (termed GD and represented by the blue curve). The centralized approach uses a step size 1 L with L = 1 N ∑N i=1 Li. The gray curve represents the performance of the conventional decentralized gradient descent (termed DGD) algorithm with local step sizes, i.e., agent i using a step size 1 Li , which is subject to a steady-state error. In this subplot, we consider two agents with heterogeneous local smoothness constants L1 = 1 3 and L2 = 3. (b) demonstrates that a greater heterogeneity between L1 and L2 leads to more acceleration via decentralization. The y-axis represents the ratio of optimization errors obtained by PEP under Algorithm 1 and GD. A ratio less than 1 indicates Algorithm 1 gaining acceleration over its centralized counterpart. (c) considers the more than twoagent setting with half of the agents having a smoothness constant of 1 3 , whereas the other half have a smoothness constant of 3. The strongly convex parameter is set to µ = 0.1.\", \"page\": 4, \"index\": 5, \"width\": 853, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-006.webp\", \"caption\": \"Figure 8: Comparison of Algorithm 1 with decentralized GD in [14] and gradient tracking in [51] using the MNIST dataset. In this experiment, all algorithms use full-batch gradient computation, meaning that each device computes gradients using all the data allocated to it.\", \"page\": 17, \"index\": 6, \"width\": 814, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-007.webp\", \"caption\": \"Figure 7: Training losses of Algorithm 1 and its centralized counterpart (labeled GD) for different network sizes N,N ∈ {2, 5, 15, 25} on the MNIST dataset.\", \"page\": 17, \"index\": 7, \"width\": 794, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-008.webp\", \"caption\": \"Figure 4: Comparison of Algorithm 1 with its centralized counterpart (labeled GD) on the SST-2 dataset. In this experiment, both algorithms use full-batch gradient computation: the centralized approach processes the entire dataset, while in our decentralization based approach, each device computes gradients using all the data allocated to it. (a)-(c) show that our approach converges faster than the centralized counterpart.\", \"page\": 9, \"index\": 8, \"width\": 814, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-009.webp\", \"caption\": \"Figure 5: Comparison of Algorithm 1 with its centralized counterpart (labeled GD) using the MNIST dataset. In this experiment, both algorithms use full-batch gradient computation: the centralized approach processes the entire dataset, while in the decentralized approach, each device computes gradients using all the data allocated to it. (a) shows that the decentralized approach achieves faster convergence than the centralized counterpart in terms of training loss, training accuracy, and test accuracy. (b) compares the training loss over eight runs with different estimated smoothness constants and random initializations. (c) illustrates the estimated smoothness constants alongside the training loss trajectories of three selected runs from the eight. On the x-axis representing the device index, the label “C” denotes the centralized case, and “D1” through “D5” represent the devices in the decentralized case.\", \"page\": 16, \"index\": 9, \"width\": 811, \"height\": 585}, {\"url\": \"assets/figures/arxiv/2604.19518v1/fig-010.webp\", \"caption\": \"Figure 6: Training losses of Algorithm 1 and its centralized counterpart (labeled GD) for heterogeneous data distributions under different Dirichlet parameters α ∈ {0.1, 0.5, 1, 5} on the MNIST dataset.\", \"page\": 16, \"index\": 10, \"width\": 794, \"height\": 207}]"
motivation: 传统观点认为去中心化优化是因通信或隐私限制而做出的性能妥协，本研究旨在验证其是否具有加速学习的潜在优势。
method: 通过在多个代理之间分布数据和计算，对比去中心化方法与中心化方法在逻辑回归和神经网络训练中的收敛表现。
result: 实验结果表明，去中心化方法在达到最优解所需的迭代次数上优于中心化方法，展现出更快的收敛特性。
conclusion: 去中心化不仅是隐私保护的手段，更是一种能够显著提升优化和机器学习效率的战略性优势。
---

## 摘要
去中心化优化使多个设备能够学习全局机器学习模型，而每个设备仅能访问其本地数据集。通过避免训练数据离开个人用户的设备，与传统的中心化学习（所有数据必须汇总到中心服务器）相比，它增强了隐私性和可扩展性。然而，去中心化优化传统上被视为一种必要的妥协，仅在由于通信限制或数据隐私问题导致中心化处理不可行时才使用。在本研究中，我们展示了去中心化反而可以加速收敛，在达到最优解所需的迭代次数上优于中心化方法。通过逻辑回归和神经网络训练的示例，我们证明了在多个代理之间分配数据和计算可以实现比中心化方法更快的学习，即使假设每次迭代所需的时间相同（无论是在完整数据集上进行中心化处理，还是在本地子集上进行去中心化处理）。这一发现挑战了长期以来的假设，并揭示了去中心化作为一种战略优势，为更高效的优化和机器学习提供了新机遇。

## Abstract
Decentralized optimization enables multiple devices to learn a global machine learning model while each individual device only has access to its local dataset. By avoiding the need for training data to leave individual users' devices, it enhances privacy and scalability compared to conventional centralized learning, where all data has to be aggregated to a central server. However, decentralized optimization has traditionally been viewed as a necessary compromise, used only when centralized processing is impractical due to communication constraints or data privacy concerns. In this study, we show that decentralization can paradoxically accelerate convergence, outperforming centralized methods in the number of iterations needed to reach optimal solutions. Through examples in logistic regression and neural network training, we demonstrate that distributing data and computation across multiple agents can lead to faster learning than centralized approaches, even when each iteration is assumed to take the same amount of time, whether performed centrally on the full dataset or decentrally on local subsets. This finding challenges longstanding assumptions and reveals decentralization as a strategic advantage, offering new opportunities for more efficient optimization and machine learning.

---

## 论文详细总结（自动生成）

# 论文总结：通过去中心化加速优化和机器学习

### 1. 核心问题与整体含义
*   **研究背景**：传统的机器学习模型训练通常采用中心化模式，即将所有数据汇总到单一服务器。虽然去中心化优化（如联邦学习、共识优化）在隐私保护和可扩展性方面具有优势，但长期以来被学术界视为一种“性能妥协”——即在无法汇总数据时的次优选择。
*   **核心问题**：去中心化是否只能是性能的牺牲品？
*   **研究动机**：作者挑战了“中心化处理总是最快”的假设，试图证明在数据分布不均（异质性）的情况下，去中心化架构可以通过利用本地数据的几何特性，在**迭代次数**上实现比中心化方法更快的收敛速度。

### 2. 方法论
*   **核心思想**：利用不同设备上数据的**局部平滑性（Local Smoothness）**。在中心化设置中，步长受限于全局最差的平滑常数；而在去中心化设置中，每个设备可以根据其本地数据的平滑常数 $L_i$ 采用定制化的异质步长 $\alpha_i = 1/L_i$。
*   **关键技术细节**：
    *   **异质步长策略**：为每个代理分配与其本地函数特性匹配的步长，从而在初期实现快速下降。
    *   **动态切换机制（Algorithm 1）**：由于异质步长会导致收敛到偏离全局最优解的点，算法引入了一个切换阈值 $\epsilon$。当梯度范数降至阈值以下时，所有设备切换回统一的全局步长，以确保精确收敛到全局最优解。
    *   **理论证明工具**：采用了**性能估计问题（Performance Estimation Problem, PEP）**框架。通过将算法的最坏情况分析转化为半正定规划（SDP）问题，给出了数学上严谨的、非渐近的精确性能界限。

### 3. 实验设计
*   **数据集与场景**：
    *   **W8A**：逻辑回归任务，用于 Web 分类。
    *   **CIFAR-10**：卷积神经网络（CNN）图像分类。
    *   **SST-2**：基于 BERT 的自然语言处理情感分析微调。
    *   **MNIST**：手写数字识别，用于消融实验。
*   **Benchmark（基准）**：中心化梯度下降（Centralized GD）。
*   **对比方法**：
    *   传统去中心化梯度下降（DGD）。
    *   梯度追踪算法（Gradient Tracking）。
    *   联邦学习优化变体：FedAdam, FedYogi, FedAdagrad, FedAvgM。

### 4. 资源与算力
*   **硬件环境**：实验运行在配备双 **AMD EPYC 9654 处理器**（共 192 个核心）和 **256 GB RAM** 的计算平台上。
*   **软件工具**：使用 Julia 语言的 JuMP 建模工具和 MOSEK 求解器来处理 PEP 相关的 SDP 问题。
*   **说明**：文中未明确提及具体的 GPU 型号（尽管涉及了 BERT 和 CNN 训练）以及具体的总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从凸优化（逻辑回归）到复杂非凸深度学习模型（CNN, BERT）的多组实验。
*   **消融实验**：
    *   测试了不同的数据划分方案（按标签、按特征范数、按特征值）。
    *   研究了数据异质性程度（Dirichlet 分布参数 $\alpha$）对加速效果的影响。
    *   评估了设备数量（$N=2$ 到 $N=25$）和代理参与率（$p=0.7$ 到 $1.0$）的鲁棒性。
*   **公平性与客观性**：在 mini-batch 实验中，作者严格控制了每轮迭代中中心化和去中心化方法处理的总样本数一致，确保了对比的公平性。PEP 理论分析与经验实验结果高度吻合，增强了结论的可信度。

### 6. 主要结论与发现
*   **去中心化加速效应**：在迭代次数维度上，去中心化方法确实可以显著超越中心化方法。
*   **异质性是动力而非阻碍**：数据在不同设备间的分布越不均匀（异质性越高），去中心化带来的加速效果越明显。
*   **理论一致性**：PEP 理论分析证实，对于特定的函数类，去中心化算法的最坏情况误差严格低于中心化算法。
*   **鲁棒性**：该加速效应在不同的网络规模和部分代理参与的情况下依然存在。

### 7. 优点
*   **视角创新**：颠覆了去中心化是“性能妥协”的传统认知，将其重新定义为一种“战略优势”。
*   **理论严谨**：不依赖于松散的解析上界，而是利用 PEP 框架提供了精确的数值性能界限。
*   **实用性强**：提出的 Algorithm 1 逻辑简单，易于在现有的服务器辅助型（如联邦学习）架构中实现。

### 8. 不足与局限
*   **通信开销未量化**：论文主要关注“迭代次数”的减少，但在实际应用中，去中心化的通信延迟可能抵消迭代次数减少带来的时间收益。
*   **平滑常数估计**：对于深度神经网络，精确获取本地平滑常数 $L_i$ 较为困难，文中采用的数值估计方法在极端非凸情况下可能存在偏差。
*   **应用限制**：目前主要讨论了服务器辅助的架构，对于完全无服务器（Server-free）的对等网络架构，其加速效果及步长协调机制仍需进一步研究。

（完）
