---
title: Communication-Efficient Distributed Learning with Differential Privacy
title_zh: 具有差分隐私的高通信效率分布式学习
authors: "Xiaoxing Ren, Yuwen Ma, Nicola Bastianello, Karl H. Johansson, Thomas Parisini, Andreas A. Malikopoulos"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02558v1"
tags: ["query:daily"]
score: 10.0
evidence: 具有差分隐私的通信高效分布式学习
tldr: 本研究针对无向网络中的非凸学习问题，提出了一种兼顾通信效率与差分隐私的分布式学习算法。通过局部训练减少通信频率，并利用梯度裁剪和加性噪声实现隐私保护。理论证明该算法能收敛至平稳点，且在相同隐私预算下，其分类任务表现优于现有先进方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02558v1/fig-001.webp\", \"caption\": \"TABLE I: Computation time over τ iterations.\", \"page\": 5, \"index\": 1, \"width\": 371, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.02558v1/fig-002.webp\", \"caption\": \"Fig. 1: Numerical results for three algorithms with differential privacy.\", \"page\": 5, \"index\": 2, \"width\": 371, \"height\": 327}]"
motivation: 旨在解决分布式学习中通信开销大以及参与者数据隐私泄露的挑战。
method: 采用局部训练减少通信次数，并结合梯度裁剪与加性噪声技术实现差分隐私保护。
result: 理论证明了算法的收敛性及隐私性，实验显示其在分类任务上优于现有主流方法。
conclusion: 该算法在保证数据隐私的同时显著提升了分布式学习的通信效率和模型性能。
---

## 摘要
我们研究了无向网络上的非凸学习问题。特别地，我们专注于设计一种既具有通信效率又能保证代理数据隐私的算法。第一个目标通过本地训练方法实现，该方法降低了通信频率。第二个目标通过在本地训练期间扰动梯度来实现，具体通过梯度裁剪和加性噪声。我们证明了所得算法在有界距离内收敛到问题的驻点。此外，我们在差分隐私框架内提供了理论隐私保证，确保无法从网络共享的训练模型中推断出代理的训练数据。我们展示了该算法在相同隐私预算下，与现有最先进方法相比，在分类任务上的优越性能。

## Abstract
We address nonconvex learning problems over undirected networks. In particular, we focus on the challenge of designing an algorithm that is both communication-efficient and that guarantees the privacy of the agents' data. The first goal is achieved through a local training approach, which reduces communication frequency. The second goal is achieved by perturbing gradients during local training, specifically through gradient clipping and additive noise. We prove that the resulting algorithm converges to a stationary point of the problem within a bounded distance. Additionally, we provide theoretical privacy guarantees within a differential privacy framework that ensure agents' training data cannot be inferred from the trained model shared over the network. We show the algorithm's superior performance on a classification task under the same privacy budget, compared with state-of-the-art methods.

---

## 论文详细总结（自动生成）

# 论文总结：具有差分隐私的高通信效率分布式学习 (LT-ADMM-DP)

### 1. 核心问题与整体含义
本文研究了在无向多智能体网络上解决**非凸分布式学习问题**。其核心动机在于平衡分布式系统中的三大挑战：
*   **通信效率**：分布式学习通常需要频繁交换模型参数，受限于网络带宽。
*   **计算效率**：智能体本地资源有限，需采用随机梯度下降等轻量化手段。
*   **隐私保护**：共享的模型参数可能泄露原始训练数据，需要严格的数学隐私保证。

论文旨在设计一种算法，在保证差分隐私（Differential Privacy, DP）的前提下，通过减少通信频率来提升效率，并确保非凸问题的收敛性。

### 2. 论文提出的方法论
论文提出了 **LT-ADMM-DP**（Local Training ADMM with Differential Privacy）算法，其核心思想和技术细节如下：
*   **局部训练（Local Training）**：基于交替方向乘子法（ADMM）框架，允许智能体在每次与邻居通信前，先在本地执行 $\tau$ 步梯度更新。这显著降低了全局通信频率。
*   **隐私保护机制**：在本地训练的每一步中，对随机梯度进行两步处理：
    1.  **梯度裁剪（Clipping）**：通过设置阈值 $\zeta$ 限制梯度的范数，从而控制算法对单个数据点的灵敏度（Sensitivity）。
    2.  **加性噪声（Noise Addition）**：在裁剪后的梯度中加入高斯噪声，以满足差分隐私要求。
*   **算法流程**：
    1.  智能体初始化本地模型和桥接变量。
    2.  **本地阶段**：执行 $\tau$ 次带裁剪和噪声的随机梯度更新。
    3.  **通信阶段**：与邻居交换更新后的变量。
    4.  **协调阶段**：利用 ADMM 的对偶更新机制确保全网达成共识。

### 3. 实验设计
*   **任务场景**：带有非凸正则化项的逻辑回归分类任务。
*   **网络拓扑**：包含 $N=10$ 个节点的环形网络（Ring Network）。
*   **对比基准（Benchmarks）**：
    *   **PORTER** (2025)：一种结合梯度裁剪和通信压缩的去中心化非凸优化算法。
    *   **PriSMA** (2025)：一种处理非均匀有界随机梯度的差分隐私分布式学习方法。
*   **评价指标**：收敛误差（梯度范数）、分类准确率、总计算/通信时间成本。

### 4. 资源与算力
论文中**未明确说明**具体的硬件资源（如 GPU 型号、数量）或具体的训练时长。
*   作者通过定义理论时间模型（本地梯度评估时间 $t_g$ 与通信轮次时间 $t_c$）来评估效率。
*   实验部分主要展示了算法在迭代次数和模拟时间维度上的表现，而非物理算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一组核心对比实验，针对特定的分类任务进行了算法性能验证。
*   **充分性评估**：
    *   **公平性**：实验在**相同的隐私预算（$\epsilon$）**下对比了不同算法的性能，这在隐私计算研究中是公平且客观的。
    *   **局限性**：实验仅限于 10 个节点的小规模网络和单一分类任务，缺乏在大规模数据集（如 ImageNet）或更复杂网络拓扑（如随机图、全连接图）上的消融实验和扩展性验证。

### 6. 论文的主要结论与发现
*   **收敛性保证**：理论证明了 LT-ADMM-DP 能够收敛到非凸问题驻点的一个有界邻域内。收敛误差受梯度方差、噪声强度和数据异构性的影响。
*   **隐私强度**：利用 Rényi 差分隐私（RDP）理论，给出了算法在 $K$ 轮迭代后的总隐私预算 $\epsilon$ 的闭式表达式。
*   **性能优越性**：在相同的隐私保护水平下，LT-ADMM-DP 比 PORTER 和 PriSMA 收敛更快，且最终的分类准确率更高。

### 7. 优点
*   **多目标优化**：成功将局部训练（提效）与梯度裁剪/噪声（隐私）结合在 ADMM 框架下，解决了以往算法功能单一的问题。
*   **理论严谨**：不仅提供了收敛性分析，还利用 RDP 提供了比传统 DP 更紧致（Tighter）的隐私界限分析。
*   **通信友好**：通过 $\tau$ 参数灵活调节通信频率，适应带宽受限的实际场景。

### 8. 不足与局限
*   **实验覆盖面有限**：仅在小型合成数据集和简单网络上验证，未在真实的大规模深度学习任务中测试。
*   **超参数敏感性**：算法引入了多个超参数（裁剪阈值 $\zeta$、噪声标准差 $\sigma_e$、局部步数 $\tau$），论文未深入讨论如何自适应地选择这些参数以达到最优平衡。
*   **数据异构性处理**：虽然假设了数据异构性（Assumption 3），但未探讨在极端非独立同分布（Non-IID）数据下算法性能的退化情况。

（完）
