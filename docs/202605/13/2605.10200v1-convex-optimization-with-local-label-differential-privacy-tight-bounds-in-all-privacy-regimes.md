---
title: "Convex Optimization with Local Label Differential Privacy: Tight Bounds in All Privacy Regimes"
title_zh: 具有本地标签差分隐私的凸优化：所有隐私机制下的紧致界
authors: "Lynn Chua, Badih Ghazi, Ravi Kumar, Pasin Manurangsi, Ziteng Sun, Chiyuan Zhang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10200v1"
tags: ["query:daily"]
score: 9.5
evidence: 本地标签差分隐私约束下的随机凸优化
tldr: 本研究探讨了在本地标签差分隐私（L-LDP）约束下的随机凸优化问题。针对以往研究中超额风险对标签空间大小 K 呈线性依赖的局限性，本文提出了一种高效的非交互式 L-LDP 算法，将依赖项从 O(K) 优化至 O(sqrt(K))。此外，研究还证明了在所有隐私机制下该界限的信息论下界，确立了该结果的紧致性。
source: arxiv
selection_source: fresh_fetch
motivation: 解决现有本地标签差分隐私随机凸优化算法中超额风险对标签空间规模 K 线性依赖导致的效率低下问题。
method: 提出一种新型高效的非交互式 L-LDP 算法，用于在保护标签隐私的同时进行随机凸优化。
result: 在所有隐私机制下将超额风险界限从 O(K) 改进为 O(sqrt(K))，并提供了匹配的信息论下界。
conclusion: 本研究成功确立了本地标签差分隐私下凸优化问题的紧致界限，实现了对标签空间规模依赖性的平方级优化。
---

## 摘要
我们研究了在本地标签差分隐私（L-LDP）约束下的随机凸优化（SCO）问题。在这种设定下，特征被视为公开的，但相应的标签是敏感的，每个用户在将其发送给不可信的分析器之前必须在本地对其进行随机化。先前关于 L-LDP 下 SCO 的工作（Ghazi 等人，2021）建立了一个与标签空间大小 $K$ 呈线性依赖的超额群体风险界限：在强隐私机制（$ε\leq 1$）下为 $O\left({\frac{K}{ε\sqrt{n}}}\right)$，在中等隐私机制（$1 \leq ε\leq \ln K$）下为 $O\left({\frac{K}{e^ε \sqrt{n}}}\right)$。这留下了一个悬而未决的问题，即这种线性代价是否是 L-LDP 模型所固有的。在本论文中，我们解决了这个问题。首先，我们提出了一种新颖且高效的非交互式 L-LDP 算法，该算法在强隐私机制（$ε\leq 1$）下实现了 $O\left({\sqrt{\frac{K}{εn}}}\right)$ 的超额风险，在中等隐私机制（$1 \leq ε\leq \ln K$）下实现了 $O\left({\sqrt{\frac{K}{e^ε n}}}\right)$ 的超额风险。这将对标签空间大小的依赖性从 $O(K)$ 二次改进为 $O(\sqrt{K})$。其次，我们证明了在所有隐私机制下，对于任何足够大的 $n$，都存在匹配的信息论下界。

## Abstract
We study the problem of Stochastic Convex Optimization (SCO) under the constraint of local Label Differential Privacy (L-LDP). In this setting, the features are considered public, but the corresponding labels are sensitive and must be randomized by each user locally before being sent to an untrusted analyzer. Prior work for SCO under L-LDP (Ghazi et al., 2021) established an excess population risk bound with a \emph{linear} dependence on the size of the label space, $K$: $O\left({\frac{K}{ε\sqrt{n}}}\right)$ in the high-privacy regime ($ε\leq 1$) and $O\left({\frac{K}{e^ε \sqrt{n}}}\right)$ in the medium-privacy regime ($1 \leq ε\leq \ln K$). This left open whether this linear cost is fundamental to the L-LDP model. In this note, we resolve this question. First, we present a novel and efficient non-interactive L-LDP algorithm that achieves an excess risk of $O\left({\sqrt{\frac{K}{εn}}}\right)$ in the high-privacy regime ($ε\leq 1$) and $O\left({\sqrt{\frac{K}{e^ε n}}}\right)$ in the medium-privacy regime ($1 \leq ε\leq \ln K$). This quadratically improves the dependency on the label space size from $O(K)$ to $O(\sqrt{K})$. Second, we prove a matching information-theoretic lower bound across all privacy regimes for any sufficiently large $n$.

---

## 论文详细总结（自动生成）

这篇论文针对**本地标签差分隐私（Local Label Differential Privacy, L-LDP）**约束下的**随机凸优化（Stochastic Convex Optimization, SCO）**问题进行了深入的理论研究，确立了该问题在所有隐私机制下的最优界限。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在本地模型（Local Model）下，如何实现在保护训练数据标签（Label）隐私的同时，最小化随机凸优化的超额群体风险（Excess Population Risk）。
*   **研究动机**：
    *   **隐私需求**：在医疗等领域，特征（如人口统计信息）可能是公开的，但标签（如诊断结果）高度敏感。
    *   **现有瓶颈**：先前的研究（Ghazi et al., 2021）在 L-LDP 约束下得到的风险界限对标签空间大小 $K$ 呈线性依赖（$O(K)$）。这导致在标签数量较多时，算法的效用（Utility）大幅下降。
    *   **科学疑问**：这种对 $K$ 的线性依赖是本地隐私模型固有的代价，还是现有算法设计的缺陷？

### 2. 论文提出的方法论
论文提出了一种高效的**非交互式 L-LDP 算法**（Algorithm 1），其核心思想包括：
*   **子集选择随机化机制**：每个用户不再仅仅发送一个随机化的标签，而是根据其私有标签 $Y_t$ 生成一个标签子集 $S_t \subseteq [K]$。
    *   **关键细节**：对于每个标签 $i \in [K]$，如果 $i = Y_t$，则以 $1/2$ 的概率将其加入 $S_t$；如果 $i \neq Y_t$，则以 $1/(e^\epsilon + 1)$ 的概率加入。这种机制被证明满足 $\epsilon$-L-LDP。
*   **无偏梯度估计**：分析器（Analyzer）接收到特征 $X_t$ 和随机化子集 $S_t$ 后，构造一个复杂的梯度估计量 $\hat{g}_t$。通过精心设计的权重系数，确保 $\hat{g}_t$ 是真实梯度 $\nabla \ell$ 的无偏估计。
*   **投影随机梯度下降（Projected SGD）**：利用上述无偏估计量进行标准的迭代更新。
*   **理论证明**：通过对方差的精细分析，证明了该算法在强隐私（$\epsilon \leq 1$）和中等隐私（$1 \leq \epsilon \leq \ln K$）机制下，对 $K$ 的依赖性从 $O(K)$ 降低到了 $O(\sqrt{K})$。

### 3. 实验设计
*   **研究性质**：这是一篇**纯理论性质**的论文（Theoretical Note）。
*   **验证方式**：论文没有进行实证性的数值实验，而是通过**数学证明**来验证其结论。
*   **Benchmark（基准）**：对比的对象是 Ghazi 等人（2021）提出的基于随机响应（Randomized Response）的 L-LDP 算法。
*   **下界证明**：论文通过将 SCO 问题归约为“具有 $\ell_2$ 误差的离散分布估计问题”，证明了信息论意义上的下界（Lower Bound），从而确认了所提算法的紧致性（Tightness）。

### 4. 资源与算力
*   **算力说明**：由于本文属于理论推导，**文中未提及任何 GPU、CPU 型号或具体的训练时长**。
*   **算法复杂度**：论文指出该算法是高效的，其计算开销主要在于每一轮需要计算 $K$ 个梯度分量来构造估计量。

### 5. 实验数量与充分性
*   **充分性评价**：虽然缺乏实证实验，但从理论研究的角度来看，其论证是**极其充分**的。
    *   论文覆盖了**所有隐私机制**（强隐私、中等隐私、弱隐私）。
    *   不仅提供了上界（算法），还提供了匹配的下界（理论极限）。
    *   附录中还讨论了基于 $d$-子集选择机制的变体算法，增强了结论的普适性。

### 6. 论文的主要结论与发现
*   **平方级改进**：成功将 L-LDP 下 SCO 的超额风险对标签空间 $K$ 的依赖从 $O(K)$ 优化至 $O(\sqrt{K})$。
*   **确立紧致界**：证明了在 $n$ 足够大时，该 $O(\sqrt{K})$ 的依赖是不可逾越的，从而彻底解决了该问题的 minimax 最优率。
*   **非交互性优势**：证明了即使不使用复杂的交互式协议，仅靠非交互式算法也能达到最优性能。

### 7. 优点
*   **理论突破**：解决了 L-LDP 领域的一个重要公开问题，完善了标签隐私的理论框架。
*   **算法简洁**：提出的随机化机制和梯度估计方法逻辑清晰，易于在分布式系统中实现。
*   **全机制覆盖**：给出了从强隐私到弱隐私平滑过渡的完整图像。

### 8. 不足与局限
*   **缺乏实证验证**：虽然理论完备，但在真实数据集（如具有数千个类别的图像分类任务）上的表现未得到实际测试，可能存在常数项较大的问题。
*   **样本量限制**：下界的证明要求样本量 $n$ 达到一定的阈值（与 $K$ 和 $\epsilon$ 相关），对于极小样本场景的结论可能不够精确。
*   **计算开销**：分析器在每一步需要计算所有 $K$ 个可能的梯度，当 $K$ 极大时，单步迭代的计算成本较高。

（完）
