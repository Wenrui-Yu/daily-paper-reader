---
title: "Less Random, More Private: What is the Optimal Subsampling Scheme for DP-SGD?"
title_zh: 减少随机性，增强隐私性：DP-SGD 的最优子采样方案是什么？
authors: "Andy Dong, Ayfer Özgür"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07072v1"
tags: ["query:daily"]
score: 9.0
evidence: 差分隐私机器学习的最优子采样方案
tldr: 本研究挑战了差分隐私随机梯度下降（DP-SGD）中泊松采样最优的传统认知。泊松采样虽易于分析，但其随机性会导致样本参与次数不均，从而产生参与方差。作者提出了平衡迭代采样（BIS）方案，确保每个样本在训练中参与固定次数。通过理论证明和蒙特卡洛会计工具，研究表明BIS在低噪声环境下比泊松采样具有更强的隐私增强效果，且在噪声极限情况下表现最优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07072v1/fig-001.webp\", \"caption\": \"Table 1: Minimum noise multiplier σ required to achieve a target (ε, δ) under different accounting methods. Bold entries indicate the lowest noise among formal end-to-end DP methods, including any values within one unit of the third significant digit for our line search precision (a relative difference of ≤ 1%). The optimistic Monte Carlo approach lacks a formal DP guarantee, but yields the lowest noise multiplier across all regimes.\", \"page\": 12, \"index\": 1, \"width\": 840, \"height\": 475}]"
motivation: 泊松采样引入的样本参与次数不确定性（参与方差）是导致隐私增强效果并非最优的根本原因。
method: 提出平衡迭代采样（BIS）方案，使每个样本参与固定次数的迭代，并开发了配套的近精确蒙特卡洛隐私会计工具。
result: "在60多种DP-SGD配置下，BIS在低噪声机制中始终优于泊松采样，最高可将所需噪声倍率降低9.6%。"
conclusion: 结构化的样本参与方式比完全随机的采样更具隐私保护优势，打破了“随机性越高隐私越好”的直觉。
---

## 摘要
泊松子采样（Poisson subsampling）是差分隐私机器学习中的默认采样方案，很大程度上是因为其非结构化的随机性能够产生易于处理的隐私放大分析。然而，这种随机性也引入了显著的参与方差（participation variance）：每个样本在训练迭代中出现的次数差异很大。在这项工作中，我们证明了这种方差不仅是一个可以容忍的实践缺陷，而且是导致次优隐私放大的根本原因。我们证明了平衡迭代子采样（Balanced Iteration Subsampling, BIS）——一种每个样本精确参与固定次数迭代的结构化方案——比泊松子采样实现了更强的隐私放大，并且在噪声谱的两个极端（σ→0 和 σ→∞）都是最优的。我们的分析表明，隐私与噪声的权衡并非由最大化随机性决定，而是由消除参与方差同时保持跨迭代的均匀边际参与所决定的。为了将这一渐近理论转化为有限噪声保证，我们为 BIS 引入了一种实用的近精确蒙特卡洛核算器（Monte Carlo accountant），消除了现有 RDP 和基于组合的 PLD 分析中的分析松弛。在 60 多个实际 DP-SGD 配置中的评估表明，在与高实用性隐私训练最相关的低噪声机制下，BIS 始终优于泊松子采样，将所需的噪声乘数降低了高达 9.6%。这些结果推翻了“更多的采样随机性必然产生更强的隐私放大”这一普遍直觉：在 DP-SGD 中，结构化参与可以既更实用又更隐私。我们的实现可在 https://github.com/dong-xin-ao-andy/bis-mc-accountant 获取。

## Abstract
Poisson subsampling is the default sampling scheme in differentially private machine learning, largely because its unstructured randomness yields tractable privacy amplification analyses. Yet this same randomness introduces substantial participation variance: each sample appears in very different numbers of training iterations. In this work, we show that this variance is not merely a practical artifact to be tolerated, but a fundamental source of suboptimal privacy amplification. We prove that Balanced Iteration Subsampling (BIS), a structured scheme in which each sample participates in exactly a fixed number of iterations, achieves stronger privacy amplification than Poisson subsampling and is optimal at both extremes of the noise spectrum ($σ\to 0$ and $σ\to \infty$). Our analysis reveals that the privacy-noise tradeoff is governed not by maximizing randomness, but by eliminating participation variance while preserving uniform marginal participation across iterations. To translate this asymptotic theory into finite-noise guarantees, we introduce a practical near-exact Monte Carlo accountant for BIS, which removes the analytical slack of existing RDP and composition-based PLD analyses. Evaluations across more than 60 practical DP-SGD configurations show that BIS consistently outperforms Poisson subsampling in the low-noise regimes most relevant for high-utility private training, reducing the required noise multiplier by up to $9.6\%$. These results overturn the common intuition that more sampling randomness necessarily yields stronger privacy amplification: in DP-SGD, structured participation can be both more practical and more private. Our implementation is available at https://github.com/dong-xin-ao-andy/bis-mc-accountant.

---

## 论文详细总结（自动生成）

这是一份关于论文《Less Random, More Private: What is the Optimal Subsampling Scheme for DP-SGD?》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在差分隐私随机梯度下降（DP-SGD）中，泊松子采样（Poisson Subsampling）因其分析简便和“随机性越高隐私越好”的直觉，长期被视为默认方案。然而，泊松采样会导致**参与方差（Participation Variance）**，即某些样本在训练中被抽中的次数远多于其他样本。
*   **核心问题**：这种非结构化的随机性是否真的是隐私保护的最优解？作者挑战了这一传统认知，探讨是否存在一种更结构化、方差更小的采样方案能提供更强的隐私放大效应。

### 2. 论文提出的方法论
*   **核心思想**：提出**平衡迭代子采样（Balanced Iteration Subsampling, BIS）**。与泊松采样不同，BIS 强制要求每个样本在总共 $T$ 次迭代中精确地参与 $k$ 次，从而消除了参与次数的随机波动。
*   **关键技术细节**：
    *   **理论证明**：作者证明了在低噪声极限（$\sigma \to 0$）和高噪声极限（$\sigma \to \infty$）下，BIS 在所有独立样本子采样方案中都是隐私最优的。
    *   **近精确蒙特卡洛核算器（MC Accountant）**：由于 BIS 的精确隐私损失计算涉及组合爆炸，作者设计了两种加速技术：
        1.  **动态规划（DP）**：将似然比评估的复杂度从指数级降低到 $O(Tk)$。
        2.  **筛选边界（Screening Bound）**：推导出一个 $O(T)$ 的快速上界作为过滤器，能够跳过约 $1-\delta$ 比例的样本计算，使大规模验证变得可行。
*   **算法流程**：为每个样本独立且均匀地从 $T$ 个位置中选出 $k$ 个参与位点，构建训练批次。

### 3. 实验设计
*   **场景与配置**：研究涵盖了 60 多种实际的 DP-SGD 超参数配置（涉及 $\epsilon, \delta, T, k$），这些配置均取自已发表的高水平论文（如 ImageNet 分类、大语言模型微调等真实场景）。
*   **Benchmark（基准）**：
    *   **Poisson**：基于隐私损失分布（PLD）的泊松采样核算（目前的最强基准）。
*   **对比方法**：
    *   **BIS-RDP**：基于 Rényi 差分隐私的 BIS 分析。
    *   **RA-PLD**：基于随机分配和组合边界的 PLD 分析。
    *   **BIS-MC（本文方法）**：分为“认证版”（提供严格 DP 保证）和“乐观版”（估计理论极限）。

### 4. 资源与算力
*   **硬件使用**：实验在 Stanford Sherlock 集群上运行，主要使用 **AMD Milan 架构的 CPU 节点**。
*   **算力分配**：每个核算任务分配 **16 至 32 个 CPU 核心**，内存需求为 **24GB 至 32GB**。
*   **训练时长**：单个配置的噪声乘数搜索耗时从 **12 分钟到 12 小时**不等，大多数任务在 4 小时内完成。
*   **特点**：该方法完全依赖 CPU 并行化，无需昂贵的 GPU/TPU 资源。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **60 组以上**的实验，覆盖了从极低噪声到极高噪声的广泛区间。
*   **充分性与客观性**：实验没有使用人为构造的参数网格，而是直接采用学术界公认的训练配置，这增强了结果的说服力。通过对比多种现有的核算工具，客观地展示了 BIS 的优势并非来自分析松弛的消除，而是来自其结构化的采样特性。

### 6. 论文的主要结论与发现
*   **打破直觉**：结构化的参与（Less Random）反而能带来更强的隐私保护（More Private）。
*   **性能提升**：在低噪声（高实用性）机制下，BIS 显著优于泊松采样，**最高可将所需的噪声乘数降低 9.6%**。
*   **渐近一致性**：在高噪声极限下，BIS 的表现收敛于泊松采样，并共同趋向于全批次更新的理论下界。
*   **核算精度**：传统的 RDP 或基于组合的 PLD 分析存在明显的分析松弛（Slack），掩盖了 BIS 的真实优势，而本文的 MC 核算器揭示了其真实潜力。

### 7. 优点
*   **理论严密**：通过渐近分析（Laplace 原理和泰勒展开）为 BIS 的最优性提供了坚实的数学基础。
*   **实用性强**：BIS 不仅隐私性更好，而且固定的参与次数在联邦学习和分布式系统中更易于实现负载均衡。
*   **工具完备**：开源了高效的 MC 核算器，解决了结构化采样难以进行紧致隐私核算的痛点。

### 8. 不足与局限
*   **计算开销**：尽管有加速算法，但蒙特卡洛核算的时间成本仍远高于泊松采样的解析解（秒级 vs 小时级）。
*   **适用范围**：目前的分析主要集中在“独立样本机制”家族，尚未探讨样本间存在依赖关系的复杂采样场景。
*   **内存占用**：对于极长序列（$T$ 非常大）的动态规划，内存管理仍具有挑战性。

（完）
