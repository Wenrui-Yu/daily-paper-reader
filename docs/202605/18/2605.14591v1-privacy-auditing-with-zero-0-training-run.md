---
title: Privacy Auditing with Zero (0) Training Run
title_zh: 零训练运行的隐私审计
authors: "Tudor Cebere, Mathieu Even, Linus Bleistein, Aurélien Bellet"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14591v1"
tags: ["query:daily"]
score: 8.0
evidence: 学习算法的隐私审计
tldr: 传统的差分隐私审计通常需要多次重新训练模型，这在大型基础模型上难以实现。本文提出了“零运行（Zero-Run）”隐私审计框架，通过已知的训练集成员和非成员数据进行事后审计。针对成员与非成员分布差异导致的混淆效应，研究借鉴因果推理，提出了自适应组合和逐点调整两种修正方法，实现了在无需干预训练过程的情况下对隐私泄露的有效评估。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14591v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the Zero-Run Privacy Auditing framework. Observational data (Xi) from members and non-members introduces a confounding distribution shift (P1 ̸= P0). By modeling this shift, a propensity score estimator π̂(Xi) adjusts raw membership guesses (Ti) via global composition or pointwise conditioning, isolating true algorithmic leakage to yield valid empirical DP lower bounds.\", \"page\": 4, \"index\": 1, \"width\": 815, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.14591v1/fig-002.webp\", \"caption\": \"Figure 2: Evaluation of zero-run auditing under both controlled synthetic conditions and real-world distribution shifts. (a) Privacy lower bounds for different distribution shifts; (b) distribution and lower quantile of the boostrapped privacy lower bounds.; (c) Propensity score distribution on iWildCam.\", \"page\": 8, \"index\": 2, \"width\": 796, \"height\": 218}]"
motivation: 现有的隐私审计方法需要多次重新训练或干预训练数据，这对于部署的大规模基础模型而言成本过高且不切实际。
method: 提出一种基于观测数据的后验审计框架，利用因果推理中的自适应组合和逐点调整技术来修正成员与非成员之间的分布偏移。
result: 在合成数据和大规模模型上的实验证明，该方法能在不重新训练的情况下提供有效的隐私下界估计。
conclusion: 零运行审计为无法进行受控实验的大型系统提供了一种实用的隐私评估方案，填补了观测性隐私审计的空白。
---

## 摘要
隐私审计为学习算法的差分隐私参数提供了经验下界。然而，现有方法需要对训练流水线进行干预式访问，要么需要多次重新训练，要么需要随机化数据的包含情况。对于基础模型等大型已部署系统，这通常是不可行的。我们引入了零运行（Zero-Run）隐私审计，这是一种事后框架，利用两个固定数据集对模型进行审计：已知为训练集成员的样本和已知为非成员的样本。在这种观测机制下，成员身份不再是随机的；相反，成员和非成员数据通常在分布上存在差异，因此成员推理分数可能反映的是分布偏移而非算法泄漏。借鉴因果推理的思想，我们将这种混杂效应形式化，并提出了两种互补的修正方法，从而实现有效的隐私审计。我们的第一种方法将分布偏移和算法泄漏的综合影响建模为自适应组合，从而产生保守的全局修正。我们的第二种方法以观测数据为条件并调整逐点成员身份猜测，从而产生更紧致的实例相关界限。在合成数据和大规模模型上的实验表明，零运行审计能够在重新训练或受控数据插入不可行的情况下实现实用的隐私评估。

## Abstract
Privacy auditing provides empirical lower bounds on the differential privacy parameters of learning algorithms. Existing methods, however, require interventional access to the training pipeline, either to retrain multiple times or to randomize data inclusion. This is often infeasible for large deployed systems such as foundation models. We introduce Zero-Run privacy auditing, a post-hoc framework for auditing models using two fixed datasets: examples known to be training-set members and examples known to be non-members. In this observational regime, membership is no longer randomized; instead, member and non-member data often differ in distribution, so membership inference scores may reflect a distribution shift rather than algorithmic leakage. Drawing on ideas from causal inference, we formalize this confounding effect and propose two complementary corrections that yield valid privacy audits. Our first approach models the combined effect of distribution shift and algorithmic leakage as an adaptive composition, producing conservative global corrections. Our second approach conditions on observed data and adjusts pointwise membership guesses, yielding sharper instance-dependent bounds. Experiments on synthetic data and large-scale models show that Zero-Run auditing enables practical privacy evaluation when retraining or controlled data insertion is infeasible.

---

## 论文详细总结（自动生成）

这是一份关于论文《Privacy Auditing with Zero (0) Training Run》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**不重新训练模型**且**不干预训练流程**的情况下，对已部署的大规模 AI 模型（如基础模型）进行差分隐私（DP）审计。
*   **研究动机**：传统的隐私审计方法（如 Multi-Run 或 One-Run）要求审计者能够控制训练数据的包含情况或多次重复训练。然而，现代大模型训练成本极高（耗费数千 GPU 天），且数据流水线固定，导致传统审计方法在实际中不可行。此外，直接在观测数据上进行成员推理攻击（MIA）会受到“分布偏移”的干扰——攻击成功可能仅仅是因为成员和非成员数据分布不同，而非模型泄露了隐私。

### 2. 方法论：Zero-Run 隐私审计框架
该论文将隐私审计从“干预式实验”转变为“观测性研究”，核心思想是利用因果推理中的**倾向分（Propensity Scores）**来修正分布偏移带来的偏差。
*   **核心思想**：通过已知的成员数据集 $D_1$ 和非成员数据集 $D_0$，估计每个样本的倾向分 $\hat{\pi}(X_i)$（即样本属于训练集的概率）。利用该分数识别并抵消由于数据分布差异导致的“虚假”隐私泄露信号。
*   **关键技术细节**：
    *   **全局修正（组合视角）**：将观测到的总泄露建模为“分布偏移机制”与“算法隐私机制”的自适应组合。通过反卷积（Deconvolution）从总泄露中减去分布偏移的影响，得到保守的全局隐私下界。
    *   **逐点修正（条件视角）**：针对每个样本进行“篡改猜测（Tampered Guess）”。如果某个样本的倾向分极高（特征太明显），审计器会以一定概率抛弃该样本的推理结果，从而确保最终的统计量仅反映算法本身的泄露。
    *   **不确定性量化**：使用 **Bootstrap（自举法）** 重采样来处理倾向分估计过程中的统计误差，确保输出的隐私下界在统计上是稳健的。

### 3. 实验设计
*   **数据集与场景**：
    1.  **合成数据**：高维高斯分布下的“噪声求和（Noisy Sum）”机制。通过调节成员与非成员分布的相关性来控制分布偏移的强度。
    2.  **真实世界数据**：使用 **WILDS iWildCam** 基准测试。利用地理位置和环境差异产生的自然分布偏移来模拟真实的观测审计场景。
*   **Benchmark 与对比方法**：
    *   对比了**未修正的 One-Run 审计**（基准）：在分布偏移下会严重高估泄露，导致审计失效。
    *   对比了**全局修正**与**逐点修正（倾向分感知 vs. 不感知）**：证明了逐点修正且感知倾向分的方法在强偏移下依然能保持紧致且有效的隐私下界。

### 4. 资源与算力
*   **论文提及情况**：文中**未明确说明**完成其实验所用的具体硬件型号（如 GPU 数量）或总训练时长。
*   **相关信息**：实验涉及 ViT-base 图像嵌入提取和 PCA 降维（50维），并使用了 $K=600$ 次 Bootstrap 重采样。虽然未给出具体算力数值，但其核心卖点正是“零训练运行”，因此其审计过程本身的计算开销远低于模型训练，主要集中在倾向分模型（如逻辑回归）的拟合上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在合成实验中，对不同强度的分布偏移进行了连续扫描。
    *   在真实数据上，对比了 IID（独立同分布）和 OOD（分布外）两种切分方式。
    *   每个审计实验评估了 $m=10,000$ 个样本，并进行了多组消融实验（如不同猜测数量 $r$ 的影响）。
*   **充分性评价**：实验设计较为充分，涵盖了从受控的理论环境到复杂的真实场景。通过 Bootstrap 引入误差线，增强了结果的客观性和说服力。

### 6. 主要结论与发现
*   **分布偏移是“混杂因素”**：如果不进行修正，分布偏移会导致审计结果严重偏离真实隐私水平，甚至得出错误的结论。
*   **零运行审计可行性**：通过倾向分修正，可以在完全不干预训练的情况下，获得与干预式审计（One-Run）接近的隐私下界。
*   **逐点修正优于全局修正**：全局修正由于采用最坏情况假设，往往过于保守；而逐点修正能利用样本间的异质性，提供更紧致的界限。

### 7. 优点与亮点
*   **极高的实用性**：填补了大型预训练模型无法进行隐私审计的空白，使第三方独立审计成为可能。
*   **理论严谨**：将隐私审计与因果推理、自适应组合理论联系起来，提供了坚实的数学证明。
*   **灵活性**：不依赖于特定的模型架构或训练算法，对黑盒模型同样有效。

### 8. 不足与局限
*   **倾向分估计依赖性**：审计的准确性高度依赖于倾向分模型的质量。在极高维或数据极度稀疏的情况下，倾向分估计可能不准，导致结果过于保守。
*   **样本重叠要求**：如果成员和非成员分布完全不重叠（即支持集不交），则无法进行有效的零运行审计（文中 Proposition 3 证明了这一点）。
*   **效率损失**：相比于理想的干预式审计，零运行审计由于需要支付“估计倾向分”的统计代价，其得到的隐私下界通常会略松（文中实验显示约有 1.0 的 $\mu$ 值损失）。

（完）
