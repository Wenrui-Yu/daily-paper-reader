---
title: Differentially Private Motif-Preserving Multi-modal Hashing
title_zh: 差分隐私保模态多模态哈希
authors: "Zehua Cheng, Wei Dai, Jiahao Sun"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15460v1"
tags: ["query:daily"]
score: 8.5
evidence: 跨模态检索和图结构数据的差分隐私哈希
tldr: 针对跨模态哈希中语义图易受攻击及差分隐私灵敏度爆炸的问题，本文提出DMP-MH框架。该框架采用“脱敏后蒸馏”策略，通过裁剪节点度限制灵敏度，并利用噪声镜像下降生成满足边缘差分隐私的合成图，最后通过双流哈希网络进行拓扑蒸馏。实验证明，该方法在保护隐私的同时，显著优于现有基准并保留了极高的检索性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15460v1/fig-001.webp\", \"caption\": \"Table 3: Mean Average Precision (mAP@50) Comparison on NUS-WIDE Dataset. The best non-private results are underlined; the best privacy-preserving results are bolded.\", \"page\": 6, \"index\": 1, \"width\": 1075, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.15460v1/fig-002.webp\", \"caption\": \"Table 2: Mean Average Precision (mAP@50) Comparison onMIRFlickr-25K Dataset. The best non-private results are underlined; the best privacy-preserving results are bolded.\", \"page\": 6, \"index\": 2, \"width\": 1069, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.15460v1/fig-003.webp\", \"caption\": \"Table 1: Comparison of Privacy-Preserving Retrieval Methods\", \"page\": 2, \"index\": 3, \"width\": 517, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2605.15460v1/fig-004.webp\", \"caption\": \"Table 4: Privacy-Utility Trade-off Analysis (I→ T mAP@50 on MIRFlickr-25K, 64 bits). 𝜖 =∞ represents the non-private upper bound (JDSH).\", \"page\": 8, \"index\": 4, \"width\": 479, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2605.15460v1/fig-005.webp\", \"caption\": \"Table 5: Impact of Degree Clipping Threshold𝐷max. mAP@50 (Higher is better) vs. Triangle Count Error (TCE, Lower is better).\", \"page\": 8, \"index\": 5, \"width\": 510, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.15460v1/fig-006.webp\", \"caption\": \"Figure 1: Overview of the DMP-MH framework. The proposed method operates through three sequential phases. Phase 1 (Private): Sensitivity-Bounded Graph Construction takes training pairs and computes a cosine similarity matrix, then clips node degrees to at most 𝐷max neighbors to bound local sensitivity and avoid hubness explosion. Phase 2 (Private): Differentially Private Motif Synthesis employs Noisy Mirror Descent with Gaussian noise N(0, 𝜎2I) added to the motif gradient, followed by rectified log-normalization to produce a sanitized synthetic graph ?̂? satisfying (𝜖, 𝛿)-Edge DP. The raw graph is then discarded as a privacy firewall. Phase 3 (Public): Dual-Stream Distillation Hashing trains an Image Net (ResNet-50 [16]) and Text Net (DistilBERT [29]) to reconstruct the topology of ?̂? via a holistic structural loss that enforces intra-modal and cross-modal alignment, with quantization regularization pushing embeddings toward binary codes. Query/test data Dte remain unseen during graph construction to ensure strict inductive evaluation.\", \"page\": 3, \"index\": 6, \"width\": 1036, \"height\": 461}]"
motivation: 跨模态哈希依赖的语义图存在隐私泄露风险，且现有差分隐私方法在处理具有枢纽节点的图数据时面临灵敏度过高的问题。
method: 提出一种“脱敏后蒸馏”框架，通过确定性度裁剪限制灵敏度，并结合噪声镜像下降生成合成图以训练双流哈希网络。
result: "在MIRFlickr-25K等数据集上，该方法比隐私保护基准提升了高达11.4个mAP点，性能达到非隐私版本的92.5%。"
conclusion: DMP-MH框架成功解决了图结构数据在差分隐私下的表示学习难题，实现了隐私保护与跨模态检索性能的高效平衡。
---

## 摘要
跨模态哈希通过将图像和文本编码为紧凑的二进制代码，实现了高效的检索。现有最先进的方法依赖于从用户交互中提取的语义相似性图进行监督，但这些图编码了敏感的行为模式，容易受到链路重构攻击。现有的隐私保护方法在图结构数据上失效：差分隐私随机梯度下降（DP-SGD）因独立处理样本而破坏了关系模态（motifs），而图合成方法在无标度网络中面临无界的局部敏感性，枢纽节点导致单条边的修改会使三角形计数产生 O(N) 的变化，从而需要注入极大的噪声。我们将这一现象称为“枢纽爆炸”（Hubness Explosion）。我们提出了 DMP-MH，这是一个“先净化后蒸馏”的框架，将隐私保护与表示学习解耦。我们的方法首先通过确定性地裁剪节点度数来限制敏感性，使三角形模态的 L2 敏感性上限与数据集大小无关。然后，在 (ε,δ)-边差分隐私下，通过噪声镜像下降生成净化的合成图。最后，双流哈希网络利用全局结构损失蒸馏该拓扑结构，从而强制实现跨模态对齐。在 MIRFlickr-25K 和 NUS-WIDE 数据集上的严格归纳协议评估下，DMP-MH 的表现优于隐私基准方法，平均精度均值（mAP）提升高达 11.4 个百分点，同时保留了高达 92.5% 的非隐私性能。

## Abstract
Cross-modal hashing enables efficient retrieval by encoding images and text into compact binary codes. State-of-the-art methods rely on semantic similarity graphs derived from user interactions for supervision, yet these graphs encode sensitive behavioral patterns vulnerable to link reconstruction attacks. Existing privacy-preserving approaches fail on graph-structured data: Differentially Private SGD destroys relational motifs by treating samples independently, while graph synthesis methods suffer from unbounded local sensitivity in scale-free networks, hub nodes cause single-edge modifications to alter triangle counts by $\mathcal{O}(N)$, necessitating prohibitive noise injection. We term this phenomenon Hubness Explosion. We propose DMP-MH, a Sanitize-then-Distill framework that decouples privacy from representation learning. Our approach first bounds sensitivity by deterministically clipping node degrees, capping the $L_2$-sensitivity of triangle motifs independently of dataset size. A sanitized synthetic graph is then generated via Noisy Mirror Descent under $(ε,δ)$-Edge Differential Privacy. Finally, dual-stream hashing networks distill this topology using a holistic structural loss that enforces cross-modal alignment. Evaluated on MIRFlickr-25K and NUS-WIDE under a strict inductive protocol, DMP-MH outperforms private baselines by up to 11.4 mAP points while retaining up to 92.5% of non-private performance.

---

## 论文详细总结（自动生成）

以下是对论文《Differentially Private Motif-Preserving Multi-modal Hashing》（差分隐私保模态多模态哈希）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：跨模态哈希（CMH）通过将图像和文本映射到统一的二进制哈希空间，实现大规模数据的高效检索。目前最先进的方法依赖于从用户交互日志（如点击、购买、共现）中构建的“语义相似性图”来提供监督信号。
*   **核心问题**：
    1.  **隐私风险**：交互图包含敏感的用户行为模式，直接训练模型会面临“链路重构攻击”，泄露用户隐私。
    2.  **现有隐私技术失效**：传统的差分隐私随机梯度下降（DP-SGD）将样本视为独立个体，会破坏图中关键的结构信息（如三角形模态）；而图合成方法在处理“无标度网络”时，由于存在连接数极高的“枢纽节点（Hub Nodes）”，会导致局部敏感性爆炸（Hubness Explosion），使得注入的噪声过大，导致模型不可用。

### 2. 方法论
论文提出了 **DMP-MH** 框架，采用“**先净化后蒸馏（Sanitize-then-Distill）**”的策略，将隐私保护与表示学习解耦：
*   **灵敏度有界图构建（Phase 1）**：为了解决“枢纽爆炸”，通过确定性地裁剪节点度数（限制最大度数为 $D_{max}$），将三角形模态的 $L_2$ 敏感性强制限制在与数据集大小无关的常数范围内。
*   **差分隐私模态合成（Phase 2）**：利用**噪声镜像下降（Noisy Mirror Descent）**算法，在梯度中加入校准后的高斯噪声，生成满足 $(\epsilon, \delta)$-边差分隐私的合成加权邻接矩阵。随后通过“修正对数归一化”过滤负噪声伪影并稳定数值。
*   **双流整体哈希蒸馏（Phase 3）**：
    *   **架构**：图像流使用 ResNet-50，文本流使用 DistilBERT。
    *   **损失函数**：提出“整体结构损失（Holistic Structural Loss）”，同时约束模态内（图像-图像、文本-文本）和跨模态（图像-文本）的相似性，使其逼近隐私净化的合成图拓扑。
    *   **量化**：引入正则化项将连续嵌入推向二进制超立方体顶点。

### 3. 实验设计
*   **数据集**：MIRFlickr-25K（2万对图像-文本）和 NUS-WIDE（约19.6万对，大规模场景）。
*   **评估协议**：采用严格的**归纳学习（Inductive Learning）**，即查询项在图构建和合成阶段完全不可见。
*   **Benchmark 与对比方法**：
    *   **经典方法**：LSH, SH。
    *   **深度非隐私方法**：DCMH, DJSRH, JDSH, PRDH。
    *   **隐私保护方法**：DP-SGD, PPPL（主动隐私保护学习）。
*   **评价指标**：平均精度均值（mAP@50/5000）和三角形计数误差（TCE）。

### 4. 资源与算力
*   **算力消耗**：论文明确对比了计算效率。在 NUS-WIDE 数据集上，DMP-MH 的总耗时为 200 分钟（其中图合成 35 分钟，训练 150 分钟）。
*   **显存占用**：峰值显存为 **12.5 GB**，相比 DP-SGD（24.2 GB）降低了约 48%。
*   **硬件说明**：文中提到该方法可在“标准消费级硬件”上部署，但未给出具体的 GPU 型号和数量。

### 5. 实验数量与充分性
*   **实验规模**：
    1.  在两个主流数据集上进行了 16/32/64 位长度的全面对比。
    2.  **隐私预算分析**：测试了 $\epsilon$ 从 0.1（极严苛）到 10.0（宽松）的变化曲线。
    3.  **消融实验**：详细探讨了度裁剪阈值 $D_{max}$ 对性能和结构保真度的影响。
    4.  **效率分析**：对比了不同阶段的时间和内存开销。
*   **充分性评价**：实验设计非常充分，涵盖了性能、隐私-效用权衡、超参数敏感性和计算成本等多个维度。采用归纳协议确保了实验结果的客观性和在实际检索场景中的公平性。

### 6. 主要结论与发现
*   **性能领先**：在 $\epsilon=2.0$ 的标准隐私预算下，DMP-MH 在 NUS-WIDE 上比 DP-SGD 高出 **11.4 个 mAP 点**。
*   **高保真度**：DMP-MH 保留了非隐私 SOTA 方法（JDSH）约 **92.5%** 的性能。
*   **强鲁棒性**：在极严苛隐私设置（$\epsilon=0.1$）下，竞争方法基本失效，而 DMP-MH 仍能保留 77% 的效用。
*   **效率优势**：训练速度比 DP-SGD 快 **2.1 倍**，且内存占用更低。

### 7. 优点
*   **理论创新**：识别并命名了“枢纽爆炸”问题，通过度裁剪巧妙地解决了图数据差分隐私中敏感性无界的难题。
*   **架构解耦**：将隐私处理放在预处理阶段（合成图），使得后续深度网络训练可以像普通监督学习一样高效进行，无需复杂的梯度裁剪。
*   **跨模态对齐**：通过整体结构损失解决了以往方法中常见的模态失调问题，确保了双向检索的平衡。

### 8. 不足与局限
*   **偏差引入**：确定性的度裁剪虽然降低了敏感性，但不可避免地会丢弃图中的长尾语义连接，可能导致部分冷门类别的检索精度受损。
*   **超参数依赖**：$D_{max}$ 的选择存在明显的“倒 U 型”性能曲线，实际应用中需要针对不同数据集进行精细调优。
*   **模态限制**：目前仅验证了图像和文本，对于视频、音频等更复杂或更高维度的多模态数据，其合成效率和对齐效果尚待验证。

（完）
