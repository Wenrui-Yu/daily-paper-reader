---
title: Improving Parameter-Efficient Federated Learning with Differentially Private Refactorization
title_zh: 通过差分隐私重构改进参数高效的联邦学习
authors: "Linh Tran, Ana Milanova, Stacy Patterson"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08443v1"
tags: ["query:daily"]
score: 10.0
evidence: 带参数高效微调的差分隐私联邦学习
tldr: 本研究针对联邦学习中LoRA与差分隐私（DP）结合时产生的聚合误差和噪声放大问题，提出了FedPower框架。该框架通过在服务端显式重建并裁剪全秩更新来限制敏感度，并利用创新的PowerDP机制将聚合更新投影回低秩空间。PowerDP在正交化前注入校准噪声，有效保持了矩阵正交性并降低了噪声干扰。实验证明，FedPower在保证隐私的同时，显著提升了模型在语言理解任务中的准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08443v1/fig-001.webp\", \"caption\": \"Figure 3: Success rates of three different membership inference attacks in non-private and private settings. We include the random guessing (0.5) as a baseline. Accuracies that are closed to or under the baseline indicates successful protection against the attacks.\", \"page\": 11, \"index\": 1, \"width\": 482, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.08443v1/fig-002.webp\", \"caption\": \"Figure 1: An illustration of the FL algorithms with LoRA in a global DP setting: existing FedLoRA and our proposed method FedPower. In both frameworks, each client trains individual LoRA modules locally and sends to server. In FedLoRA, the server aggregates each LoRA modules separately and adds Gaussian noise, and the resulting global weight contains extra error terms which degrade model accuracy. In FedPower, the server aggregates the products of each LoRA pairs and jointly privatizes and refactorizes the new LoRA modules via PowerDP. At the end of FL training, the server releases the trained global model to downstream users who may perform membership inference attack on the client’s private data.\", \"page\": 2, \"index\": 2, \"width\": 948, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2605.08443v1/fig-003.webp\", \"caption\": \"Table 2: Test accuracy of FedPower on 4 GLUE datasets with different privacy noise injection schemes.\", \"page\": 10, \"index\": 3, \"width\": 756, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2605.08443v1/fig-004.webp\", \"caption\": \"Table 4: Overview of datasets used in the experiments.\", \"page\": 13, \"index\": 4, \"width\": 507, \"height\": 593}, {\"url\": \"assets/figures/arxiv/2605.08443v1/fig-005.webp\", \"caption\": \"Figure 4: True positive rate vs. False positive rate of each membership inference attack across all privacy budgets.\", \"page\": 12, \"index\": 5, \"width\": 1022, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2605.08443v1/fig-006.webp\", \"caption\": \"Figure 2: Accuracy as a function of global rounds (a and b) and transferred bits (c and d) on QQP with 𝜖 = {6, 3}.\", \"page\": 9, \"index\": 6, \"width\": 1044, \"height\": 226}]"
motivation: 现有的差分隐私LoRA联邦学习方法在低秩空间中容易受到噪声干扰，导致聚合误差大且模型精度下降。
method: 提出FedPower框架，通过服务端全秩聚合与PowerDP差分隐私低秩分解机制，在保持正交性的同时降低噪声影响。
result: 在多种语言理解任务中，FedPower在严苛的隐私预算下表现稳健，且计算开销极小，能有效抵御成员推理攻击。
conclusion: FedPower通过重塑服务端聚合逻辑，成功解决了隐私保护与低秩适配之间的权衡难题，为安全高效的联邦学习提供了新方案。
---

## 摘要
结合参数高效微调（如低秩自适应 LoRA）的联邦学习（FL）能够在分布式数据上实现可扩展的模型训练。然而，当与差分隐私（DP）结合时，LoRA 往往会在全局聚合过程中引入误差，并放大 DP 噪声的负面影响。现有的跨机构（cross-silo）联邦学习方法通过冻结一个 LoRA 模块并应用输出扰动来减轻聚合误差。然而，在受限的低秩子空间中，这种加性噪声经常会掩盖权重矩阵的信号，导致准确率欠佳。为了解决这一漏洞，我们提出了 FedPower，这是一个重塑了服务器端聚合的差分隐私跨机构联邦学习框架。FedPower 不再对不匹配的低秩因子进行扰动，而是显式地重建并裁剪全秩客户端更新以限制敏感度。随后，服务器使用 PowerDP（一种新型差分隐私低秩分解机制）将精确的聚合更新投影回安全的低秩空间。PowerDP 基于同步子空间迭代，在最终的正交化步骤之前注入校准后的 DP 噪声，通过保持矩阵正交性有效地减轻了 DP 噪声的负面影响。我们提供了严谨的理论分析，确立了子空间投影的敏感度边界，证明了 FedPower 实现了样本级和客户端级的 DP。在跨机构联邦学习设置下，针对各种语言理解任务的大量实验表明，FedPower 在紧凑的隐私预算下具有鲁棒性，同时增加的计算开销可以忽略不计。对不同 DP 噪声注入方案的额外实证研究验证了 PowerDP 在改善准确性与隐私权衡方面的有效性。对三种不同成员推理攻击的评估验证了所提框架的鲁棒性和隐私保护能力。

## Abstract
Federated Learning (FL) with parameter-efficient fine-tuning, such as Low-Rank Adaptation (LoRA), enables scalable model training on distributed data. However, when combined with Differential Privacy (DP), LoRA often introduces errors during global aggregation and amplifies the negative effect of DP noise. Existing cross-silo FL approaches mitigate the aggregation error by freezing one LoRA module and applying output perturbation. However, in a restricted low-rank subspaces, this additive noise frequently overwhelms the signals of the weight matrices, leading to suboptimal accuracy. To address this vulnerability, we propose FedPower, a differentially private cross-silo FL framework that reshapes server-side aggregation. Instead of perturbing mismatched low-rank factors, FedPower explicitly reconstructs and clips full-rank client updates to bound the sensitivity. The server then projects the exact aggregated update back into a secure low-rank space using PowerDP, a novel differentially private low-rank factorization mechanism. Based on simultaneous subspace iteration, PowerDP injects calibrated DP noise prior to the final orthonormalization step, effectively mitigates the negative effect of DP noise by preserving matrix orthogonality. We provide rigorous theoretical analyses establishing sensitivity bounds for subspace projections, proving that FedPower achieves both sample-level and client-level DP. Extensive experiments on various language understanding tasks in cross-silo FL settings show that FedPower is robust against tight privacy budgets while adding negligible computational overheads. Additional empirical study on different DP noise injection schemes validates the effectiveness of PowerDP in improving the tradeoff in accuracy and privacy. Evaluation on three different membership inference attacks validates the robustness and privacy-preserving capability of the proposed framework.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Parameter-Efficient Federated Learning with Differentially Private Refactorization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是在**联邦学习（FL）**环境下，如何安全且高效地对**大语言模型（LLM）**进行微调。
*   **背景**：LoRA（低秩自适应）是联邦学习中减少通信开销的标准技术。然而，当引入**差分隐私（DP）**来保护数据免受成员推理攻击时，传统的“FedLoRA”方法存在严重的数学缺陷：服务器独立平均低秩矩阵 $A$ 和 $B$ 会产生“交叉项”误差，且 DP 噪声在低秩空间中会放大这种误差，导致模型准确率大幅下降。
*   **核心问题**：如何在保持 LoRA 通信效率的同时，消除聚合误差并降低 DP 噪声对模型性能的负面影响。

### 2. 方法论：FedPower 框架与 PowerDP 机制
论文提出了 **FedPower** 框架，其核心思想是将“隐私化”和“低秩化”的过程从客户端转移到服务器端的特定步骤中：
*   **全秩重建与裁剪**：客户端本地训练 LoRA 模块并上传。服务器接收后，先将其还原为全秩等效更新 $\Delta W_i = B_i A_i$，对其进行全秩裁剪以精确限制敏感度，然后进行精确聚合。这消除了低秩模块独立聚合带来的数学不匹配。
*   **PowerDP 机制**：这是论文的关键创新。服务器使用一种改进的“幂迭代（Power Iteration）”算法将聚合后的全秩矩阵重新分解为低秩的 $A$ 和 $B$。
    *   **过程**：在子空间迭代过程中，PowerDP 在最终的正交化步骤**之前**注入校准后的高斯噪声。
    *   **优势**：通过在正交化前加噪，噪声被“吸收”进子空间的方向中，而不是直接破坏信号强度，从而保持了矩阵的正交结构，显著提升了隐私-准确性权衡。
*   **理论保证**：论文严格证明了该方法满足 $(\epsilon, \delta)$-DP，并给出了样本级和客户端级隐私的敏感度边界。

### 3. 实验设计
*   **数据集**：使用了 GLUE 基准测试中的四个核心任务：MNLI（匹配与不匹配）、SST-2、QQP 和 QNLI。
*   **模型**：以 RoBERTa-large 作为基础模型。
*   **Benchmark 与对比方法**：
    *   **FedLoRA**：标准低秩聚合方法。
    *   **FFA-LoRA**：现有的 SOTA 方法，通过冻结一个 LoRA 模块来减少误差。
    *   **消融实验**：对比了 PowerDP 与传统的“输入扰动”和“输出扰动”方案。
    *   **攻击测试**：使用了三种成员推理攻击（影子模型攻击、基于损失的攻击、校准攻击）来验证安全性。

### 4. 资源与算力
*   **硬件**：实验运行在一个包含 **3 台 V100 32GB GPU** 的集群上。
*   **训练细节**：设置了 200 个全局轮次，客户端采样率为 0.5，本地 Batch Size 为 128。
*   **开销**：论文提到 FedPower 相比 FedLoRA 增加了约 1.2% 到 9.3% 的服务器端计算开销（取决于重构频率），但对客户端（资源受限方）没有增加任何额外负担。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集、3 种隐私预算（$\epsilon=3, 6, 9$）、3 种加噪方案对比、以及不同重构频率的消融实验。
*   **重复性**：所有实验均使用 5 个不同的随机种子重复运行并取平均值，确保了结果的客观性。
*   **公平性**：在对比实验中保持了相同的超参数（如 Batch Size、轮次、裁剪阈值等），实验设计较为充分且公平。

### 6. 主要结论与发现
*   **性能优越**：FedPower 在所有隐私预算下均优于 FedLoRA 和 FFA-LoRA。在 $\epsilon=3$ 的严苛条件下，平均准确率比 FedLoRA 高出 1.65%。
*   **收敛更快**：FedPower 达到目标准确率所需的通信轮次和传输比特数显著少于对比方法。
*   **防御有效**：在开启 DP 后，所有成员推理攻击的成功率均降至接近随机猜测（0.5 左右），证明了极强的隐私保护能力。
*   **PowerDP 的关键性**：实验证明在迭代过程中加噪（PowerDP）远优于在分解后直接加噪（输出扰动）。

### 7. 优点（亮点）
*   **结构创新**：巧妙地通过服务器端“先全秩聚合、后私密重构”的逻辑，解决了 LoRA 聚合不匹配的顽疾。
*   **算法优雅**：PowerDP 结合了数值线性代数（幂迭代）与差分隐私，利用正交化过程来缓解噪声干扰，具有很强的启发性。
*   **实用性强**：不增加客户端负担，非常适合跨机构（Cross-silo）联邦学习场景。

### 8. 不足与局限
*   **服务器压力**：虽然论文称开销可控，但在处理超大规模参数（如千亿级模型）时，服务器端重建全秩矩阵并进行幂迭代可能会面临内存瓶颈。
*   **数据分布假设**：实验主要基于 IID（独立同分布）数据，虽然结论部分提到了 Non-IID 是未来方向，但目前尚未在高度异构的数据环境下验证其稳健性。
*   **场景限制**：目前主要针对跨机构（Cross-silo）场景，对于客户端数量极巨且不稳定的跨设备（Cross-device）场景，其全秩重构的逻辑可能需要进一步优化。

（完）
