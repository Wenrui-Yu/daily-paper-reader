---
title: Private Vertical Federated Inference for Time-Series
title_zh: 面向时间序列的隐私保护纵向联邦推理
authors: "Lucas Fenaux, Larris Xie, Aditya Bang, Alex Zhang, Kevin Wilson, Florian Kerschbaum"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08343v1"
tags: ["query:daily"]
score: 9.0
evidence: 使用MPC进行安全推理的纵向联邦学习
tldr: 针对时间序列数据的协作推理中隐私保护与效率难以兼得的问题，本文提出PPHH-VFL架构。该方法将模型头拆分为高效的明文公共部分和轻量级的MPC私有部分，并结合对抗训练减少嵌入层的隐私泄露。实验表明，该方法在保持高精度的同时，显著降低了通信开销并提升了推理速度，为大规模纵向联邦推理提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08343v1/fig-001.webp\", \"caption\": \"Table 1: Online inference runtime across model sizes and network profiles for the Fraud and Rossmann datasets. We report s/batch using 3 batches of size 1 for E2E MPC, 15 of size 64 for the rest of Fraud, and 2 of size 64 for Rossmann. We specify the architecture used, with the associated (hpriv, apriv) for VFL+MPC, and (hpub, apub | hpriv, apriv) for PPHH-VFL. The results are averaged over 9 runs and we include the 95% confidence intervals. We bold the best performer between equivalent model architectures (P1 ≃ H1, P2 ≃ H2, . . . ).\", \"page\": 7, \"index\": 1, \"width\": 614, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2605.08343v1/fig-002.webp\", \"caption\": \"Table 4: Microbenchmark of the % of runtime for each inference stage for PPHH-VFL under LAN and WAN. Averaged over 3 runs.\", \"page\": 13, \"index\": 2, \"width\": 722, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2605.08343v1/fig-003.webp\", \"caption\": \"Figure 4: Online inference runtime in seconds (log-scale) as the number of parties scales for a batch size of 64 on the Fraud dataset for each model size.\", \"page\": 13, \"index\": 3, \"width\": 810, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2605.08343v1/fig-004.webp\", \"caption\": \"Figure 1: Comparison of traditional end-to-end MPC (left) versus the VFL with secure head evaluation (right).\", \"page\": 4, \"index\": 4, \"width\": 829, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.08343v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of the PPHH-VFL pipeline utilizing the public/private head split and MPC fusion.\", \"page\": 5, \"index\": 5, \"width\": 818, \"height\": 273}]"
motivation: 现有的多方计算方案在处理大规模Transformer架构时效率极低，而纵向联邦学习又面临嵌入层隐私泄露的风险。
method: 提出一种混合架构，将模型头分为明文公共头和MPC保护的私有头，并利用对抗训练来抑制公共嵌入中的敏感信息。
result: "该方法比端到端MPC快六个数量级，且在广域网环境下比标准VFL+MPC基准提速44.4倍，通信量减少了91.2%。"
conclusion: PPHH-VFL在保证隐私安全的前提下，实现了大规模时间序列模型的高效、低成本联邦推理。
---

## 摘要
机构可以从时间序列数据的协同推理中获益。在需要隐私保护的场景下，多方计算（MPC）是提供强有力保障的直接方法，但其成本极高，且在现代 Transformer 架构上的扩展性较差。纵向联邦学习（VFL）虽然效率较高，但在嵌入（embedding）层面存在隐私泄露风险，且通过 MPC 保护整个 VFL 模型头部对于大型模型而言仍然速度极慢且通信开销巨大。为了实现实用且大规模的安全推理，我们提出了“公有/私有混合头部纵向联邦学习”（PPHH-VFL）。这种混合架构将模型头部拆分为一个高效的明文公有头部和一个安全、轻量级的 MPC 私有头部。通过对公有嵌入应用对抗训练，我们缓解了隐私泄露；同时，小型私有头部安全地保留了高下游效用所需的敏感信息流。在参数量高达 8600 万的模型上进行的实证评估表明，与端到端 MPC 相比，PPHH-VFL 将推理速度提升了多达六个数量级。与标准的 VFL+MPC 基准相比，我们的方法具有显著更好的扩展性，在广域网（WAN）中实现了高达 44.4 倍的加速，通信成本降低了 91.2%（每批次从 1.7 GB 降至 19 MB），同时将下游分类准确率提高了 2.50%，回归均方根误差（RMSE）降低了 40.7%。

## Abstract
Institutions may benefit from collaborative inference on time-series data. In settings where privacy is necessary, multi-party computation (MPC) is a straightforward approach to providing strong guarantees, yet it remains prohibitively expensive and scales poorly with modern transformer architectures. Vertical Federated Learning (VFL) offers efficiency but suffers from privacy leakage at the embedding level, and securing the entire VFL model head via MPC remains prohibitively slow and communication-heavy for larger models. To enable practical, secure inference at scale, we propose "Public/Private Hybrid Head-VFL" (PPHH-VFL). This hybrid architecture splits the model head into an efficient plaintext public head and a secure, lightweight MPC private head. By applying adversarial training to the public embeddings, we mitigate privacy leakage; concurrently, the small private head securely preserves the flow of sensitive information needed for high downstream utility. Empirical evaluations on models ranging up to 86 million parameters demonstrate that PPHH-VFL accelerates inference by up to six orders of magnitude compared to end-to-end MPC. Compared to a standard VFL+MPC baseline, our approach scales significantly better, achieving a speedup of up to 44.4x in WAN and a 91.2x reduction in communication costs (dropping from 1.7 GB to 19 MB per batch), while simultaneously improving downstream classification accuracy by 2.50% and regression RMSE by 40.7%.

---

## 论文详细总结（自动生成）

这篇论文针对时间序列数据的隐私保护协作推理问题，提出了一种兼顾效率与安全性的混合架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在金融欺诈检测、临床监测等领域，多家机构往往需要对时间序列数据进行协作推理。然而，现有的解决方案存在明显的“效率-隐私”权衡：
*   **多方计算 (MPC)**：虽然能提供极强的理论隐私保证，但在处理现代 Transformer 架构时，由于非线性激活函数（如 Softmax）产生的大量通信开销，导致推理速度极慢，难以在大规模模型上扩展。
*   **纵向联邦学习 (VFL)**：效率较高，但客户端发送给服务器的嵌入（Embeddings）存在隐私泄露风险，攻击者可能通过嵌入反推原始数据。
*   **研究动机**：如何在大规模 Transformer 模型上实现既能保护隐私，又能满足实时性要求的协作推理？

### 2. 方法论：PPHH-VFL 架构
论文提出了 **PPHH-VFL (Public/Private Hybrid Head-VFL)**，其核心思想是将模型头部（Head）进行“公私分离”：
*   **混合头部设计**：将模型头拆分为三个部分：
    1.  **公有头 (Public Head)**：规模较大，在明文环境下运行，处理不含敏感信息的公有嵌入。
    2.  **私有头 (Private Head)**：规模极小，在 MPC 环境下运行，处理包含关键敏感信息的私有嵌入。
    3.  **融合头 (Fusion Head)**：极小的 MPC 层，用于合并公有和私有头的输出并给出最终预测。
*   **对抗训练 (Adversarial Training)**：为了防止公有嵌入泄露隐私，引入一个判别器（Discriminator）。在训练阶段，编码器（Encoder）需要生成让判别器无法区分其来源客户端的嵌入，从而在经验上消除公有嵌入中的敏感特征。
*   **时间步切分**：不同于传统 VFL 切分特征维度，本文提出按“时间步”切分序列（例如：银行 A 拥有用户前 3 天的交易，银行 B 拥有后 2 天的交易），这为时间序列 VFL 提供了新视角。

### 3. 实验设计
*   **数据集**：
    1.  **Credit Card Fraud Detection**：合成信用卡欺诈数据集（分类任务）。
    2.  **Rossmann Sales**：真实零售店销售数据集（回归任务）。
*   **基准测试 (Benchmarks)**：
    1.  **End-to-End MPC (E2E)**：全流程 MPC 推理（理论上限，但极慢）。
    2.  **VFL + MPC**：标准 VFL 架构，但整个模型头都在 MPC 中运行。
*   **网络环境**：模拟了局域网 (LAN: 1ms 延迟, 1Gbps 带宽) 和广域网 (WAN: 40ms 延迟, 100Mbps 带宽)。

### 4. 资源与算力
*   **硬件配置**：使用配备 NVIDIA A100 GPU (80GB 显存) 的服务器，拥有 32 核 CPU 和 1TB RAM。
*   **软件框架**：基于 PyTorch 和 CrypTen（MPC 框架）。
*   **算力分配**：每个客户端进程分配一个独立的 GPU。论文详细列出了从 100 万到 8600 万参数量的四种模型架构（P1-P4, H1-H4）的训练与推理配置。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了不同模型大小（1M 到 86M 参数）、不同网络环境（LAN/WAN）、不同参与方数量（3 到 8 个客户端）。
*   **重复性**：推理时间结果取 9 次运行的平均值，并提供了 95% 的置信区间。
*   **充分性**：实验不仅对比了运行时间，还深入分析了通信轮数、通信数据量、下游任务准确率/均方根误差以及对抗训练后的经验隐私水平，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **极致加速**：相比端到端 MPC，PPHH-VFL 提速高达 **6 个数量级**（将 157 天的推理缩短至 8.6 秒）。
*   **扩展性优异**：在 86M 参数的大模型上，PPHH-VFL 在 WAN 环境下比 VFL+MPC 基准快 **44.4 倍**。
*   **通信开销骤减**：通信量减少了 **91.2%**（从 1.7 GB 降至 19 MB 每批次），这使得大模型在带宽受限的环境下推理成为可能。
*   **效用提升**：由于对抗训练起到了正则化作用，PPHH-VFL 的分类准确率比 VFL+MPC 提升了 2.50%，回归 RMSE 降低了 40.7%。

### 7. 优点
*   **架构创新**：巧妙地利用“公私分流”解决了 MPC 处理大模型时的瓶颈问题。
*   **实用性强**：大幅降低了通信量和延迟，使基于 Transformer 的联邦推理在现实广域网中具备可行性。
*   **兼顾隐私与效用**：通过私有头保留了关键信息流，弥补了对抗训练可能带来的精度损失。

### 8. 不足与局限
*   **精度差距**：虽然优于 VFL+MPC，但与中心化训练的 E2E 模型相比，仍有约 10% 的准确率差距（主要归因于 VFL 无法在全序列上做全局注意力）。
*   **经验隐私 vs 理论隐私**：公有头依赖对抗训练提供“经验隐私”，而非 MPC 的“信息论/密码学隐私”，对于极高安全要求的场景可能存在争议。
*   **模型切分依赖**：性能高度依赖于如何划分公有头和私有头的比例，论文中对此超参数的敏感度分析尚可进一步加强。

（完）
