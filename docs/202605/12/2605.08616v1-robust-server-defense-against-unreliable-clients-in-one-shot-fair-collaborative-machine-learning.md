---
title: Robust Server Defense Against Unreliable Clients in One-Shot Fair Collaborative Machine Learning
title_zh: 单次公平协作机器学习中针对不可信客户端的鲁棒服务器防御
authors: "Chia-Yuan Wu, Frank E. Curtis, Daniel P. Robinson"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08616v1"
tags: ["query:daily"]
score: 9.0
evidence: 在数据分布设置中解决数据隐私的协作机器学习
tldr: 针对单轮协作学习中不可信客户端提供偏见数据导致全局模型公平性受损的问题，本文提出一种基于双层优化的服务端防御框架。该方法利用服务端极少量的可信根数据集，通过学习客户端权重来削弱偏见数据的影响并强化公平性约束。实验证明，该方法在不可信客户端占多数的情况下仍能有效提升公平性且几乎不损失精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08616v1/fig-001.webp\", \"caption\": \"TABLE III PERFORMANCE COMPARISON FOR THE REALISTIC SCENARIOS\", \"page\": 8, \"index\": 1, \"width\": 484, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.08616v1/fig-002.webp\", \"caption\": \"TABLE II PERFORMANCE COMPARISON BETWEEN OUR METHOD AND THE BASELINE METHOD FOR THE REALISTIC SCENARIOS DESCRIBED IN SECTION IV-D.\", \"page\": 8, \"index\": 2, \"width\": 512, \"height\": 570}, {\"url\": \"assets/figures/arxiv/2605.08616v1/fig-003.webp\", \"caption\": \"Fig. 3. Bi-criteria comparison of accuracy and fairness across different methods for the realistic scenario setting discussed in Section IV-D.\", \"page\": 8, \"index\": 3, \"width\": 529, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.08616v1/fig-004.webp\", \"caption\": \"Fig. 2. Client-level fairness measures of proxy data for different unreliable client percentages, different scenarios, and different fairness metrics.\", \"page\": 6, \"index\": 4, \"width\": 474, \"height\": 572}, {\"url\": \"assets/figures/arxiv/2605.08616v1/fig-005.webp\", \"caption\": \"TABLE I COMPARISON OF FINAL CLIENT WEIGHTS ON THE LAW SCHOOL AND DUTCH DATASETS FOR THE IDEAL SCENARIOS IN SECTION IV-D\", \"page\": 7, \"index\": 5, \"width\": 508, \"height\": 977}, {\"url\": \"assets/figures/arxiv/2605.08616v1/fig-006.webp\", \"caption\": \"Fig. 1. Illustration of the one-shot CML scenario considered in this paper, where the server receives some biased proxy data from unreliable clients and performs server-side defense. Icons are obtained from [22].\", \"page\": 2, \"index\": 6, \"width\": 529, \"height\": 241}]"
motivation: 单轮协作学习中由于缺乏迭代纠错机制，不可信客户端提供的偏见代理数据会严重损害全局模型的公平性。
method: 提出一种基于双层优化的服务端防御框架，利用少量可信根数据集动态学习客户端权重并实施公平性约束。
result: 实验表明该方法在显著提升模型公平性的同时保持了高精度，且在不可信客户端占多数的极端情况下依然稳健。
conclusion: 该防御框架为单轮协作学习提供了鲁棒的公平性保障，有效解决了不可信客户端带来的偏见风险。
---

## 摘要
协作机器学习（CML）允许多个客户端在数据分布式环境下共同训练全局模型。为了解决数据隐私和通信效率问题，单次（one-shot）CML 得到了越来越多的应用，其中客户端仅通过共享合成或处理后的代理数据与服务器进行一次通信。然而，这种单轮通信消除了服务器进行迭代修正的可能性，使得学习过程特别容易受到客户端不可信性的影响。在这种情况下，不可信的客户端（无论是恶意的还是非恶意的）可能会提供偏向某些群体的有偏代理数据，从而降低全局模型的公平性，并损害少数群体或弱势群体的利益。在这项工作中，我们提出了一个基于双层优化公式的服务器端防御框架。该方法通过学习客户端级别的权重来减轻有偏客户端代理数据的影响，同时利用服务器端可用的极小型可信根数据集来强制执行公平性约束。在基准数据集上的实验结果表明，在不可信客户端贡献有偏代理数据的情况下，我们的方法在几乎不损失准确性的前提下提高了公平性。此外，即使在不可信客户端占据系统多数的情况下，所提方法依然有效，且始终优于现有的其他方法。

## Abstract
Collaborative machine learning (CML) enables multiple clients to train a global model jointly in a data-distributed setting. To address data privacy and communication efficiency, one-shot CML has been increasingly adopted, where clients communicate with the server only once by sharing synthetic or processed proxy data. This single-round communication, however, eliminates the possibility of iterative correction at the server, making the learning process particularly vulnerable to client unreliability. In this setting, unreliable clients, whether malicious or non-malicious, may provide biased proxy data that favors certain groups, thereby degrading the fairness of the global model and harming minority or unprivileged groups. In this work, we propose a server-side defense framework based on a bilevel optimization formulation. The proposed approach learns client-level weights to mitigate the influence of biased client proxy data while enforcing fairness constraints by using a very small trusted root dataset available at the server. Experimental results on benchmark datasets show that our method improves fairness with little accuracy loss under biased proxy data contributions from unreliable clients. Moreover, the proposed approach remains effective even when unreliable clients make up a majority of the system, consistently outperforming other existing methods.

---

## 论文详细总结（自动生成）

### 论文总结：单次公平协作机器学习中针对不可信客户端的鲁棒服务器防御

#### 1. 核心问题与研究动机
*   **研究背景**：协作机器学习（CML）通常需要多轮通信，而“单次（One-Shot）CML”通过让客户端仅发送一次代理数据（如合成数据或蒸馏数据）来提升通信效率和隐私保护。
*   **核心痛点**：由于只有一轮通信，服务器失去了在后续迭代中纠正客户端偏差的机会。
*   **研究动机**：不可信客户端（包括恶意攻击者或非恶意的低质量数据提供者）可能提供带有严重偏见的代理数据。这会导致全局模型在公平性指标上大幅退化，损害少数群体的利益。现有的鲁棒聚合方法大多依赖多轮迭代或假设恶意客户端是少数，难以直接应用于单次 CML 场景。

#### 2. 方法论
*   **核心思想**：提出一个基于**双层优化（Bilevel Optimization）**的服务端防御框架，通过学习客户端级别的聚合权重来过滤偏见贡献。
*   **关键技术细节**：
    *   **双层架构**：外层优化（Upper-level）负责在服务器端学习客户端权重 $w$，目标是最小化在少量可信“根数据集”上的损失并满足公平性约束；内层优化（Lower-level）负责根据当前权重在聚合的代理数据上训练全局模型参数 $\theta$。
    *   **公平性建模**：引入决策边界协方差（Decision Boundary Covariance, DBC）作为统计差异（SP）和机会均等（EO）的凸近似约束。
    *   **隐式微分（Implicit Differentiation）**：为了求解双层优化，利用内层最优解的 KKT 条件，通过隐式函数定理计算外层目标对权重的梯度。
    *   **罚函数法与投影**：将约束问题转化为罚函数形式，并使用高效的排序算法将权重投影回标准单纯形（Simplex），确保权重之和为 1 且非负。

#### 3. 实验设计
*   **数据集**：
    *   **Law School**：预测学生是否通过律师资格考试（敏感属性：种族）。
    *   **Dutch**：预测个人是否拥有高声望职业（敏感属性：性别）。
*   **实验场景**：模拟了不可信客户端占比分别为 20%、40% 和 60% 的情况。
*   **Benchmark 与对比方法**：
    *   **Baseline**：等权重聚合所有客户端数据。
    *   **FedASL**：一种基于损失分布中位数和标准差的鲁棒加权聚合方法。
    *   **FedNolowe**：一种基于归一化损失的加权聚合策略。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **算法实现细节**：内层优化使用 L-BFGS 求解器（最大 1000 次迭代），外层优化使用 Adam 优化器（2000 次迭代），正则化参数 $\lambda_\theta$ 设为 $10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个数据集，分别在 SP 和 EO 两种公平性准则下进行了测试，涵盖了从少数（20%）到多数（60%）不可信客户端的极端场景。
*   **充分性评价**：实验设计较为充分。每组实验均重复 5 次并取平均值，通过“理想场景”和“现实场景”两种设置验证了权重的分配逻辑。对比了现有的鲁棒联邦学习算法，证明了在单次通信约束下的优越性。

#### 6. 主要结论与发现
*   **公平性显著提升**：在几乎不损失准确率的情况下，该方法能将偏见（如 SPD 指标）降低 60% 以上。
*   **极端环境下的鲁棒性**：当不可信客户端占多数（60%）时，传统基于统计学（如中位数）的防御方法（如 FedASL）会失效，而本方法依然能通过“根数据集”引导，精准地将不可信客户端的权重降至接近 0。
*   **权重学习的有效性**：实验证明，系统能够自动识别并下调那些提供高偏见数据的客户端权重。

#### 7. 优点
*   **突破传统假设**：解决了“恶意客户端必须是少数”才能防御的局限性。
*   **单次通信友好**：专门为 One-Shot 场景设计，无需多轮交互，符合低带宽和高隐私需求。
*   **数学框架严谨**：采用双层优化理论，相比启发式规则具有更好的收敛性和解释性。

#### 8. 不足与局限
*   **根数据集依赖**：防御效果高度依赖于服务器端持有的 0.5% 可信根数据集。如果该数据集本身有偏或规模过小，防御性能可能会下降。
*   **计算复杂度**：双层优化涉及 Hessian 矩阵的相关运算，虽然使用了隐式微分，但计算开销仍高于简单的加权平均。
*   **模型局限性**：实验主要集中在逻辑回归等线性分类器上，对于深度神经网络（DNN）在大规模数据上的扩展性和二阶导数计算压力尚待进一步验证。

（完）
