---
title: "DisAgg: Distributed Aggregators for Efficient Secure Aggregation in Federated Learning"
title_zh: DisAgg：联邦学习中高效安全聚合的分布式聚合器
authors: "Haaris Mehmood, Giorgos Tatsis, Dimitrios Alexopoulos, Karthikeyan Saravanan, Jie Xu, Anastasios Drosou, Mete Ozay"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13708v1"
tags: ["query:daily"]
score: 10.0
evidence: 联邦学习中的安全聚合，保护客户端更新不被服务器获取
tldr: 联邦学习中的安全聚合常面临通信轮数多、计算开销大及客户端掉线难处理等挑战。本文提出DisAgg协议，通过引入由少量客户端组成的聚合器委员会，利用秘密共享机制实现分布式聚合。该方法无需复杂的同态加密或本地掩码，在保证隐私的同时显著降低了计算负担，在10万级客户端规模下比现有最优协议OPA快4.6倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13708v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of the SECAGG protocol compared with the proposed DISAGG. SECAGG uses masks for the models to hide individual inputs and the server aggregates the masked models, whereas DISAGG secret-shares part of the model parameters to the Aggregators for them to perform the partial aggregation instead of the server.\", \"page\": 2, \"index\": 1, \"width\": 394, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2605.13708v1/fig-002.webp\", \"caption\": \"Table 1. A comparison of computational and communication time complexity across different protocols. N is the number of selected clients for training, A is the size of the Aggregators group, M is the model size. SECAGG is reported from (Bonawitz et al., 2017), SECAGG+/LIGHTSECAGG from (So et al., 2022) and OPA from (Karthikeyan & Polychroniadou, 2024).\", \"page\": 7, \"index\": 2, \"width\": 1004, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2605.13708v1/fig-003.webp\", \"caption\": \"Figure 7. Speedup of DISAGG over OPA for one FL iteration with M = 10k, N = 10k, γ = 0.1, δ = 0.2, and varying committee size A. DISAGG achieves 3× speedup including the setup phase (top) and 3.1× without it (bottom).\", \"page\": 10, \"index\": 3, \"width\": 423, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2605.13708v1/fig-004.webp\", \"caption\": \"Figure 2. High-level overview of the proposed secure aggregation protocol DISAGG. U & A denote the set of the clients and Aggregators respectively, x̂i denotes the secret (model update) for the ith client. PKE stands for the public-key-encryption protocol.\", \"page\": 4, \"index\": 4, \"width\": 485, \"height\": 579}, {\"url\": \"assets/figures/arxiv/2605.13708v1/fig-005.webp\", \"caption\": \"Figure 9. Empirical verification for convergence analyses of DISAGG compared to plain-text under FL\", \"page\": 12, \"index\": 5, \"width\": 469, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2605.13708v1/fig-006.webp\", \"caption\": \"Figure 18. Time comparison for one FL iteration of DISAGG and OPA with (60% 5G, 40% 4G) clients on the left, (60% 5G, 40% 3G) on the right and a shared y-axis for each row. The top row includes the setup times while the bottom row excludes it. DisAgg is within ±30% of OPA timings under such conditions.\", \"page\": 20, \"index\": 6, \"width\": 450, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2605.13708v1/fig-007.webp\", \"caption\": \"Figure 16. Training accuracy with different datasets and model using plaintext FL. Q denotes the use of quantization required for cryptographic primitives in secure aggregation. F denotes floating point precision as used in standard FL.\", \"page\": 20, \"index\": 7, \"width\": 972, \"height\": 212}]"
motivation: 现有的安全聚合方案在处理大规模客户端时存在通信轮数多、加密计算开销巨大以及难以应对客户端掉线的问题。
method: 提出一种分布式聚合协议，通过将客户端更新向量秘密共享给一组聚合器委员会，由其计算部分和后再由服务器重构。
result: 在包含10万个5G客户端和10万维向量的实验中，DisAgg相比于之前的最优协议OPA实现了4.6倍的加速。
conclusion: DisAgg通过在通信与计算成本之间取得最优权衡，为大规模联邦学习提供了一种高效且具备隐私保护能力的安全聚合方案。
---

## 摘要
联邦学习实现了跨分布式客户端的协作模型训练，但原始的联邦学习会将客户端更新暴露给中央服务器。安全聚合方案可以保护隐私免受诚实但好奇的服务器侵害，但现有方法通常面临通信轮次多、公钥操作繁重或难以处理客户端掉线等问题。最近的方法如单次隐私聚合（One-Shot Private Aggregation, OPA）将轮次缩减为每次联邦学习迭代仅需一次服务器交互，但它们给服务器和客户端都带来了巨大的密码学和计算开销。我们提出了一种名为 DisAgg 的新协议，该协议利用由一小部分客户端组成的委员会（称为聚合器）来执行聚合本身：每个客户端将其更新向量秘密共享给聚合器，聚合器在本地计算部分和，并仅返回聚合后的份额用于服务器端重构。这种设计消除了本地掩码和昂贵的同态加密，在降低端点计算量的同时，保护了隐私免受好奇服务器和有限比例共谋客户端的侵害。通过利用通信和计算成本之间的最佳权衡，DisAgg 在处理来自 10 万个 5G 客户端的 10 万维更新向量时，与之前的最优协议 OPA 相比，实现了 4.6 倍的加速。

## Abstract
Federated learning enables collaborative model training across distributed clients, yet vanilla FL exposes client updates to the central server. Secure-aggregation schemes protect privacy against an honest-but-curious server, but existing approaches often suffer from many communication rounds, heavy public-key operations, or difficulty handling client dropouts. Recent methods like One-Shot Private Aggregation (OPA) cut rounds to a single server interaction per FL iteration, yet they impose substantial cryptographic and computational overhead on both server and clients. We propose a new protocol called DisAgg that leverages a small committee of clients called Aggregators to perform the aggregation itself: each client secret-shares its update vector to Aggregators, which locally compute partial sums and return only aggregated shares for server-side reconstruction. This design eliminates local masking and expensive homomorphic encryption, reducing endpoint computation while preserving privacy against a curious server and a limited fraction of colluding clients. By leveraging optimal trade-offs between communication and computation costs, DisAgg processes 100k-dimensional update vectors from 100k 5G clients with a 4.6x speedup compared to OPA, the previous best protocol.

---

## 论文详细总结（自动生成）

这是一份关于论文《DisAgg: Distributed Aggregators for Efficient Secure Aggregation in Federated Learning》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在联邦学习（FL）中，保护客户端隐私的**安全聚合（Secure Aggregation, SA）**至关重要。然而，现有的 SA 协议面临三大挑战：
*   **通信轮数多：** 传统协议（如 SecAgg）需要多轮握手，容易受到网络延迟和同步屏障的影响。
*   **计算开销大：** 现代单次交互协议（如 OPA）虽然减少了轮数，但依赖复杂的密码学原语（如 LWE/LWR 掩码），导致客户端和服务器的计算负担沉重。
*   **掉线处理难：** 客户端掉线时，服务器往往需要额外的恢复步骤来移除掉线者的掩码。
**研究动机：** 开发一种既能保持“单次交互”优势，又能显著降低端点计算开销，且对客户端掉线具有鲁棒性的高效安全聚合协议。

### 2. 论文提出的方法论：DisAgg 协议
**核心思想：** 引入一个由少量客户端组成的**聚合器委员会（Aggregators）**。不再由服务器直接聚合掩码模型，而是由客户端将模型更新通过秘密共享分发给聚合器，由聚合器完成分布式求和。

**关键技术细节：**
*   **分布式聚合：** 客户端使用 **Lagrange Coded Computing (LCC)** 秘密共享技术将更新向量 $x_i$ 拆分为多个份额（Shares）。
*   **三阶段流程：**
    1.  **Round 0 (Setup)：** 服务器选择参与者并交换公钥。
    2.  **Round 1 (Sharing)：** 客户端训练模型，生成秘密份额，加密后发送给服务器，由服务器转发给选定的聚合器。
    3.  **Round 2 (Reconstruction)：** 聚合器在本地对收到的份额求和，并将部分和返回给服务器。服务器通过拉格朗日插值重构最终的全局聚合结果。
*   **安全性：** 只要共谋的聚合器数量不超过设定的阈值 $t_c$，服务器和恶意客户端就无法获知任何单个客户端的更新。

### 3. 实验设计
*   **数据集：** MNIST、CIFAR-10、CIFAR-100、SST-2（文本分类）、CelebA（人脸属性）。
*   **模型架构：** CNN、TinyNet、DistilBERT、EfficientNet。
*   **Benchmark 与对比方法：**
    *   **SecAgg+ / LightSecAgg：** 经典的基于掩码和稀疏图的协议。
    *   **OPA (One-Shot Private Aggregation)：** 之前的 SOTA（最先进）单次交互协议。
    *   **Plaintext：** 无安全保护的原始联邦学习（作为基准上限）。
*   **场景模拟：** 模拟了 5G、4G、3G 不同网络带宽下的表现，以及 1k 到 100k 不同规模的客户端群体。

### 4. 资源与算力
*   **硬件环境：** 
    *   **CPU：** Intel Xeon Gold 5220 @ 2.20GHz（服务器级）。
    *   **GPU：** NVIDIA GeForce RTX 2080 Ti (12GB VRAM)，用于模型训练实验。
    *   **内存：** 128GB RAM。
*   **训练时长：** 进行了 30 轮联邦学习迭代。
*   **并行度：** 使用多进程模拟大规模客户端（少于 50k 客户端用 16 进程，超过 50k 用 30 进程）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 1,000 到 100,000 个客户端的超大规模扩展性测试，以及模型维度从 1k 到 1M 的测试。
*   **维度丰富：** 
    *   **端到端训练实验：** 验证了 DisAgg 不损失模型精度。
    *   **消融/参数分析：** 详细分析了聚合器数量 $A$、打包因子 $\rho$ 对速度的影响。
    *   **鲁棒性测试：** 模拟了不同比例的掉线（Dropout）和共谋（Collusion）。
*   **评价：** 实验设计非常充分且客观。作者不仅提供了实测数据，还建立了一个基于复杂度的理论时间框架，在不同网络环境下验证了协议的普适性。

### 6. 论文的主要结论与发现
*   **显著加速：** 在 10 万客户端、10 万维模型的 5G 环境下，DisAgg 比 OPA 快 **4.6 倍**。
*   **计算效率：** DisAgg 极大地降低了客户端和服务器的计算压力，因为它避开了昂贵的同态加密或大规模矩阵乘法。
*   **精度无损：** 实验证明 DisAgg 的聚合结果与明文聚合完全一致，不影响模型收敛。
*   **掉线鲁棒性：** 协议能自然处理客户端掉线，只要存活的聚合器数量达到重构阈值即可。

### 7. 优点
*   **架构创新：** 将聚合任务从服务器“下放”到客户端委员会，巧妙地利用了分布式计算资源。
*   **低延迟：** 保持了单次交互（One-shot）的特性，消除了多轮同步带来的延迟。
*   **可扩展性极强：** 复杂度分析和实验均显示其在超大规模（100k 级别）设备上的表现优于现有所有协议。
*   **理论完备：** 提供了详细的拜占庭容错分析和秘密共享阈值选择算法。

### 8. 不足与局限
*   **聚合器负担：** 虽然减轻了普通客户端的压力，但**聚合器（Aggregators）**需要下载大量份额数据。在模型极大（如数亿参数）时，聚合器的下行带宽可能成为瓶颈。
*   **网络依赖：** 实验显示在 3G 等极慢网络下，DisAgg 的优势会缩小甚至被 OPA 反超（因为 DisAgg 的通信量相对较大）。
*   **安全性假设：** 依赖于“诚实但好奇”的服务器模型，虽然讨论了拜占庭容错，但在极端恶意环境下的防御机制（如可验证计算）未深入展开。

（完）
