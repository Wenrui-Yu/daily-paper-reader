---
title: "FedHPro: Federated Hyper-Prototype Learning via Gradient Matching"
title_zh: FedHPro：基于梯度匹配的联邦超原型学习
authors: "Huan Wang, Jun Shen, Haoran Li, Zhenyu Yang, Jun Yan, Ousman Manjang, Yanlong Zhai, Di Wu, Guansong Pang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13475v1"
tags: ["query:daily"]
score: 9.0
evidence: 联邦学习在保护隐私的同时实现协作训练
tldr: 联邦学习中基于原型的模型常因局部原型聚合导致语义漂移。本文提出FedHPro框架，引入可学习的全局“超原型”，通过梯度匹配直接对齐客户端真实样本的类别特征，而非仅在原型层面聚合。结合客户端特定边际的对比学习和一致性惩罚，该方法显著增强了类间分离度和类内均匀性，在多个异构场景下达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13475v1/fig-001.webp\", \"caption\": \"Figure 2. Top: The L2 distance of centralized prototypes calculated by centralized training using all clients’ samples to global prototypes from FedAvg (McMahan et al., 2017) and FedProto (Tan et al., 2022a), and from our hyper-prototypes. Bottom: The corresponding accuracy on the Digits (Peng et al., 2019) dataset, ‘Using HP’ means replacing the global prototypes with our hyperprototypes in the FedProto’s loss function.\", \"page\": 2, \"index\": 1, \"width\": 418, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.13475v1/fig-002.webp\", \"caption\": \"Table 1. Comparison results under label and quantity skew. † denotes the results obtained by exchanging both the prototypes and model parameters (please see analysis in Appendix Table A7) during the FL training. Best results are in red bold, with second best in blue bold.\", \"page\": 7, \"index\": 2, \"width\": 999, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.13475v1/fig-003.webp\", \"caption\": \"Figure 9. Comparison of convergence in the Average Accuracy Trend on the Digits (Left) and Office-Caltech (Right). We also provide quantitative results for the convergence rates in Table A4.\", \"page\": 9, \"index\": 3, \"width\": 786, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.13475v1/fig-004.webp\", \"caption\": \"Figure 5. Framework illustration of Federated Hyper-Prototype Learning (FedHPro). The clients upload the gradients {g1, ...,gk}\", \"page\": 5, \"index\": 4, \"width\": 975, \"height\": 288}]"
motivation: 现有原型联邦学习方法通过平均局部原型来更新全局信号，容易导致客户端间的语义漂移和信号失准。
method: 提出FedHPro框架，利用梯度匹配优化全局超原型，并结合带边际的互对比学习与一致性惩罚来对齐特征。
result: 在多种异构基准数据集上的实验证明，FedHPro在性能上优于现有主流联邦学习算法。
conclusion: 超原型能产生更具语义一致性的全局信号，有效解决了联邦学习中的非独立同分布数据挑战。
---

## 摘要
联邦学习（FL）在保护隐私的同时，实现了分布式客户端的协作训练。为了增强联邦学习的泛化能力，基于原型的联邦学习备受关注，因为共享的全局原型为对齐客户端特定的本地原型提供了语义锚点。然而，现有方法在原型层面通过平均本地原型或细化全局锚点来更新全局原型，这往往会导致客户端之间的语义漂移，并随后产生失配的全局信号。为了缓解这一问题，我们引入了超原型（hyper-prototypes），它由一组可学习的全局类级原型定义，旨在保留客户端之间潜在的语义知识。超原型通过梯度匹配进行优化，以与直接从客户端真实样本中提取的类相关特征相对齐，而非原型层面的描述符。我们进一步提出了 FedHPro，一个联邦超原型学习框架，利用超原型通过带有客户端特定间隔的互对比学习来提升类间可分性，同时通过一致性惩罚鼓励类内均匀性。在多种异构场景下的全面实验证实：1）超原型产生了语义更一致的全局信号；2）FedHPro 在多个基准数据集上达到了最先进的性能。代码可在 https://github.com/mala-lab/FedHPro 获取。

## Abstract
Federated Learning (FL) enables collaborative training of distributed clients while protecting privacy. To enhance generalization capability in FL, prototype-based FL is in the spotlight, since shared global prototypes offer semantic anchors for aligning client-specific local prototypes. However, existing methods update global prototypes at the prototype-level via averaging local prototypes or refining global anchors, which often leads to semantic drift across clients and subsequently yields a misaligned global signal. To alleviate this issue, we introduce hyper-prototypes, defined by a set of learnable global class-wise prototypes to preserve underlying semantic knowledge across clients. The hyper-prototypes are optimized via gradient matching to align with class-relevant characteristics distilled directly from clients' real samples, rather than prototype-level descriptors. We further propose FedHPro, a Federated Hyper-Prototype Learning framework, to leverage hyper-prototypes to promote inter-class separability via mutual-contrastive learning with client-specific margin, while encouraging intra-class uniformity through a consistency penalty. Comprehensive experiments under diverse heterogeneous scenarios confirm that 1) hyper-prototypes produce a more semantically consistent global signal, and 2) FedHPro achieves state-of-the-art performance on several benchmark datasets. Code is available at \href{https://github.com/mala-lab/FedHPro}{https://github.com/mala-lab/FedHPro}.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《FedHPro: Federated Hyper-Prototype Learning via Gradient Matching》** 的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在异构联邦学习（FL）中，数据非独立同分布（Non-IID）会导致客户端局部模型目标不一致。现有的“基于原型”的方法（如 FedProto）通常通过简单平均局部原型来生成全局原型，但这会引发**语义漂移（Semantic Drift）**。因为局部原型本身源于偏置的局部数据，直接聚合会导致全局信号失准，难以有效指导局部训练。
*   **研究动机**：作者认为，如果能模拟“中心化训练”的特征空间，就能获得更具语义一致性的原型。因此，研究目标是寻找一种不泄露隐私、但能直接从客户端真实样本特征中提取全局一致性信号的方法。

### 2. 方法论：FedHPro 框架
FedHPro 的核心思想是放弃简单的原型平均，转而使用**梯度匹配**来优化一组可学习的全局“超原型”。

*   **超原型（Hyper-Prototypes）与梯度匹配（GM）**：
    *   服务器维护一组可学习的向量（超原型）。
    *   客户端计算真实样本在特征层面的平均梯度并上传。
    *   服务器通过最小化“超原型在虚拟损失下的梯度”与“客户端真实样本梯度”之间的余弦距离（$L_{GM}$），来更新超原型。这使得超原型能够捕捉到真实数据的分布轨迹，而非仅仅是偏置的特征均值。
*   **超原型对比学习（HPCL）**：
    *   在局部训练时，引入**客户端特定边际（Client-specific Margin）**。通过计算局部类间距离动态调整边际，拉近样本与对应超原型的距离，推开与其他类超原型的距离，增强类间分离度。
*   **超原型对齐学习（HPAL）**：
    *   使用平滑的 $L_1$ 损失（Smooth Regularization）将样本特征直接向超原型对齐，减少特征层面的偏差，确保类内均匀性。

### 3. 实验设计
*   **数据集与场景**：
    *   **标签偏移（Label Skew）**：CIFAR10/100, HAM10000, TinyImageNet（使用 Dirichlet 分布划分）。
    *   **数量偏移（Quantity Skew）**：CIFAR10/100-LT, TinyImageNet-LT（长尾分布）。
    *   **领域偏移（Domain Skew）**：Digits（MNIST, USPS, SVHN, SYN）, Office-Caltech。
*   **Benchmark（对比方法）**：
    *   传统 FL：FedAvg, FedProx。
    *   对比学习 FL：MOON, FedRCL。
    *   原型学习 FL：FedProto, FedTGP, FedGMKD, FedSA（2025年最新方法）。
*   **模型架构**：ResNet-10, MobileNetV2。

### 4. 资源与算力
*   **硬件**：实验明确提到使用了 **NVIDIA RTX A5000 GPU**。
*   **软件/参数**：全局通信轮数 $R=100$，局部迭代轮数 $E=10$，超原型优化轮数 $M=30$。
*   **开销说明**：文中指出 FedHPro 的通信开销与 FedAvg 相当（仅额外传输了平均梯度），计算开销（FLOPs）略高于 FedAvg，但与 SOTA 方法（如 FedSA）处于同一水平，训练时间在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 8 个数据集、3 种主要的异构场景。
*   **消融实验**：分别验证了 HPCL、HPAL 以及梯度匹配模块的有效性。
*   **鲁棒性分析**：测试了不同客户端数量（$K=10$ 到 $100$）、不同局部迭代轮数、不同特征维度（$d=64$ 到 $1024$）的影响。
*   **扩展性测试**：额外验证了**模型异构性**（不同客户端使用不同网络架构）和**文本模态**（AG News 数据集）下的表现。
*   **评价**：实验设计非常充分且客观，通过引入“†”标记（即为对比方法提供更强的参数交换条件）确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：FedHPro 在所有测试场景下均达到了 SOTA 性能，尤其在复杂的领域偏移（Office-Caltech）中提升显著（比次优方法高出约 4%）。
*   **语义一致性**：可视化证明，超原型比传统聚合原型更接近中心化训练生成的原型，能有效缓解语义漂移。
*   **收敛性**：理论证明了 FedHPro 在非凸情况下的收敛性，实验显示其收敛速度快于大多数基准方法。

### 7. 优点
*   **创新性强**：首次将梯度匹配引入联邦原型学习，从优化轨迹的角度解决语义对齐问题。
*   **即插即用**：实验证明超原型可以作为组件替换现有方法（如 FedProto）中的全局原型，并直接提升其性能。
*   **适应性广**：不仅能处理数据异构，还能在模型架构异构（不同客户端用不同模型）的情况下保持稳健。

### 8. 不足与局限
*   **超参数依赖**：超原型的长度 $|I|$ 和优化轮数 $M$ 需要手动调优，虽然文中给出了推荐值，但在极端新场景下可能需要重新搜索。
*   **计算成本**：虽然文中称开销适中，但服务器端在每一轮都需要进行 $M$ 轮的梯度匹配优化，对于资源极度受限的服务器可能存在压力。
*   **隐私边界**：虽然梯度匹配不直接传输原始数据，但上传类级平均梯度在极高安全要求的场景下是否会增加成员推理攻击的风险，文中未做深入的安全性量化分析。

（完）
