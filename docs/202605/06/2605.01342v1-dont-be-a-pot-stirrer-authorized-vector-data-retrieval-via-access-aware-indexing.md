---
title: "Don't Be a Pot Stirrer! Authorized Vector Data Retrieval via Access-Aware Indexing"
title_zh: 拒绝混淆！通过访问感知索引实现授权向量数据检索
authors: "Shanshan Han, Vishal Chakraborty, Sharad Mehrotra"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01342v1"
tags: ["query:daily"]
score: 8.0
evidence: 授权向量数据检索与访问控制
tldr: 针对向量数据库中基于角色的访问控制（RBAC）难题，本文提出了Veda及其高效变体EffVeda索引策略。传统方法在查询效率与存储开销之间难以平衡，而新方法通过构建访问感知格结构，将数据集划分为不相交的数据块，并在存储预算内进行合并与索引。查询时采用协调搜索机制，利用授权节点的距离边界剪枝非授权区域，在保证安全性的同时显著提升了检索效率和召回率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01342v1/fig-001.webp\", \"caption\": \"Figure 11: Supplementary results of Exp 10: QPS vs. efs.\", \"page\": 21, \"index\": 1, \"width\": 506, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-002.webp\", \"caption\": \"Figure 3: Illustration of copy operations of EffVeda.\", \"page\": 7, \"index\": 2, \"width\": 506, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-003.webp\", \"caption\": \"Figure 1: Indexing strategies over exclusive access-control blocks. Each dotted box marks a group 𝐺 covered by a single index.\", \"page\": 3, \"index\": 3, \"width\": 1010, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-004.webp\", \"caption\": \"Figure 8: Multi-role query evaluations.\", \"page\": 13, \"index\": 4, \"width\": 506, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-005.webp\", \"caption\": \"Figure 7: Additional dataset and workload evaluations.\", \"page\": 13, \"index\": 5, \"width\": 1056, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-006.webp\", \"caption\": \"Table 1: Frequently used notation.\", \"page\": 4, \"index\": 6, \"width\": 446, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-007.webp\", \"caption\": \"Figure 5: Index creation evaluation.\", \"page\": 12, \"index\": 7, \"width\": 1056, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-008.webp\", \"caption\": \"Figure 6: SIFT-1M query evaluation.\", \"page\": 12, \"index\": 8, \"width\": 1056, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-009.webp\", \"caption\": \"Figure 4: Illustration of coordinated search.\", \"page\": 9, \"index\": 9, \"width\": 523, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2605.01342v1/fig-010.webp\", \"caption\": \"Figure 10: Base-layer sweep: search time as efs varies at fixed |idx|. The linear model (𝑅2=0.994) fits better than efs log efs (𝑅2=0.981), which over-predicts at large efs.\", \"page\": 16, \"index\": 10, \"width\": 382, \"height\": 327}]"
motivation: 现有的向量检索访问控制方法在处理权限过滤时，面临着搜索效率低下或存储空间过度冗余的权衡困境。
method: 利用访问感知格结构将数据划分为不相交块，根据存储预算合并节点并构建HNSW索引，配合协调搜索与剪枝策略进行查询。
result: 该方法通过最小化授权数据覆盖节点集和跨索引剪枝，在受限的存储预算下实现了高效的Top-k近似最近邻搜索。
conclusion: Veda系列策略为向量数据库提供了一种灵活且高性能的访问控制索引方案，有效平衡了安全性、存储成本与检索性能。
---

## 摘要
向量数据库越来越多地实施基于角色的访问控制：每个 top-k 近似最近邻查询必须仅返回查询角色有权访问的向量。设计空间由两个极端情况界定。单个全局索引避免了数据重复，但在未授权向量上浪费了搜索开销并降低了召回率；而理想索引（oracle index）基于查询角色的所有授权向量构建，仅搜索授权向量，但会重复存储角色或查询之间共享的每个向量。我们提出了 Veda 及其高效变体 EffVeda，这是两种基于访问感知格（access-aware lattice）构建的索引策略，旨在解决向量数据库中的访问控制问题。该方法首先根据角色组合将数据集划分为不相交的数据块，然后利用访问感知格的结构，在用户指定的存储预算下应用复制和合并操作，对共同访问的块进行分组。格中的大节点随后使用 HNSW 进行索引，而小节点则保留用于线性扫描。对于每个角色，我们的方法会构建一个查询计划，选择覆盖该角色授权数据的最小节点集。在查询时，协调搜索首先查询纯净（仅限授权）节点以填充全局 top-k 堆。由此产生的距离边界随后会剪枝对非纯净节点的探索，从而避免了独立执行每个索引所导致的搜索开销膨胀。

## Abstract
Vector databases increasingly enforce role-based access control: each top-k approximate nearest neighbor query must return only vectors the querying role is authorized to access. Two extremes bracket the design space. A single global index avoids data duplication but wastes search effort on unauthorized vectors and degrades recall, while an oracle index, built with all authorized vectors of the query roles, searches only authorized vectors but duplicates every shared vector between roles or queries. We present Veda and its efficient variant EffVeda, two indexing strategies built on an access-aware lattice to address access control in vector databases. The methods first partitions the dataset into disjoint data blocks by role combination, then leverage the structure of the access-aware lattice to apply copy and merge operations to group co-accessed blocks under a user-specified storage budget. Large nodes in the lattice are then indexed with HNSW, while small nodes are retained for linear scan. For each role, our methods construct a query plan that selects the minimal set of nodes that covers the role's authorized data. At query time, coordinated search first queries pure (authorized-only) nodes to populate a global top-k heap. The resulting distance bound then prunes exploration on impure nodes, avoiding the inflated search that independent per-index execution would require.

---

## 论文详细总结（自动生成）

这是一份关于论文《Don't Be a Pot Stirrer! Authorized Vector Data Retrieval via Access-Aware Indexing》的结构化深入总结：

### 1. 论文的核心问题与整体含义
随着向量数据库在语义搜索和检索增强生成（RAG）中的广泛应用，如何在检索层强制执行**基于角色的访问控制（RBAC）**成为关键挑战。
*   **研究动机**：现有的两种极端方案均不理想。**全局索引（Global Index）**存储开销小但查询时需过滤大量未授权数据，导致召回率下降和计算浪费；**理想索引（Oracle Index）**为每个角色构建专属索引，查询极快但会导致严重的数据存储冗余。
*   **核心问题**：如何在给定的存储预算（Storage Amplification, SA）下，最小化查询成本（Query Amplification, QA），同时保证高召回率。

### 2. 论文提出的方法论
论文提出了 **Veda** 及其高效变体 **EffVeda**，核心思想是利用数据块的权限重叠特性进行优化。

*   **访问感知格（Access-Aware Lattice）**：
    *   将数据集划分为不相交的“排他块”（Exclusive Blocks），每个块对应唯一的角色组合。
    *   构建一个有向无环图（格结构），节点代表数据组，边代表权限的包含关系。
*   **索引构建策略（Copy & Merge）**：
    *   **Copy（复制）**：将子节点数据复制到父节点。优点是保持节点“纯净”（仅含授权数据），缺点是增加存储开销。
    *   **Merge（合并）**：将多个块合并为一个索引。优点是节省存储，缺点是引入“杂质”（包含未授权数据），查询时需膨胀搜索范围。
    *   **Veda** 采用贪心算法，根据“单位存储收益”选择操作；**EffVeda** 采用自底向上的启发式搜索，大幅提升构建速度。
*   **索引选择与查询计划**：
    *   对于每个角色，通过求解最小覆盖问题，选择一组最少的索引节点来覆盖其所有授权数据。
    *   大于阈值 $\Lambda$ 的节点构建 HNSW 索引，小的节点保留用于线性扫描。
*   **协调搜索（Coordinated Search）**：
    *   **阶段1**：先搜索“纯净”索引和线性扫描块，获取初步的 Top-k 结果及距离边界。
    *   **阶段2**：利用该边界对“非纯净”索引进行剪枝。如果非纯净索引的局部结果无法击败全局边界，则停止搜索，从而避免了盲目的搜索范围膨胀。

### 3. 实验设计
*   **数据集**：
    *   **SIFT-1M**（图像向量，128维）
    *   **PAPER**（文本向量，200维）
    *   **AMZN**（亚马逊评论文本，384维）
*   **Benchmark 与对比方法**：
    *   **基准**：Global Index（全局索引）、Oracle Index（理想索引）。
    *   **先进方法**：ACORN（过滤式图索引）、SIEVE（索引集合选择）、HoneyBee（动态分区）。
*   **评价指标**：存储放大率（SA）、查询放大率（QA）、每秒查询数（QPS）、召回率（Recall@10）。

### 4. 资源与算力
*   **硬件环境**：macOS 26.3 (Darwin 25.3.0)，Apple M4 Max 处理器（14核），36 GiB 统一内存。
*   **训练/构建时长**：论文对比了构建时间，EffVeda 的构建速度显著快于 Veda 和 HoneyBee，在 SIFT-1M 数据集上，分区时间通常在数百秒量级。
*   **算力说明**：实验主要在单机 CPU 环境下完成，未涉及大规模 GPU 集群训练，符合向量数据库索引构建的典型场景。

### 5. 实验数量与充分性
*   **实验规模**：论文共设计了 14 组主要实验（Exp 1 - Exp 14），涵盖了索引构建时间、存储分布、索引数量、QA/SA 权衡、QPS 与召回率曲线、查询敏感度、多角色查询等多个维度。
*   **充分性评价**：实验设计非常充分。不仅对比了不同数据集，还深入分析了参数（如索引阈值 $\Lambda$、搜索参数 $efs$）的影响。通过引入“协调搜索”的消融分析，证明了该机制在减少无效搜索方面的有效性。对比方法涵盖了最新的顶会论文（如 2025 年的 SIEVE 和 HoneyBee），具有很强的时效性和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：在相同的存储预算下，Veda 和 EffVeda 的查询效率（QPS）显著优于现有的分区方法（HoneyBee, SIEVE）和过滤方法（ACORN）。
*   **存储效率**：仅需少量存储放大（如 SA=1.1），即可获得接近 Oracle 索引的查询性能。
*   **协调搜索的威力**：协调搜索机制在超过 67% 的情况下能成功跳过非纯净索引的二次搜索，显著降低了查询延迟。
*   **适用场景**：分区策略在处理高选择性查询（授权数据占比较小）时优势巨大；当授权范围覆盖全表 80% 以上时，回退到全局过滤索引更为经济。

### 7. 优点
*   **精细化建模**：通过访问感知格精确刻画了 RBAC 的权限重叠，比简单的分区更科学。
*   **软硬结合**：不仅优化了索引结构（离线），还提出了协调搜索算法（在线），双管齐下解决杂质数据导致的性能下降。
*   **灵活性**：提供了 Veda（追求极致性能）和 EffVeda（追求构建速度）两种变体，适应不同规模的策略更新需求。

### 8. 不足与局限
*   **动态性挑战**：虽然附录提到了动态更新的思路，但实验主要基于静态数据集。在权限频繁变动或数据高频增删的场景下，格结构的维护成本和索引重构频率尚需进一步验证。
*   **多角色查询的复杂性**：虽然实验涉及了多角色查询，但当用户拥有极多角色导致授权范围极广时，维护多个子索引的开销可能会抵消其带来的收益。
*   **硬件依赖性**：成本模型 $C_{\theta}$ 需要针对特定硬件进行校准，虽然这提高了准确性，但也增加了部署时的调优工作量。

（完）
