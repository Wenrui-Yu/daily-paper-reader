---
title: Conditional Imputation for Within-Modality Missingness in Multi-Modal Federated Learning
title_zh: 多模态联邦学习中模态内缺失的条件插补
authors: "Wugeng Zheng, Ziwen Kan, Katie Wang, Chen Chen, Song Wang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23112v1"
tags: ["query:daily"]
score: 9.0
evidence: 多模态联邦学习中的隐私保护协作训练
tldr: 本研究针对多模态联邦学习中常见的模态内数据缺失问题，提出了CondI框架。该框架利用条件扩散模型显式地填补缺失的原始数据，通过两阶段训练流程（先填补后优化特征提取器）来恢复完整的语义结构。在三个临床数据集上的实验证明，CondI在处理严重数据缺失时具有显著的鲁棒性，性能优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23112v1/fig-001.webp\", \"caption\": \"Table 3. Classification performance for the MIMIC-IV dataset across different tasks under varying missing-data configurations.\", \"page\": 8, \"index\": 1, \"width\": 446, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.23112v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of the proposed CondI framework. (a) Global Federated Workflow. The server coordinates the distribution and aggregation of global model parameters Θ and prompt embeddings W across clients via FedAvg. (b) Local Two-Phase Pipeline & Inference. Inside each client, the process is structurally decoupled. In Phase A, the missing pattern (Wcond) is extracted to explicitly guide the diffusion model in imputing missing data. In Phase B, the model optimizes the modality profile (Wmod) and trains the instance encoder. During the inference stage (dotted line), the explicitly imputed data is fed back into the encoder to extract refined instance features (Wins), which are then concatenated with Wmod and Wcond for downstream task prediction.\", \"page\": 4, \"index\": 2, \"width\": 882, \"height\": 389}]"
motivation: 现有的多模态联邦学习方法在处理模态内数据缺失时，往往无法准确恢复真实的数据分布，导致模型性能受限。
method: 提出CondI框架，利用条件扩散模型结合多模态上下文进行显式数据填补，并采用两阶段训练策略优化特征提取和联合嵌入空间。
result: 在PTB-XL、SLEEP-EDF和MIMIC-IV三个临床数据集上的实验表明，CondI在应对严重数据缺失时表现出色，达到了最先进的水平。
conclusion: 显式的数据填补能确保模型在完整的语义结构上运行，显著增强了多模态联邦学习对数据不完整性的抵抗力。
---

## 摘要
多模态联邦学习（MMFL）能够实现保护隐私的协作训练，但在实际临床应用中，由于传感器间歇性或不规则采样，经常会出现模态内数据缺失的问题。现有方法通过架构对齐或缺失嵌入来隐式表示未观测数据，通常无法恢复真实分布，导致性能欠佳。我们提出了 CondI，这是一个利用条件扩散模型显式解决此类缺失问题的联邦框架。CondI 采用两阶段训练流水线：首先，利用可用的多模态上下文和条件嵌入来插补未观测的时间分量；其次，优化特定模态的提取器和联合嵌入空间。在推理过程中，插补后的原始数据通过训练好的提取器生成鲁棒特征，为下游任务提供整体表示。显式数据插补确保模型在完整的语义结构上运行，显著增强了对严重数据不完整性的抵御能力。在三个临床数据集（PTB-XL、SLEEP-EDF、MIMIC-IV）上的实验表明，CondI 取得了与最先进基准模型相当的结果。代码：https://github.com/ZhengWugeng/CondI

## Abstract
Multimodal Federated Learning (MMFL) enables privacy-preserving collaborative training, but real-world clinical applications often suffer from within-modality missingness caused by sensor intermittency or irregular sampling. Existing methods implicitly represent unobserved data via architectural alignment or missing embeddings, often failing to recover the true distribution and yielding sub-optimal performance. We propose CondI, a federated framework explicitly addressing this missingness using conditional diffusion models. CondI employs a two-phase training pipeline: first, imputing unobserved temporal components using available multimodal context and conditional embeddings; second, optimizing modality-specific extractors and joint embedding spaces. During inference, imputed raw data pass through trained extractors to generate robust features, providing a holistic representation for downstream tasks. Explicit data imputation ensures models operate on complete semantic structures, significantly enhancing resilience against severe data incompleteness. Experiments on three clinical datasets (PTB-XL, SLEEP-EDF, MIMIC-IV) demonstrate CondI achieves comparable results to state-of-the-art baselines. Code: https://github.com/ZhengWugeng/CondI

---

## 论文详细总结（自动生成）

这篇论文针对多模态联邦学习（MMFL）中细粒度的数据缺失问题提出了创新的解决方案。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实临床场景（如ICU监测）中，多模态数据不仅存在**模态级缺失**（整个检查缺失），更普遍存在**模态内缺失**（Within-Modality Missingness）。这种缺失由传感器间歇性脱落或采样频率不一致导致，破坏了生理信号的时间连续性。
*   **研究动机**：现有的联邦学习方法多采用“隐式”处理方式（如使用占位符嵌入或简单的特征对齐），这无法恢复真实的生理信号分布，导致模型在严重缺失情况下语义模糊、性能大幅下降。作者旨在通过“显式”的数据插补（Imputation）来重建完整的语义结构。

### 2. 方法论：CondI 框架
CondI 的核心思想是利用**条件扩散模型（Conditional Diffusion Models）**在保护隐私的前提下，跨客户端协作学习如何恢复缺失的原始信号。

*   **两阶段训练流水线**：
    *   **阶段 A（条件估计）**：训练一个条件扩散模型。它引入了**跨模态条件嵌入 ($W_{cond}$)**，该嵌入能够捕捉不同传感器之间的关联规则（例如血压下降与心率上升的相关性）。模型利用现有的观测数据作为上下文，指导扩散模型生成缺失的时间段。
    *   **阶段 B（任务优化）**：在插补的基础上，优化特定模态的特征提取器和分类器。引入了**模态特定嵌入 ($W_{mod}$)** 和 **实例特定嵌入 ($W_{ins}$)**，将结构化先验与动态特征结合。
*   **关键技术细节**：
    *   **混合专家系统（MoE）**：在扩散模型中用于动态路由和提取观测到的上下文特征。
    *   **联邦聚合**：服务器通过 FedAvg 算法聚合各客户端的全局参数 $\Theta$ 和 Prompt 嵌入矩阵 $W$，实现跨机构的知识共享，即使某些客户端完全缺失某类传感器，也能从其他客户端学习到插补逻辑。
    *   **推理流程**：推理时先进行显式插补，再将“完整”的数据输入提取器，确保语义一致性。

### 3. 实验设计
*   **数据集**：
    1.  **PTB-XL**：大型心电图（ECG）诊断数据集（12导联视为12个模态）。
    2.  **SLEEP-EDF**：多导睡眠图记录，涉及EEG、EOG、EMG等5个模态。
    3.  **MIMIC-IV**：重症监护病房（ICU）电子健康记录，用于预测住院死亡率（48-IHM）和住院时长（48-LOS）。
*   **基准测试（Benchmark）**：设置了不同的样本缺失率 ($p_s$) 和模态内缺失率 ($p_w$)，涵盖从轻微到极端严重（80%缺失）的四种场景。
*   **对比方法**：包括经典的 **FedAvg**、针对多模态的 **FedMM**、以及目前最先进的（SOTA）处理缺失数据的联邦框架 **PEPSY**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了代码已开源，实验设置中包含了通信轮数（PTB-XL/SLEEP-EDF 为 70 轮，MIMIC-IV 为 30 轮），客户端参与率为 0.5。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 个不同类型的临床数据集上进行了测试，每个数据集包含 4 种缺失配置，总计至少 12 组核心对比实验。
*   **消融实验**：专门设计了消融实验验证 $W_{cond}$ 模块和插补模块的必要性。
*   **特征重建分析**：通过 L2 距离和余弦相似度分析了 628 个测试样本，验证了插补后的特征确实比“补零”更接近真实完整数据。
*   **评价**：实验设计较为充分，涵盖了分类准确率、F1 分数、AUROC 等多维度指标，且对比了最新的 SOTA 方法，客观性较强。

### 6. 主要结论与发现
*   **性能领先**：CondI 在绝大多数缺失场景下均优于基准方法，尤其在极端缺失（80% 缺失）情况下，性能优势更明显（如在 PTB-XL 上比 PEPSY 高出约 3%）。
*   **稳定性强**：随着缺失率增加，CondI 的性能下降幅度远小于隐式对齐方法，证明了显式插补对维持语义连续性的重要性。
*   **特征恢复有效**：重建分析证明扩散模型生成的信号在特征空间上高度接近原始未缺失信号，而非仅仅是随机噪声。

### 7. 优点与亮点
*   **显式插补视角**：突破了传统 MMFL 仅做特征对齐的局限，通过扩散模型恢复原始数据分布，提高了模型的可解释性和鲁棒性。
*   **参数化条件逻辑**：通过学习全局共享的 $W_{cond}$ 矩阵，巧妙地解决了联邦学习中无法直接共享原始数据来学习相关性的难题。
*   **解耦设计**：两阶段训练策略使得插补任务与下游分类任务互不干扰，又能在推理时协同工作。

### 8. 不足与局限
*   **计算开销**：扩散模型（Diffusion）的推理过程通常涉及多次迭代，虽然论文未提及，但在资源受限的边缘设备（如医院终端）上可能会面临推理延迟问题。
*   **小样本限制**：在 SLEEP-EDF 数据集的低缺失率场景下，CondI 表现略逊于 PEPSY，这表明当缺失样本较少时，扩散模型可能因缺乏足够的训练目标而难以发挥优势。
*   **模态类型局限**：实验主要集中在生理时间序列信号上，对于图像（如医学影像）与文本结合的更复杂多模态场景尚未验证。

（完）
