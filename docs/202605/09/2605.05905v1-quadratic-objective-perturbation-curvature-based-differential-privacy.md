---
title: "Quadratic Objective Perturbation: Curvature-Based Differential Privacy"
title_zh: 二次目标扰动：基于曲率的差分隐私
authors: "Daniel Cortild, Coralia Cartis"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05905v1"
tags: ["query:daily"]
score: 9.5
evidence: 差分隐私经验风险最小化
tldr: "本研究针对差分隐私经验风险最小化（DP-ERM）中线性目标扰动（LOP）依赖梯度有界性假设的局限，提出了二次目标扰动（QOP）机制。通过引入随机二次项，利用曲率增强模型稳定性和强凸性，在更宽松的假设下实现了(ε, δ)-差分隐私，并证明了其在近似解下的隐私保护能力及优越的效用表现。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05905v1/fig-001.webp\", \"caption\": \"Figure 1: Optimal upper bound on expected loss obtained by minimizing the right-hand side of Corollary 3.4 (left) and of Theorem 3.3 (right) over all free parameters.\", \"page\": 7, \"index\": 1, \"width\": 1044, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.05905v1/fig-002.webp\", \"caption\": \"Table 2: Summary of problems and their corresponding solutions used throughout the paper.\", \"page\": 12, \"index\": 2, \"width\": 806, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2605.05905v1/fig-003.webp\", \"caption\": \"Figure 2: Empirical risks plotted on a logarithmic scale.\", \"page\": 9, \"index\": 3, \"width\": 627, \"height\": 435}]"
motivation: 传统的线性目标扰动机制要求损失函数梯度有界，这限制了其在许多现代机器学习模型中的应用。
method: 提出二次目标扰动（QOP）方法，通过向目标函数添加随机二次型项，利用扰动的谱特性而非梯度假设来控制敏感度。
result: "在插值机制下以更弱的假设实现了(ε, δ)-差分隐私，并证明了该方法在非精确求解时仍能保持隐私性。"
conclusion: QOP通过曲率扰动提供了比线性扰动更广泛的适用性和更好的效用保证，且能通过现代分裂算法高效求解。
---

## 摘要
目标扰动是差分隐私经验风险最小化中的一种标准机制。具体而言，线性目标扰动（LOP）通过添加随机线性项来强制实现隐私，而强凸性和稳定性则由额外的确定性二次项来保证。然而，这种方法需要损失函数梯度有界的强假设，这排除了许多现代机器学习模型。在这项工作中，我们引入了二次目标扰动（QOP），它使用随机二次型对目标进行扰动。这种扰动诱导了强凸性，并通过曲率增强了问题的稳定性，从而实现了隐私保护，并允许通过扰动的谱特性而非对梯度的假设来控制敏感度。因此，我们在插值机制下，在较弱的假设下获得了 (ε, δ)-差分隐私。此外，我们将分析扩展到考虑近似解的情况，证明了在非精确求解下隐私保证仍然得以保留。此外，我们推导了经验超额风险方面的效用保证，并提供了与 LOP 的理论和数值比较，突出了基于曲率扰动的优势。最后，我们讨论了算法方面，并展示了所得问题可以使用现代分裂方案高效求解。

## Abstract
Objective perturbation is a standard mechanism in differentially private empirical risk minimization. In particular, Linear Objective Perturbation (LOP) enforces privacy by adding a random linear term, while strong convexity and stability are ensured by an additional deterministic quadratic term. However, this approach requires the strong assumption of bounded gradients of the loss function, which excludes many modern machine learning models. In this work, we introduce Quadratic Objective Perturbation (QOP), which perturbs the objective with a random quadratic form. This perturbation induces strong convexity and enforces stability of the problem through curvature, thereby enabling privacy and allowing sensitivity to be controlled through spectral properties of the perturbation rather than assumptions on the gradients. As a result, we obtain $(\varepsilon, δ)$-differential privacy under weaker assumptions, in the interpolation regime. Furthermore, we extend the analysis to account for approximate solutions, showing that privacy guarantees are preserved under inexact solves. Additionally, we derive utility guarantees in terms of empirical excess risk, and provide a theoretical and numerical comparison to LOP, highlighting the advantages of curvature-based perturbations. Finally, we discuss algorithmic aspects and show that the resulting problems can be solved efficiently using modern splitting schemes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Quadratic Objective Perturbation: Curvature-Based Differential Privacy》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在差分隐私经验风险最小化（DP-ERM）中，**线性目标扰动（LOP）**是保护隐私的经典方法。然而，LOP 存在一个致命的理论缺陷：它要求损失函数的梯度必须全局有界。这在现代机器学习（如深度学习、过参数化模型）中往往难以满足。
*   **核心问题**：如何在不依赖“梯度有界”这一强假设的前提下，实现具有严谨隐私保证且高效的 DP-ERM？
*   **整体含义**：论文提出了一种全新的**二次目标扰动（QOP）**机制，通过引入随机二次型扰动，利用“曲率”而非“梯度”来控制算法的敏感度，从而在更广泛的模型类上实现了隐私保护。

### 2. 论文提出的方法论
*   **核心思想**：不再向目标函数添加随机线性项，而是添加一个随机二次项：$\frac{\sigma^2}{2}(\theta - \theta^*)^T W (\theta - \theta^*)$。其中 $W$ 是一个随机正定矩阵（如 Wishart 矩阵），$\theta^*$ 是所有损失函数的共同极小值点（插值机制假设）。
*   **关键技术细节**：
    *   **曲率控制敏感度**：通过随机矩阵 $W$ 的谱特性（特征值分布）来确保目标的强凸性，从而限制数据集微小变动对最优解的影响。
    *   **插值机制（Interpolation Regime）**：假设模型足够强大，可以完美拟合每个数据点。这在现代过参数化模型中是常见的现象。
    *   **近似解处理**：考虑到实际中无法获得精确最优解，论文引入了高斯噪声 $b$ 来抵消近似求解带来的隐私泄露，并证明了在 $\tau$-精度下的隐私稳定性。
*   **算法流程**：
    1.  预先计算或获取一个不依赖当前数据集的参考点 $\tilde{\theta}^*$。
    2.  从 Wishart 分布中采样随机矩阵 $W$。
    3.  构建扰动后的目标函数，并使用**随机三算子分裂算法（StoTOS）**进行求解。
    4.  在最终解上添加少量高斯噪声后发布。

### 3. 实验设计
*   **实验场景**：线性最小二乘法结合 LASSO 回归（$L_1$ 正则化）。
*   **数据集**：合成数据集，设计为满足插值条件（即存在能使所有样本损失为 0 的解）。
*   **Benchmark 与对比方法**：
    *   **LOP (Linear Objective Perturbation)**：传统的线性扰动方法。
    *   **LOP-Clip**：带有梯度裁剪的线性扰动方法（现代常用变体）。
*   **对比维度**：主要考察**约束集直径（$\kappa$）**对经验风险的影响。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M5 芯片**、16GB 内存的 MacBook Pro 上完成。
*   **算力消耗**：论文未提及 GPU 使用，主要依赖 CPU 计算。由于 StoTOS 算法的高效性，单次实验运行时间极短（平均约 0.01-0.015 秒）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了理论效用边界的数值优化模拟（针对不同 $\epsilon, \delta$）。
    *   针对约束直径 $\kappa$ 进行了对比实验，每组参数运行 10 次取平均值。
*   **充分性评价**：实验在验证“缩放规律”方面是充分的，清晰地展示了 QOP 在处理大范围约束空间时的稳定性。但实验主要集中在合成数据和线性模型上，缺乏在大规模真实深度学习任务上的验证。

### 6. 论文的主要结论与发现
*   **隐私保证**：QOP 在不需要梯度有界假设的情况下，实现了 $(\epsilon, \delta)$-差分隐私。
*   **效用优势**：在梯度可能很大的场景下，LOP 的风险会随着约束空间的增大而线性增长，而 **QOP 的风险保持恒定**，表现出极强的鲁棒性。
*   **近似求解可行性**：证明了即使在非精确求解（Inexact solves）的情况下，通过适当的噪声补偿，依然可以维持严谨的隐私水平。

### 7. 优点：方法或设计亮点
*   **理论突破**：摆脱了困扰目标扰动机制多年的“梯度有界”假设，为非光滑、无界梯度问题的隐私化提供了新路径。
*   **多效合一**：随机二次项同时完成了“隐私保护”、“增强稳定性”和“诱导强凸性”三个任务。
*   **算法兼容性**：QOP 产生的子问题具有良好的复合结构，可以直接利用现代高效的随机优化算法（如 StoTOS）求解。

### 8. 不足与局限
*   **插值假设限制**：QOP 的隐私证明高度依赖于“所有损失函数共享极小值点”的假设。虽然这在过参数化模型中常见，但在欠拟合或噪声极大的数据集中可能不成立。
*   **参考点获取**：算法需要一个 $\tilde{\theta}^*$（参考点），虽然文中提到可以通过公开数据或微调获得，但在某些冷启动场景下可能难以获取。
*   **实验维度单一**：目前仅在线性模型和合成数据上验证，尚未在非凸问题（如神经网络）中进行大规模实测。

（完）
