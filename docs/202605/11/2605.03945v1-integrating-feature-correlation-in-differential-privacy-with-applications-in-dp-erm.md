---
title: Integrating Feature Correlation in Differential Privacy with Applications in DP-ERM
title_zh: 在差分隐私中整合特征相关性及其在 DP-ERM 中的应用
authors: "Tianyu Wang, Luhao Zhang, Rachel Cummings"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03945v1"
tags: ["query:daily"]
score: 9.0
evidence: 差分隐私经验风险最小化 (DP-ERM) 框架
tldr: 针对标准差分隐私对所有特征施加统一约束而忽略特征敏感性差异的问题，本文提出了一种考虑特征相关性的新框架CorrDP。该框架利用全变分距离量化敏感与非敏感特征间的相关性，允许对非敏感特征放宽隐私要求。通过在DP-ERM算法中引入与距离相关的梯度噪声，该方法在保证隐私的同时显著提升了模型效用，并在合成及真实数据集上验证了其优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03945v1/fig-001.webp\", \"caption\": \"Figure 1: Privacy-utility trade-offs for least-square regression, logistic regression, and neural network training. The shaded area around each curve shows the standard deviation error band. We do not run the Partial algorithm on the Sepsis, Credit Card, and Medical Cost datasets since it is not comparable to use the same architecture only with partial features.\", \"page\": 11, \"index\": 1, \"width\": 979, \"height\": 618}, {\"url\": \"assets/figures/arxiv/2605.03945v1/fig-002.webp\", \"caption\": \"Figure 2: Privacy-utility trade-offs for least-square regression under (ϵ, δ)-DP or (ϵ, δ)-CorrDP on synthetic data.\", \"page\": 35, \"index\": 2, \"width\": 781, \"height\": 729}]"
motivation: 标准差分隐私忽略了实际应用中敏感与非敏感特征的异质性，导致对非敏感特征过度保护而损失模型效用。
method: 提出CorrDP框架，利用全变分距离量化特征相关性，并在经验风险最小化中根据相关程度动态调整梯度噪声。
result: 实验结果表明，CorrDP算法在处理包含非敏感特征的数据集时，其模型效用一致优于传统的差分隐私框架。
conclusion: 通过合理利用特征间的相关性并区分敏感度，可以在维持隐私保护强度的前提下，有效提升差分隐私机器学习模型的性能。
---

## 摘要
标准的差分隐私对所有特征施加统一的隐私约束，忽略了实际应用中敏感特征与非敏感特征之间的内在区别。在本文中，我们引入了一种松弛的差分隐私定义，该定义考虑了这种异质性，允许某些特征即使在与敏感特征相关时也被视为非敏感特征。我们提出了一个相关性感知框架 $\textsf{CorrDP}$，它在放宽非敏感特征隐私要求的同时，考虑了它们与敏感特征的相关性，并使用全变分距离对相关性进行量化。我们在 $\textsf{CorrDP}$ 框架下设计了差分隐私经验风险最小化（DP-ERM）算法，通过在梯度中加入与距离相关的噪声，以提高理论效用保证。当相关性距离未知时，我们从数据集中对其进行估计，并证明其能实现相当的隐私-效用保证。我们在合成数据集和真实数据集上进行了实验，结果表明，在存在非敏感特征的情况下，基于 $\textsf{CorrDP}$ 的 DP-ERM 算法的表现始终优于标准的差分隐私框架。

## Abstract
Standard differential privacy imposes uniform privacy constraints across all features, overlooking the inherent distinction between sensitive and insensitive features in practice. In this paper, we introduce a relaxed definition of differential privacy that accounts for such heterogeneity, allowing certain features to be treated as insensitive even when correlated with sensitive ones. We propose a correlation-aware framework, $\textsf{CorrDP}$, which relaxes privacy for insensitive features while accounting for their correlations with sensitive features, with the correlations quantified using total variation distance. We design algorithms for differentially private empirical risk minimization (DP-ERM) under the $\textsf{CorrDP}$ framework, incorporating distance-dependent noise into gradients for improved theoretical utility guarantees. When the correlation distance is unknown, we estimate it from the dataset and show that it achieves a comparable privacy-utility guarantee. We perform experiments on synthetic and real-world datasets and show that $\textsf{CorrDP}$-based DP-ERM algorithms consistently outperform the standard DP framework in the presence of insensitive features.

---

## 论文详细总结（自动生成）

这是一份关于论文《Integrating Feature Correlation in Differential Privacy with Applications in DP-ERM》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：标准的差分隐私（DP）假设数据集中所有特征具有相同的敏感度，并施加统一的隐私约束。但在实际应用中，特征敏感性往往是异质的（例如：病历中的“健康状况”比“年龄组”更敏感）。
*   **研究动机**：如果对所有特征添加等量噪声，会忽略特征间的相关性，导致过度保护非敏感特征，从而造成严重的模型效用（Utility）损失。
*   **整体含义**：本文旨在建立一个能够识别并利用特征间相关性的隐私框架，在保护敏感信息的同时，减少对非敏感但相关特征的干扰，从而优化隐私与效用之间的权衡。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **$\textsf{CorrDP}$（相关性感知差分隐私）** 框架。该框架通过量化非敏感特征与敏感特征之间的相关性，动态调整隐私预算的分配。
*   **关键技术细节**：
    *   **相关性度量**：使用**全变分距离（Total Variation Distance, TV）**来量化相关性。如果改变一个非敏感特征对敏感特征的条件分布影响很小，则该特征所需的隐私保护强度（及噪声量）也相应降低。
    *   **$\textsf{CorrDP-SGD}$ 算法**：在随机梯度下降（SGD）过程中，根据特征的敏感类型和相关性距离 $d(D, D')$ 注入非各向同性的高斯噪声。
    *   **噪声缩放公式**：对于敏感特征，保持标准噪声；对于非敏感特征，噪声规模取决于其与敏感特征的 TV 距离的最大值。
    *   **距离估计**：针对相关性距离未知的实际情况，设计了一种基于样本的 TV 距离估计程序，并引入上置信界（UCB）来补偿估计误差，确保隐私保证的稳健性。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：多元高斯分布数据，用于验证理论界限。
    *   **真实数据集**：Adult（收入预测）、Sepsis（败血症生存预测）、Credit Card（信用卡违约）、Medical Cost（医疗费用预测）。
*   **对比方法（Benchmarks）**：
    *   **Standard**：标准的 DP-SGD（对所有特征添加统一噪声）。
    *   **Semi**：半敏感 DP（仅在敏感特征上加噪，忽略相关性导致的泄露风险）。
    *   **Partial**：仅使用非敏感特征进行非隐私训练（丢弃敏感特征）。
    *   **$\textsf{CorrDP}$**：本文提出的方法。
*   **模型类型**：线性回归（OLS）、逻辑回归（Logistic Regression）和两层神经网络（2-NN）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M1 芯片（8 核 CPU）和 16GB RAM** 的个人笔记本电脑上完成。
*   **时间消耗**：完成所有实验总计耗时约 **40 小时**。
*   **软件框架**：基于 Python 的 `scikit-learn` 和 `PyTorch` 实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同性质的数据集（1 个合成 + 4 个真实），涉及分类和回归两种任务。
*   **充分性评价**：
    *   **多维度验证**：实验不仅对比了不同隐私预算 $\epsilon$ 下的效用，还通过改变敏感特征的数量（$m_s$）进行了灵敏度分析。
    *   **客观性**：通过引入 `Semi` 这一理论上的效用上限（虽然隐私保护较弱）和 `Standard` 这一工业标准，清晰地展示了 $\textsf{CorrDP}$ 的定位。
    *   **公平性**：所有方法在相同的梯度裁剪阈值、迭代次数和学习率等超参数下进行比较。

### 6. 主要结论与发现
*   **效用提升**：在相同的隐私预算 $\epsilon$ 下，$\textsf{CorrDP}$ 的模型精度（或经验风险）显著优于标准 DP，且非常接近不考虑相关性泄露的 `Semi` 方法。
*   **理论突破**：证明了当非敏感特征与敏感特征弱相关时，$\textsf{CorrDP}$ 可以将效用误差从 $\tilde{O}(\sqrt{m}/n\epsilon)$ 降低到 $\tilde{O}(\sqrt{m_s}/n\epsilon)$，其中 $m_s$ 是敏感特征数。
*   **估计可行性**：实验证明，即使相关性距离是从数据中估计出来的，该框架依然能保持强大的隐私保护能力和效用优势。

### 7. 优点与亮点
*   **精细化建模**：打破了 DP 中“一刀切”的假设，更符合医疗、金融等领域数据特征敏感度不一的现实。
*   **理论与实践结合**：不仅提供了严格的隐私证明和收敛性分析，还解决了“相关性距离如何计算”这一实际落地难题。
*   **通用性**：该框架可无缝扩展至广义线性模型（GLM）和深度神经网络。

### 8. 不足与局限
*   **高维估计挑战**：在特征维度极高时，准确估计全变分（TV）距离会变得非常困难且计算开销巨大（维度灾难）。
*   **平滑性假设**：算法的理论保证依赖于损失函数对特征变化的平滑性假设（Assumption 3.4），对于某些高度非线性的复杂模型，该假设可能难以严格满足。
*   **标签隐私限制**：目前的框架主要关注特征隐私，假设标签是公开的或完全敏感的，对于标签与特征间复杂相关性的处理仍有提升空间。

（完）
