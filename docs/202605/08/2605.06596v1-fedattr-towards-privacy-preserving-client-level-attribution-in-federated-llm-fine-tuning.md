---
title: "FedAttr: Towards Privacy-preserving Client-Level Attribution in Federated LLM Fine-tuning"
title_zh: FedAttr：联邦大语言模型微调中保护隐私的客户端级归因
authors: "Su Zhang, Junfeng Guo, Heng Huang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06596v1"
tags: ["query:daily"]
score: 9.5
evidence: 联邦大语言模型微调中的隐私保护客户端归因
tldr: 本研究针对联邦学习中大模型微调的数据确权难题，提出FedAttr协议。它在维持安全聚合隐私性的同时，利用配对子集差分机制精准识别使用水印数据的客户端。实验表明该方法以极低开销实现了极高的检测准确率，有效解决了联邦环境下的客户端归属识别挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06596v1/fig-001.webp\", \"caption\": \"Table 3: Federated LoRA fine-tuning hyperparameters (default configuration). Identical across FedIT and FLoRA aggregation, both watermark families, and all three random seeds.\", \"page\": 30, \"index\": 1, \"width\": 718, \"height\": 581}, {\"url\": \"assets/figures/arxiv/2605.06596v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of Data Attribution in Federated Learning. (a) The corpus owner distributes watermarked documents to clients under a data-use license. (b) Clients collaboratively fine-tune a shared model via federated learning through secure aggregation (SA). (c) A global radioactivity test can detect the watermark signal of the trained model, but cannot identify which clients are responsible without violating SA. (d) FedAttr identifies which clients use the watermark documents through SA.\", \"page\": 2, \"index\": 2, \"width\": 790, \"height\": 230}, {\"url\": \"assets/figures/arxiv/2605.06596v1/fig-003.webp\", \"caption\": \"Table 7: Ablation variants. Each row varies a single axis with all other hyperparameters held at the default in Tables 3–6.\", \"page\": 33, \"index\": 3, \"width\": 794, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2605.06596v1/fig-004.webp\", \"caption\": \"Table 1: Client-level watermark attribution performance (T=5 rounds, γ=4.0, mean±std over 3 seeds). FedAttr is the only method that achieves 100% TPR, 0% FPR in four settings through secure aggregation. For FLForensics, † denotes the original implementation using HDBSCAN clustering4, and ‡ denotes our adaptation using k-means clustering, since HDBSCAN fails at small K=10.\", \"page\": 8, \"index\": 4, \"width\": 814, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2605.06596v1/fig-005.webp\", \"caption\": \"Table 6: FedAttr protocol hyperparameters (default configuration).\", \"page\": 32, \"index\": 5, \"width\": 739, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2605.06596v1/fig-006.webp\", \"caption\": \"Table 4: KGW watermark generation hyperparameters.\", \"page\": 31, \"index\": 6, \"width\": 652, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2605.06596v1/fig-007.webp\", \"caption\": \"Table 5: Fictitious-Knowledge watermark generation hyperparameters.\", \"page\": 31, \"index\": 7, \"width\": 768, \"height\": 274}]"
motivation: 联邦学习的安全聚合机制在保护隐私的同时，导致服务器无法识别哪些客户端使用了受版权保护的水印数据进行模型微调。
method: 提出一种配对子集差分机制，通过两次安全聚合查询的差值估算客户端更新，并结合差分评分与Stouffer方法进行跨轮次判定。
result: "FedAttr实现了100%的真阳性率和0%的假阳性率，性能优于基准方法至少44.4%，且仅带来6.3%的额外时间开销。"
conclusion: FedAttr为联邦大模型微调提供了一种兼顾隐私保护与数据归属识别的高效方案，具有较强的鲁棒性和实用价值。
---

## 摘要
水印放射性测试类方法可以检测模型是否在含水印的文档上进行了训练，已成为大语言模型（LLM）微调中保护数据所有权的关键工具。现有工作已证明其在集中式LLM微调中的有效性。然而，这类方法在联邦学习（FL）中面临若干挑战且尚未得到充分探索。联邦学习是一种广泛应用的范式，用于在不同用户的私有数据上协作微调LLM。FL主要通过安全聚合（SA）确保隐私，SA允许服务器聚合更新，同时保持客户端更新的私密性。这种机制保护了隐私，但也使得识别哪个客户端在水印文档上进行了训练变得困难。在这项工作中，我们提出了FedAttr，一种用于FL的新型客户端级归因协议。FedAttr通过配对子集差分机制识别哪些客户端在水印数据上进行了训练，同时保留了SA的隐私保证和FL的性能。FedAttr分为三个步骤：（i）通过对两个SA查询求差来估计每个客户端的更新；（ii）通过差分评分利用水印检测器对估计值进行评分；（iii）通过Stouffer方法合并各轮评分。我们从理论上证明，FedAttr产生每个客户端更新的无偏估计量，且互信息泄漏有界（即每轮更新为$O(d^*/N)$）。此外，实验表明FedAttr实现了100%的真阳性率（TPR）和0%的假阳性率（FPR），在TPR上至少优于所有基准方法44.4%，在FPR上至少优于19.1%，且相对于FL训练时间仅有6.3%的开销。消融研究证实，FedAttr对协议参数和配置具有鲁棒性。

## Abstract
Watermark radioactivity testing type of methods can detect whether a model was trained on watermarked documents, and have become key tools for protecting data ownership in the fine-tuning of large language models (LLMs). Existing works have proved their effectiveness in centralized LLM fine-tuning. However, this type of method faces several challenges and remains underexplored in federated learning (FL), a widely-applied paradigm for fine-tuning LLMs collaboratively on private data across different users. FL mainly ensures privacy through secure aggregation (SA), which allows the server to aggregate updates while keeping clients' updates private. This mechanism preserves privacy but makes it difficult to identify which client trained on watermarked documents. In this work, we propose FedAttr, a new client-level attribution protocol for FL. FedAttr identifies which clients trained on watermarked data via a paired-subset-difference mechanism, while preserving the privacy guarantees of SA and FL performance. FedAttr proceeds in three steps: (i) estimate each client's update by differencing two SA queries, (ii) score the estimate with the watermark detector via differential scoring, and (iii) combine scores across rounds via Stouffer method. We theoretically show that FedAttr produces an unbiased estimator of each client's update with bounded mutual information leakage (i.e., $O(d^*/N)$ per-round update). Moreover, FedAttr empirically achieves 100% TPR and 0% FPR, outperforming all baselines by at least 44.4% in TPR or 19.1% in FPR, with only 6.3% overhead relative to FL training time. Ablation studies confirm that FedAttr is robust to protocol parameters and configurations.

---

## 论文详细总结（自动生成）

这是一份关于论文《FedAttr: Towards Privacy-preserving Client-Level Attribution in Federated LLM Fine-tuning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**联邦学习（FL）**环境下，如何对大语言模型（LLM）微调过程中的**数据所有权进行保护**。
*   **背景**：数据所有者通常通过在文档中植入“水印”来监控授权使用情况。在集中式训练中，检测模型是否包含水印信号（放射性测试）相对成熟。
*   **核心挑战**：联邦学习普遍采用**安全聚合（SA）**机制，服务器只能看到多个客户端更新的加和，无法观察单个客户端的明文更新。这导致服务器虽然能检测出全局模型被水印数据训练过，却无法定位具体是哪个客户端违反了许可协议。
*   **目标**：在不破坏安全聚合隐私保证的前提下，实现精准的客户端级归因。

### 2. 方法论：核心思想与技术细节
论文提出了 **FedAttr** 协议，其核心流程包含三个关键步骤：
1.  **客户端更新估计（更新估计器）**：
    *   利用**配对子集差分（Paired-subset-difference）**机制。服务器发起两次授权的SA查询：一次包含目标客户端 $i$ 的子集，另一次不包含。
    *   通过计算两次查询结果的差值，构造出目标客户端更新 $\Delta_i$ 的无偏估计量。
    *   引入**拒绝采样**确保非目标客户端提供的“掩码噪声”足够大，从而将每轮的互信息泄露限制在 $O(d^*/N)$。
2.  **差分评分（Differential Scoring）**：
    *   由于全局模型会随时间累积水印信号，直接测试更新会导致极高的假阳性（FPR）。
    *   FedAttr 计算“全局模型 + 估计更新”的得分与“原全局模型”得分之差，从而隔离出该轮次中特定客户端带来的增量贡献。
3.  **跨轮次 Stouffer 聚合**：
    *   单轮次的水印信号通常较弱。FedAttr 使用 **Stouffer 方法**将 $T$ 个通信轮次的差分评分进行标准化累加。
    *   理论证明，随着轮次 $T$ 的增加，水印客户端与良性客户端的得分差距以 $\sqrt{T}$ 速度增长，使错误率呈指数级下降。

### 3. 实验设计
*   **数据集与场景**：主要使用 UltraChat200K 进行指令微调，并扩展到 Alpaca-52K 和 OpenOrca。
*   **模型架构**：Llama-3.2 (1B/3B) 和 Qwen-2.5 (3B)，采用 LoRA 适配器进行微调。
*   **水印类型**：测试了两种主流水印——KGW（基于绿色标记偏差）和 Fictitious Knowledge（基于伪造实体属性记忆）。
*   **Benchmark 与对比方法**：
    *   **Direct (Oracle)**：直接访问客户端明文更新（违反SA，作为上限）。
    *   **FLDetector / FLForensics**：现有的联邦学习取证方法（主要针对恶意投毒攻击设计）。
    *   **Global Model Test**：仅对全局模型进行整体检测。

### 4. 资源与算力
*   **硬件环境**：使用了配备 **NVIDIA H200 (141GB)** GPU 的集群。
*   **算力消耗**：
    *   默认配置（10个客户端，5轮训练）在 4 张 H200 上运行。
    *   **训练时长**：标准 FL 微调约 8.5 小时。
    *   **额外开销**：FedAttr 带来的 SA 查询开销约 5 分钟（1.0%），水印评分开销约 27 分钟（5.3%），**总额外开销仅为 6.3%**。

### 5. 实验数量与充分性
论文进行了极其详尽的实验验证，具有很高的客观性和说服力：
*   **基础性能**：在不同 FL 聚合策略（FedIT, FLoRA）下均实现了 **100% TPR（真阳性率）和 0% FPR（假阳性率）**。
*   **消融实验**：涵盖了水印客户端数量、子集大小 $N$、查询次数 $M$、水印植入比例等参数。
*   **鲁棒性测试**：验证了不同 LoRA 秩、不同基础模型、不同数据集以及**非 IID（异构数据）**分布下的表现。
*   **可扩展性**：将客户端数量从 10 扩展到 100，证明了在大规模场景下的有效性。
*   **统计显著性**：所有实验均基于 3 个随机种子取平均值，并提供了 p-value 分析。

### 6. 主要结论与发现
*   **精准归因**：FedAttr 是目前唯一能在保持安全聚合隐私性的同时，实现 100% 准确归因的方法。
*   **差分评分的必要性**：实验证明，如果不使用差分评分，即使拥有明文更新，假阳性率（FPR）也会高达 57% 以上。
*   **隐私与效能平衡**：通过理论和实验证明，可以在极低的隐私泄露风险下（互信息量极小）获得极高的检测精度。
*   **非 IID 影响**：在极端数据异构情况下性能会有所下降，但可以通过增加通信轮次 $T$ 来修复。

### 7. 优点（亮点）
*   **兼容性强**：完全兼容现有的安全聚合（SA）协议，无需修改客户端侧的代码或加密逻辑。
*   **理论完备**：不仅提出了算法，还给出了无偏性证明、方差界限以及互信息泄露的上界分析。
*   **开销极低**：6.3% 的额外时间开销在实际生产环境中几乎可以忽略不计。
*   **黑盒特性**：将水印检测器视为黑盒评分函数，适用于多种不同类型的水印技术。

### 8. 不足与局限
*   **语料库所有者依赖**：归因过程需要语料库所有者参与（提供检测密钥/评分），无法由服务器独立完成。
*   **非 IID 挑战**：虽然增加轮次可以解决，但在极端异构环境下，初始几轮的归因置信度较低。
*   **合规性边界**：虽然论文证明了隐私泄露极小，但在极度严苛的法律环境下，这种“通过多次查询推断个体贡献”的行为是否符合所有隐私法规（如 GDPR 的某些极端解释）仍有讨论空间。

（完）
