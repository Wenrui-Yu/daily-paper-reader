---
title: Enabling Federated Inference via Unsupervised Consensus Embedding
title_zh: 通过无监督共识嵌入实现联邦推理
authors: "Yui Hashimoto, Takayuki Nishio, Yuichi Kitagawa, Takahito Tanimura"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05718v1"
tags: ["query:daily"]
score: 9.0
evidence: 保持数据和参数私密的联邦推理
tldr: 本研究针对分布式环境下独立部署模型间的协作推理需求，提出了CE-FI框架。该框架旨在解决现有方法依赖共享数据或参数而导致的隐私问题。CE-FI通过在无标签数据上训练共识嵌入层和协作输出层，实现了异构模型在推理阶段的无缝协作，无需共享原始输入或模型内部参数。实验证明，该方法在图像、文本和时间序列任务中均表现出色，显著提升了推理准确性，为隐私保护下的跨组织模型协作提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05718v1/fig-001.webp\", \"caption\": \"Fig. 1. Cooperative inference with CE-FI. Each device maps the intermediate feature of an input sample into a shared feature space via the Consensus Embedding Layer, shares the resulting consensus embeddings with other cooperative devices, performs inference based on the received embeddings, and integrates all outputs to obtain improved predictions.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-002.webp\", \"caption\": \"TABLE II ACCURACY OF CE-FI WITH DIFFERENT OUTPUT ENSEMBLE METHODS (MEAN ± STD IN 3 DEVICES).\", \"page\": 11, \"index\": 2, \"width\": 1075, \"height\": 776}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-003.webp\", \"caption\": \"Fig. 5. Accuracy across label partition strategies under different datasets and architectural settings (3 devices). (a) CIFAR-10 / Unified-architecture (ViT), (b) CIFAR-10 / Heterogeneous-architecture, (c) CIFAR-100 / Unifiedarchitecture (ViT), (d) CIFAR-100 / Heterogeneous-architecture.\", \"page\": 10, \"index\": 3, \"width\": 509, \"height\": 782}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-004.webp\", \"caption\": \"Fig. 6. t-SNE visualization of feature representations across devices in Unified-architecture setting on CIFAR-100: (a)intermediate features extracted from a single pretrained encoder (ImageNet-supervised). (b)intermediate features from all devices projected into a common space. (c)the consensus embedding space learned by CE-FI, showing enhanced cross-device alignment.\", \"page\": 10, \"index\": 4, \"width\": 516, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-005.webp\", \"caption\": \"Fig. 7. Accuracy across label partition strategies on different modalities (3 devices). (a) IMDB, (b) UCI-HAR.\", \"page\": 10, \"index\": 5, \"width\": 509, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-006.webp\", \"caption\": \"Fig. 9. Accuracy under various label partition settings with 3 devices: (a) MNIST, (b) FashionMNIST, (c) CIFAR-10 dataset.\", \"page\": 13, \"index\": 6, \"width\": 509, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-007.webp\", \"caption\": \"Fig. 10. Accuracy under various label partition settings with increasing the number of devices to 5 and 10.\", \"page\": 13, \"index\": 7, \"width\": 498, \"height\": 583}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-008.webp\", \"caption\": \"Fig. 15. Reconstruction attack results on CIFAR-10. For visualization, eight representative samples randomly selected from the test set are shown. The top row shows the original images, and the bottom row shows the reconstructed images. Results are shown for two representation types—consensus embeddings and intermediate features—and two training settings: pretrained encoder and training from scratch.\", \"page\": 17, \"index\": 8, \"width\": 525, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.05718v1/fig-009.webp\", \"caption\": \"Fig. 8. Comparison of accuracy between Solo Inference and CE-FI under Dirichlet partitioning (α = 0.1, 0.5), using pretrained encoder (5 trials). Horizontal axis: accuracy of Solo Inference using a device’s pretrained model; vertical axis: accuracy of CE-FI using the device’s consensus embedding.\", \"page\": 12, \"index\": 9, \"width\": 474, \"height\": 818}]"
motivation: 现有协作推理框架通常需要共享原始数据、模型参数或通用编码器，限制了其在隐私敏感或跨组织场景中的应用。
method: 提出CE-FI框架，通过无监督学习在共享的无标签数据上训练共识嵌入层和协作输出层，将异构中间表示映射到统一空间。
result: 在CIFAR-10/100等基准测试中，CE-FI在多种非独立同分布条件下均优于单模型推理，且性能接近需要更强共享假设的传统方法。
conclusion: 该研究证明了在不共享敏感信息的情况下实现模型协作的可行性，并指出表示对齐是提升此类联邦推理性能的关键瓶颈。
---

## 摘要
在分布式环境中，跨独立部署的机器学习模型进行协作推理的需求日益增长，因为在利用多个模型的同时保持其数据和模型参数私密性的需求不断增加。然而，现有的协作框架通常依赖于共享输入数据、模型参数或通用编码器，这限制了它们在隐私敏感或跨组织场景中的适用性。为了应对这一挑战，我们提出了基于共识嵌入的联邦推理（CE-FI），该框架使预训练模型能够在推理阶段进行协作，而无需共享模型参数或原始输入，也不假设存在通用编码器。CE-FI 引入了两个组件：一个将异构中间表示映射到公共嵌入空间的共识嵌入（CE）层，以及一个根据这些嵌入生成预测的协作输出（CO）层。这两层仅使用共享的无标签数据进行训练，因此协作阶段不需要额外的有标签数据。在 CIFAR-10 和 CIFAR-100 图像分类基准测试以及多种非独立同分布（non-IID）条件下的实验表明，CE-FI 的表现始终优于单模型推理，且与需要更强共享假设的传统方法性能相当。对文本和时间序列任务的额外评估表明，该方法在图像分类之外也具有适用性，尽管性能取决于集成策略。进一步的分析表明，表示对齐是主要的瓶颈。

## Abstract
Cooperative inference across independently deployed machine learning models is increasingly desirable in distributed environments, as there is a growing need to leverage multiple models while keeping their data and model parameters private. However, existing cooperative frameworks typically rely on sharing input data, model parameters, or a common encoder, which limits their applicability in privacy-sensitive or cross-organizational settings. To address this challenge, we propose Consensus Embedding-based Federated Inference (CE-FI), a framework that enables pretrained models to cooperate at inference time without sharing model parameters or raw inputs and without assuming a common encoder. CE-FI introduces two components: a Consensus Embedding (CE) layer that maps heterogeneous intermediate representations into a common embedding space, and a Cooperative Output (CO) layer that produces predictions from these embeddings. Both layers are trained using shared unlabeled data only, so the cooperative stage does not require additional labeled data. Experiments on image classification benchmarks -- CIFAR-10 and CIFAR-100 -- under diverse non-IID conditions show that CE-FI consistently outperforms solo inference and performs comparably to conventional methods that require stronger sharing assumptions. Additional evaluations on text and time-series tasks indicate applicability beyond image classification, although performance depends on the ensemble strategy. Further analysis identifies representation alignment as the primary bottleneck.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CE-FI（Consensus Embedding-based Federated Inference）** 的新型联邦推理框架，旨在解决分布式 AI 模型在严格隐私限制下的协作难题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现代分布式 AI 场景中，不同组织或设备往往拥有针对相同任务但独立训练的异构模型。为了提升推理性能，模型间的协作至关重要。然而，现有的协作框架（如联邦学习或传统的联邦推理）通常面临以下瓶颈：
*   **隐私限制：** 无法共享原始输入数据（如医疗影像）。
*   **知识产权保护：** 无法共享模型参数或架构。
*   **异构性：** 不同模型可能使用完全不同的编码器架构，导致中间特征空间不兼容。
*   **标注成本：** 协作阶段往往缺乏有标签的数据。

**核心问题：** 如何在不共享输入、不共享模型参数、不假设通用编码器且无标签数据的极端受限条件下，实现多个异构预训练模型的有效协作推理？

### 2. 方法论：CE-FI 框架
CE-FI 的核心思想是利用**无监督学习**构建一个“共识空间”，将不同模型的异构中间特征对齐。

*   **核心组件：**
    1.  **共识嵌入层 (CE Layer):** 部署在每个设备上，负责将本地模型的中间特征（Head Network 的输出）映射到一个全局统一的“共识嵌入空间”。
    2.  **协作输出层 (CO Layer):** 负责将共识嵌入映射回类别的 Logits 预测值。
*   **关键技术细节：**
    *   **无监督 CE 训练：** 使用共享的无标签数据集，通过**对比学习 (Contrastive Learning)** 优化。它鼓励不同设备对同一输入生成的嵌入向量相互靠近（对齐），同时推开不同输入的嵌入向量。为了降低计算复杂度，论文采用对齐到“质心（平均嵌入）”的策略。
    *   **无监督 CO 训练：** 采用**知识蒸馏 (Knowledge Distillation)**。本地预训练模型作为“教师”，CO 层作为“学生”。CO 层学习如何从共识嵌入中重现本地模型的预测分布，全程无需标签。
    *   **推理集成：** 在推理时，设备共享共识嵌入而非原始数据。接收端通过 CO 层生成预测，最后通过**基于能量的得分 (Energy-based Score)** 或软投票等策略进行集成。

### 3. 实验设计
*   **数据集与场景：**
    *   **图像分类：** CIFAR-10, CIFAR-100, MNIST, Fashion MNIST。
    *   **文本分类：** IMDB 电影评论（27 类流派分类）。
    *   **时间序列：** UCI-HAR（人类活动识别）。
*   **非独立同分布 (Non-IID) 设置：** 设计了 Mild, Moderate, Skewed, Disjoint 四种手动划分等级，以及基于 Dirichlet 分布 ($\alpha=0.1, 0.5$) 的随机划分，模拟极端的类别不平衡。
*   **对比方法 (Benchmarks)：**
    *   **Solo Inference:** 单个模型独立推理。
    *   **Input-sharing FI:** 假设可以共享原始输入的联邦推理（性能上限）。
    *   **Edge Ensemble:** 假设所有设备共享相同编码器的联邦推理。
    *   **Oracle Ensemble:** 理想状态下的最优选择集成。

### 4. 资源与算力
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练配置：** 提到了使用 Adam 优化器，学习率为 $10^{-3}$，CE 层训练 100 epoch，CO 层训练 20 epoch。Batch size 在 64 到 512 之间。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了非常详尽的实验，涵盖了 3 到 10 个设备的扩展性测试。
*   **充分性：** 实验设计非常全面，不仅验证了图像任务，还跨越到了文本和传感器数据。
*   **客观性：** 引入了多种架构设置（统一 ViT 架构 vs. 异构架构如 ResNet/ConvNeXt），并进行了消融实验（如领域偏移测试、瓶颈分析、隐私攻击测试），评估视角客观且多维。

### 6. 主要结论与发现
*   **性能提升：** CE-FI 在所有测试场景下均显著优于 Solo Inference，且在某些情况下接近 Input-sharing FI 的性能。
*   **鲁棒性：** 即使在共享无标签数据与目标任务存在“领域偏移”（如用 CIFAR-100 训练 CE 层来推理 CIFAR-10）时，框架依然有效。
*   **瓶颈所在：** 瓶颈分析表明，**表示对齐（CE 层的对齐质量）**是限制性能的核心因素，而非 CO 层的蒸馏过程。
*   **隐私安全性：** 通过数据重构攻击 (DRA) 测试发现，从共识嵌入中还原原始图像极其困难，证明了其比共享原始数据更安全。

### 7. 优点
*   **极低的共享门槛：** 真正实现了“三不”协作（不共享数据、不共享模型、不共享编码器），极具实用价值。
*   **完全无监督：** 协作阶段不需要任何人工标注，降低了部署成本。
*   **多模态适用：** 框架设计通用，不局限于特定模型架构或数据类型。
*   **理论支撑：** 论文对 CE-FI 与传统 FI 的一致性进行了理论分析。

### 8. 不足与局限
*   **集成策略敏感：** 在文本和时间序列任务中，性能高度依赖于集成方法（如能量得分 vs. 软投票），缺乏一种通用的最优策略。
*   **性能差距：** 尽管优于单模型，但与“理想集成 (Oracle)”之间仍存在较大差距，说明共识空间的语义表达仍有提升空间。
*   **通信开销：** 虽然不共享数据，但训练 CE 层仍需在设备间多次交换嵌入向量，对于带宽极低的边缘设备可能存在压力。
*   **类别覆盖依赖：** 如果所有参与协作的设备在某个类别上都表现极差，CE-FI 无法凭空产生该类别的预测能力。

（完）
