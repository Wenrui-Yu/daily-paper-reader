---
title: Predictability as a Fine-Grained Measure for Privacy
title_zh: 可预测性作为隐私的细粒度度量
authors: "Linda Lu, Karthik Sridharan"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20546v1"
tags: ["query:daily"]
score: 9.0
evidence: 提出名为可预测性的细粒度隐私度量
tldr: 差分隐私的worst-case性质导致隐私-准确性权衡成本高昂。为此，提出可预测性作为细粒度隐私度量，它通过攻击者在观察算法输出后预测未知个体敏感信息的能力增量来量化隐私泄露，明确纳入攻击者核心知识和数据生成过程。可预测性与差分隐私一般不可比较，但在最坏情况下（仅一人未泄露且所有二元查询敏感）可转化为互信息差分隐私。利用广义矩估计（GMM）分析渐近可预测性，并设计可预测性校准的输出扰动方案，与差分隐私互补，实现细粒度隐私控制。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20546v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 236, \"label\": \"Table\"}]"
motivation: 差分隐私的worst-case性质导致隐私-准确性权衡昂贵，需要更细粒度的隐私度量以匹配特定攻击者模型。
method: 提出可预测性度量，基于攻击者核心知识和随机过程，量化预测能力的增量；利用GMM分析渐近可预测性。
result: 可预测性与DP不可比较，但最坏情况下可导出互信息DP；提出可预测性校准的输出扰动方案。
conclusion: 提供与DP互补的细粒度隐私控制，适用于特定敏感信息和攻击者模型。
---

## 摘要
差分隐私（DP）即使在最知情的攻击者面前也能确保严格的个体级隐私保障，但其最坏情况的性质可能导致昂贵的隐私-精度权衡。我们通过可预测性引入了隐私，这是一个细粒度的框架，明确地包含了攻击者的核心知识、由随机过程生成的受损数据集部分以及指定的查询族。可预测性将隐私泄露衡量为攻击者在观察算法输出后，相对于已从受损数据中推断出的信息，预测未知个体敏感信息能力的增量收益。我们证明了可预测性与DP通常是不可比的：一个可以很小而另一个很大。然而，在最坏情况下，当除一个个体外所有个体均受损且所有二元查询都被视为敏感时，可预测性意味着互信息差分隐私。更一般地，可预测性提供了针对特定敏感信息和特定攻击者模型的更细粒度的隐私度量。我们引入了一个通用框架，利用广义矩估计（GMM）来分析当受损数据由平稳、遍历、混合过程生成时的渐近可预测性。通过此分析，我们为经验风险最小化（ERM）推导了一种可预测性校准的输出扰动方案。我们的方法是对DP的补充，可以与DP一起使用以提供细粒度的隐私控制。

## Abstract
Differential privacy (DP) ensures rigorous individual-level privacy guarantees against even the most knowledgeable attackers, but its worst-case nature can impose a costly privacy-accuracy tradeoff. We introduce privacy via predictability, a fine-grained framework that explicitly incorporates the attacker's core knowledge, a compromised portion of the dataset generated by a stochastic process, and a specified family of queries. Predictability measures privacy leakage as the incremental gain in an attacker's ability to predict sensitive information about unknown individuals after observing the algorithm's output, beyond what can already be inferred from the compromised data. We show that predictability and DP are generally incomparable: each can be small while the other is large. However, in the worst-case regime where all but one individual is compromised, and all binary queries are considered sensitive, predictability implies mutual-information DP. More generally, predictability provides a finer-grained privacy metric tailored to specific sensitive information and specific attacker models. We introduce a general framework, using the generalized method of moments (GMM), to analyze asymptotic predictability when the compromised data is generated by a stationary, ergodic, mixing process. Using this analysis, we derive a predictability-calibrated output perturbation scheme for ERM. Our approach is complementary to DP and can be used alongside DP to provide fine-grained privacy control.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：差分隐私（DP）虽然提供严格的个体级隐私保障，但其最坏情况假设（攻击者知道除一个个体外的所有数据）导致隐私-准确性权衡非常昂贵，实际部署中常采用较大的隐私参数 ε（如 ε=20），使得隐私保证极弱。此外，实际数据泄露往往只暴露部分数据（如一个数据分片），而非整个数据集，但 DP 的保证并未针对此类攻击者模型优化。因此，需要一种更细粒度的隐私度量，明确考虑攻击者实际拥有的核心知识和具体敏感查询。
- **整体含义**：本文提出“可预测性”（Predictability）作为隐私的细粒度度量，衡量攻击者在观察算法输出后，预测未知个体敏感信息的能力相对于仅凭受损数据的增量。该度量与 DP 互补，可以在 DP 基础上提供针对特定攻击者和查询的更强隐私控制。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：定义攻击者的核心知识 C(S) 为通过随机过程 P 生成的受损数据子集；敏感查询族 Q；算法 A 的输出。可预测性 γ 定义为在任意数据集 S 上，攻击者观察算法输出后，对随机未知个体 x（均匀来自未受损部分）的敏感查询 q(x) 的预测损失减少量的上界。采用贝叶斯最优估计器（Bayes optimal estimator）衡量预测能力，损失函数可选取对数损失（对应 KL 散度）或平方损失。
- **关键技术细节**：
  - 理论证明了可预测性与 DP 一般不可比较：存在算法满足 DP 但可预测性高，反之亦然。但在最坏情况下（仅一人未泄露，所有二元查询敏感），可预测性导出互信息差分隐私（MI-DP）。
  - 利用广义矩估计（GMM）分析渐近可预测性：将攻击者的核心知识建模为从平稳、遍历、混合过程中抽取的样本。算法输出视为一个含噪声的矩条件。通过计算有无算法输出时有效估计量的方差差，得到可预测性的渐近界。
  - 推导出单查询渐近可预测性的闭合表达式：γ ≤ H(1+log4)/n · Var[q'] · √(ρ_cc² - c₀)，其中 ρ_cc 是查询与矩条件之间的典型相关，c₀ 是噪声衰减项。对于随机分片过程（均匀无放回采样），n = (1-α)αN。
  - 提出可预测性校准的输出扰动方案：针对 ERM，自适应噪声协方差为 σ² · E[w(x)H_z]^{-1} E[w(x)²∇z∇z^T] E[w(x)H_z]^{-1}，使得噪声方向与损失曲率和梯度方差对齐，得到更优的准确性-可预测性权衡。线性回归实例显示，当经验损失较小时，校准噪声比各向同性噪声对模型精度影响更小。
- **算法流程（文字说明）**：给定数据集、攻击者过程 P、敏感查询族 Q，算法设计者已知 P 但不知实际受损数据。首先，将输出算法（如统计量、ERM）表示为矩条件；然后，根据 P 的平稳分布计算权重 w(x)；接着，构建有无算法输出时的 GMM 估计量；计算有效估计量的渐近方差，代入公式得到可预测性上界。设计可预测性校准的噪声时，根据公式计算噪声协方差矩阵，然后添加噪声后发布。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- 本文是**纯理论论文**，未进行任何实验验证。没有使用任何数据集、benchmark 或对比方法。所有结果均为数学推导和理论证明。
- 文中仅通过理论示例说明可预测性与 DP 的不可比性（如吸烟者比例的例子），以及线性回归中校准噪声与各向同性噪声的准确性差异的理论上界。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- 文中**未提及**任何计算资源、GPU 型号、数量或训练时长。作为理论工作，不需要实验算力。仅提到计算噪声协方差的复杂度为 O(Nd² + d³)。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **无实验**。因此无法评价实验的充分性、客观性或公平性。所有结论均基于数学证明，其有效性取决于假设的合理性（如平稳遍历混合过程、GMM 正则条件、贝叶斯最优估计器的渐进效率等）。

### 6. 论文的主要结论与发现

- 可预测性与差分隐私一般不可比较，但在最坏情况假设下可导出互信息差分隐私。
- 可预测性提供了针对特定攻击者模型（如随机分片）和特定敏感查询的细粒度隐私保证，比 DP 更灵活且可能更宽松。
- 利用 GMM 框架可以分析渐近可预测性，得到简洁的表达式，核心取决于查询与矩条件的典型相关和噪声衰减。
- 可预测性校准的输出扰动方案优于各向同性噪声，在 ERM 中能获得更好的准确性-隐私权衡。
- 可预测性满足后处理性和组合性（类似于条件互信息），可以与 DP 叠加使用。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论创新**：首次提出“可预测性”作为显式纳入攻击者核心知识和具体敏感查询的隐私度量，填补了 DP 粗粒度不足的空白。
- **分析工具**：引入 GMM 分析渐近可预测性，得到了可计算的上界，使隐私度量脱离纯定义，具备可操作性。
- **实用导向**：提出的可预测性校准噪声方案针对 ERM，具有明确的公式和较低的计算复杂度，易于整合到现有 DP 框架中。
- **理论完备性**：证明了可预测性与 DP 的关系，给出了后处理性和组合性，为未来构建组合隐私预算工具提供了基础。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏实验验证**：全部为理论推导，没有在真实数据集或实际攻击场景中验证可预测性的有效性。理论假设（如平稳遍历、GMM 正则条件、贝叶斯最优估计器渐进效率）在实际中可能不满足。
- **仅考虑渐近情况**：分析基于大样本极限（n,N→∞），有限样本下的行为仅通过 CLT 近似，未给出有限样本精确界。
- **适用范围有限**：要求攻击者过程已知且满足较强统计性质（平稳遍历、α-混合）。实际数据泄露过程可能更复杂、不确定。
- **未处理更复杂的算法**：非凸 ERM、深度学习等场景未涉及，仅对凸 ERM 和简单统计量给出结果。对 BGD 的讨论仅限于将其转化为矩条件，但仍依赖梯度可导和收敛。
- **未讨论自适应查询选择**：可预测性定义中攻击者可以选择最有利的查询，但实际攻击者可能利用多次查询或相互依赖的查询，本文仅给出组合性定理，未深入分析自适应查询的隐私衰减。

（完）
