---
title: "$π$-RAG: Oblivious Retrieval via Semantic Quantization and Transcendental Addressing for Large Language Models"
title_zh: $\pi$-RAG：基于语义量化与超越地址的大语言模型 oblivious 检索
authors: "Aniket Wattamwar, Mrunal Kakirwar"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22153v1"
tags: ["query:daily"]
score: 8.0
evidence: 利用π基间接层的遗忘检索
tldr: 传统检索增强生成存在向量嵌入暴露和非确定性检索失败风险。π-RAG利用π数字作为不可变熵源，并通过语义量化将用户输入映射到规范意图质心，生成确定性π键指向标准化载荷。该架构实现遗忘推理，保证模型对数据不可知，同时统一确定性随机性、可审计性和差分隐私。在金融、医疗等高合规场景表现出高效能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22153v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 673, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22153v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 507, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22153v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 489, \"height\": 555, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22153v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1728, \"height\": 710, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22153v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1150, \"height\": 764, \"label\": \"Table\"}]"
motivation: 解决传统RAG中向量嵌入易被逆向攻击及检索非确定性的安全问题。
method: 利用π数字作为不可变熵源，构建语义量化层将输入映射到规范意图质心，生成π键指向标准化载荷。
result: 实现遗忘推理，保证LLM对数据不可知，并统一确定性、审计性与差分隐私。
conclusion: π-RAG在高合规领域（金融、医疗）具有高效性和安全性。
---

## 摘要
本文介绍了 $\pi$-RAG，一种新颖的 oblivious 检索架构，它在不牺牲语义理解的前提下将大语言模型（LLM）与敏感数据存储解耦。传统的检索增强生成（RAG）架构会将原始向量嵌入暴露给潜在的反演攻击和不确定的检索失败。为了解决这个问题，我们利用 $\pi$ 的数值作为超越熵的来源，在 LLM 和私有记录之间创建了一个不可变的间接层。$\pi$ 的值具有不可变性，不可编辑且受数学支配。该架构还引入了语义量化层，将用户输入投影到预计算的规范意图质心流形上。RAG 执行向量余弦相似度，但这里通过加密盐将质心映射到确定性偏移量。由此产生的 $\pi$-key 是指向实际数据存储中标准化负载的指针。通过用这个超越层替代通过 LLM 直接访问数据存储，$\pi$-RAG 在数学上保证了推理对数据保持 oblivious。该架构统一了确定性随机性、可审计性和差分隐私，显示出在金融和医疗等高度合规领域的高效性。

## Abstract
This paper introduces $π$-RAG, a novel architecture for oblivious retrieval that decouples Large Language Models (LLMs) from sensitive data storage without sacrificing semantic understanding. Traditional Retrieval-Augmented Generation (RAG) architectures expose raw vector embeddings to potential inversion attacks and nondeterministic retrieval failures. To address this, we utilize the digits of $π$ as a source of transcendental entropy, creating an immutable indirection layer between the LLM and private records. The value $π$ provides immutability, is uneditable and math governs it. The architecture also introduces a Semantic Quantization Layer. This layer projects user inputs onto a pre-computed manifold of Canonical Intent Centroids. RAG performs vector cosine similarity but here it maps the centroids to deterministic offsets via cryptographic salt. The resulting $π$-key is a pointer to standardized payload from the actual datastore. By replacing direct access to the datastore via LLM with this transcendental layer, $π$-RAG mathematically guarantees that the inference remains oblivious to the data. This architecture unifies deterministic randomness, auditability, and differential privacy, demonstrating high efficacy for high-compliance sectors such as finance and healthcare.

---

## 论文详细总结（自动生成）

# 论文《π-RAG: Oblivious Retrieval via Semantic Quantization and Transcendental Addressing for Large Language Models》结构化总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：传统RAG架构中，LLM直接与向量数据库交互，检索过程依赖原始向量嵌入。这存在两大风险：
  - **嵌入反演攻击**：攻击者可通过向量反演或模型窃取恢复敏感数据。
  - **非确定性检索失败**：数据更新或模型变动导致检索结果不一致、不可审计。
- **研究动机**：在金融、医疗等高合规领域，需要一种**构造性安全**的检索方案，确保LLM推理时对数据内容保持 oblivious（不可知），同时保持语义理解和可审计性。
- **核心含义**：π-RAG 通过引入**π的数字序列**作为不可变、公开可验证的熵源，在LLM与私有数据间建立间接层，实现“洞察式无关”检索。

## 2. 方法论：核心思想、关键技术细节
### 核心思想
- 利用 π 的**超越数属性**（固定、不可篡改、确定性）作为全局公有地址簿，将语义意图映射为 π 中的数值偏移，生成 **π-key**，再用该 key 索引实际数据存储中的标准化负载。从而将LLM与敏感数据物理隔离。

### 两阶段架构
- **Phase 1: 零知识意图注册（一次性）**
  - 定义**规范意图 (Canonical Intents)**，例如 BK-01 表示“检查账户余额”。
  - 为每个意图计算一个 π-key：`θ = f(ID, S_private)`，其中 `S_private` 是服务器端私密盐。
  - 计算偏移后截取 π[θ:θ+L] 作为 π-key，并将其映射到安全的 SQL 模板或数据检索函数中（密钥不包含任何实际数据）。
  - 目的：确保索引本身不含语义信息，即使泄露也无法直接恢复数据。

- **Phase 2: 查询与检索（在线）**
  - 用户查询经**语义量化层**（LLM路由）分类为固定意图 ID（如 BK-01）。
  - 在安全环境下组合 ID 与私密盐生成偏移 θ，然后从 π 中截取 π-key。
  - π-key 发送至**气隙授权检索系统**，后者验证后获取数据，并仅返回经过**微过滤**的所需字段（如余额数值），不返回完整记录。

### 关键技术公式
- 偏移计算：`θ = f(ID, S_private)`  (其中 f 为带盐哈希)
- π-key 生成：`Kπ = π[θ : θ + L]` (L=12)

### 安全保障机制
- **复制攻击失效**：攻击者无法绕过私密盐。
- **隔离性**：LLM 与数据存储之间无直接连接。
- **微过滤**：授权子系统只暴露最小必要信息。

## 3. 实验设计
- **数据集/场景**：使用**合成的银行相关数据**，设计了 6 个规范意图（BK-01 ~ BK-06），涵盖余额查询、交易历史、路由号码等典型金融场景。
- **评估方法**：对 33 条用户查询变体进行意图分类，测试语义量化层的准确率。
- **对比方法**：**并未与任何现有RAG方法进行直接对比**（如传统向量搜索、DP-RAG等）。仅比较了两种不同规模的LLM（Gemma 3:1b 与 Gemma 3n:e4b）的分类性能。
- **无标准基准**：实验未使用公开Benchmark，属于小规模概念验证。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、训练时长或算力消耗。
- 语义路由模型使用规模较小的 Gemma 系列（1B和4B参数），推理耗时<1秒/查询，未涉及大规模训练。

## 5. 实验数量与充分性
- **实验数量**：仅一组分类准确率测试（33条查询×2个模型），生成π-key的示例表（6个意图）。
- **消融实验**：无消融实验，未比较不同盐长度、不同π截取长度、不同哈希函数的影响。
- **充分性评估**：实验规模小，覆盖场景有限（仅合成银行数据），缺乏与基准方法的对比，也未进行攻击模拟或鲁棒性测试。实验不够充分，但足以作为概念验证。

## 6. 主要结论与发现
- **分类可行性**：弱模型（Gemma 3:1b）在33条查询上正确分类28条（约85%），强模型（Gemma 3n:e4b）正确分类27条（约82%），说明语义量化层可行，但依赖模型推理能力分类边界模糊的查询可能出错。
- **π-key生成确定性**：基于私密盐的偏移计算可产生唯一π-key，可验证、可审计。
- **架构安全性**：证明将LLM与数据存储物理隔离的方法理论上可防范嵌入反演和索引篡改。
- **适用领域**：适合高合规场景（金融、医疗、政府），可作为零信任环境下安全部署的蓝图。

## 7. 优点
- **创新性**：首次将超越数π应用于RAG隐私保护，利用其不可变性替代可被操控的随机数生成器。
- **审计性**：任何人都可验证某偏移指向的π数字，杜绝后门或篡改。
- **最小信息暴露**：微过滤确保只返回请求字段，不传递完整记录。
- **架构清晰**：将系统分为“非受信区（语义推理）”和“受信区（数据检索）”，物理隔离，设计合理。

## 8. 不足与局限
- **实验不充分**：仅使用合成数据，规模小，缺乏与传统RAG在准确率、延迟、隐私抗性上的量化对比。
- **未评估真实攻击**：未模拟嵌入反演、模型窃取、侧信道攻击等实际威胁。
- **灵活性受限**：规范意图需要预先注册，新意图的扩展需重配索引，缺乏动态适应性。
- **延迟开销**：语义量化层增加了额外推理步骤，相比向量搜索可能增加端到端延迟，尤其在高QPS场景下风险较高。
- **碰撞风险非零**：虽然使用私密盐和大长度π，理论碰撞概率极低，但在有限π数字串（工程中仅用前10,000位）下仍需谨慎。
- **未展示大规模部署验证**：实际生产环境中性能、可维护性、扩展性未经验证。

（完）
