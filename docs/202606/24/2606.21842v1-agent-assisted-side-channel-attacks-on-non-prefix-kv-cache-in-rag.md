---
title: Agent-Assisted Side-Channel Attacks on Non-Prefix KV Cache in RAG
title_zh: 基于Agent的RAG非前缀KV缓存侧信道攻击
authors: "He Sun, Shinan Liu, Siyuan Ma, Junhao Li, Mingjun Xiao, Wenhao Jiang"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21842v1"
tags: ["query:daily"]
score: 8.0
evidence: KV缓存侧信道攻击泄露用户隐私
tldr: "针对RAG系统中非前缀KV缓存融合的结构漏洞，提出SpliceLeak攻击，利用确定性微架构时序泄露“Step-Wave”信号，分两阶段提取隐藏提示长度和语义内容。在vLLM与LMCache上验证，攻击成功率100%，每token仅需63次请求。为防御该攻击，提出SpliceDefense框架，包含量化块填充和恒定时间边界融合，几乎不引入性能开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1785, \"height\": 706, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 925, \"height\": 430, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 437, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 819, \"height\": 383, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 938, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1849, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 917, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21842v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 904, \"height\": 385, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21842v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1785, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21842v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 897, \"height\": 392, \"label\": \"Table\"}]"
motivation: 现有KV缓存侧信道攻击要求严格线性前缀对齐，无法应对RAG中用户特有的隐私前缀，需探索非前缀融合的微架构脆弱性。
method: SpliceLeak攻击分两步：先通过Step-Wave时序指纹确定隐藏提示长度，再利用边界碰撞逐token提取语义内容。
result: "在vLLM+LMCache中，攻击成功率达100%，每token仅需63次请求，可穿透连续批处理噪声。"
conclusion: 提出SpliceDefense，结合量化块填充和恒定时间边界融合，有效消除侧信道信号，且吞吐开销极低。
---

## 摘要
现代大语言模型（LLM）服务引擎越来越依赖检索增强生成（RAG）和非前缀键值（KV）缓存融合来加速长上下文、多租户推理。虽然现有的KV缓存侧信道攻击需要严格的线性前缀对齐——这使得它们对包含独特用户特定私有前缀的真实RAG查询无效——但我们发现了一类关键的、与分块感知内存调度相关的结构性漏洞。我们证明了用于对齐和融合不连续内存块的确定性微架构机制无意中泄露了连续的“阶梯波”时序特征。利用这一物理观察，我们引入了SpliceLeak，这是首个针对非前缀KV缓存融合的端到端侧信道攻击。SpliceLeak执行系统性的两阶段隐私破坏：首先结构性指纹化隐藏私有提示的确切长度，随后操纵边界碰撞逐令牌提取确切的语义内容。在生产级框架（集成LMCache的vLLM）上的广泛评估表明，SpliceLeak在有界熵场景下实现了高达100%的提取成功率。受确定性+104毫秒硬件延迟空洞驱动，该攻击每令牌仅需63次请求，就能穿透真实的连续批处理噪声。为了解决内存去重与安全性之间的固有冲突，我们提出了SpliceDefense，一个由量化块填充（QCP）和恒定时间边界融合（CTBF）组成的双向防御框架。我们的评估证实，SpliceDefense能有效压制侧信道信号（Delta TTFT ~ 0），且吞吐开销可忽略，从而保留了全局缓存共享的关键优势。

## Abstract
Modern Large Language Model (LLM) serving engines increasingly rely on Retrieval-Augmented Generation (RAG) and non-prefix Key-Value (KV) cache fusion to accelerate long-context, multi-tenant inference. While existing KV cache side-channel attacks require strict linear prefix alignment--rendering them ineffective against real-world RAG queries that contain unique, user-specific private prefixes--we uncover a critical class of structural vulnerabilities inherent to chunk-aware memory scheduling. We demonstrate that the deterministic micro-architectural mechanisms used to align and fuse disjoint memory chunks inadvertently leak a continuous "Step-Wave" timing signature. Exploiting this physical observation, we introduce SpliceLeak, the first end-to-end side-channel attack targeting non-prefix KV cache fusion. SpliceLeak executes a systematic two-phase privacy breach: it first structurally fingerprints the exact length of hidden private prompts, and subsequently manipulates boundary collisions to extract exact semantic content token-by-token. Extensive evaluations on production-grade frameworks (vLLM integrated with LMCache) demonstrate that SpliceLeak achieves up to a 100% extraction success rate in bounded-entropy scenarios. Driven by a deterministic +104 ms hardware latency void, the attack requires as few as 63 requests per token, piercing through realistic continuous batching noise. To resolve the inherent conflict between memory deduplication and security, we propose SpliceDefense, a bipartite mitigation framework consisting of Quantized Chunk Padding (QCP) and Constant-Time Boundary Fusion (CTBF). Our evaluations confirm that SpliceDefense effectively flattens the side-channel signal (Delta TTFT ~ 0) with negligible throughput overhead, preserving the critical benefits of global cache sharing.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文《Agent-Assisted Side-Channel Attacks on Non-Prefix KV Cache in RAG》生成的结构化、深入、客观的中文总结。

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现代大语言模型（LLM）服务引擎广泛采用检索增强生成（RAG）和非前缀键值（KV）缓存融合技术来加速长上下文、多租户推理。然而，现有的KV缓存侧信道攻击（如PROMPTPEEK、Shadow in the Cache）依赖于严格的前缀对齐，即攻击者必须完美匹配用户提示的起始部分才能触发缓存命中。在实际RAG场景中，用户提示通常包含独特的私有前缀（如个性化系统指令、对话历史），导致传统攻击的前缀对齐假设崩塌，时序信号瞬间消失（ΔTTFT ≈ 0），给业界带来了错误的安全感。

- **核心问题**：揭示非前缀KV缓存融合机制中存在的、被忽视的微架构侧信道漏洞，并证明即使没有前缀对齐，攻击者仍能利用确定性分块调度泄露的“阶梯波”时序信号，系统性地窃取用户的私有结构和语义信息。

- **整体含义**：本文首次将侧信道攻击从严格前缀匹配扩展到RAG非前缀融合场景，指出当前威胁模型严重低估了多租户LLM即服务（LLMaaS）环境中的隐私风险，并提出了相应的攻击与防御方案。

## 2. 论文提出的方法论

- **核心思想**：利用非前缀KV缓存融合中确定性分块内存调度导致的“阶梯波（Step-Wave）”微架构时序信号，分两阶段突破用户隐私隔离：第一阶段精确提取私有前缀长度，第二阶段利用边界碰撞逐token提取语义内容。

- **关键技术细节**：
  - **Step-Wave效应**：当引擎将不连续的记忆块（chunk）缝合时，会触发固定的分块路由开销（如38.6 ms/chunk）和线性尾部重计算开销（如0.13 ms/tok）。这导致TTFT随文档长度呈现阶梯波分布，而非理论上的锯齿波。攻击者可据此反向推断隐藏前缀长度（±30 token精度）。
  - **两阶段攻击（SpliceLeak）**：
    - **Phase I：结构指纹识别**：通过测量TTFT，利用Step-Wave Oracle精确计算私有前缀的token长度（Nvic），将语义熵降到极低。
    - **Phase II：语义提取**：基于已知长度Nvic，攻击者构造探测提示：`Ppad ∥ tguess ∥ Ctarget`，其中Ppad长度为Nvic-1，tguess为候选token。如果tguess与目标边界token匹配，则触发缓存命中，TTFT显著降低。利用LLM驱动的提取代理（ALLM）生成高概率候选子集，逐token从右向左恢复完整前缀。

- **算法流程**（文字描述）：
  1. 初始化提取序列 `Sextracted = []`。
  2. 从 `i = Nvic` 向下遍历到1：
     - 生成长度为 `i-1` 的填充序列 `Ppad`。
     - 通过LLM代理 `ALLM` 预测候选子集 `Vsub`（基于已提取内容 `Sextracted` 和公共块 `Ctarget`）。
     - 对每个候选 `tguess`：
       - 构造探测 `Xadv = Ppad ∥ tguess ∥ Sextracted ∥ Ctarget`。
       - 测量TTFT。
       - 若TTFT低于基线阈值，则记录为正确的边界token。
     - 将正确token添加到 `Sextracted` 前端。
  3. 返回 `Sextracted`。

## 3. 实验设计

- **使用的数据集/场景**：
  - **Scenario A（机密输入提取）**：使用PromptBase中的Cloze填空式模板（已知模板，未知变量如疾病名）+ 2WikiMQA/Musique文档。
  - **Scenario B（专有模板逆向）**：使用awesome-chatgpt-prompts和alpaca-gpt4数据集模拟专有系统指令。
  - **Scenario C（零知识隐私重建）**：使用Ultrachat数据集中的非结构化多轮对话。

- **Benchmark**：生产级框架vLLM（v0.4.x）集成LMCache，采用连续批处理调度（最大batch size 16）。目标模型：LongChat-7B-v1.5-32K（MHA）和Qwen2.5-7B-Instruct（GQA）。

- **对比方法**：由于现有前缀对齐攻击（如PROMPTPEEK）在RAG场景下失效，本文主要通过验证其信号消失来证明新攻击的必要性，未在相同实验条件下直接对比其成功率（而是通过图3对比时序信号差异）。防御评估对比了“无防御”和“SpliceDefense”。

## 4. 资源与算力

- **硬件环境**：本地GPU集群，配备4块NVIDIA A40 GPU（每块48GB VRAM）和256GB系统RAM。
- **模型与服务端**：推理服务端为vLLM + LMCache，无需额外训练，仅涉及攻击推理时的请求和TTFT测量。
- **未明确说明**：论文未提及攻击过程的总耗时、辅助LLM的推理资源消耗或具体训练时长。

## 5. 实验数量与充分性

- **实验数量**：
  - Scenario A：30个Cloze任务，分别在MHA和GQA模型上各测试一次（共60组）。
  - Scenario B：50个专有模板提取任务（每个任务连续提取10个token的边界后缀）。
  - Scenario C：15个零知识用户查询提取任务（同样10-token连续提取）。
  - 微基准测试：单token提取（5种候选）和连续5个token的右向左提取。
  - 防御评估：案例研究（5个token）+ 15个任务的批量评估（共75个提取步骤）。
- **充分性与公平性**：
  - **充分**：覆盖三种典型威胁场景，两种主流注意力架构（MHA和GQA），模拟了真实的多租户背景噪声（泊松到达率0.004 req/s）。实验结果输出成功率、反转比率、攻击成本等指标，统计结果清晰。
  - **客观公平**：实验设计合理，考虑了batch噪声和网络抖动，对比了有/无防御的时序信号差异。但未与现有侧信道攻击（如PROMPTPEEK）在相同环境下进行端到端成功率对比（因前者根本无法工作）。少数场景（Scenario A的GQA结果）仅显示中位数，缺乏方差信息。

## 6. 论文的主要结论与发现

- SpliceLeak攻击在实际RAG系统中具有极高的有效性：在Scenario A下实现100%的成功率（SR@1），在Scenario B/C下实现100%的成功率和反转比率（RR=100%）。
- 攻击成本极低：每token仅需约63次请求（场景B/C），在已知模板的Scenario A中仅需约21次请求/输入。
- 时序信号极为稳定：正确与错误token间的TTFT差距（ΔTTFT）可达+104~+138 ms，远超系统噪声。
- 防御方案SpliceDefense能有效消除侧信道信号（ΔTTFT ≈ 0），且吞吐开销极低（QCP引入<5ms延迟）。

## 7. 优点

- **创新性**：首次系统性探索非前缀KV缓存融合的微架构侧信道，打破“前缀对齐”的思维定势，填补了RAG安全研究在控制平面（调度/内存管理）的空白。
- **实用性**：攻击在黑盒设置下仅需标准API访问，无需系统特权，且能适应三种典型威胁场景。防御方案（QCP+CTBF）易于部署，且能保持缓存共享的吞吐收益。
- **实验充分**：覆盖两种模型架构、多种场景、批量攻击与微基准，证明了攻击的通用性和鲁棒性。防御评估验证了信号消除和低开销。
- **理论建模**：对Step-Wave效应进行了形式化建模，揭示了分块大小、路由开销和尾部重计算之间的线性关系。

## 8. 不足与局限

- **实验覆盖局限**：仅测试了两种模型（LongChat-7B-MHA, Qwen2.5-7B-GQA），且仅基于PagedAttention和LMCache的线性非前缀融合。未在更先进的RadixTree系统（如SGLang）或上下文压缩架构上验证Step-Wave效应的存在性。
- **依赖前提条件**：攻击需要时间共驻（共享块在缓存中未被驱逐）、能合理推断公共知识块（Ctarget）、以及系统网络抖动较小。在极端WAN波动或频繁缓存逐出环境中，攻击成功率可能下降。
- **提取效率受限于辅助LLM**：在零知识场景（Scenario C）中，若用户使用高度异常的词汇，LLM代理的候选子集可能遗漏正确token，导致提取暂停。论文仅给出了可控实验（确保候选包括真值），未评估真实LLM预测准确率对攻击成本的量化影响。
- **防御的容量代价未深入量化**：量化块填充（QCP）会额外存储填充token，可能在高内存压力下略微降低最大并发批处理容量。论文仅给出延迟开销，未量化内存开销。
- **对比缺失**：尽管现有攻击在RAG下失效，但论文未在实验中直接运行这些攻击并对比其失败模式，削弱了“击败先前工作”的说服力。此外，未与其他RAG安全研究（如数据投毒、DoS）进行系统性对比。

（完）
