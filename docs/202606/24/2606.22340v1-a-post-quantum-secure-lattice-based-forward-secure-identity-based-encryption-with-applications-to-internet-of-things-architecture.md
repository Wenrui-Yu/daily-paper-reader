---
title: A Post-Quantum Secure Lattice-Based Forward-Secure Identity Based Encryption with Applications to Internet of Things Architecture
title_zh: 一种抗量子安全的基于格的前向安全身份基加密及其在物联网架构中的应用
authors: "Abhishek Kumar, Vikas Srivastava, Sumit Kumar Debnath, Pantelimon Stănică"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22340v1"
tags: ["query:daily"]
score: 9.0
evidence: 物联网加密密码学
tldr: 物联网设备资源受限且易受密钥提取攻击，传统公钥基础设施开销大。身份基加密(IBE)通过设备身份直接派生公钥，但现有IBE缺乏前向安全，且基于经典假设的方案无法抵抗量子攻击。本文提出基于环上带误差学习(RLWE)的前向安全IBE方案，采用二叉树最小覆盖机制与陷门委托实现紧凑公钥参数和高效密钥更新。与基于LWE的现有方案相比，公钥、私钥和密文尺寸均显著降低，更适用于实际物联网环境。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22340v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1038, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22340v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1123, \"height\": 693, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22340v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1251, \"height\": 1080, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22340v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1107, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22340v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 503, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22340v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 980, \"height\": 309, \"label\": \"Table\"}]"
motivation: 物联网设备面临侧信道和密钥提取攻击，需要前向安全IBE；经典假设方案不抗量子，而现有格基前向安全IBE密钥和密文过大，不适合资源受限设备。
method: 基于RLWE假设，结合环上陷门委托和二叉树最小覆盖机制设计密钥更新与加密算法，实现紧凑公钥参数和高效密钥更新。
result: 公钥、私钥和密文尺寸相比现有LWE基前向安全IBE方案显著减小，计算效率更高。
conclusion: 该方案在实现前向安全的同时保持后量子安全，适用于资源受限的物联网架构。
---

## 摘要
物联网的快速发展导致异构且资源受限设备间的数据交换规模空前。在此类环境中确保机密性和安全的密钥管理充满挑战。传统公钥基础设施需要繁重的证书处理开销。身份基加密通过直接从设备身份派生公钥提供了一种轻量级替代方案，使其对物联网部署具有吸引力。然而，物联网设备极易受到侧信道和密钥提取攻击，这激发了对前向安全身份基加密的需求，即当前密钥的泄露不会威胁过去的通信。现有基于经典困难假设的前向安全身份基加密构造在后量子时代不安全，而基于格（LWE）的前向安全方案存在密钥和密文尺寸过大的问题，限制了其在资源受限的物联网系统中的适用性。本文提出了一种新的环上基于格的前向安全身份基加密方案，依赖RLWE假设实现后量子安全性和显著的效率提升。我们的设计使用基于二叉树的最小覆盖机制的陷门委托。它实现了紧凑的公共参数和高效的每周期密钥更新。与先前基于LWE的构造相比，我们的方案减小了公钥、私钥和密文尺寸，从而更适用于实际的物联网环境。

## Abstract
The rapid expansion of the Internet of Things (IoT) has led to an unprecedented scale of data exchange across heterogeneous and resource-constrained devices. Ensuring confidentiality and secure key management in such environments is challenging. Traditional public-key infrastructures require heavy certificate-handling overhead. Identity-Based Encryption (IBE) offers a lightweight alternative by deriving public keys directly from device identities, making it attractive for IoT deployments. However, IoT devices are highly vulnerable to side-channel and key-extraction attacks, motivating the need for Forward-Secure IBE(FS-IBE), where the compromise of a current secret key does not threaten past communications. Existing FS-IBE constructions based on classical hardness assumptions are not secure in the era of post-quantum, while the lattice-based (LWE-based) forward-secure scheme suffer from large key and ciphertext sizes, limiting their suitability for constrained IoT systems. Here, we propose a new lattice-based fs-IBE scheme in the ring setting, relying on the RLWE assumption to achieve post-quantum security and significant efficiency gains. Our design uses trapdoor delegation with a minimal-cover mechanism over a binary tree. It results in compact public parameters and efficient per-epoch key updates. Compared to prior LWE-based constructions, our scheme reduces public key, secret key, and ciphertext sizes, and thus, making it better suited for practical IoT environments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：物联网设备资源受限且易受侧信道和密钥提取攻击，传统公钥基础设施（PKI）开销大，而身份基加密（IBE）省去证书管理，但缺乏前向安全（当前密钥泄露不影响过去通信）。现有基于经典困难假设的FS-IBE方案不抗量子攻击，而基于LWE的格基FS-IBE方案密钥和密文尺寸过大，不适合资源受限的IoT。
- **整体含义**：提出首个基于环上带误差学习（RLWE）假设的前向安全IBE方案（RFS-IBE），在实现后量子安全的同时大幅降低存储和通信开销，使前向安全加密能够在受限IoT设备中实际部署。

### 2. 方法论

- **核心思想**：在环设置（\(R_q = \mathbb{Z}_q[x]/\langle x^n+1\rangle\)）下，利用RLWE的代数结构压缩矩阵规模；结合二叉树结构与最小覆盖机制实现周期密钥更新与陷门委托，保证前向安全。
- **关键技术细节**：
  - 使用`ringGenTrap`生成主密钥与公共参数（包含环元素 \(a\in R_q^{l+k}\) 和陷门 \(R_a\)）。
  - 对于每个身份 \(id\) 和周期 \(t\)，构建向量 \(a_{id,t} = [a, a_{id,v_{1,j_1}}, \dots, a_{id,v_{d-1,t+1}}]\)（通过哈希 \(H(id, v_{i,j_i})\) 得到），以及 \(y_{id,t}=G(id,t)\)。
  - 利用二叉树最小覆盖集 \(MinCov(v_{d-1,t+1})\) 确定当前周期需保留的陷门集合，通过`ringExtBasis`扩展陷门，`ringRandBasis`随机化，然后使用`ringGenSamplePre`生成采样向量 \(e_{id,t}\)，构成密钥 \(sk_{id,t}=(T_{id,t}, e_{id,t})\)。
  - 加密：随机选择 \(s\in R_q\)，计算 \(p = a_{id,t}s + x\)（\(x\) 为小错误）及 \(c = y_{id,t}^T s + x' + b\lfloor q/2 \rfloor\)。
  - 解密：计算 \(b' = c - e_{id,t}^T p\)，逐系数判定是否接近0或\(q/2\)恢复消息。
- **算法流程**（5个算法）：
  - `Setup(λ, N)`：生成公共参数params和主密钥msk。
  - `KeyGen(params, msk, id)`：输出初始密钥\(sk_{id,0}\)。
  - `Update(params, t, id)`：从\(sk_{id,t}\)更新为\(sk_{id,t+1}\)。
  - `Encrypt(params, (id,t), M)`：输出密文(p, c)。
  - `Decrypt(params, C, sk_{id,t})\)：恢复消息或输出⊥。

### 3. 实验设计

- **场景与Benchmark**：在资源受限的IoT设备（如医院网关与传感器）环境中，重点评估执行时间和内存开销。
- **对比方法**：与Jin等人[10]（LWE基FS-IBE方案）进行对比。
- **实现细节**：使用Python 3.10、NumPy库，在Linux系统（Intel Core i5, 8GB RAM）上运行。

### 4. 资源与算力

- 文中**未明确说明使用的GPU**，仅提及CPU（Intel Core i5）和8GB内存，未报告训练时长或大规模并行计算资源。实验属于轻量级原型实现，未涉及大规模算力需求。

### 5. 实验数量与充分性

- 实验数量**较少**：仅比较了KeyGen、Encrypt、Decrypt三个操作的平均执行时间和内存占用（每个操作一个数值，未提供多次重复的标准差或置信区间）。
- 充分性评估：缺乏多组参数下的对比（如不同安全级别、不同周期数N）、未进行消融实验、未在真实IoT硬件（如ARM Cortex-M）上部署测试。因此实验的充分性和公平性有限，主要作为概念验证。

### 6. 主要结论与发现

- 所提RFS-IBE方案在公钥、私钥和密文尺寸上均优于Jin等人的LWE方案。具体地，私钥和密文尺寸从\(\Omega(n^2 (\log q)^2 \log N)\)降至\(O(n\cdot \text{polylog}(N))\)。
- 执行时间和内存占用更低（例如KeyGen时间从2.74ms降至1.82ms，内存从58.3KB降至42.6KB）。
- 方案在随机预言机模型下证明为fs-sID-CPA安全（基于RLWE假设），并可通过标准变换扩展至CCA安全。

### 7. 优点

- **创新性**：首个RLWE基前向安全IBE方案，结合二叉树最小覆盖与环上陷门委托。
- **效率提升显著**：通过环结构将高维矩阵压缩为环元素，大幅降低存储与通信开销，更适合IoT。
- **安全性**：后量子安全，且形式化安全证明完整。
- **实用性**：给出了在医疗物联网架构中的应用案例，具有实际参考价值。

### 8. 不足与局限

- **实验覆盖不足**：仅做了单次测量，缺乏统计可靠性；未在不同参数或更大周期数下测试；未与更多同类方案对比（如基于NTRU的FS-IBE）。
- **实现局限**：原型使用Python和NumPy，未针对硬件优化，实际部署可能需用C/C++或专用加速器。
- **应用限制**：仅实现CPA安全，CCA安全需额外转换会增加开销；树深度d影响效率，但未讨论最优选择。
- **偏差风险**：对比方案[10]未被完全复现，而是引用了其论文中的数值，可能存在实现环境差异。

（完）
