---
title: General Techniques for Reducing Key-Switching Overhead in Privacy-Preserving Two-Party Transformer Inference
title_zh: 减少隐私保护两方Transformer推理中密钥切换开销的通用技术
authors: "Wenshao Yang, Zhenhua Liu, Dongdong Yao"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25349v1"
tags: ["query:daily"]
score: 9.0
evidence: 降低两方隐私保护Transformer推理开销
tldr: 隐私保护双方Transformer推理中，FHE线性层的密钥切换开销占主导。现有优化依赖特定打包策略，通用性差。本文从独立于打包的视角，提出预处理辅助安全注意力计算，将密钥切换移至离线；进一步提出存储-通信权衡减少预计算存储；并针对乘法后旋转模式提出融合密钥切换。分析表明在线密钥切换显著降低，且无需修改打包策略。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25349v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 501, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25349v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1885, \"height\": 1158, \"label\": \"Table\"}]"
motivation: 现有降低密钥切换开销的方法依赖特定打包算法，缺乏通用性。
method: 提出预处理辅助注意力分解、存储-通信权衡和融合密钥切换技术，三者均独立于打包策略。
result: 在线密钥切换开销显著降低，并提供存储与通信的灵活权衡。
conclusion: 本文技术通用有效，可应用于现有FHE或混合系统，无需修改打包策略。
---

## 摘要
在安全两方Transformer推理中，线性层通常通过全同态加密（FHE）进行明文-密文或密文-密文矩阵乘法来评估，其中密钥切换主要发生且主导了基于FHE和混合FHE-MPC系统的计算开销。现有优化严重依赖于特定打包算法，限制了其通用性。  针对这一包无关视角的开销，我们提出了一种用于安全注意力计算的预处理辅助方法。通过将注意力分解为可预计算操作和在线交互，该方法在不修改现有打包策略的情况下减少了在线推理阶段的密钥切换。  然而，第一种方法将密钥切换转移到离线阶段引入了额外的存储需求。为解决此问题，我们提出了存储-通信权衡技术，用适度的在线通信替换大型预计算密文，从而在不同资源约束下实现灵活部署。  虽然在混合方案和基于FHE的方案的第一层中，密文-密文矩阵乘法被卸载到预处理阶段，但这些操作仍然存在于离线阶段和后续FHE层中。为进一步优化，我们提出了一种针对现有RNS-CKKS矩阵乘法方案中频繁出现的乘法后接旋转模式的融合密钥切换技术。通过将重线性化和旋转合并为单一过程，该技术降低了相关计算成本。  分析评估表明，我们提出的技术显著减少了在线密钥切换开销，并在无需修改现有打包策略的情况下提供了存储与通信之间的灵活权衡。

## Abstract
In secure two-party Transformer inference, linear layers are typically evaluated using Fully Homomorphic Encryption (FHE) through plaintext-ciphertext or ciphertext-ciphertext matrix multiplications, where key switching primarily occurs and dominates computational overhead in both FHE-based and hybrid FHE-MPC systems. Existing optimizations rely heavily on packing-specific algorithms, limiting their general applicability.   Targeting this overhead from a packing-independent perspective, we propose a preprocessing-assisted method for secure attention computation. By decomposing attention into precomputable operations and online interactions, this method reduces online inference-phase key switching without modifying existing packing strategies.   However, the first method shifting key switching offline introduces additional storage requirements. To address this, we propose storage-communication trade-off techniques that replace large precomputed ciphertexts with modest online communication, enabling flexible deployment under varying resource constraints.   While ciphertext-ciphertext matrix multiplication is offloaded to the preprocessing phase in hybrid schemes and the first layer of FHE-based schemes, these operations still persist in the offline stage and subsequent FHE layers. To further optimize it, we propose a fused key-switch technique targeting the multiplication-followed-by-rotation pattern, which frequently arises in existing RNS-CKKS matrix multiplication schemes. By combining relinearization and rotation into a single procedure, this technique reduces the associated computation costs.   Analytical evaluations demonstrate that our proposed techniques significantly reduce online key-switch overhead and provide flexible trade-offs between storage and communication without requiring modifications to existing packing strategies.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在隐私保护的两方Transformer推理中，线性层（如注意力机制中的矩阵乘法）通常基于全同态加密（FHE）实现，其中**密钥切换（key-switching）操作**占据绝大部分计算开销。现有优化方法高度依赖于特定的明文打包（packing）方案（如面向小批次或大批次的设计），导致通用性差，难以跨系统迁移。
- **整体含义**：本文旨在提出一种**打包无关（packing-independent）** 的通用技术，在不修改现有打包策略的前提下，系统性地减少在线推理阶段的密钥切换开销，从而提升隐私保护Transformer推理的实用性和灵活性。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
### 核心思想
通过**预处理辅助**将注意力计算中的部分密文操作提前至离线阶段完成，结合**存储-通信权衡**和**融合密钥切换**技术，减少在线阶段的计算开销，同时保持与各种打包方案的兼容性。

### 关键技术细节
- **预处理辅助注意力分解**：
  - 将输入 `X` 分解为 `X = U + (X - U)`，其中 `U` 是客户端生成的随机掩码矩阵。
  - 将注意力分数 `QK^T` 展开为四项之和，其中三项包含预计算的密文（如 `[[Qu]], [[Ku]]`）与明文/密文的混合运算，可将大部分密文-密文乘法（及对应的密钥切换）移至离线预处理阶段。
  - 算法流程（Algorithm 1）：离线生成 `[[Qu]], [[Ku]], [[Vu]]` 及其变换集 `CQ, CK, CV`；在线时只需计算与 `(X - U)` 相关的明文-密文操作和少量密文运算。
- **存储-通信权衡策略**：
  - 针对预处理阶段产生的大量密文变换集（如 `CQ, CK, CV`）导致的存储开销，提出通过在线传输少量明文矩阵（如 `B1, B2, E, F, J, P`）来替代部分预计算密文。
  - 给出FHE版的Algorithm 2和混合FHE-MPC版的Algorithm 3、4，通过引入随机掩码和加密校正项，实现存储与通信的灵活折中。
- **融合密钥切换技术**（Algorithm 5）：
  - 针对FHE中常见的“乘法后接旋转”模式（如RNS-CKKS矩阵乘法），提出将重线性化（relinearization）和旋转（rotation）合并为一次密钥切换过程。
  - 核心：直接使用融合评估密钥 `evk_fused` 对三次多项式密文同时进行 `τ→s` 和 `τ²→s` 的转换，节省一次ModDown操作（通常占密钥切换的主要成本）。

## 3. 实验设计：数据集、基准方法、对比方法
- **实验类型**：本文为**分析评估（analytical evaluation）**，未提供实际代码实现或端到端基准测试。
- **对比方法与场景**：
  - 选取两个代表性方案：**Arion**（纯FHE方案）和**BLB**（混合FHE-MPC方案）。
  - 对每个方案考虑三个变体：原始版本；集成Algorithm 1后的版本（记为Arion(1)/BLB(1)）；集成存储-通信权衡后的版本（Arion(2)/BLB(2)）。
  - 评估指标：密钥切换操作次数、预处理存储密文数、在线通信量（数据流量）。
- **场景参数**：假设序列长度 `m=512`，嵌入维度 `d=1024`，头数 `H=16`，ring degree `N=2^15`。该参数匹配中等规模的Transformer模型（如GPT-2）。

## 4. 资源与算力
- 论文**未提及**任何实际GPU型号、数量、训练时长或推理时间测量。所有结论基于理论计数（key-switch次数、密文数量等）推导，无实际运行时数据。

## 5. 实验数量与充分性
- **实验数量**：仅有一个理论对比表格（Table II），涵盖三个计算阶段（Q/K/V投影、注意力分数计算、值聚合），对比了各变体在四个指标上的数值。
- **充分性评价**：
  - **正面**：理论分析覆盖了主要计算阶段，指标清晰，对比逻辑完整。
  - **不足**：缺乏实际运行验证（如推理延迟、吞吐量、精确度等）；未做消融实验（如单独评估融合密钥切换的影响）；仅固定一组参数，未测试不同序列长度、批量大小或打包策略的影响；未与除Arion、BLB以外的系统对比。因此**实验充分性有限**，论文自身也声明后续将发布包含端到端实现的更新版本。

## 6. 论文的主要结论与发现
- **在线密钥切换显著减少**：在注意力分数和值聚合阶段，原方案需数百至数千次密钥切换，集成本文技术后可降至零或极少（如BLB从346次降至0次；Arion从1536次降至0次）。
- **存储-通信权衡有效**：原方案需存储大量密文（如Arion(1)需8384个），通过权衡仅需384个密文加在线传输少量明文（B1/B2），显著降低存储压力。
- **融合密钥切换节省一次ModDown**：在乘法后旋转模式中，可减少约一半的ModDown计算量。
- **方法通用性**：所有技术均与具体打包方案无关，可集成到现有FHE或混合FHE-MPC系统。

## 7. 优点
- **方法层面的亮点**：
  - 提出**打包无关**的优化视角，突破了现有工作必须针对特定打包方案设计的局限。
  - 预处理辅助分解 + 存储-通信权衡提供了灵活的**计算-存储-通信**三角权衡，适应不同资源场景。
  - 融合密钥切换算法简洁有效，可直接应用于常见计算模式。
- **理论分析清晰**：给出了明确的密钥操作计数和存储/通信量化对比，便于理解收益。
- **安全性分析**（Section V-C）：
  - 基于半诚实模型的真实-理想模拟范式，给出了客户端和服务器的模拟器构造，论证了协议在IND-CPA假设下的安全性，增强了可信度。
- **可集成性强**：方法不要求修改底层的编码或打包方案，可作为插件式优化。

## 8. 不足与局限
- **缺乏实证验证**：论文未提供任何实际运行结果（如延迟、吞吐量、精度），属于纯理论分析，结论的实际有效性未经实验证实。
- **实验覆盖不足**：
  - 仅测试一组固定参数，未探索不同序列长度、嵌入维度、头数或非标准打包策略。
  - 未进行消融实验（如单独评估预处理分解 vs. 融合密钥切换的贡献）。
  - 仅对比Arion和BLB，未涵盖其他常见系统（如THOR, BOLT, NEXUS等）。
- **应用限制**：
  - 纯FHE场景下，仅第一层可应用本文方法；后续层仍需在线密钥切换。
  - 混合场景下虽可扩展至所有层，但增加了交互轮次和通信量。
  - 安全模型为**半诚实（honest-but-curious）**，未考虑恶意敌手。
- **潜在偏差风险**：理论计数可能忽略实际计算中的其他开销（如NTT转换、内存带宽、通信延迟等），优化收益可能被高估。

（完）
