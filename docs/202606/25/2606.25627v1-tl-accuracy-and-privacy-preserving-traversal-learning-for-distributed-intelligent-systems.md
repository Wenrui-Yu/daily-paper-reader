---
title: "TL++: Accuracy and Privacy Preserving Traversal Learning for Distributed Intelligent Systems"
title_zh: TL++：面向分布式智能系统的准确性与隐私保护遍历学习
authors: "Erdenebileg Batbaatar, Young Yoon"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25627v1"
tags: ["query:daily"]
score: 9.0
evidence: 分布式系统中的隐私保护遍历学习
tldr: "分布式智能系统训练面临数据孤岛、通信开销和隐私泄露问题。联邦学习与分割学习各有局限，TL++提出双模式遍历学习框架，基模式交换切层激活与梯度，安全模式通过非共谋服务器秘密共享激活张量。在CIFAR-10上TL++基模式准确率91.41%，安全模式90.93%，均超过最强基线12个百分点，通信量降低13.1倍。TL++以较低通信成本实现近集中训练性能，并提供激活级隐私保护。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25627v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 988, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25627v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1338, \"height\": 1148, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25627v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1346, \"height\": 762, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25627v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1193, \"height\": 630, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1449, \"height\": 955, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 837, \"height\": 617, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1397, \"height\": 424, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1136, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 689, \"height\": 457, \"label\": \"Table\"}]"
motivation: 现有联邦学习通信开销大且异构性敏感，分割学习暴露激活与梯度，需兼顾精度与隐私的分布式训练方法。
method: TL++构建虚拟批次恢复集中小批量梯度行为，基模式交换切层激活与梯度，安全模式秘密共享激活张量至半诚实双服务器。
result: "CIFAR-10上TL++基模式准确率91.41%（SD 0.19），安全模式90.93%（SD 0.17），通信较全模型同步降低13.1倍。"
conclusion: TL++在接近集中训练性能的同时，显著降低通信并提供了激活级秘密共享隐私保护。
---

## 摘要
分布式智能系统越来越需要在数据孤岛之间进行训练，而无需集中原始数据。联邦学习保持数据本地化，但可能因异构分区而受影响，并且需要重复的全模型交换。分割学习通过切层激活减少通信，但标准协议通常无法恢复集中式小批量梯度行为，并可能以明文形式暴露激活和梯度。我们提出TL++，一种双模式遍历学习框架，该框架跨节点构建虚拟批次，以在显式同步假设下恢复集中式小批量梯度行为。基础模式交换切层激活和梯度，而非完整模型。安全模式在编排器与非共谋辅助者之间秘密共享每个切层激活和梯度，防止任一服务器观察到明文切层张量。此保护限于半诚实双服务器设置；标签和损失相关输出对编排器仍可见。在本文评估的轻量级安全路径中，精确性要求线性或仿射服务器路径，而非线性操作需要非线性MPC或近似。我们对TL++进行形式化，分析通信和计算成本，并在CIFAR-10和BioGPT/PubMedQA上使用全微调和LoRA与联邦学习和分割学习基线进行对比。在CIFAR-10上，TL++基础切层1和精确安全切层3分别达到91.41%（SD 0.19）和90.93%（SD 0.17）的准确率，超过最强非TL++基线12个百分点以上。TL++基础切层1相对于全模型同步将每步通信减少了13.1倍。PubMedQA结果同样有利于TL++。总体而言，TL++接近集中式训练性能，同时减少通信并提供激活级秘密共享。

## Abstract
Distributed intelligent systems increasingly need to train across data silos without centralizing raw data. Federated learning keeps data local but can suffer under heterogeneous partitions and requires repeated full-model exchange. Split learning reduces communication through cut-layer activations, but standard protocols generally do not recover centralized mini-batch gradient behavior and may expose activations and gradients in plaintext. We present TL++, a two-mode traversal-learning framework that constructs virtual batches across nodes to recover centralized mini-batch gradient behavior under explicit synchronization assumptions. Base mode exchanges cut-layer activations and gradients rather than full models. Secure mode secret-shares each cut-layer activation and gradient between an orchestrator and a non-colluding helper, preventing either server from observing plaintext cut-layer tensors. This protection is limited to a semi-honest two-server setting; labels and loss-related outputs remain visible to the orchestrator. In the lightweight secure path evaluated here, exactness requires a linear or affine server path, while nonlinear operations require nonlinear MPC or approximation. We formalize TL++, analyze communication and computation costs, and evaluate it against federated and split-learning baselines on CIFAR-10 and BioGPT/PubMedQA using full fine-tuning and LoRA. On CIFAR-10, TL++ base cut 1 and exact secure cut 3 achieve accuracies of 91.41% (SD 0.19) and 90.93% (SD 0.17), respectively, exceeding the strongest measured non-TL++ baseline by more than 12 percentage points. TL++ base cut 1 also reduces per-step communication by 13.1-fold relative to full-model synchronization. PubMedQA results similarly favor TL++. Overall, TL++ approaches centralized-training performance while reducing communication and providing activation-level secret sharing.

---

## 论文详细总结（自动生成）

## 论文总结：TL++：面向分布式智能系统的准确性与隐私保护遍历学习

### 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：分布式智能系统（如临床监控、自动驾驶、工业传感器）需要在数据孤岛之间训练模型，但又不能集中原始数据。现有两种主流范式——**联邦学习（FL）**和**分割学习（SL）**——各有明显缺陷：
  - **FL** 传输完整模型参数，通信开销大；在非独立同分布（Non-IID）数据下，局部梯度偏离全局目标，导致收敛退化；模型更新可能泄露训练样本。
  - **SL** 通过传输切层激活而非完整模型降低了通信，但每次仅处理一个客户端的样本，无法实现跨参与者的梯度聚合，从而丧失大批次带来的优势；且激活和梯度以明文传输，易受梯度反转攻击。
- **核心挑战**：同时满足 **精度无损（与集中训练等价）**、**通信高效** 和 **中间计算安全** 三个要求。现有方法无法同时满足这三者。
- **整体含义**：TL++ 旨在填补上述空白，通过构建“虚拟批次”恢复集中式小批量梯度，并利用秘密共享保护切层激活和梯度，在不牺牲精度的前提下，显著降低通信开销并提供可控的隐私保护。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：基于遍历学习（Traversal Learning）的虚拟批次构造，使得分布式梯度更新在数学上与集中式小批量SGD等价；并通过加性秘密共享（Additive Secret Sharing）在非共谋的双服务器（编排器+辅助服务器）之间分布切层激活和梯度，防止任一服务器看到明文中间张量。
- **关键技术细节**：
  - **虚拟批次构建**：编排器收集各节点的样本数，全局随机打乱并划分为非重叠的虚拟批次。每个批次包含来自多个节点的样本，因此计算出的梯度等同于集中训练同一批次的梯度。
  - **两种模式**：
    - **基础模式（Base Mode）**：节点直接向编排器发送明文切层激活，编排器完成前向/后向传播并返回梯度。该模式与原始TL等价，适用于可信环境。
    - **安全模式（Secure Mode）**：节点将切层激活拆分为两个加性秘密份额（随机掩码+差值），分别发送给编排器和辅助服务器。两服务器独立处理自己的份额，仅在线性/仿射操作下可精确恢复结果；非线性操作需额外安全协议或近似。输出层损失计算需重构完整预测，故标签对编排器可见。
  - **梯度等价性命题**：在相同模型参数、优化器状态和批次索引条件下，TL++基础模式产生的梯度与集中式小批量SGD完全一致。安全模式在线性服务器路径下亦保持数值等价。
  - **可选差分隐私噪声**：可向激活份额和梯度份额添加高斯噪声，以增强隐私（但会引入精度损失）。
- **算法流程**（文字描述）：
  1. 编排器收集各节点样本数，构造全局打乱顺序的虚拟批次。
  2. 编排器将每个虚拟批次分解为各节点的子批次索引，分发给对应节点。
  3. 节点并行计算切层激活，在安全模式下拆分份额并分别发送至两服务器。
  4. 两服务器分别独立传播，通过一次交互重构输出用于损失计算，然后分别计算部分梯度份额。
  5. 各节点收到梯度份额后重构梯度，反向传播更新节点模型；编排器聚合节点模型梯度并更新节点参数。

### 3. 实验设计
- **数据集与场景**：
  - **图像分类**：CIFAR-10（32×32 RGB），使用自定义VGG风格CNN，测试不同类别数（2类到10类）。
  - **自然语言处理**：BioGPT（生成式预训练Transformer）在PubMedQA（生物医学问答）上进行全微调和低秩适配（LoRA）。
- **基准方法**：与集中训练、FedAvg、FedProx、SCAFFOLD、标准SL、SplitFed等多种分布式学习范式对比。
- **评价指标**：测试准确率（均值±标准差，5次独立运行）、通信量（线缆载荷）、关键路径网络时间、计算时间。
- **对比设置**：
  - TL++基础模式和精确安全模式在不同切层位置（切层1/2/3）下分别评估。
  - 安全切层1和2被标记为“近似”配置，因为其服务器路径包含非线性操作。
  - 包含全微调和LoRA两种微调方案。

### 4. 资源与算力
- **未明确说明**：论文未给出训练使用的具体GPU型号、数量、训练时长等硬件信息。仅提供了基于FLOPs分析和网络模型参数的理论计算时间估计（节点100 GFLOPs/s，服务器1000 GFLOPs/s，带宽假设等），并非实际运行测量。
- **因此，缺乏实际算力消耗的定量报告。**

### 5. 实验数量与充分性
- **实验数量**：较为充分：
  - CIFAR-10上覆盖类别数2~10共9种设置，每个设置5次运行。
  - 对比了6种基线方法。
  - 消融实验：切层位置（1/2/3）对比、标签空间复杂度的影响。
  - NLP：两种微调方案（全微调与LoRA），每种5次运行。
  - 通信和计算成本分析：基于设定带宽和延迟模型的详细估计。
- **充分性与公平性**：
  - 大部分实验是公平的：相同随机种子、参数初始化和优化器设置。
  - 但论文承认，由于没有基于配对轨迹的逐种子检验，部分比较是描述性的（如TL++安全模式精度超过基础模式被视为运行间波动而非系统优势）。
  - 未进行真实分布式环境的多机部署测试，缺少端到端吞吐量、内存压力、掉队者行为等实际测量。

### 6. 主要结论与发现
- TL++在CIFAR-10上基础切层1达到91.41%（标准差0.19），精确安全切层3达到90.93%（标准差0.17），均超过最强非TL++基线（标准SL的78.88%）12个百分点以上。
- TL++基础切层1的每步通信载荷相对于全模型同步（FedAvg）降低13.1倍，网络延迟也显著降低。
- 在PubMedQA上，TL++基线和安全模式均接近集中微调性能（全微调：80.87% vs 81.33%；LoRA：81.20% vs 81.07%），且优于所有分布式基线。
- TL++能够实现“精度无损 + 通信高效 + 可选隐私保护”的三角平衡，前提是满足信任假设（安全模式下的半诚实非共谋双服务器）和线性/仿射服务器路径条件。

### 7. 优点
- **方法创新**：巧妙地利用虚拟批次和加性秘密共享，同时解决了FL/SL的精度、通信和隐私三方面核心弊端，是首个同时满足这三者的完整框架。
- **理论严谨**：对梯度等价性进行了形式化证明，并清晰界定了安全模式精确性成立的条件（线性/仿射操作），以及非线性情况下的近似性。
- **实验覆盖全面**：涵盖图像和生物医学NLP两类典型任务，对比了多种基线方法，并进行了切层敏感性和通信成本分析。
- **实用导向**：提供了基础和安全两种模式，允许用户根据信任环境和隐私需求灵活选择部署方案。

### 8. 不足与局限
- **实验局限**：
  - 缺乏真实分布式系统中的部署测量（端到端时延、GPU利用率、内存压力、掉队者等）。
  - 未对差分隐私噪声设置进行系统评估或提供形式化DP保证。
  - 安全模式的非线性近似情况（切层1/2）的实际精度损失未深入分析。
- **方法局限**：
  - 精确安全模式要求服务器路径为线性/仿射，限制了模型架构选择（例如Transformer中的softmax、LayerNorm等需特殊处理）。
  - 标签信息对编排器可见，对于标签敏感的医疗等场景仍需额外保护。
  - 安全模式假设编排器和辅助服务器不共谋，且只抵抗半诚实（honest-but-curious）攻击，未考虑恶意攻击。
  - 通信分析基于理论模型而非实际包级测量，且安全切层3的通信量由于节点梯度同步和辅助器中继变得很高（接近FL水平），需要同步摊销或参数高效更新才能实用。
- **应用限制**：
  - 未在更大规模的数据集（如ImageNet、长上下文NLP）或更复杂的网络（如大型Transformer）上验证。
  - 未讨论跨设备异构性、网络拓扑、异步训练等实际部署问题。

（完）
