---
title: Privacy-Enhanced Zero-Order Federated Learning via xMK-CKKS over Wireless Channels
title_zh: 通过无线信道上的 xMK-CKKS 实现隐私增强的零阶联邦学习
authors: "Anthony Ayli, Khalil Harris, Jihad Fahs, Mohamad Assaad"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30123v1"
tags: ["query:daily"]
score: 10.0
evidence: 多密钥同态加密实现无线联邦学习隐私聚合
tldr: 在无线联邦学习中，基于单密钥同态加密的空中计算面临客户端密钥泄露风险，现有方法依赖信道估计，多密钥方案能够增强安全性但存在大模数干扰。本文提出基于xMK-CKKS多密钥同态加密的四阶段无线聚合协议，通过同一信道重传公钥和密文，使解密时大模数项代数抵消，从而无需信道估计。该协议与零阶联邦学习结合，各设备每轮只发送一个加密标量，通信开销与模型维度无关，理论证明收敛速率为O(1/√K)，仅引入可忽略的噪声平层。协议能抵抗诚实但好奇服务器与至多N-1个客户端的勾结，在MNIST上的实验验证了理论分析，展示了隐私增强与高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30123v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30123v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30123v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 897, \"height\": 577, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30123v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 776, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30123v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 865, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30123v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 680, \"height\": 432, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30123v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 622, \"height\": 193, \"label\": \"Table\"}]"
motivation: 单密钥同态加密在无线联邦聚合中易受客户端隐私泄露威胁，多密钥方案需解决信道估计和大模数干扰问题。
method: 利用无线信道互易性，设计四阶段重传协议，使xMK-CKKS加密在解密时代数抵消大模数项，避免信道估计与预均衡。
result: 证明收敛速率O(1/√K)至可忽略噪声平层，每轮通信开销与模型维度无关，MNIST实验验证收敛性和安全性。
conclusion: 提供无线多密钥联邦学习隐私增强方案，无需信道估计且通信开销恒定，能抵抗服务器与多数客户端勾结，高效实用。
---

## 摘要
同态加密（HE）允许服务器在不解密的情况下对加密数据进行操作，从而在联邦学习（FL）中实现隐私保护聚合。现有的空中传输 HE 方法主要依赖单密钥 HE 方案，并需要信道估计或预均衡来补偿无线衰落。然而，单密钥 HE 仍然容易受到共享相同密钥的好奇但诚实的客户端的攻击。此外，妥协单个客户端可能会危及整个网络的安全，而多密钥 HE 方案通过为每个设备分配自己的秘密密钥，提供了更强的客户端级安全性。我们提出了一种四阶段协议，该协议使著名的多密钥 HE 方案 xMK-CKKS 能够在共享无线信道上进行聚合，而无需信道估计。该协议通过相同的信道实现重传部分公钥和密文，使得占主导地位的大模数加密项在解密过程中代数抵消。我们将该协议与零阶 FL 集成，在缓慢变化的视距（LoS）主导信道上运行，其中每个设备每轮传输一个加密标量，通信/加密开销与模型维度无关。我们证明了在忽略不计的噪声底限下，解码的加密噪声保持了 O(1/√K) 的收敛速度。该协议对于与最多 N-1 个客户端合谋的好奇但诚实的服务器是安全的，并且在 MNIST 上的数值结果验证了分析。

## Abstract
Homomorphic encryption (HE) enables privacy-preserving aggregation in federated learning (FL) by allowing the server to operate on encrypted data without decryption. Existing HE-over-the-air methods mainly rely on single-key HE schemes and require channel estimation or pre-equalization to compensate for wireless fading. However, single-key HE remains vulnerable to honest-but-curious clients sharing the same secret key. In addition, compromising a single client may compromise the security of the entire network, while multi-key HE schemes provide stronger client-level security by assigning each device its own secret key. We propose a four-phase protocol that enables xMK-CKKS, a famous multi-key HE scheme, aggregation over a shared wireless channel without channel estimation. The protocol retransmits partial public keys and ciphertexts through the same channel realization, so that the dominant large-modulus encryption terms cancel algebraically during decryption. We integrate this protocol with zero-order FL over slowly varying LoS-dominant channels, where each device transmits a single encrypted scalar per round and the communication/encryption overhead is independent of the model dimension. We prove that the decoded encryption noise preserves the \(O(1/\sqrt{K})\) convergence rate up to a negligible noise floor. The protocol is secure against an honest-but-curious server colluding with up to \(N-1\) clients, and numerical results on MNIST validate the analysis.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容生成的详细中文总结。

### 1. 论文的核心问题与整体含义

- **核心问题**:
    - 如何将多密钥同态加密（MK-HE）与无线联邦学习（FL）的空中计算（Over-the-Air Computation）相结合。现有空中HE方法主要使用单密钥方案，这存在一个关键安全漏洞：共享秘钥的客户端可能解密其他客户端的更新，单个客户端被攻破会导致整个网络不安全。多密钥HE（如xMK-CKKS）通过为每个设备分配独立秘钥解决了此问题，提供了更强的客户端级别安全性。
    - 然而，直接将多密钥HE应用于无线信道面临一个巨大挑战：信道衰落会导致解密时出现大模数（large-modulus）残余误差。该误差可能比真实消息大数十个数量级，导致解密失败。传统依赖信道估计和预均衡的方法在此场景下会失效，因为微小的信道估计误差会与大模数相乘，从而破坏消息。

- **整体含义**:
    - 该研究旨在证明，通过巧妙的协议设计，可以根本性地避免信道估计及其带来的误差放大问题。其核心思想是将无线信道的叠加特性与xMK-CKKS方案的代数结构相结合，实现一种安全、可靠且通信开销不随模型维度增长的隐私增强联邦学习方案。

### 2. 论文提出的方法论

- **核心思想**:
    - 利用无线信道的互易性（即在短时间内信道近似不变），在一个通信轮次内通过四个阶段的重传，使得xMK-CKKS方案中大模数项（密钥相关项）在解密时经历相同的信道衰落，从而精确地代数抵消，无需进行信道估计或预均衡。

- **关键技术细节与算法流程（四阶段协议）**:
    - **阶段一：密钥聚合**。所有设备在上行链路同时发送其部分公钥 `b_i`。服务器接收空中叠加的信号 `˜b_k`，其中包含了信道系数 `h_{i,k}`。服务器向所有设备广播 `˜b_k`。
    - **阶段二：加密**。每个设备使用接收到的聚合公钥 `˜b_k` 加密其零阶梯度估计标量 `m_{i,k}`，生成密文对 `(c_0, c_1)`。设备在上行链路发送密文，服务器接收叠加的密文 `(˜c_{0,k}, ˜c_{1,k})`，并向所有设备广播 `˜c_{1,k}`。
    - **阶段三：解密份额**。每个设备使用自己的秘密钥 `s_j` 和接收到的 `˜c_{1,k}` 计算部分解密份额 `D_{j,k}`，并上行发送。服务器接收叠加的份额 `˜D_k`。
    - **阶段四：恢复与更新**。服务器计算 `M_k = ˜c_{0,k} + ˜D_k`。在这个计算中，由于 `˜b_k` 和 `˜c_{1,k}` 经历了相同的信道实现 `h_{i,k}`，公式中的大模数项 `V_k(˜b_k + ˜S_k * a)` 会简化为 `V_k * ˜E_{agg,k}`，即两个小噪声（加密噪声和信道噪声）的乘积，从而实现了代数抵消。最终，`M_k` 近似等于原始梯度估计的加权和加上一个极小的解码噪声。服务器广播 `M_k`，各设备用它计算梯度估计 `g_k` 并更新模型。

- **公式说明**:
    - 抵消的关键在于: `˜b_k = −˜S_k * a + ˜E_{agg,k}`，其中 `˜S_k = Σ h_{i,k} * s_i` 是信道加权秘钥。解密时，`˜b_k + ˜S_k * a` 正好消除了大模数多项式 `a`，剩余项为小噪声 `˜E_{agg,k}`。

### 3. 实验设计

- **数据集**: 使用了 **MNIST** 数据集上的0与1二元分类任务。
- **模型**: 逻辑回归模型，模型维度 `d=785`。
- **对比方法**:
    - **无加密基准 (Unencrypted Baseline)**。
    - **基于信道状态信息 (CSI) 的预均衡方法**: 与需要信道估计的零迫（ZF）和最小均方误差（MMSE）预均衡方法进行了对比，用于证明传统方法在多密钥和大模数下的失败。
- **场景配置**: 模拟了一个包含 `N=10` 个设备的联邦学习环境。无线信道被建模为具有视距（LoS）分量的慢变块衰落信道，即 `h_{i,k} ~ N(μ_i, σ_i^2)`，其中 `μ_i = 1`，测试了两种信道方差 `σ =1` 和 `σ =10` 的场景。

### 4. 资源与算力

- **未明确说明**: 论文未提及训练使用的具体硬件资源（如GPU型号、数量）及相应的训练时长。分析可能侧重算法和协议层面的验证，而非大规模算力消耗评估。

### 5. 实验数量与充分性

- **实验组数**: 论文进行了相对聚焦的实验，主要评估关键指标：
    1.  **训练损失对比**: 比较了在两种HE参数配置（`n=4096` 和 `n=8192`）与无加密方案下，在两种信道方差（`σ=1`, `σ=10`）下的训练损失曲线。
    2.  **预均衡方法失败验证**: 专门设置实验展示了基于 CSI 的 ZF 和 MMSE 预均衡方法在 `q≈2^110` 时的发散情况。
    3.  **测试准确率对比**: 给出了在不同HE参数和信道方差下最终的测试准确率表格。
- **充分性评估**:
    - **优点**: 实验设计直接针对论文的核心主张，清晰验证了所提方案能收敛且加密噪声可忽略不计，同时直观展示了传统方法的缺陷。
    - **局限**: 实验仅限于MNIST二分类和线性模型，缺乏在更复杂数据集（如CIFAR-10）、更复杂模型（如CNN）以及不同合谋攻击数量下的测试。消融研究不够深入，例如未探究不同客户端数量N或不同信道均值`μ`对性能的影响。实验评估维度相对单一。

### 6. 论文的主要结论与发现

- 提出的四阶段协议成功地将多密钥同态加密xMK-CKKS与无线空中计算相结合，且**完全不需要信道状态信息（CSI）或预均衡**。
- 协议通过设计使解密时大模数加密项经历相同的信道并发生代数抵消，使得残留的解码加密噪声极小（方差约为 `10^{-16}` 量级），对学习算法而言是**可以忽略的噪声底限**。
- 理论证明了所提方案保持了与无加密零阶联邦学习相同的 `O(1/√K)` 收敛速度。
- 安全分析表明，该协议能**安全地抵御诚实但好奇的服务器与多达 N-1 个客户端的合谋**。无线信道的叠加特性作为额外一层物理层保护，防止服务器分离单个客户端的传输。

### 7. 优点

- **强安全性**: 将多密钥HE应用于无线FL，解决了单密钥方案中客户端间的安全漏洞，提供了更强的共谋防御能力。
- **无需信道估计**: 巧妙地避开了无线通信中复杂且容易出错信道估计环节，从根本上解决了大模数下信道估计误差被放大导致解密失败的问题。
- **恒定的通信开销**: 结合零阶优化，每个设备每轮只需传输一个加密标量，使得通信和加密开销与模型维度无关，非常适用于大模型训练。
- **坚实的理论基础**: 提供了严格的收敛性证明和安全分析，增进了方案的可信度。

### 8. 不足与局限

- **信道模型假设较强**: 协议的有效性依赖于块衰落信道在四个协议阶段内保持完全恒定，这要求信道相干时间远大于协议执行时间，**极大限制了其在高速移动等快变场景下的应用**。
- **实验验证有限**: 仅在简单的MNIST二分类任务和逻辑回归模型上进行了验证，**缺乏在更复杂、更贴合实际应用的任务和模型上的实证支持**。
- **通信开销的权衡**: 虽然与模型维度无关，但同态加密本身带来了巨大的通信扩展（约1.36万到5.58万倍）。论文仅在THz带宽下证明了其可行性，**在带宽受限的低频段场景下，这种开销可能成为瓶颈**。
- **密钥重传开销**: 每轮都需要重传部分公钥，虽然这是其核心设计的一部分，但也是一种持续性的通信开销，文中未深入探讨在慢变信道下减少此类开销的可能。

（完）
