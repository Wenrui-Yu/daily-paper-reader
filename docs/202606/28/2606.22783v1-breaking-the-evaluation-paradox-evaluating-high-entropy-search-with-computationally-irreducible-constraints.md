---
title: "Breaking the Evaluation Paradox: Evaluating High-Entropy Search with Computationally Irreducible Constraints"
title_zh: 打破评估悖论：用计算不可约约束评估高熵搜索
authors: "Juntao Wu, Wei Wen, Xianting Huang, Shuai Pang, Ruizhi Qiao, Xing Sun, Ke Wang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22783v1"
tags: ["query:daily"]
score: 8.0
evidence: LLM搜索评估悖论
tldr: 评估大模型穷举搜索时，人类无法提供完整真值导致基准有偏。提出VERITAS框架，利用计算不可约约束构建可验证的稀疏答案搜索任务，能自动生成无限测试用例，具有完美真值和难度控制。该框架提供鲁棒基准和训练数据生成方法，推动系统搜索能力改进。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22783v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22783v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1620, \"height\": 695, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22783v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1611, \"height\": 718, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22783v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1326, \"height\": 635, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22783v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1665, \"height\": 724, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22783v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1630, \"height\": 1391, \"label\": \"Table\"}]"
motivation: 打破评估悖论，解决高熵搜索任务中人类真值缺失导致基准有偏的问题。
method: 基于计算不可约约束构建可验证的稀疏答案搜索任务，自动生成无限测试用例。
result: VERITAS生成具有完美真值和精确难度控制的测试用例，边际成本低。
conclusion: 提供鲁棒基准和可扩展的训练数据来源，提升模型系统搜索能力。
---

## 摘要
评估大型语言模型（LLM）的穷举搜索能力面临一个基本悖论：验证完整性需要完整的真实答案，但高熵枚举任务使得人类无法创建这样的真实答案。这导致基准测试会系统地惩罚那些表现优于人类标注者的模型。尽管网络搜索和深度研究智能体取得了快速进展——它们现在能发出数百次查询，遍历各种网站，并综合生成长篇报告——但评估仍然主要依赖于部分标注的答案集、基于LLM的评判者，或避免真正穷举搜索场景的单答案问题。我们通过将评估范式从模拟混乱的现实转向构建计算上纯粹的挑战来打破这一悖论。我们引入了VERITAS（可验证遍历评估框架），这是一个基于计算不可约约束原则构建的框架。通过引入新颖的、不可优化的约束，我们创建了可验证、稀疏答案的搜索任务，这些任务在计算上等价于穷举枚举。这些约束易于验证，但LLM或搜索引擎无法优化，迫使智能体真正遍历整个搜索空间。VERITAS能够自动生成几乎无限数量的测试用例，具有完美的真实答案和精确的难度控制，边际实例成本主要由哈希计算主导。这不仅为评估不确定性下的系统探索提供了稳健的基准，也为生成训练数据以提高这些关键但尚不发达的能力提供了可扩展的方法。

## Abstract
Evaluating the exhaustive search capabilities of large language models (LLMs) is plagued by a fundamental paradox: verifying completeness requires complete ground truth, yet high-entropy enumeration tasks make such ground truth impossible for humans to create. This causes benchmarks to systematically penalize models for outperforming their human annotators. Despite rapid progress in web-search and deep research agents -- which now issue hundreds of queries, traverse diverse sites, and synthesize long reports -- evaluation still largely relies on partially annotated answer sets, LLM-based judges, or single-answer questions that avoid genuinely exhaustive search scenarios. We break this paradox by shifting the evaluation paradigm from simulating a messy reality to constructing computationally pure challenges. We introduce VERITAS (Verifiable Traversal Assessment for Search), a framework built on the principle of computationally irreducible constraints. By introducing novel, non-optimizable constraints, we create verifiable, sparse-answer search tasks that are computationally equivalent to exhaustive enumeration. These constraints are easy to verify but impossible for LLMs or search engines to optimize, forcing agents to genuinely traverse the entire search space. VERITAS can automatically generate a virtually infinite number of test cases with perfect ground truth and precise difficulty control, with marginal instance cost dominated by hash computations. This provides not only a robust benchmark for evaluating systematic exploration under uncertainty but also a scalable method for generating training data to improve these crucial, yet underdeveloped, capabilities.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：论文提出了一个“评估悖论”：评估大型语言模型（LLM）的**穷举搜索**（Exhaustive Search）能力时，验证搜索结果是否“完整”需要一份完整的“真值”答案集。然而，对于“高熵查询”（HEQ，即需要列举大量开放式的候选答案的任务）而言，人类自身也无法创建这份完整的真值集。
*   **悖论的影响**：由于人类标注的答案集必然不完整，LLM若发现了超出标注集的有效答案，会被判定为“幻觉”而受到惩罚。这导致现有基准测试（如GAIA、WideSearch）无法客观衡量模型的真实搜索能力，反而奖励了仅复现人类遗漏项的模型。
*   **解决方案的哲学**：作者认为，与其在模拟混乱的现实世界中追求穷举搜索的评估，不如切换范式，构建一个**计算上纯粹的、可验证的挑战**。核心思想是：将“列出所有项”的评估目标，替换为“在给定约束下找到特定项”，并确保这种约束在实践中不可被优化。

### 2. 论文提出的方法论

*   **核心思想**：引入**计算不可约约束**。使用密码学哈希函数（如MD5）作为约束。核心思想是：找到一个满足特定哈希约束的唯一项，在计算上等价于对全部候选空间进行穷举枚举。
    *   **哈希约束的特性**：
        *   **单向性（One-way Verification）**：无法从哈希值反推出原始内容，但验证时只需计算候选内容的哈希并与目标哈希值比对，复杂度为O(1)。
        *   **语义无关性（Semantic Nullity）**：哈希值与内容语义毫无关联，无法被搜索引擎利用来优化查询或过滤结果，从而防止模型通过语义捷径绕过穷举过程。
*   **关键技术：VERITAS框架**：
    1.  **任务构建**：以真实的Web数据源（如HuggingFace模型列表、GitHub仓库、歌曲列表）为基础。例如，任务不是“列出Google在HuggingFace上的所有模型”，而是“找出Google在HuggingFace上的模型，其ID的MD5恰好等于`f30...08b`”。
    2.  **难度控制**：通过调整候选空间大小（N）和需要满足约束的目标数量（k）来精确控制难度。基于公式 `Psuccess ≈ (T/N)^k` (T为搜索预算)，增大N或k都会显著降低模型在固定预算下的成功率。
    3.  **四个难度层级**：
        *   **Easy (单源枚举)**：在单一数据源（如一个作者）的所有项中搜索。
        *   **Medium (复合搜索)**：在7个独立数据源进行并行枚举，并对结果进行比较。
        *   **Hard (深度属性提取)**：不仅需要枚举项，还需从每个项的子页面（如Git SHA）提取隐藏属性后再进行哈希验证。
        *   **Extra Hard (嵌套多任务)**：结合了Hard的深度提取和Medium的多源搜索，构成7个更复杂的高熵子任务。
*   **验证**：通过计算 `MD5(候选答案)` 与 `目标哈希` 是否相等来验证。这是确定性的、零错误率的，且无需人类标注。

### 3. 实验设计

*   **评估场景与基准**：使用名为 **VERITAS** 的自建基准测试。该基准包含**Medium、Hard、Extra Hard** 三个难度层级（排除了Easy，聚焦于更具挑战性的任务）。数据集涵盖了英文、中文和日文的查询，保证跨语言评估的平衡性。
*   **对比方法（模型）**：评估了9个模型，包括：
    *   **开源模型**：GLM-4, MiniMax-M2, DeepSeek-v3.1, Qwen3-235B-A22, Kimi-K2
    *   **闭源模型**：GPT-4o, Gemini-2.5-Pro, 以及前沿模型 **GPT-5** 和 **Gemini-3-Pro**
*   **评价指标**：
    *   **Pass@4**：在4次尝试中至少成功1次的概率。
    *   **Avg@4**：在4次尝试中的平均成功率。
    *   **工具调用次数**：统计每次尝试中，搜索（Search）和访问（Visit）网页的调用次数。

### 4. 资源与算力

*   论文**未明确说明**训练任何模型，也未提及训练所需的算力（如GPU型号、数量、训练时长）。
*   实验部分主要是评估事先训练好的现有模型（如GPT-5, Gemini-3-Pro等API或开源模型），因此资源消耗主要集中在**推理和搜索过程**。
*   论文提到 **VERITAS生成测试用例的边际成本主要由哈希计算主导**，这意味着生成基准数据的成本极低。评估过程中的算力消耗则取决于被评估模型及其搜索策略。

### 5. 实验数量与充分性

*   **实验数量**：主要实验（表2）报告了一次评估结果，但论文在附录C中额外进行了一组验证实验，以`Gemini 3 Pro`为代理，测试其在`k=1,2,4,8,16`个哈希约束目标下的成功率，用以验证理论框架。
*   **充分性与公平性分析**：
    *   **充分**：实验设计精巧，通过三个难度层级清晰地展示了模型能力的梯度，并揭示了性能差距。工具使用情况的统计（图3）有力地证明了VERITAS迫使模型进行更多搜索，且与WideSearch形成对比。附录中的验证实验也很好地支撑了理论分析。
    *   **客观/公平**：实验严格执行了以下约束以确保公平：
        *   **严格单次工具调用约束**：为防止模型利用哈希反馈循环进行暴力破解，系统仅允许模型**执行一次Python代码**。若模型试图再次调用，系统会拦截并强制其给出答案。这确保了模型无法靠“试错”来撞大运，必须依赖一次性的枚举与验证。
        *   **排除商业端对端系统**：论文解释了为何不评估商业系统（如ChatGPT），因为这些系统是黑盒，无法施加“单次工具调用”这一公平约束，从而可能导致不公平的优胜。
    *   **局限**：仅以Pass@4为指标，可能对具有更高策略稳定性（低方差）的模型有利。此外，实验仅评估了9个模型，虽具代表性，但样本数量有限。

### 6. 论文的主要结论与发现

1.  **验证了评估悖论**：通过一个“虚拟歌手KAF”的小型案例，展示了不同人类标注者会得出截然不同且都不完整的结论，佐证了评估悖论的存在。
2.  **VERITAS能有效衡量模型能力**：在VERITAS基准上，模型性能呈现清晰的层级结构。前沿模型（GPT-5, Gemini-3-Pro）的表现远超其他模型，表明该基准能有效区分当前模型的能力。
3.  **Extra Hard层级仍是挑战**：即使是表现最好的GPT-5，在Extra Hard难度下成功率也仅为20.0%，远低于Medium（47.5%）。大多数模型在Extra Hard上几乎完全失效（0-3.6%），表明当前LLM在处理高度复杂的穷举搜索任务上存在根本性能力差距。
4.  **搜索策略质量差异明显**：Pass@4与Avg@4的对比显示，弱模型成功率低且波动大，而强模型（如GPT-5）的Avg@4与Pass@4差距较小，表明其搜索策略更稳定、系统。
5.  **VERITAS强制真正枚举**：图3显示，在VERITAS上模型使用的工具调用次数远多于WideSearch（1.4x - 4.2x），但成功率依然很低，这印证了VERITAS消除了语义捷径，迫使模型进行真正的穷举搜索。

### 7. 优点

1.  **创新性解决了评估悖论**：这是论文最大的亮点。它没有在现有范式内修补，而是通过引入计算不可约约束，从根本上改变了评估目标，使得自动、精确、可扩展的评估成为可能。
2.  **极强的可扩展性**：VERITAS可以基于任何结构化数据源自动生成无限量、具有完美真值的测试用例，且成本极低。这彻底摆脱了对昂贵、易错的人类标注的依赖。
3.  **精确的难度控制**：通过`(T/N)^k`理论框架，可以定量地控制任务难度，使基准测试难度梯度清晰、可解释。
4.  **优秀的实验设计**：严格约束“单次工具调用”的机制非常巧妙，保证了评估的公平性和科学性。附录中的验证实验也具有很强的说服力。
5.  **明确的应用前景**：除了作为评估基准，VERITAS自动生成大量带标签数据的能力，使其成为通过强化学习等方法训练搜索智能体的理想数据源。

### 8. 不足与局限

1.  **当前仅为评估基准**：作者明确指出，VERITAS目前仅用于评估，尚未利用其生成的数据来训练模型。虽然潜力巨大，但“训练数据生成”这一效用尚未得到实证验证。
2.  **覆盖域有限**：任务基于结构化数据源（如API、数据库），与真实世界中混沌、非结构化的Web搜索仍存在差距。模型在所有“结构化”任务上的表现好，不代表其在开放式Web上的穷举能力也一定强。
3.  **只关注“枚举”能力**：论文有意剥离了语义理解和查询构建能力，仅聚焦于“枚举”这一核心挑战。然而，在真实应用中，这三个能力是耦合的。VERITAS衡量的是一种专项能力，而非综合能力。
4.  **潜在偏差**：评估依赖于特定的哈希函数（MD5）和Python库实现。模型可能因在预训练阶段见过类似模式（如MD5字符串）而获得意外优势或劣势，虽然论文认为哈希的语义无关性已足够防止这种偏差，但仍需警惕。
5.  **模型选择局限**：评估仅限于9个模型，缺乏对更多开源模型（如LLaMA系列）的评估，可能导致结论的普适性受限。

（完）
