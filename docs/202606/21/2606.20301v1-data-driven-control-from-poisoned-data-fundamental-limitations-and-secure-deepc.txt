Title: Data-Driven Control from Poisoned Data: Fundamental Limitations and Secure DeePC

URL Source: https://arxiv.org/pdf/2606.20301v1

Published Time: Fri, 19 Jun 2026 01:03:57 GMT

Number of Pages: 8

Markdown Content:
# Data-Driven Control from Poisoned Data: Fundamental Limitations and Secure DeePC 

Takumi Shinohara, Henrik Sandberg, and Karl Henrik Johansson 

Abstract — We study a data-driven control problem in the presence of arbitrary data poisoning attacks. We assume that a subset of offline output data is stored in unprotected locations and may be poisoned by an adversary. We first establish fun-damental limitations for data-driven control arising from such poisoned data: poisoning attacks are not detected/identified from the dataset alone; unprotected data are non-informative for controller design with worst-case guarantees; and hard con-straints on unprotected outputs are not certifiable. Motivated by these limitations and the data-enabled predictive control (DeePC) technique, we propose Secure DeePC , a data-driven control algorithm that is resilient against poisoning attacks. It first runs output-truncated DeePC using only the protected dataset until the online input becomes persistently exciting. It then uses online measurements to reconstruct the partial offline dataset, and finally returns to full-output DeePC. Secure DeePC achieves MPC-equivalent performance in finite time almost surely under certain conditions. Simulation results illustrate the efficacy of the proposed framework against poisoning attacks. 

I. I NTRODUCTION 

Recent advances in data-driven methods have spurred sig-nificant interest in direct data-driven control [1]–[4], where controllers are synthesized directly from measured offline input-output (I/O) data of systems with unknown dynam-ics, thereby avoiding explicit model identification. Among data-driven control schemes, data-enabled predictive control (DeePC) [2], [3] has emerged as a prominent approach that leverages Willems’ Fundamental Lemma to formulate a model predictive control (MPC) problem directly in terms of Hankel matrices constructed from data. Most of these works implicitly assume that the collected data are genuine and have not been corrupted. However, adversaries can manipulate the data by exploiting vulnera-bilities in data storage or communication channels. Indeed, some studies [5]–[7] have shown that poisoning attacks can severely degrade performance and even destabilize data-driven controllers. From the defender’s viewpoint, [8], [9] develop data-driven secure controllers that operate under intermittent loss of offline data induced by denial-of-service (DoS) attacks. Yet, secure data-driven control against arbi-trary data poisoning has received limited attention. Unlike 

This work was supported in part by the Knut and Alice Wallenberg Foun-dation Wallenberg Scholar Grant, Swedish Research Council (Project 2023-04770), Swedish Research Council Distinguished Professor Grant (Project 2017-01078), Swedish Civil Defence and Resilience Agency (Project MAD-VAMCHS), and VINNOVA project “Control-computing-communication co-design for autonomous industry (3C4AI)” (Project 2025-01119). The authors are with the Department of Decision and Control Systems, KTH Royal Institute of Technology, and also with Digital Futures, 100 44 Stockholm, Sweden. (e-mail: tashin@kth.se, hsan@kth.se, kallej@kth.se). (offline)  Data collection +  Poisoning attacks    

> Unknown
> plant
> (online) Data -driven secure control
> Secure
> DeePC
> Real -time (online) data
> Poisoned offline data
> ︙
> : Protected
> : Unprotected
> Reference
> Unknown
> plant
> Offline
> data
> Problem
> (15)
> Problem
> (9)
> Protected
> offline data
> Recovered offline data

Fig. 1: An overview of data poisoning attacks and Secure DeePC. 

DoS-induced packet losses, in which missing samples are explicitly identifiable and can be treated as erasure data, sophisticated poisoning attacks provide neither explicit ev-idence of an attack nor reliable indicators of which chan-nels/samples are corrupted. Such adversarial characteristics make data-driven control significantly more challenging. In this paper, we address the data-driven control prob-lem in the presence of arbitrary data poisoning attacks. A schematic overview of our formulation is shown in Fig. 1, where the input data and a subset of output data are stored in protected locations, while the remaining outputs are stored in unprotected locations and may be arbitrarily corrupted. We place no restrictions on the attack sequence against unprotected data. Our primary contributions are threefold: 1) We establish fundamental limitations for data-driven control from arbitrarily poisoned data: poisoning at-tacks cannot be detected/identified from the dataset alone, unprotected data are non-informative for data-driven controller design with worst-case guarantees, and safety constraints on unprotected outputs are not fundamentally guaranteed. 2) We show that, if the protected outputs render the system observable and the online input becomes per-sistently exciting, then the attack-free offline data in a certain interval can be exactly reconstructed from protected offline data and clean online measurements. 3) Building on the fundamental limitations, we propose 

Secure DeePC , a two-phase architecture that uses output-truncated DeePC before reconstruction and full-output DeePC after reconstruction, thereby recovering MPC-equivalent operation after a finite transient time. The remainder of this paper is organized as follows: Section II presents the system model, attack model, data model, and a brief overview of DeePC. In Section III, we 

> arXiv:2606.20301v1 [eess.SY] 18 Jun 2026

show the fundamental limitations for establishing data-driven controllers using poisoned data. In Section IV, we develop Secure DeePC. In Section V, we show the simulation results using an inverted pendulum model to illustrate the proposed framework. We conclude the paper in Section VI. 

Notation: The symbols R, Rn, Z, and Z+ denote the set of real numbers, n-dimensional Euclidean space, integers, and non-negative integers, respectively. For vectors v1, . . . , v k,define col( v1, . . . , v k) ≜ [v⊤ 

> 1

, . . . , v ⊤ 

> k

]⊤. The notation |I| is used to denote the cardinality of a set I. For a vector x

and a positive definite matrix P , we write ∥x∥P = √x⊤P x .For a, b ∈ Z with a ≤ b, we define the integer interval 

[a, b ] ≜ {c ∈ Z : a ≤ c ≤ b}. Given v : Z → Rn and the interval [i, j ], we define a block vector v[i,j ] as v[i,j ] ≜

col( v(i), v (i + 1) , . . . , v (j − 1) , v (j)) . Let q be a positive integer such that q ≤ j − i + 1 and define the block Hankel matrix of depth q, associated with v[i,j ], as 

Hq



v[i,j ]

≜



v(i) v(i+1) · · · v(j −q+1) 

v(i+1) v(i+2) · · · v(j −q+2) 

... ... . . . ...

v(i+q−1) v(i+q) · · · v(j)

.

Note that the subscript q refers to the number of block rows of the Hankel matrix. Then, v[i,j ] is said to be persistently exciting (PE) of order q if the Hankel matrix Hq (v[i,j ]) has full row rank [10]. II. P ROBLEM STATEMENT 

In this section, we introduce the system model, attack model, and data representation. We then briefly outline the conventional DeePC algorithm. 

A. System Description 

Consider the discrete-time system given by 

x(k + 1) = ¯Ax (k) + ¯Bu (k), y(k) = ¯Cx (k), (1) where x(k) ∈ Rn denotes the unknown system state, u(k) ∈

Rm the control input, and y(k) ∈ Rp the system output. We assume a minimal realization ( ¯A, ¯B, ¯C), i.e., ( ¯A, ¯B)

is controllable and ( ¯A, ¯C) is observable. For notational convenience, define P ≜ {1, . . . , p } as the index set of the sensors. Given a desired reference trajectory (ru, r y ),an input constraint set Cu ⊆ Rm, and an output constraint set Cy ⊆ Rp, our goal is to choose inputs so that the system tracks the reference while satisfying the constraints and minimizing a performance cost function. 

B. Data Collection and Poisoning Attack 

We collect I/O data during an offline operation of the system (1) over the time interval [0 , N − 1] , where N is a sufficiently large integer. Let u ≜ col( u(0) , . . . , u(N − 1)) ∈

RmN denote a sequence of N inputs applied to (1) and y ≜

col( y(0) , . . . , y(N − 1)) ∈ RpN denote the corresponding outputs. The boldface variables (e.g., u, y) indicate data in an offline operation. The collected data are stored in storage locations for later use by a data-driven controller. The input data u are stored in protected storage. The output data from each sensor are stored in the corresponding storage locations; that is, we have p output storage locations, and the ith storage location stores the ith output sequence yi ≜ col( yi(0) , . . . , yi(N −

1)) ∈ RN . Assume that some output storage locations are protected 1, whereas other locations are unprotected, and an adversary may corrupt the stored output data. Denote the sets of protected and unprotected storage locations by S ⊆ P and 

U ⊆ P , respectively. Note that S ∩ U = ∅ and S ∪ U = P.Without loss of generality, assume that the first sp output channels are protected, i.e., S ≜ {1, . . . , s p} and U ≜

{sp + 1 , . . . , p }, where sp may be zero. The system operator knows which storage locations are protected/unprotected. For i ∈ U, we denote the attack sequence against the ith measurement data in unprotected storage by ai ≜

col( ai(0) , . . . , ai(N − 1)) ∈ RN , where ai(k) ∈ R denotes the poisoning attack against the ith sensor data at time k

designed by a malicious attacker. The attacker is assumed to have complete knowledge of the system’s state, I/O data, system model, and storage information, and can generate an attack sequence with arbitrary statistical properties, magni-tude, and temporal correlations. Although such an omniscient attacker is unrealistic in practice, this assumption allows us to analyze the worst-case scenario. Define the poisoned output sequence as 

ey ≜ col ( y(0) , . . . , y(N −1))+col ( a(0) , . . . , a(N −1)) , (2) where a(k) ≜ [ a1(k), . . . , ap(k) ] ⊤ ∈ Rp represents a poisoning attack against the output data at time k. Since the data in protected storage are secure, ai ≡ 0 and eyi = yi for all i ∈ S . Denote the index set of the compromised storage as A ≜ {i ∈ U : ai̸ ≡ 0}. If all unprotected storage locations are compromised, then A = U.We assume that the system parameters ¯A, ¯B, and ¯C

are unknown, whereas the I/O data (u, ey), and the pro-tected/unprotected storage information are known. The attack sequence a, the attack-free output data yU in unprotected storage U, and the compromised storage set A are all unknown. In this paper, we focus on offline poisoning attacks against output data, whereas the online measurements obtained during controller operation are assumed to be attack-free 2. Secure control problems in the presence of both offline/online attacks are important topics for future work. 

C. Data Representation 

For a given set Γ ⊆ P , denote 

eyΓ ≜ col( eyΓ(0) , . . . , eyΓ(N − 1)) ∈ R|Γ|N , (3) where eyΓ(k) ∈ R|Γ| is the subvector obtained from ey(k)

indexed by the set Γ. Hence, eyS and eyU denote the output            

> 1The partial protection assumption is prevalent in security-oriented es-timation and control, as it is often prohibitively expensive to protect all sensing, communication, and data resources. This protection can be accomplished through encryption and authentication mechanisms, as well as by enhancing physical security.
> 2Several studies propose data-driven controllers against malicious online
> attacks. See, e.g., [11]–[13]. Note that these studies considered only online attacks and do not account for offline poisoning attacks studied here.

data stored in protected storage and unprotected storage, respectively. For notational simplicity, denote the full I/O data by D ≜ (u, ey) = ( u, eyS , eyU ). Also, define the I/O data in the protected storage (i.e., protected data) as DS ≜ (u, eyS ) = (u, yS ) ⊆ D. Note that D = ( DS , eyU ).Given two nonnegative integers Tp and Tf , we partition the full I/O data into two parts, referred to as the past and 

future data , as 

 Up

Uf



≜ HTp+Tf (u) ∈ Rm(Tp+Tf )×(N −(Tp+Tf )+1) , (4) 

" eYp

eYf

#

≜ HTp+Tf (ey) ∈ Rp(Tp+Tf )×(N −(Tp+Tf )+1) , (5) where Up consists of the first Tp block rows of HTp+Tf (u)

and Uf consists of the last Tf block rows of the Hankel matrix (similarly for eYp

and eYf

). For a set Γ ⊆ P , define the Hankel matrix associated with eyΓ as 

" eYp

> Γ

eYf

> Γ

#

≜ HTp+Tf (eyΓ) ∈ R|Γ|(Tp+Tf )×(N −(Tp+Tf )+1) . (6) 

D. An Overview of DeePC [2] 

Consider the attack-free scenario (i.e., a ≡ 0). Then, the DeePC problem in the online control phase for (1) at time 

k ∈ Z+ can be formulated as follows: 

minimize  

> g,u f,y f

J  uf , y f  (7a) 

s.t.



Up

Yp

Uf

Yf

 g =



up

yp

uf

yf

 , u f ∈ C Tf 

> u

, y f ∈ C Tf 

> y

, (7b) where CTf 

> u

is the Tf -fold Cartesian product of Cu (similarly for CTf 

> y

), and up = u[k−Tp,k −1] and yp = y[k−Tp,k −1] are the most recent Tp-step past I/O online measurements of the system (1), respectively. At the activation time k = 0 , it is assumed that the controller has access to the past online measurements u[−Tp,−1] and y[−Tp,−1] .The decision variables are defined as g ∈ RN −(Tp+Tf )+1 ,

uf ≜ col( uf (k), . . . , u f (k + Tf − 1)) ∈ RmT f , and yf ≜

col( yf (k), . . . , y f (k +Tf −1)) ∈ RpT f . The objective function is defined as 

J  uf , y f ≜ 

> k+Tf−1

X

> t=k



yf (t)−ry (t) 2 

> Q

+ uf (t)−ru(t) 2

> R



,

where ry (k) ∈ Rp and ru(k) ∈ Rm denote the desired output and input references at time k, respectively, and Q ∈ Rp×p

and R ∈ Rm×m are positive definite matrices. For each k ∈ Z+, the DeePC problem (7) is computed, and the given input sequence for the unknown system is applied as u(k), . . . , u (k + s) = uf (k), . . . , u f (k + s) for some s < T f − 1. Then, setting k to k + s and updating up

and yp, we can iterate the DeePC algorithm for all k.If ( ¯A, ¯B) is controllable, ( ¯A, ¯C) is observable, Tp ≥

n, and u is PE of order n + Tp + Tf , then the optimal control sequence and corresponding system output obtained by DeePC coincide with those of the MPC problem with prediction horizon Tf without identifying any model pa-rameters [2, Corollary 5.1]. However, when the data are poisoned, the Hankel matrices Yp and Yf are corrupted by the attack, which may prevent the computation of optimal inputs and may even destabilize the system. This motivates the development of Secure DeePC. III. F UNDAMENTAL LIMITATIONS OF DATA -D RIVEN 

CONTROL USING POISONED DATA 

Before deriving Secure DeePC, we show the fundamental limitations of data-driven control using poisoned data, where an adversary can arbitrarily corrupt the unprotected data. 

A. Impossibility of Certified Detection and Identification 

Let D : D 7 → { Attack , NoAttack } and I : D 7 → 2P

denote an arbitrary data-driven attack detector and identifier, respectively, where 2P denotes the power set of P. The following result provides the impossibility of certified data-driven attack detection and identification. 

Proposition 1: Suppose that there exists at least one un-protected storage location, i.e., U̸ = ∅. There exist an attack-free dataset D0, a minimal system (A′, B ′, C ′), and a nonzero poisoning attack sequence a yielding a poisoning dataset D such that D0 = D. Consequently, for any data-driven attack detector D and identifier I , D(D0) = D(D)

and I (D0) = I (D), which implies the attack a cannot be detected and identified. 

Proof: Let D0 ≜ (u, y) be the attack-free dataset such that yj̸ ≡ 0 for some j ∈ P . Let x(·) be the offline state trajectory of ( ¯A, ¯B) under input u and initial condition x(0) .Fix any i ∈ U and, for α > 0, define Tα ≜ Ip + αe ie⊤ 

> j

and 

C′ ≜ Tα ¯C, where ei and ej are the ith and jth canonical basis of Rp, respectively. Consider another system (A′, B ′, C ′) with A′ ≜ ¯A and 

B′ ≜ ¯B. Since Tα is invertible, (A′, C ′) is observable, and thus (A′, B ′, C ′) is minimal. Consider a nonzero attack sequence a such that 

aℓ(k) = 

 −αyj (k), ℓ = i, 

0, otherwise . (8) Then, the ith poisoned output ey′ 

> i

of (A′, B ′, C ′) is given by 

ey′

> i

(k) = C′

> i

x(k) + ai(k)=   ¯Ci + α ¯Cj

 x(k) − αyj (k) = ¯Cix(k) = yi(k).

Also, for ℓ ∈ P \ { i}, we have ey′

> ℓ

(k) = yℓ(k), which implies 

ey ≡ y; therefore, D0 = D. Consequently, for any data-driven attack detector D and identifier I , D(D0) = D(D) and 

I (D0) = I (D).This proposition implies that any detector D declares 

NoAttack on some attack-free dataset necessarily declares 

NoAttack on some poisoned dataset as well. Similarly, for any identifier I , if it returns ∅ on some attack-free dataset, then it also returns ∅ on some poisoned dataset. The result establishes the fundamental impossibility of certified detection and identification of poisoning attacks based solely on poisoned data. Hence, even if some data stored in an unprotected location are attack-free (i.e., |A| < |U| ), it is impossible to identify them with certainty, which implies that creating an additional attack-free dataset from unprotected storage is impossible. In order to detect and identify poison-ing attacks using only data, additional assumptions on the attacks and/or adversaries are required, such as the existence of a sufficiently long attack-free interval in the dataset [14]. Importantly, this impossibility is not alleviated by assum-ing a small number of attacked channels. Also, knowing the maximal number of corrupted channels |A| does not remove this impossibility. This stands in sharp contrast to model-based settings, where the measurement structure (i.e., the matrix ¯C) is known and redundancy conditions, such as sparse observability, enable attack detection and identification [15]. On the other hand, in the data-driven setting, the underlying measurement structure is unknown; thus, redundancy in the number of output channels does not yield certifiable guarantees under arbitrary poisoning attacks. 

B. Non-informativity of Unprotected Data 

We next show that the unprotected data eyU are non-informative for data-driven control. To this end, we first define the set of systems consistent with the full data D as 

Σ ( D) ≜ (A, B, C ) : ∃x0, ∃a ∈ RpN s.t. x(0) = x0,

x(k + 1) = Ax(k) + Bu(k), eyS (k) = CS x(k),

eyU (k) = CU x(k) + aU (k), ∀k ∈ [0 , N − 1] } , (9) where CS ∈ R|S|× n denotes the submatrix obtained from C

indexed by S (similar for CU ). Similarly, the set of systems consistent with the protected data DS is given as 

ΣS (DS ) ≜ {(A, B, C ) : ∃x0 s.t. x(0) = x0,

x(k + 1) = Ax(k) + Bu(k), eyS (k) = CS x(k),

∀k ∈ [0 , N − 1] } . (10) Then, we have the following result. 

Proposition 2: For given D, ΣS (DS ) = Σ( D).

Proof: The inclusion Σ( D) ⊆ ΣS (DS ) is immediate from the definitions. Conversely, let (A, B, C ) ∈ ΣS (DS ),and let x(·) be a trajectory certifying this membership. Define 

aU (k) ≜ eyU (k) − CU x(k) with aS (k) ≡ 0. Then, eyU (k) = 

CU x(k) + aU (k), so (A, B, C ) ∈ Σ( D). Hence, ΣS (DS ) ⊆

Σ( D), and therefore ΣS (DS ) = Σ( D).Under arbitrary poisoning attacks, the set of systems consistent with the full data D matches the set of systems expressible using only the protected data DS , irrespective of the particular realization of the unprotected data eyU . Building on this property, we next show the non-informativity of the unprotected data. Let Π denote the class of admissible controllers in the system (1), and let A : D 7 → π ∈ Π denote an arbitrary data-driven design rule that outputs a controller π. For all protected datasets DS , define a data-driven design rule 

AS : DS 7 → π ∈ Π based on protected data as AS (DS ) ≜

A (( DS , 0)) . Denote an arbitrary loss function for a data-driven controller by L : Π × Σ( D) → R ∪ { +∞} . Then, we have the following result. 

Proposition 3: For given protected data DS ,

inf  

> A

sup 

> eyU

sup   

> (A,B,C )∈Σ(( DS,eyU))

L (A (( DS , eyU )) , (A, B, C )) = inf 

> AS

sup   

> (A,B,C )∈ΣS(DS)

L (AS (DS ) , (A, B, C )) . (11) 

Proof: For notational clarity, write θ = ( A, B, C ) for a candidate system. By Proposition 2, ΣS (DS ) = Σ( D) for all eyU , which implies that the left-hand side of (11) is 

inf  

> A

sup 

> eyU

sup   

> θ∈ΣS(DS)

L (A (( DS , eyU )) , θ ) .

Fix any design rule A . Since the supremum over eyU domi-nates the value at eyU = 0 , it follows that 

sup 

> eyU

sup   

> θ∈ΣS(DS)

L (A (( DS ,eyU )) ,θ ) ≥ sup   

> θ∈ΣS(DS)

L (A (( DS ,0)) ,θ ) .

Hence, taking the infimum over A on both sides and using 

A (( DS , 0)) = AS (DS ), this can be written as 

inf  

> A

sup 

> eyU

sup   

> θ∈ΣS(DS)

L (A (( DS , eyU )) , θ )

≥ inf 

> AS

sup   

> θ∈ΣS(DS)

L (AS (DS ) , θ ) . (12) Conversely, fix any design rule AS , and define its full-data extension A e by A e(D′S , eyS ) ≜ AS (D′S ) for all D′S

and eyU , namely, A e ignores the unprotected data. Then, 

sup 

> eyU

sup   

> θ∈ΣS(DS)

L (A e (( DS , eyU )) , θ ) = sup   

> θ∈ΣS(DS)

L (AS (DS ) , θ ).

Hence, we have 

inf  

> A

sup 

> eyU

sup   

> θ∈ΣS(DS)

L (A (( DS , eyU )) , θ )

≤ sup 

> eyU

sup   

> θ∈ΣS(DS)

L (A e (( DS , eyU )) , θ )= sup   

> θ∈ΣS(DS)

L (AS (DS ) , θ )

Now that AS is arbitrary, taking the infimum over AS yields the reverse relation of (12). Therefore, (11) holds. In a minimax formulation where the system operator seeks to minimize the worst-case performance over all systems consistent with the stored data and over all admissible poisoning attacks, Proposition 3 indicates that exploiting eyU

cannot improve the worst-case performance beyond what is achievable using DS alone. Therefore, for any data-driven controller with worst-case guarantees, designing it solely using the protected data is optimal. 

C. Impossibility of Certified Guarantee for Output Safety 

We next show that hard safety constraints on outputs Cy

cannot be guaranteed from the poisoned dataset. 

Proposition 4: Suppose U̸ = ∅, and the output constraint set Cy imposes a bounded interval on some component 

i ∈ U . Let D be any offline poisoned dataset generated by the true plant ( ¯A, ¯B, ¯C) with ( ¯A, ¯CS ) observable and any attack sequence a. Assume that the given Tp-step on-line sequence (u[−Tp,−1] , y [−Tp,−1] ) has terminal state x⋆

satisfying ¯Cj x⋆̸ = 0 for some j ∈ S . Then, there exists a minimal system (A′, B ′, C ′) ∈ Σ( D) which leads to the ith output constraint’s violation in online control operation. Consequently, no data-driven design rule based solely on D

can guarantee the hard constraint on outputs in U uniformly over the consistency class Σ( D).

Proof: Let [ci, c i] be the bounded interval constraint imposed on the ith output. Let (u[−Tp,−1] , y [−Tp,−1] ) be the assumed online sequence of the true plant, and let x⋆ = x(0) 

be its terminal state satisfying ¯Cj x⋆̸ = 0 for some j ∈ S . As with Proposition 1, for α̸ = 0 , define Tα ≜ Ip + αe ie⊤ 

> j

and 

C′ ≜ Tα ¯C. Since i ∈ U and j ∈ S , it holds that C′S = ¯CS .Considering A′ ≜ ¯A and B′ ≜ ¯B, (A′, B ′, C ′) is minimal. Let x(·) be the offline state trajectory of ( ¯A, ¯B) that generates the protected dataset DS under input u and initial condition x(0) . Since the protected rows are unchanged, the same trajectory also certifies that (A′, B ′, C ′) ∈ ΣS (DS ). By Proposition 2, ΣS (DS ) = Σ( D) yields (A′, B ′, C ′) ∈ Σ( D).Next, consider the same initial state at time −Tp as in the true plant and apply the same online input sequence 

u[−Tp,−1] . Since the state dynamics are unchanged, the resulting online state trajectory is identical to that of the true plant, and in particular, it reaches the same terminal state 

x⋆ at time 0. Moreover, because C′S = ¯CS , the protected online outputs over [−Tp, −1] are unchanged. Hence, the same protected online sequence (u[−Tp,−1] , y [−Tp,−1]  

> S

) is also consistent with (A′, B ′, C ′).Under (A′, B ′, C ′), the ith output at time 0 is y′

> i

(0) = 

C′

> i

x⋆ =   ¯Ci + α ¯Cj

 x⋆. Since ¯Cj x⋆̸ = 0 , we can choose 

α with sufficiently large magnitude and appropriate sign so that y′

> i

(0) /∈ [ci, c i], which implies the violation of the ith output constraint at time 0.This proposition does not assert that the true plant neces-sarily violates the hard constraint. Rather, it shows that the dataset D cannot exclude consistent plants that do. This is the precise sense in which hard safety on unprotected outputs is not guaranteed under arbitrary offline poisoning attacks. This suggests a security-by-design principle: every output channel that enters hard safety constraints should be included in the protected storage. IV. S ECURE DEE PC Proposition 3 justifies that, under arbitrary offline poison-ing attacks, ignoring the unprotected data eyU is a minimax-optimal choice. We turn this limitation into a design principle by proposing Secure DeePC , whose basic ideas can be summarized as follows:  

> •

Until the online input data become PE, compute the output-truncated DeePC that uses only the protected dataset DS , taking into account the fundamental limita-tions of Propositions 3–4. In this phase, a small dither signal is added to the input to enhance PE.  

> •

Once the online data become PE, construct a linear mapping from (u, y S ) to yU . Using this linear mapping, reconstruct the offline, attack-free data within a spec-ified interval. Simultaneously, detect and identify any attacks present in the offline data during the interval.  

> •

After the online input data become PE, execute the original DeePC (7) using the reconstructed offline data. 

Algorithm 1 Secure DeePC 

Input: offline data D = ( u, eyS , eyU ), ¯Cu, ¯Cy , Cu, Cy , Tp, Tf ,objective functions JS and J, and W ⊂ Rm. 

> 1:

τpe ← +∞. 

> 2:

Construct (Up, eYp 

> S

, Uf , eYf 

> S

) using (u, eyS ). 

> 3:

for each k ∈ Z+ do  

> 4:

Retrieve past I/O data (up, y p). 

> 5:

if k < τ pe then  

> 6:

if u[−Tp,k −1] is PE of order 2Tp then  

> 7:

Set τpe ← k; τsw ← k + 1 . 

> 8:

end if  

> 9:

Compute (13) using (Up, eYp 

> S

, Uf , eYf 

> S

) for uf . 

> 10:

Apply u(k) = uf (k) + w(k), where w(k) is an i.i.d. random signal satisfying w(k) ∈ W . 

> 11:

end if  

> 12:

if k = τsw then  

> 13:

Compute F (τsw ) as F (τsw ) = YU (τsw )Z(τsw )†. 

> 14:

Recover byU (k) = F (τsw )z(k) for k ∈ [Tp, N −1] . 

> 15:

Construct (bUp

, bYp

, bUf

, bYf

) using the recovered 

dataset bD = ( u[Tp,N −1] , ey[Tp,N −1]  

> S

, by[Tp,N −1]  

> U

). 

> 16:

Identify the attacks as baU (k) = eyU (k) − byU (k). 

> 17:

end if  

> 18:

if k ≥ τsw then  

> 19:

Compute (7) using (bUp

, bYp

, bUf

, bYf

) for uf . 

> 20:

Apply u(k) = uf (k). 

> 21:

end if  

> 22:

end for 

Algorithm 1 presents the Secure DeePC framework. As with the original DeePC, at the activation time k = 0 , the past measurements u[−Tp,−1] and y[−Tp,−1] are given. This algorithm has a two-phase structure. In Phase I (k < τ sw ),the controller relies exclusively on the protected dataset DS

and enforces only the protected-output constraints, following Propositions 3–4. The time τsw , which is defined as τsw ≜

τpe +1 , indicates the switching time from Phase I to Phase II, where τpe denotes the first time at which the online input data u[−Tp,τ pe −1] is PE of order 2Tp. Then, in Phase II 

(k ≥ τsw ), Secure DeePC learns the linear mapping ¯F from 

(u, y S ) to yU and uses it to reconstruct the attack-free offline unprotected outputs; then it solves the full-output DeePC (7) using the recovered dataset. In what follows, we explain the details of this algorithm and provide some theoretical results. 

A. k < τ sw : Truncated DeePC with Active Input Excitation 

Our starting point is the output-truncated DeePC that uses Hankel matrices built from the protected data DS :

minimize  

> g,u f,y f
> S

JS

 uf , y f

> S

 (13a) 

s.t.



Up

eYp

> S

Uf

eYf

> S

 g =



up

yp

> S

uf

yf

> S

 , u f ∈ ¯CTf 

> u

, y f 

> S

∈ ¯CTf 

> y

, (13b) where yp 

> S

= y[k−Tp,k −1]  

> S

is the Tp most recent past output measurements in the set S from the system (1). Problem (13) imposes constraints only on input and protected output components, following Proposition 4. We define the induced constraint on the protected outputs as 

¯Cy ≜ proj S (Cy ) = 

n

yS ∈ R|S| : ∃ψ ∈ C y s.t. y S = ψS

o

.

The set ¯CTf 

> y

is the Tf -fold Cartesian product of ¯Cy . The decision variable yf 

> S

is given as yf 

> S

≜ col( yf 

> S

(k), . . . , y f 

> S

(k +

Tf − 1)) ∈ R|S| Tf . The objective function JS (uf , y f 

> S

) is also defined by only considering the outputs in S.This output-truncated DeePC implementation is intuitive, motivated by the fundamental limitations; however, because constraints and references concerning unprotected compo-nents are not applied, performance and safety cannot be ensured over time. Hence, in our Secure DeePC framework, we reconstruct (sanitize) the offline dataset and then run the original DeePC on the reconstructed data to ensure the same performance and safety with MPC within a finite time. As shown next, once the online input data is PE of order 

2Tp, one can recover the offline, attack-free, unprotected data yU under certain conditions. To achieve the excitation condition as soon as possible, we adopt the active input excitation strategy. Specifically, the input in Phase I is computed as u(k) = uf (k) + w(k), where uf (k) is computed by (13) with the input constraint ¯Cu ≜ Cu ⊖ W , where 

⊖ denotes the Pontryagin set difference, and w(k) ∈ W 

denotes an i.i.d. random signal with an absolutely continuous distribution supported on W ⊂ Rm satisfying 0 ∈ int W

(e.g., uniform on W). This constraint reformulation is needed to ensure input safety even under the active input excitation. By adding the i.i.d. signal w(k) to the input, the input sequence u[−Tp,k −1] is PE of order 2Tp almost surely if 

k ≥ 2( m + 1) Tp − 1 [14, Lemma 1]. In practice, the magnitude of w(k) can be small enough to preserve nominal closed-loop performance, while still providing the excitation required for the rank computation. 

B. k = τsw : Offline Data Reconstruction 

We next present the procedure for reconstructing the offline attack-free dataset. For each k ∈ Z+ in an online 

operation, define the vector based on the most recent Tp-step online input and output measurements in S:

ζ(k) ≜ col 



u[k−Tp,k −1] , y [k−Tp,k −1] 

> S



∈ R(m+|S|)Tp .

Also, define the matrix whose columns are z(·):

Z(k) ≜  ζ(0) · · · ζ(k − 1)  =

" HTp

 u[−Tp,k −2] 

HTp



y[−Tp,k −2] 

> S

#

,

Define the output matrix corresponding to the set U:

YU (k) ≜  yU (0) · · · yU (k − 1)  ∈ R|U|× k.

We first show the existence of a linear mapping from ζ to 

yU .

Lemma 1: Suppose that ( ¯A, ¯CS ) is observable and Tp ≥

n. Then, there exists a constant matrix ¯F ∈ R|U|× (m+|S| )Tp

such that, for any trajectory of the true plant ( ¯A, ¯B, ¯C),

yU (k) = ¯F ζ (k) for all k ∈ Z+.

Proof: Fix k ∈ Z+ and let xp ≜ x(k − Tp). Then, by the system dynamics (1), we obtain 

y[k−Tp,k −1]  

> S

= OTp xp + TTp u[k−Tp,k −1] , (14) where OTp ∈ R|S| Tp×n is the Tp-step observability matrix of 

( ¯A, ¯CS ) and TTp ∈ R|S| Tp×mT p is the associated lower-block-Toeplitz matrix. Since ( ¯A, ¯CS ) is observable and Tp ≥ n,

OTp has full column rank n. Hence, multiplying (14) by 

O† 

> Tp

gives 

xp = O† 

> Tp

y[k−Tp,k −1]  

> S

− O † 

> Tp

TTp u[k−Tp,k −1] . (15) Unrolling the state dynamics from time k − Tp to k yields 

x(k) = ¯ATp xp +RTp u[k−Tp,k −1] , RTp ≜  ¯ATp−1 ¯B · · · ¯B.

Therefore, using yU (k) = ¯CU x(k) and substituting (15), 

yU (k) = ¯CU ¯ATp xp + ¯CU RTp u[k−Tp,k −1] 

= ¯F col 



u[k−Tp,k −1] , y [k−Tp,k −1] 

> S



= ¯F ζ (k) (16) for all k ∈ Z+, where 

¯F ≜

h ¯CU RTp − ¯CU ¯ATp O† 

> Tp

TTp

¯CU ¯ATp O†

> Tp

i

.

The following lemma derives conditions under which the linear map can be constructed from the online measurements. 

Lemma 2: Suppose that ( ¯A, ¯B) is controllable, ( ¯A, ¯CS )

is observable, and Tp ≥ n. Assume that the online input 

u[−Tp,τ pe −1] is PE of order 2Tp for some τpe ∈ Z+. Define 

F (τsw ) ≜ YU (τsw )Z(τsw )† with τsw ≜ τpe +1 . Then, for any trajectory of the true plant ( ¯A, ¯B, ¯C), yU (k) = F (τsw )ζ(k)

for all k ∈ Z+.

Proof: By Lemma 1, there exists a constant matrix ¯F

such that yU (k) = ¯F ζ (k) for all k. Applying this relation column-wise to the online data for k ∈ [0 , τ sw − 1] yields 

YU (τsw ) = ¯F Z (τsw ). Now let ζ(k) be the Tp-step window generated by an arbitrary trajectory of the same plant. Since 

( ¯A, ¯B) is controllable, Tp ≥ n, and u[−Tp,τ pe −1] is PE of order 2Tp ≥ n + Tp, Willems’ Fundamental Lemma [10] implies that ζ(k) ∈ im Z(τpe + 1) = im Z(τsw ). Hence, 

F (τsw )ζ(k) = YU (τsw )Z(τsw )†ζ(k)= ¯F Z (τsw )Z(τsw )†ζ(k) = ¯F ζ (k) = yU (k), ∀k ∈ Z+.

Under the conditions of Lemma 2, the linear mapping from ζ to yU can be learned as F (τsw ) using the online measurements. Note that, by adding the i.i.d. dither signal 

w(k) to the input in Phase I, u[−Tp,τ pe −1] is PE of order 2Tp

in finite time almost surely, and accordingly, F (τsw ) can be constructed in finite time almost surely. Using F (τsw ), we can reconstruct the offline , unprotected output sequence in the interval [Tp, N − 1] .

Corollary 1: As with ζ(k), for the offline data, define 

z(k) ≜ col 



u[k−Tp,k −1] , y[k−Tp,k −1] 

> S



∈ R(m+|S|)Tp ,for k ∈ [Tp, N −1] . If the same conditions of Lemma 2 hold, then the offline, attack-free, unprotected output yU (k) can be recovered as yU (k) = F (τsw )z(k) for all k ∈ [Tp, N − 1] .

Proof: The offline experiment is another trajectory of the same LTI plant. Since the protected data are attack-free, 

eyS = yS . Applying Lemma 2 to the offline trajectory yields 

yU (k) = F (τsw )z(k) for all k ∈ [Tp, N − 1] .Define the recovered, offline, partial data by byU (k) ≜

F (τsw )z(k) for k ∈ [Tp, N − 1] . Then, we can reconstruct the attack-free offline dataset from time Tp to N − 1 as bD ≜

(u[Tp,N −1] , ey[Tp,N −1]  

> S

, by[Tp,N −1]  

> U

) (Line 15 in Algorithm 1). It should be emphasized that the recovered data byU can be used for the detection and identification of poisoning attacks, i.e., the poisoning attacks can be identified by 

baU (k) = eyU (k) − byU (k) for all k ∈ [Tp, N − 1] (Line 16 in Algorithm 1). Note that this detection and identification result does not contradict Proposition 1 because the online data serve as attack-free side information. 

C. k ≥ τsw : Original DeePC Using Reconstructed Dataset 

Once the attack-free dataset bD is reconstructed, we can use the original DeePC (7) based on the dataset while considering the reference signals and constraints for all input and output channels (Line 19 in Algorithm 1). Consequently, we have the following theorem showing the finite-time recovery and MPC-equivalent operation of Secure DeePC. 

Theorem 1: Suppose that ( ¯A, ¯B) is controllable, ( ¯A, ¯CS )

is observable, and Tp ≥ n. Assume further that the partial offline input u[Tp,N −1] is PE of order n + Tp + Tf . Secure DeePC (Algorithm 1) achieves MPC-equivalent performance and constraint satisfaction after a finite transient time almost surely if the algorithm is feasible at the switching time τsw 

and recursively feasible thereafter. 

Proof: By the dithered input policy and [14, Lemma 1], the online input becomes PE of order 2Tp after finite steps almost surely; namely, τpe < ∞ almost surely. When k =

τsw , by Lemma 2, the linear mapping can be constructed by 

F (τsw ) ≜ YU (τsw )Z(τsw )†. Then, by Corollary 1, the offline, attack-free output data can be reconstructed as byU (k) =

yU (k) = F (τsw )z(k) for all k ∈ [Tp, N − 1] . Thus, the reconstructed dataset bD is equivalent to the attack-free dataset 

(u[Tp,N −1] , y[Tp,N −1]  

> S

, y[Tp,N −1]  

> U

) for the interval [Tp, N − 1] .For Phase II of Secure DeePC (i.e., k ≥ τsw ), since ( ¯A, ¯B)

is controllable, ( ¯A, ¯C) is observable, Tp ≥ n, and u[Tp,N −1] 

is PE of order n + Tp + Tf , the optimal control sequence and corresponding system output obtained by this DeePC coincide with those of the MPC problem with prediction horizon Tf [2, Corollary 5.1]. Provided that the algorithm is feasible at the switching time τsw and recursively feasible thereafter, this implies MPC-equivalent performance and constraint satisfaction of Secure DeePC in Phase II. Once the online input becomes sufficiently exciting (which is achievable almost surely in finite time), the attack-free offline data in [Tp, N − 1] can be recovered exactly from the protected outputs. The Secure DeePC controller thereafter coincides with the original full-output DeePC and hence with the corresponding MPC under the conditions of Theorem 1. (friction) 

> (friction)
> Fig. 2: Inverted pendulum.

The observability condition on ( ¯A, ¯CS ) is needed to guarantee performance in Phase I. If an attack-free dataset is available and the offline input data is PE of order n + Tp

with Tp ≥ n, one can select an optimal placement of S

so that ( ¯A, ¯CS ) is observable from the dataset by using the rank-condition of Hankel matrices [16, Proposition 1]. This paper focuses on noise-free systems. Since the pro-posed method relies on the standard DeePC technique, the natural extension to noisy systems is to incorporate regu-larized DeePC formulations, which are developed in [2]. However, in the presence of noise, exact recovery of the linear mapping ¯F cannot be achieved in general, so another approach is needed to guarantee attack resilience against poisoning attacks. This is left for future work. V. N UMERICAL EXAMPLES 

We illustrate the attack-resilience performance of Secure DeePC by comparing it with the regularized DeePC [2] and output-truncated DeePC (13) via numerical simulations. We use an inverted pendulum system depicted in Fig. 2. 

A. Settings 

We adopt a linearized model. The physical parameters are chosen as follows: M = 0 .5 kg , m = 0 .2 kg , ℓ = 0 .3 m ,

I = 0 .006 kg · m2, and b = 0 .1 N · s/m. The states of the system are given as (x, ˙x, θ, ˙θ) and the input is given as 

u = F . Assume that the sensors measure (x, θ, ˙x). Then, the discretized system with the sampling time Ts = 0 .1 s is characterized by (1). We collect N = 120 data samples during an offline exper-iment. The offline input sequence u is generated randomly, which is PE of sufficiently high order. For the output data, we assume that only the first two output channels are protected, i.e., S = {1, 2} and U = {3}. Referring (8), we design the poisoning attack sequence as a3(k) = −αy1(k) for all 

k ∈ [0 , 119] with α = 1 .The regularized DeePC uses the full dataset D, including the poisoned data. The output-truncated DeePC uses only the protected dataset DS . For the objective function J(uf , y f ),we set Q = diag(10 2, 10 2, 10 2) and R = 1 . We impose the following constraints: 

Cu = {u : −5 ≤ u ≤ 5}, Cy =

hy1

> y2
> y3

i

:

 −4 

> −π
> 6
> −1



≤

hy1

> y2
> y3

i

≤

h 4

> π
> 6
> 1

i

.

The regularization parameters (for details, see [2]) are set as 

λg = 1 and λσ = 10 2.

B. Results 

Fig. 3 compares the closed-loop I/O trajectories of 20 trials obtained with regularized DeePC, the output-truncated DeePC, and Secure DeePC under offline data poisoning, with 0 5 10 15 20 

-5.0 

0.0 

5.0 

> u

0 5 10 15 20 

-5.0 

0.0 

5.0 

> y1

0 5 10 15 20 

-0.5 

0.0 

0.5 

> y2

0 5 10 15 20 Time [sec] (a) Regularized DeePC 

-1.0 

0.0 

1.0 

> y3

0 5 10 15 20 

-5.0 

0.0 

5.0 

> u

0 5 10 15 20 

-5.0 

0.0 

5.0 

> y1

0 5 10 15 20 

-0.5 

0.0 

0.5 

> y2

0 5 10 15 20 Time [sec] (b) Output-Truncated DeePC 

-1.0 

0.0 

1.0 

> y3

0 5 10 15 20 

-5.0 

0.0 

5.0 

> u

0 5 10 15 20 

-5.0 

0.0 

5.0 

> y1

0 5 10 15 20 

-0.5 

0.0 

0.5 

> y2

0 5 10 15 20 Time [sec] (c) Secure DeePC 

-1.0 

0.0 

1.0 

> y3

Fig. 3: I/O trajectories (solid black) of the regularized DeePC, the output-truncated DeePC, and Secure DeePC under poisoning attacks for 20 trials. The solid red lines indicate the reference signals while the dotted blue lines indicate the constraints. The green lines in (c) indicate the first time τpe at which the online input data is PE of order 2Tp, which was the same in all 20 trials. After one step of τpe ,Secure DeePC reconstructs the offline attack-free data and thereafter uses the reconstructed data. 

Tp = 10 and Tf = 20 . For each trial, the initial position is randomly chosen from −1 to 1, and the other initial parameters are set to 0. Each DeePC operates from time 

k = 0 . In Phase I of Secure DeePC, we add the random dither 

w(k), drawn from the uniform distribution on [−10 −4, 10 −4],to the system. The regularized DeePC (Fig. 3(a)) exhibits a diverging response in all trials, indicating that conventional DeePC can become unstable under data poisoning attacks. The output-truncated DeePC (Fig. 3(b)) tracks the references well; however, since y3 (an output in U) is not included in the optimization, it violates the safety bound on y3 (see around 

5 and 15 seconds in the figure). Secure DeePC (Fig. 3(c)) shows a small initial violation of the constraint on y3 because that constraint is not imposed in Phase I. However, once the online input data is PE, it tracks the references while respecting all constraints. Therefore, Secure DeePC achieves MPC-equivalent performance and constraint satisfaction in finite time, demonstrating its resilience to poisoning attacks. VI. C ONCLUSION 

This paper investigated data-driven control in the presence of data poisoning attacks on partially unprotected outputs. We first showed fundamental limitations: poisoning attacks cannot be detected and identified from the dataset alone, unprotected data are non-informative in a minimax sense, and hard safety constraints on unprotected outputs are not guaranteed. Building on these limitations, we proposed Se-cure DeePC, a two-phase framework that is resilient to offline poisoning attacks. We showed that Secure DeePC achieves MPC-equivalent guarantees in finite time, almost surely, un-der certain conditions. Numerical simulations demonstrated the efficacy of Secure DeePC against offline poisoning attacks. Future work will pursue secure control problems in the presence of both offline/online attacks. Another important direction is to address noisy data, including robustness anal-ysis. REFERENCES [1] C. D. Persis and P. Tesi, “Formulas for data-driven control: Stabiliza-tion, optimality, and robustness,” IEEE Trans. Autom. Control , vol. 65, no. 3, pp. 909–924, Mar. 2020. [2] J. Coulson, J. Lygeros, and F. D¨ orfler, “Data-enabled predictive control: In the shallows of the DeePC,” in Proc. Eur. Control Conf. ,Napoli, Italy, 2019, pp. 307–312. [3] L. Huang, J. Zhen, J. Lygeros, and F. D¨ orfler, “Robust data-enabled predictive control: Tractable formulations and performance guaran-tees,” IEEE Trans. Autom. Control , vol. 68, no. 5, pp. 3163–3170, 2023. [4] J. Berberich, J. K¨ ohler, M. A. M¨ uller, and F. Allg¨ ower, “Data-driven model predictive control with stability and robustness guarantees,” 

IEEE Trans. Autom. Control , vol. 66, no. 4, pp. 1702–1717, 2021. [5] A. Russo and A. Proutiere, “Poisoning attacks against data-driven control methods,” in Proc. 2021 Amer. Control Conf. , New Orleans, LA, 2021, pp. 3234–3241. [6] Y. Yu, R. Zhao, S. Chinchali, and U. Topcu, “Poisoning attacks against data-driven predictive control,” in Proc. 2023 Amer. Control Conf. , San Diego, CA, 2023, pp. 545–550. [7] H. Sasahara, “Adversarial attacks to direct data-driven control for destabilization,” in Proc. 62nd IEEE Conf. Decis. Control , Singapore, 2023, pp. 7094–7099. [8] R. Zhang, R. Yang, Y. Zhu, and P. Shi, “Data-driven security control for CPSs under aperiodic DoS attacks: A switched system approach,” 

IEEE Control Syst. Lett. , vol. 9, pp. 1688–1693, 2025. [9] S. Hu, D. Yue, Z. Jiang, X. Xie, and J. Zhang, “Data-driven security controller design for unknown networked systems,” Automatica , vol. 171, 111843, 2025. [10] J. C. Willems, P. Rapisarda, I. Markovsky, and B. L. M. De Moor, “A note on persistency of excitation,” Syst. Control Lett. , vol. 54, no. 4, pp. 325–329, 2005. [11] W. Liu, J. Sun, G. Wang, F. Bullo, and J. Chen, “Data-driven resilient predictive control under denial-of-service,” IEEE Trans. Autom. Con-trol , vol. 68, no. 8, pp. 4722–4737, 2023. [12] W. Liu, L. Li, J. Sun, F. Deng, G. Wang, and J. Chen, “Data-driven control against false data injection attacks,” Automatica , vol. 179, 112399, 2025. [13] A. Bhowmik, S. D. Bopardikar, and J. P. Hespanha, “Data-driven robust control under input-output stealthy attacks,” IEEE Control Syst. Lett. , vol. 9, pp. 2885–2890, 2025. [14] T. Shinohara, K. H. Johansson, and H. Sandberg, “Detection and identification of sensor attacks using partially attack-free data,” 2025. [Online]. Available: https://arxiv.org/abs/2510.02183 [15] Y. Shoukry and P. Tabuada, “Event-triggered state observers for sparse sensor noise/attacks,” IEEE Trans. Autom. Control , vol. 60, no. 8, pp. 2079–2091, 2016. [16] H. J. van Waarde, “Beyond persistent excitation: Online experiment design for data-driven modeling and control,” IEEE Control Syst. Lett. ,vol. 6, pp. 319–324, 2022.