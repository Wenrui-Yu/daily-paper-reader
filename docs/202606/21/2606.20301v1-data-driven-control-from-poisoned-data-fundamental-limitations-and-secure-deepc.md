---
title: "Data-Driven Control from Poisoned Data: Fundamental Limitations and Secure DeePC"
title_zh: 受毒化数据的数据驱动控制：基本局限性与安全DeePC
authors: "Takumi Shinohara, Henrik Sandberg, Karl Henrik Johansson"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20301v1"
tags: ["query:daily"]
score: 8.0
evidence: 针对中毒攻击的安全数据驱动控制
tldr: 数据驱动控制面临数据投毒攻击威胁，离线输出数据可能被篡改。本文分析了基本局限性：攻击不可检测、未保护数据无信息量、约束不可认证。提出Secure DeePC算法，先使用受保护数据运行截断DeePC至持续激励，再在线重建部分数据集恢复全输出DeePC。该算法在一定条件下几乎必然在有限时间内达到MPC等价性能。为安全数据驱动控制提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20301v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20301v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 379, \"height\": 222, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20301v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 634, \"label\": \"Figure\"}]"
motivation: 数据驱动控制中离线数据可能被投毒，现有方法缺乏安全性分析，需设计能抵御任意数据投毒攻击的算法。
method: 提出Secure DeePC：先仅用受保护数据运行输出截断DeePC至在线输入持续激励，然后用在线测量重建部分离线数据集，最后恢复全输出DeePC。
result: 在一定条件下，Secure DeePC几乎必然在有限时间内达到与模型预测控制（MPC）等价的性能，仿真验证了其有效性。
conclusion: 揭示了数据投毒攻击下数据驱动控制的基本局限性，并提供了首个可行的安全控制方案Secure DeePC。
---

## 摘要
我们研究了在任意数据毒化攻击下的数据驱动控制问题。假设部分离线输出数据存储在不安全的位置，可能被攻击者毒化。我们首先建立了由这种毒化数据引起的数据驱动控制的基本局限性：仅凭数据集无法检测/识别毒化攻击；未受保护的数据对于具有最坏情况保证的控制器设计是非信息性的；未受保护输出的硬约束无法被验证。受这些局限性和数据使能预测控制（DeePC）技术的启发，我们提出了安全DeePC，一种对毒化攻击具有弹性的数据驱动控制算法。它首先仅使用受保护的数据集运行输出截断的DeePC，直到在线输入变得持续激励。然后利用在线测量重建部分离线数据集，最后返回全输出DeePC。在特定条件下，安全DeePC几乎必然在有限时间内达到等效于MPC的性能。仿真结果说明了所提框架对抗毒化攻击的有效性。

## Abstract
We study a data-driven control problem in the presence of arbitrary data poisoning attacks. We assume that a subset of offline output data is stored in unprotected locations and may be poisoned by an adversary. We first establish fundamental limitations for data-driven control arising from such poisoned data: poisoning attacks are not detected/identified from the dataset alone; unprotected data are non-informative for controller design with worst-case guarantees; and hard constraints on unprotected outputs are not certifiable. Motivated by these limitations and the data-enabled predictive control (DeePC) technique, we propose Secure DeePC, a data-driven control algorithm that is resilient against poisoning attacks. It first runs output-truncated DeePC using only the protected dataset until the online input becomes persistently exciting. It then uses online measurements to reconstruct the partial offline dataset, and finally returns to full-output DeePC. Secure DeePC achieves MPC-equivalent performance in finite time almost surely under certain conditions. Simulation results illustrate the efficacy of the proposed framework against poisoning attacks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：数据驱动控制（如 DeePC）依赖离线采集的输入输出数据，但数据在存储或传输中可能被恶意篡改（数据投毒攻击）。现有工作多假设数据是真实的，少有针对离线投毒攻击的防御方案。
- **核心问题**：当离线输出数据部分受保护、部分可被任意篡改时，数据驱动控制面临哪些基本局限？能否设计出在有限时间内恢复安全控制性能的算法？
- **整体含义**：本文揭示了任意投毒攻击下数据驱动控制无法被探测、未保护数据无信息量、硬约束不可保证等三个根本局限，并提出了首个能抵御此类攻击的两阶段安全控制算法 Secure DeePC。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：利用部分受保护的输出数据（与系统可观测性相关）和在线清洁测量，先执行输出截断的 DeePC（只使用保护数据）并主动添加小扰动信号使在线输入持续激励；一旦达到持续激励条件，即可在有限时间内精确重构被污染的离线数据，然后切换到全输出 DeePC 以获得 MPC 等效性能。
- **关键技术细节**：
  - **第一阶段（Phase I，k < τ_sw）**：
    - 使用保护数据集 D_S（包括输入和保护输出）构建 Hankel 矩阵，求解输出截断 DeePC 问题（只约束保护输出和输入）。
    - 为加速持续激励，在计算出的 u_f(k) 上叠加独立同分布随机扰动 w(k)（满足一定支撑集）。
    - 当在线输入序列 u[−T_p, k-1] 达到 2T_p 阶持续激励时，记录时间 τ_pe，设定切换时间 τ_sw = τ_pe+1。
  - **第二阶段（Phase II，k = τ_sw）**：
    - 利用过去 T_p 步的在线数据（输入和保护输出）构造 ζ(k) 向量，并基于已收集的在线数据计算线性映射矩阵 F(τ_sw) = Y_U(τ_sw) Z(τ_sw)†，其中 Z(τ_sw) 是 ζ 矩阵，Y_U(τ_sw) 是对应的未保护输出矩阵。
    - 利用该映射从离线保护数据重建被污染的未保护输出：ŷ_U(k) = F(τ_sw) z(k) for k∈[T_p, N-1]。
    - 可同时识别攻击：â_U(k)= ẽ_U(k)−ŷ_U(k)。
  - **后续阶段（k ≥ τ_sw）**：
    - 使用重建的完整攻击-free 数据集（u[T_p,N-1], y_S[T_p,N-1], ŷ_U[T_p,N-1]）运行标准全输出 DeePC，处理所有输入/输出约束和参考信号。
- **算法流程简述**：Algorithm 1 给出完整步骤：初始化 τ_pe=+∞；循环每个时间步 k；若 k<τ_pe，检查持续激励条件并执行截断 DeePC+扰动；到达 τ_sw 时重建数据并识别攻击；此后使用重建数据运行原始 DeePC。
- **理论保证**：在系统可控可观测、T_p≥n、离线输入 PE 足够高阶、算法可行性等条件下，Secure DeePC 几乎必然在有限时间内达到与 MPC 等效的性能和约束满足。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **系统模型**：线性化后的倒立摆（状态：x, ẋ, θ, θ̇，输入：F，输出：x, θ, ẋ），采样时间0.1s。
- **离线数据**：N=120 样本，输入随机生成且 PE 足够高阶；输出中前两个通道受保护（S={1,2}），第三个通道未保护（U={3}），攻击设计为 a₃(k)=−α y₁(k)（α=1）。
- **对比方法**：
  - **常规正则化 DeePC**：使用全数据集（含毒化数据）。
  - **输出截断 DeePC**：仅使用保护数据集 D_S。
  - **Secure DeePC**（本文方法）。
- **参数**：T_p=10，T_f=20，Q=diag(100,100,100), R=1，约束：−5≤u≤5，y1∈[−4,4], y2∈[−π/6, π/6], y3∈[−1,1]。
- **场景**：20次试验，每次随机初始位置（−1~1），其他初始状态为0。

### 4. 资源与算力

- **文中未明确提及**使用的 GPU 型号、数量或训练时长。实验基于 Matlab 仿真（推测），未涉及大规模计算资源。

### 5. 实验数量与充分性

- **实验数量**：仅一个倒立摆系统，20 次随机初始化试验对比三种算法。
- **充分性评估**：
  - 实验展示了常规 DeePC 发散、截断 DeePC 违反未保护输出约束、Secure DeePC 在短暂瞬态后完全满足约束并跟踪参考，定性验证了算法有效性。
  - 但仅一个线性化模型，缺乏对非线性、噪声或不同攻击模式（如稀疏攻击、间歇攻击）的测试，实验覆盖有限。
  - 未提供定量指标（如平均跟踪误差、瞬态时间分布）或消融实验（如不同保护通道数、不同扰动幅值）。
  - 结论虽理论扎实，但实验客观性和公平性基本可接受（控制变量一致）。

### 6. 论文的主要结论与发现

1. **基本局限**：在任意离线投毒攻击下，攻击无法仅从数据集检测/识别；未保护数据对最坏情况下控制器设计无信息量；硬约束在未保护输出上无法被保证。
2. **重建可行性**：若保护输出使系统可观、在线输入持续激励，则可在有限时间内精确重建被污染离线数据。
3. **Secure DeePC的性能**：在可控、可观测、数据足够丰富等条件下，Secure DeePC 几乎必然在有限时间内达到与对应 MPC 等效的性能与约束满足，且能识别攻击。
4. **仿真验证**：常规 DeePC 因毒化数据不稳定，截断 DeePC 无法保证未保护输出安全，Secure DeePC 可在短暂瞬态后恢复全性能。

### 7. 优点

- **理论贡献**：首次系统分析任意投毒攻击下数据驱动控制的基本局限（无检测、无信息量、无安全保证），为安全设计提供理论基础。
- **算法设计巧妙**：利用保护输出与在线测量的可观测性，通过持续激励和线性映射重建离线数据，无需假设攻击模式。
- **有限时间恢复**：几乎必然在有限时间内切换到等效 MPC，实际可操作性强。
- **攻击识别能力**：重建后可直接计算攻击序列，支持事后审计。

### 8. 不足与局限

- **噪声场景未处理**：论文明确假设无噪声系统，扩展至有噪声需正则化及近似恢复，精度可能下降。
- **仅考虑离线输出投毒**：未考虑在线测量也被攻击的情况，实际系统可能面临双重威胁。
- **实验单一**：只使用一个线性化倒立摆模型，未在更复杂非线性系统或真实硬件平台验证，结论泛化性存疑。
- **约束完整性**：保护输出需涵盖所有硬约束通道（Proposition 4），实际中可能难以完全满足。
- **参数选择未讨论**：T_p、扰动幅度等对收敛时间和性能的影响未分析。
- **计算效率**：未与 MPC 或模型辨识方法对比计算开销，DeePC 本身需要求解较大规模优化问题。

（完）
