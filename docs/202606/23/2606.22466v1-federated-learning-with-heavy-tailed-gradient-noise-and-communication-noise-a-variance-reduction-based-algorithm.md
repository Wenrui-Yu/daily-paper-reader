---
title: "Federated learning with heavy-tailed gradient noise and communication noise: a variance-reduction based algorithm"
title_zh: 具有重尾梯度噪声和通信噪声的联邦学习：一种基于方差缩减的算法
authors: "Shengchao Zhao, Yongchao Liu"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22466v1"
tags: ["query:daily"]
score: 9.0
evidence: 联邦学习中的重尾梯度与通信噪声及隐私保护
tldr: "联邦学习面临重尾梯度噪声和通信噪声的挑战。VRA-FedSGD算法通过动量方差缩减和非线性映射减轻梯度噪声，利用方差缩减聚合抑制通信噪声。理论证明在非凸目标下收敛率为O(K^{-(p-1)/(2p-1)})，强凸下几乎必然收敛率为\\tilde{O}(K^{-(1-1/(p-ε))})。实验验证了算法有效性，为联邦学习处理重尾噪声提供了新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22466v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1513, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22466v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1537, \"height\": 547, \"label\": \"Figure\"}]"
motivation: 现有联邦学习方法在重尾噪声下收敛慢或不稳定，需要兼顾梯度噪声和通信噪声的鲁棒算法。
method: 提出VRA-FedSGD，结合动量方差缩减、非线性映射和方差缩减聚合机制，同时处理重尾梯度与通信噪声。
result: "非凸目标均值收敛率O(K^{-(p-1)/(2p-1)})，强凸目标几乎必然收敛率\\tilde{O}(K^{-(1-1/(p-ε))})。"
conclusion: 首次为联邦学习在重尾梯度与通信噪声下提供理论保障和有效算法，经实验验证。
---

## 摘要
联邦学习（FL）是一种新兴的分布式机器学习范式，它使得本地设备能够在数据分散且私密的情况下共同训练全局模型。我们提出了一种基于方差缩减的算法VRA-FedSGD，用于处理存在重尾梯度噪声和通信噪声的联邦学习场景，这些噪声在无线网络和物联网部署中的大规模机器学习中普遍存在。VRA-FedSGD采用动量方差缩减技术与非线性映射来减轻重尾梯度噪声，并使用方差缩减的聚合机制来抑制重尾通信噪声。在均值意义上，对于非凸目标函数，VRA-FedSGD实现了$\mathcal{O}\left(K^{-(p-1)/(2p-1)}\right)$的收敛速率，其中$p$是重尾噪声的尾指数。在几乎必然意义上，对于强凸目标函数，VRA-FedSGD实现了$\tilde{\mathcal{O}}\left(K^{-(1-1/(p-\varepsilon))}\right)$的收敛速率，其中$\varepsilon$是一个任意小的常数。在实际数据的逻辑回归问题上的模拟实验验证了VRA-FedSGD的有效性。

## Abstract
Federated learning (FL) is an emerging distributed machine learning paradigm that enables local devices to jointly train a global model while keeping data decentralized and private. We propose a variance-reduction based algorithm, VRA-FedSGD, for FL in the presence of heavy-tailed gradient noise and communication noise, where these noises are prevalent in large-scale machine learning over wireless networks and Internet of Things deployments. VRA-FedSGD employs a momentum variance reduction technique together with a nonlinear mapping to mitigate heavy-tailed gradient noise, and uses a variance-reduced aggregation mechanism to suppress heavy-tailed communication noise. In the mean sense, VRA-FedSGD achieves a convergence rate of {\small$\mathcal{O}\left(K^{-(p-1)/(2p-1)}\right)$} for nonconvex objective functions, where $p$ is the tail index of heavy-tailed noise. In the almost sure sense, VRA-FedSGD achieves a convergence rate of $\tilde{\mathcal{O}}\left(K^{-(1-1/(p-ε))}\right)$ for strongly convex objective functions, where $ε$ is an arbitrarily small constant. Simulated experiments on a logistic regression problem with real-world data verify the effectiveness of VRA-FedSGD.

---

## 论文详细总结（自动生成）

# 论文总结：具有重尾梯度噪声和通信噪声的联邦学习：一种基于方差缩减的算法

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题背景**：联邦学习（FL）在无线网络和物联网部署中常面临**重尾梯度噪声**（来自隐私噪声、随机计算误差、数据抽样等）和**重尾通信噪声**（来自量化、信道衰落和干扰）。现有FL算法（如FedAvg、SCAFFOLD）大多基于梯度噪声和通信噪声方差有界的假设，但实际数据（如实验中使用的Diabetes数据集）中噪声往往具有重尾特性（甚至无限方差），导致传统算法发散或性能下降。
- **研究动机**：需要一种能同时应对重尾梯度噪声和重尾通信噪声的鲁棒FL算法，并提供严格的理论收敛保证（包括均值收敛率和几乎必然收敛率）。
- **整体含义**：本文提出VRA-FedSGD，首次为联邦学习在两种重尾噪声并存场景下提供了理论保障，填补了几乎必然收敛率分析在该领域的空白。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：采用动量方差缩减技术（类似STORM）配合非线性映射抑制重尾梯度噪声；采用方差缩减聚合（VRA）机制抑制重尾通信噪声；允许部分客户端参与以提升通信效率。
- **关键技术细节**：
  - **客户端局部模型估计**：服务器广播差分量 $\Delta x_k = (x_k - x_{k-1})/\beta_k + x_{k-1}$，客户端接收带下行噪声的版本，并通过指数移动平均更新局部模型 $x_{i,k} = (1-\beta_k)x_{i,k-1} + \beta_k(\Delta x_k + \zeta_{i,k})$，实现方差缩减。
  - **全局梯度估计**：客户端计算梯度差分 $\Delta g_{i,k} = \frac{\nabla F_i(x_{i,k};\xi_{i,k}) - \nabla F_i(x_{i,k-1};\xi_{i,k})}{\eta_k} + \nabla F_i(x_{i,k-1};\xi_{i,k})$，上传受上行噪声干扰的版本；服务器通过动量更新全局梯度估计 $m_k$（满客户端参与时），或分别维护每个客户端的局部梯度估计并全局平均（部分参与时），从而同时抑制梯度噪声和通信噪声。
  - **模型参数更新**：$x_{k+1} = x_k - \alpha_{k+1} \mathcal{N}(m_k)$，其中 $\mathcal{N}(\cdot)$ 是非线性映射（如裁剪、归一化、BiClip），防止极端值破坏更新。
- **算法流程**：Algorithm 1 完整给出VRA-FedSGD的伪代码。

## 3. 实验设计：数据集 / 场景、benchmark、对比方法
- **数据集**：Diabetes数据集（8维特征，768个样本），逻辑回归问题（加入L2正则项）。数据集通过Dirichlet分布分配给n=20个客户端，使数据非独立同分布。
- **场景**：
  - 完美通信（作为理想对比）
  - 高斯通信噪声
  - α-stable通信噪声（典型重尾分布）
- **Benchmark/对比方法**：
  - FAT-Clipping-PI [42]
  - SClip-EF [45]
  - Bi²Clip [20]
  - VRA-FedSGD的三种非线性映射变体：VRA-FedSGD(clip)、VRA-FedSGD(norm)、VRA-FedSGD(biclip)
- **额外实验**：在完美通信场景下，进一步测试VRA-FedSGD(clip)在不同参与率（r=0.6, 0.8, 1.0）下的性能，分别使用常数步长和衰减步长。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量或训练时长。仅提到所有算法均运行4000次迭代（iterations）。实验可能是在普通CPU或单GPU上完成，未强调大规模算力需求。

## 5. 实验数量与充分性
- **实验数量**：图1展示了3种通信场景下各算法的归一化梯度范数随迭代的变化；图2展示了部分参与率实验（3种率×2种步长策略）。主要对比了3个基线算法和自己3个变体，总共约7条曲线。
- **充分性与客观性**：
  - 优点：实验设计覆盖了多种噪声类型和非线性映射，部分参与率分析补全了理论设定。
  - 不足：仅使用单一数据集（Diabetes，小规模d=8）和一个任务（逻辑回归），未在图像分类、语言模型等更大规模任务上验证；未与更多近期算法（如FedAvg with gradient clipping、Federated STORM等）对比；消融实验仅对比了三种非线性映射，缺乏对其他参数选择（如β_k, η_k, α_k组合）的系统敏感性分析；实验重复次数未说明，无法评估方差。

## 6. 论文的主要结论与发现
- **理论结论**：
  - 非凸目标函数下，均值收敛率为 $\mathcal{O}\left(K^{-\frac{p-1}{2p-1}}\right)$（满客户端参与），部分参与时为 $\mathcal{O}\left(K^{-\frac{p-1}{3p-2}}\right)$。
  - 强凸目标函数下，几乎必然收敛率为 $\tilde{\mathcal{O}}\left(K^{-(1-1/(p-\varepsilon))}\right)$，首次建立此类收敛保证。
- **实验结论**：
  - 相比基线算法，VRA-FedSGD在完美通信下达到更小的最终误差；在高斯和α-stable通信噪声下仍能稳定收敛，基线算法（FAT-Clipping-PI等）在噪声下未报告或未比较（图1中仅展示VRA-FedSGD在噪声下的结果，未绘制基线在噪声下的结果，这有一定局限性）。
  - 部分参与率下，VRA-FedSGD在常数步长时鲁棒，而衰减步长对参与率变化敏感。

## 7. 优点
- **理论创新**：同时处理重尾梯度噪声和通信噪声，并给出几乎必然收敛率，这是FL领域首次。
- **算法设计**：巧妙融合动量方差缩减（抑制梯度噪声）和方差缩减聚合（抑制通信噪声），且支持部分客户端参与，实际适用性较高。
- **非线性映射通用性**：证明多种常见算子（裁剪、归一化、BiClip）都满足假设，便于实际选用。
- **实验验证**：在至少三种噪声场景和不同参与率下验证了算法的稳定性，与多个基线对比。

## 8. 不足与局限
- **实验覆盖有限**：仅在一个小数据集（8维）和一个任务（逻辑回归）上测试，未扩展至深度神经网络或更大规模联邦学习场景（如图像分类、语言模型），结论的泛化性存疑。
- **基线对比不完整**：在噪声通信场景下（图1b, 1c）只展示了VRA-FedSGD变体的结果，未将基线算法（FAT-Clipping-PI, SClip-EF, Bi²Clip）置于相同噪声条件下比较，难以体现其相对优势。
- **消融实验不够充分**：仅对比了三种非线性映射，未分析算法各组件（动量方差缩减、VRA机制）的贡献，也未探讨β_k, η_k, α_k选择的影响。
- **缺失计算效率分析**：未讨论VRA-FedSGD相比基线的额外通信或计算开销（如客户端需要存储和计算两个梯度）。
- **未考虑隐私或防御攻击**：论文未涉及差分隐私或对抗性攻击，而重尾噪声可能源于隐私扰动，此处未讨论。

（完）
