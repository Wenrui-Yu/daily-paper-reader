---
title: "Federated Learning Architecture: Data Privacy and System Security Approaches"
title_zh: 联邦学习架构：数据隐私与系统安全方法
authors: "Cagdas Karatas, Hibanur Karadogan, Ahmet Yasin Ertug, Busra Buyuktanir, Kazim Yildiz, Gozde Karatas Baydogmus"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09391v1"
tags: ["query:daily"]
score: 10.0
evidence: 集成同态加密和差分隐私保护联邦学习，直接解决分布式机器学习中的隐私问题
tldr: 联邦学习虽保留数据在本地，但模型更新仍可能泄露隐私。本研究结合同态加密与差分隐私技术，在Framingham等三个数据集上验证，可在不显著降低模型准确率的前提下增强隐私保护。同时分析了数据异构性的影响，提出合理选择差分隐私参数与训练设置、使用更大数据集等策略可提升联邦学习效率。成果表明隐私保护与高性能AI系统可安全应用于医疗和金融等敏感领域。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09391v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1677, \"height\": 665, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09391v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1188, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09391v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1189, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09391v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1189, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09391v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1530, \"height\": 910, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09391v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 1810, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09391v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 833, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09391v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 766, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09391v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1469, \"height\": 557, \"label\": \"Table\"}]"
motivation: 联邦学习中模型更新可能泄露用户隐私，需在不牺牲模型性能的前提下增强数据隐私与安全性。
method: 集成同态加密实现密文计算，结合差分隐私防止个体信息提取，在联邦学习架构中保护客户端数据。
result: 在三个数据集上测试，隐私增强未显著降低模型准确率，且通过调整差分隐私参数和数据集大小可提升效率。
conclusion: 隐私保护联邦学习可安全应用于医疗和金融等高敏感场景，实现高性能与隐私保护的平衡。
---

## 摘要
本研究探索了同态加密与差分隐私技术的集成，以增强联邦学习系统中的数据隐私与安全性。联邦学习允许数据保留在本地设备上，无需集中式数据收集，然而模型更新过程中仍可能泄露敏感信息。为解决这一问题，同态加密支持对加密数据进行计算，而差分隐私则通过对模型输出应用统计技术来防止个体信息被提取。所提出的架构在弗雷明汉、皮马印第安人糖尿病和银行营销数据集上进行了测试，结果表明可以在不显著降低模型准确性的情况下实现增强隐私。此外，分析了客户端间数据异构性对模型性能的影响，并得出结论：通过谨慎选择差分隐私参数和训练设置，以及使用更大数据集等策略，可以提高联邦学习的效率。研究结果表明，隐私保护且高性能的人工智能系统可以安全地应用于医疗和金融等敏感领域。

## Abstract
This study explores the integration of homomorphic encryption and differential privacy techniques to enhance data privacy and security in Federated Learning (FL) systems. FL allows data to remain on local devices, eliminating the need for centralized data collection; however, sensitive information may still be leaked during model updates. To address this issue, homomorphic encryption enables computations on encrypted data, while differential privacy prevents the extraction of individual information through statistical techniques applied to model outputs. The proposed architecture was tested on the Framingham, Pima Indians Diabetes, and Bank Marketing datasets, revealing that enhanced privacy can be achieved without significantly compromising model accuracy. Furthermore, the impact of data heterogeneity among clients on model performance was analyzed, and it was concluded that strategies such as the careful selection of differential privacy parameters and training settings, along with the use of larger datasets, can improve the efficiency of FL. The findings demonstrate that privacy-preserving and high-performance artificial intelligence systems can be securely applied in sensitive domains such as healthcare and finance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：联邦学习（FL）虽然通过数据留在本地避免了集中式收集，但模型更新（如梯度或权重）在传输过程中仍可能泄露用户敏感信息，例如通过模型推理或数据重构攻击。
- **研究动机**：现有FL系统面临隐私漏洞，需要额外的保护机制。同态加密（HE）允许对加密数据直接计算，差分隐私（DP）通过向输出添加噪声防止个体信息被推断。两者结合有望在维护模型性能的同时提供强隐私保障。
- **研究背景**：人工智能和机器学习依赖大量数据，但数据隐私法规（如GDPR、HIPAA）和用户隐私关切要求更安全的训练范式。FL是分布式学习的主流方案，但安全威胁（如恶意服务器、窃听者）仍需解决。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 2.1 核心思想
- 在联邦学习中嵌入两层隐私保护：
  - **同态加密（HE）**：客户端将本地训练后的模型权重使用CKKS方案加密，传输给服务器，服务器在加密状态下进行FedAvg聚合，再解密更新全局模型。确保传输过程中权重不泄露。
  - **差分隐私（DP）**：在每个客户端的本地训练过程中，使用Opacus的PrivacyEngine向梯度添加噪声（高斯机制），限制单个样本对模型的影响，防止从最终模型或参数反推个体信息。

### 2.2 关键技术细节
- **同态加密方案**：CKKS（Cheon–Kim–Kim–Song），一种近似计算的HE方案，支持对实数加密并允许加法和乘法操作，误差可控制。加密过程基于多项式环 \( R = \mathbb{Z}[X]/(X^N+1) \)，N为安全参数。公式：\(\text{Enc}(m_1) + \text{Enc}(m_2) \Rightarrow \text{Enc}(m_1+m_2)\)，乘法类似。
- **差分隐私机制**：使用高斯机制，噪声标准差 \(\sigma \geq \frac{\sqrt{2\ln(1.25/\delta)} \cdot \Delta f}{\varepsilon}\)，其中灵敏度 \(\Delta f\) 取L1范数。隐私预算 \(\varepsilon\) 通过累计组合性质累加。
- **算法流程**（伪代码Algorithm 1）：
  - **准备阶段**：划分数据集为训练/测试集；将训练数据分配给N个客户端；初始化全局模型、HE上下文、DP参数；为每个客户端创建PrivacyEngine。
  - **训练循环**（每轮）：
    - 每个客户端：复制全局模型权重，设置隐私引擎，在本地数据上训练E个epoch（本例为5），计算损失；使用PrivacyEngine追踪\(\varepsilon\)；将训练后权重用HE加密；发送加密权重。
  - 服务器收集所有加密权重，执行FedAvg（加权平均），解密后更新全局模型。
  - 在测试集上评估全局模型（准确率、精确率、召回率、F1）。

### 2.3 模型结构
- 三层全连接神经网络：输入层（维度等于特征数）→隐藏层1（256神经元+LayerNorm+ReLU+Dropout 50%）→隐藏层2（128神经元+LayerNorm+ReLU+Dropout 50%）→输出层（1神经元+Sigmoid）。优化器：Adam，学习率0.001。

### 2.4 数学公式示例
- DP定义：\(\Pr[\mathcal{A}(D) \in S] \leq e^\varepsilon \cdot \Pr[\mathcal{A}(D') \in S]\)，其中D和D'为相邻数据集。
- HE加法同态：\(\text{Enc}(m_1) + \text{Enc}(m_2) \approx \text{Enc}(m_1+m_2)\)（近似）。

## 3. 实验设计：数据集、场景、基准与对比方法
### 3.1 数据集
| 数据集 | 样本数 | 特征数 | 任务类型 | 数据预处理 |
|--------|--------|--------|----------|------------|
| Framingham | 4240 | 15 → 处理后 | 10年冠心病二分类预测 | 缺失值去除，标准化，SMOTE平衡 |
| Pima Indians Diabetes | 768 | 8 | 糖尿病二分类 | 零值视为缺失，中位数填充，IQR去异常，标准化，SMOTE |
| Bank Marketing | 41188 | 17 | 定期存款订阅二分类 | IQR裁剪异常，标准化，One-Hot编码，下采样（缓解类别不平衡） |

### 3.2 场景设置
- 客户端数量：3、5、10（模拟分布式环境）。
- 局部训练轮数：每轮每个客户端训练5个epoch，全局通信轮数（rounds）：10。
- 差分隐私参数固定：\(\sigma=5.0\)，max_grad_norm=0.5，\(\delta=10^{-5}\)。
- 聚合算法：FedAvg（同态加密下加权平均）。

### 3.3 基准与对比
- **基准方法**：不使用DP（"Without DP"）但保留HE的联邦学习训练（即无噪声注入，仅加密传输）。
- **对比指标**：准确率（Accuracy）、精确率（Precision）、召回率（Recall）、F1分值。
- **隐私度量**：累积\(\varepsilon\)值（越小越私密）。

### 3.4 评估方式
- 每个数据集、每个客户端配置下，进行10轮全局训练，记录每轮测试集指标和累积\(\varepsilon\)。最后比较有DP和无DP的最终指标（表3、表4）。

## 4. 资源与算力
- **论文未明确说明**使用的GPU型号、数量、训练时长、CPU或内存等硬件信息。仅提到实验在模拟环境中进行（非真实分布式设备），且客户端数量有限（最多10个）。因此无法评估实际计算成本。

## 5. 实验数量与充分性
- **实验数量**：
  - 3个数据集 × 3种客户端配置 × 2种隐私设置（有/无DP）= 18组主要实验结果（表3和表4共9行有DP+9行无DP）。
  - 每个配置下10轮训练，产生ε变化曲线（图2-4，图5展示了累积ε）。
  - 无额外的消融实验（如调整DP参数、改变聚合算法等）。
- **充分性评价**：
  - **优点**：覆盖了不同数据规模（Bank约4万，Framingham约4千，Pima约700），展示了隐私预算与数据量、客户端数的关系，对比了有无DP的性能差异。
  - **不足**：
    - 仅使用单一模型架构（3层全连接），未测试更复杂模型（如CNN、Transformer）。
    - 数据集均为表格型分类数据，未涉及图像、文本等模态。
    - 未模拟真实数据异构性（non-IID分布），文中虽提及数据异构性影响，但实验未具体设置异构程度。
    - 未与其他隐私保护方法（如仅HE、仅DP、安全多方计算）进行对比。
    - 未进行统计显著性检验或多轮重复实验以评估方差。
  - **客观性**：结果报告了带有DP时性能下降（约2-5个百分点），并解释了隐私-准确率权衡，没有刻意美化。

## 6. 论文的主要结论与发现
- **隐私-性能平衡可行**：集成HE和DP的联邦学习在三个数据集上实现了隐私保护，且模型准确率下降幅度有限（Bank下降约4-6%，其他类似）。例如Bank数据集3客户端时准确率从86.20%降至81.85%，ε仅0.3566。
- **客户端数影响ε**：客户端越多，每个客户端数据越少，DP所需噪声越大，累积ε增长更快（如Bank，3客户端ε=0.3566，10客户端ε=0.6785）。
- **数据集规模重要**：大数据集（Bank）在相同客户端数下产生的ε更小，性能损失也更小，说明充足数据有助于缓解DP噪声影响。
- **数据分布重要性**：数据异构性显著影响模型性能，建议使用个性化更新或自适应聚合方法。
- **结论**：提出的隐私保护联邦学习架构可安全应用于医疗、金融等敏感领域，实现隐私与性能的双赢。

## 7. 优点
- **技术融合创新**：首次在统一框架中同时集成CKKS同态加密和差分隐私，提供端到端的数据安全（传输加密+输出隐私保证）。
- **实验覆盖全面**：测试了三种不同规模、不同领域的数据集，分析了客户端数量、数据量对隐私预算的影响，提供了实证指导。
- **算法透明**：给出了清晰的伪代码（Algorithm 1），并详细说明了HE和DP的具体参数设置（如σ=5.0、max_grad_norm=0.5等），可复现。
- **结果分析深入**：不仅报告了性能指标，还绘制了ε随轮次的变化曲线，揭示了隐私消耗的规律。
- **公平对比**：设置了无DP的对照组，直接展示了隐私保护带来的代价，结果客观。

## 8. 不足与局限
- **实验范围有限**：
  - 客户端数量仅有3/5/10，远小于真实FL场景（成百上千），且每个客户端数据量相对较大（300-4000样本），不能反映稀疏客户端场景。
  - 未模拟真实通信延迟、设备异构性、客户端掉线等问题。
  - 未评估非独立同分布（non-IID）数据分布，而这是FL的主要挑战。
- **模型与算法简单**：
  - 仅使用浅层全连接网络，未测试CNN、RNN、Transformer等复杂模型。对于高维数据（如图像）效果未知。
  - 隐私预算管理仅依靠基本组合性质，未采用RDP（Rényi differential privacy）或自适应噪声调节等更先进方法。
- **同态加密开销未量化**：未报告加密/解密时间、模型大小增加、通信负载等实际计算成本，影响了方案实用性的说服力。
- **缺乏与其他基准的对比**：未与仅用DP或仅用HE的方法比较，也未与集中式训练（非联邦）做基准对比，无法判断联合优势。
- **部分数据集预处理不一致**：Framingham和PID使用了SMOTE过采样，而Bank使用了欠采样，可能影响跨数据集比较的一致性。
- **结果稳定性**：未报告多次重复实验的均值和方差，无法判断性能波动的置信度。

（完）
