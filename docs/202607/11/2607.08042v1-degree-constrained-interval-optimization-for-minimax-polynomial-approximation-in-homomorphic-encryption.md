---
title: Degree-Constrained Interval Optimization for Minimax Polynomial Approximation in Homomorphic Encryption
title_zh: 同态加密中极小极大多项式逼近的度约束区间优化
authors: "Jiheon Woo, Donggyun Ryu, Yongjune Kim"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08042v1"
tags: ["query:daily"]
score: 9.0
evidence: 同态加密实现隐私保护的加密推理多项式逼近
tldr: 同态加密中非多项式激活函数需用多项式近似，minimax近似区间影响鲁棒性与精度。本文提出分布感知的区间优化方法，联合域扩展多项式控制区间外输入，通过代理目标与误差分解实现均方误差最小化，理论推导了实现误差上界。该方法在固定多项式度数下优化了区间选择，提升了近似精度与鲁棒性的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08042v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 651, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 798, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 776, \"height\": 2106, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 761, \"height\": 2097, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 760, \"height\": 1347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08042v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1582, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08042v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1613, \"height\": 556, \"label\": \"Table\"}]"
motivation: 同态加密要求多项式近似非多项式函数，但minimax近似区间选择存在鲁棒性与精度的权衡，缺乏分布感知的优化方法。
method: 将区间优化建模为最小化均方误差的分布感知问题，结合域扩展函数代理目标与域扩展多项式实现，推导实现误差分解与上界。
result: 推导了代理目标与实现误差上界，为分布感知的区间优化提供了理论保证与可计算目标。
conclusion: 通过区间优化与域扩展多项式结合，有效平衡了近似精度与区间外鲁棒性，适用于同态加密中的多项式近似设计。
---

## 摘要
同态加密（HE）在算术约束下实现隐私保护推理，该约束将加密计算限制为加法和乘法。因此，非多项式激活函数必须由多项式近似替代。在多项式逼近方法中，极小极大逼近（通常通过Remez算法计算）是一种标准方法，因为它能在给定设计区间上最小化最大逼近误差。对于极小极大多项式设计，逼近区间是一个关键超参数：更宽的区间可提高对大幅值输入的鲁棒性，但同时会在固定度数预算下增加极小极大逼近误差。在本文中，我们将这种权衡表述为一种分布感知的区间优化问题，其中选择逼近区间以最小化相对于感兴趣的预激活分布的均方误差（MSE）。为了有效控制区间外输入，我们将极小极大多项式与域扩展函数（DEF）及其HE可实现的多项式对应物——域扩展多项式（DEP）相结合，这些多项式近似了设计区间外的裁剪操作，从而抑制了不受控制的多项式外推。我们首先推导了一个基于DEF的可解析处理的代理目标，该目标捕捉了区间内极小极大逼近误差与区间外裁剪误差之间的权衡。然后，通过实现误差分解及其伴随的上界，我们将这一理想化目标与HE可实现的DEP构造联系起来。

## Abstract
Homomorphic encryption (HE) enables privacy-preserving inference under arithmetic constraints that restrict encrypted evaluation to additions and multiplications. As a result, non-polynomial activation functions must be replaced by polynomial approximations. Among polynomial approximation methods, minimax approximation, typically computed by the Remez algorithm, is a standard approach because it minimizes the maximum approximation error over a given design interval. For minimax polynomial design, the approximation interval is a critical hyperparameter: a wider interval improves robustness to large-magnitude inputs while increasing the minimax approximation error under a fixed degree budget. In this paper, we formulate this trade-off as a distribution-aware interval optimization problem, where the approximation interval is chosen to minimize the mean-squared error (MSE) with respect to the pre-activation distribution of interest. To effectively control outside-interval inputs, we combine minimax polynomials with domain extension functions (DEFs) and their HE-realizable polynomial counterparts, domain extension polynomials (DEPs), which approximate a clipping operation outside the design interval and thereby suppress uncontrolled polynomial extrapolation. We first derive an analytically tractable DEF-based proxy objective that captures the trade-off between within-interval minimax approximation error and outside-interval clipping error. We then connect this idealized objective to HE-realizable DEP constructions through an implementation-error decomposition with an accompanying upper bound.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：同态加密（HE）支持对加密数据进行加法和乘法运算，可用于隐私保护的机器学习推理。但神经网络中的非多项式激活函数（如ReLU、sigmoid、tanh、GELU）无法直接计算，必须用多项式近似替换。
- **核心问题**：极小极大（minimax）多项式逼近是常用方法，它能保证在设计区间内的最坏情况误差最小。然而，设计区间的选择存在关键权衡：区间过小会使得输入落在区间外时多项式产生不可控的极大外推误差；区间过大会增加给定度数下的极小极大误差，降低区间内的逼近精度。现有方法缺乏系统地根据预激活分布来优化区间的框架。此外，即使采用域扩展函数（DEF/ DEP）控制区间外行为，设计区间本身仍需优化。
- **研究意义**：本文首次系统地提出了分布感知的区间优化问题，通过最小化均方误差（MSE）来平衡区间内误差与区间外裁剪误差，为HE下的多项式逼近设计提供了理论指导和可计算的目标。

### 2. 论文提出的方法论

- **核心思想**：将逼近区间 \([−t, t]\) 视为可优化超参数，目标是最小化MSE：
  \[
  J_{\text{DEF}}(t) = \mathbb{E}[(f(X) - p_{n,t}(D_t(X)))^2]
  \]
  其中 \(p_{n,t}\) 是区间上的度数为 \(n\) 的极小极大多项式，\(D_t\) 是硬裁剪域扩展函数（DEF），将区间外输入映射到端点。
- **关键技术细节**：
  - **理想DEF代理目标 \(\widehat{J}_{\text{DEF}}(t)\)**：将MSE分解为区间内项和区间外项。对于区间内，利用极小极大残差的等振荡特性，假设残差服从反正弦分布，得到期望二阶矩为 \(E_n(t)^2/2\)（乘以区间内概率）。对于区间外，将多项式端点值替换为目标函数端点值，得到裁剪误差的代理项。最终代理目标为：
    \[
    \widehat{J}_{\text{DEF}}(t) = \frac{E_n(t)^2}{2}\Pr(|X|\le t) + \widehat{J}_{\text{DEF,out}}(t)
    \]
  - **齐次函数简化**：对ReLU、LeakyReLU等正齐次函数，有 \(E_n(t)=tE_n(1)\)，可解析求解最优归一化半径 \(\alpha = t/\sigma\)（高斯分布下），给出严格凸性证明和唯一全局解的一阶条件。
  - **非齐次函数处理**：对sigmoid、tanh、GELU，需采样多个 \(t\) 计算极小极大误差，并用15次多项式拟合得到 \(E_n(t)\) 的代理曲线。
  - **HE可实现DEP实现**：使用域扩展多项式（DEP）组合代替非多项式的硬裁剪DEF。推导了DEP实现误差 \(J_{\text{impl}}(t)\) 的上界，通过Minkowski不等式得到实际MSE的上界。
  - **DEP构造**：采用三次DEP基函数 \(B_{\text{cubic}}(x) = x - \frac{4}{27}x^3\)，并通过缩放和组合（\(m_L(t)\) 级）覆盖保守区间 \([-R_0,R_0]\)。实验中选择了两种扩展因子 \(L=1.6\) 和 \(L=2.5\)。

### 3. 实验设计

- **输入分布**：高斯分布 \(N(0,1)\) 和拉普拉斯分布 \(\text{Laplace}(0,1/\sqrt{2})\)（单位方差）。
- **激活函数**：ReLU、LeakyReLU（斜率0.01）、sigmoid、tanh、GELU。
- **多项式度数**：所有实验使用度数为15的极小极大多项式（\(n=15\)）。
- **基准方法**：固定区间Remez基线，区间直接设为保守区间 \([-R_0,R_0]=[-20\sigma,20\sigma]\)，度数与最终极小极大多项式相同，但不包含DEP阶段。
- **对比方法**：
  - 本文提出的代理目标 \(\widehat{J}_{\text{DEF}}(t)\)（解析或数值代理）
  - 数值理想DEF目标 \(J_{\text{DEF}}(t)\)（直接数值积分）
  - 实际DEP目标 \(J_{\text{DEP},L_1}(t)\) 和 \(J_{\text{DEP},L_2}(t)\)（分别对应扩展因子1.6和2.5）
- **覆盖率选择**：参考CIFAR-10下ResNet-20约1.88×10⁹次激活评估，设置尾概率预算η=10⁻³，得到 \(R_0/\sigma \approx 20\)，即 \(R_0=20\sigma\)。
- **未提及具体数据集**：实验仅基于预设分布（高斯、拉普拉斯）的合成预激活，未使用真实数据集或端到端神经网络推理。

### 4. 资源与算力

- **文中未明确说明**：论文未提及使用的GPU型号、数量、训练时长等算力信息。所有数值实验使用Chebfun库计算极小极大多项式，计算量较小，不涉及大规模深度学习训练。

### 5. 实验数量与充分性

- **实验组数**：共测试5种激活函数 × 2种输入分布 = 10组主要配置。每组实验给出了最优半径 \(t^*\) 和对应的MSE值（表I和表II）。此外，还绘制了MSE随t变化的曲线图（图3-5）。未进行消融实验（如改变多项式度数、DEP参数等），但对比了两种扩展因子。
- **充分性评价**：实验覆盖了常见的激活函数类型（齐次和非齐次）和两种典型分布（高斯、拉普拉斯）。代理目标与理想DEF目标一致性良好，验证了理论推导。但缺乏在真实神经网络数据集上的验证，也未考虑层间累积误差。实验设计客观，基准对比公平（相同度数，区间不同），但DEP与固定区间基线的HE成本不完全匹配（DEP增加了额外乘法深度和级数）。

### 6. 论文的主要结论与发现

- **代理目标有效性**：解析/数值代理目标 \(\widehat{J}_{\text{DEF}}\) 的最优半径与理想DEF目标 \(J_{\text{DEF}}\) 的最优半径高度一致，验证了反正弦残差模型和端点替代近似的合理性。
- **区间优化效果**：优化后的DEP实现（\(J_{\text{DEP},L_2}\)）相比固定区间基线，MSE降低显著，对sigmoid、tanh、GELU降低数个数量级。
- **扩展因子选择**：在测试设置下，较大扩展因子 \(L=2.5\)（需要更少DEP级数）比 \(L=1.6\) 实现了更低的实际MSE，表明累计组合误差大于单级近似误差。
- **理论贡献**：为DEP实现误差导出了上界，连接了理想DEF目标与HE可实现的DEP目标。

### 7. 优点

- **理论创新**：首次将区间选择和预激活分布联系起来，构建了可解析分析的优化框架，对齐次函数给出了闭式最优条件。
- **可计算性**：代理目标形式简单，便于高效求解最优区间。
- **实用性**：与DEP结合，提供了HE兼容的实现路径；误差上界指导参数选择。
- **实验验证**：在多种激活函数和分布上验证了代理目标与理想目标的一致性，对比了两种DEP扩展因子。

### 8. 不足与局限

- **实验覆盖有限**：仅使用合成分布（高斯、拉普拉斯），未在真实神经网络部署中验证（如CIFAR-10推理）。未考虑层间依赖及累积误差。
- **模型假设简化**：残差分布采用反正弦模型（基于等振荡特征），但实际中残差分布可能偏离该模型，尤其当多项式非最优时。端点替代假设同样引入近似误差。
- **HE成本匹配问题**：固定区间基线未包含DEP阶段，其HE成本（乘法深度和级数）低于DEP实现，直接MSE对比不完全公平。
- **资源评估缺失**：未报道实际运行时间和计算开销，也无法评估在具体HE库中的可行性。
- **参数选择有限**：仅测试了度数为15和两种扩展因子，未进行消融实验（如不同度数、不同θ的预激活分布参数）。
- **理论推广性**：分析假设预激活服从高斯分布，最优条件推导依赖该假设，对其他分布需重新推导。

（完）
