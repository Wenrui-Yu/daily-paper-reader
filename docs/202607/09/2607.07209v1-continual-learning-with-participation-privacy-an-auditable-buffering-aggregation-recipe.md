---
title: "Continual Learning With Participation Privacy: An Auditable Buffering-Aggregation Recipe"
title_zh: 持续学习中的参与隐私：一种可审计的缓冲-聚合方案
authors: "T-H. Hubert Chan, Elaine Shi, Mengshi Zhao, Mingxun Zhou"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07209v1"
tags: ["query:daily"]
score: 9.0
evidence: 持续学习中的参与隐私，可审计的缓冲聚合方案
tldr: "针对参与隐私下的单编辑用户流（一个插入/删除导致后续更新偏移），标准汉明邻居连续释放分析失效。本文提出随机缓冲包装器，将流数据分桶为[U,2U]大小，转化为每桶汉明风格更新流，并给出延迟/积压保证；同时证明非自适应汉明邻居DP证明可提升至自适应输入的条件。结合标准树前缀和等原始，实现轨迹级(ε,δ)-DP，通过U显式关联隐私与延迟。该模块化配方为连续学习提供可审计隐私保护。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07209v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1706, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07209v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 521, \"height\": 452, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07209v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1144, \"height\": 576, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07209v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 957, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07209v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 536, \"height\": 273, \"label\": \"Table\"}]"
motivation: 单编辑用户流中一个插入/删除会偏移所有后续更新，破坏标准汉明邻居DP分析，亟需新的隐私方案。
method: "设计随机缓冲包装器将流分桶为[U,2U]大小，转化为汉明邻居流；并证明非自适应DP证明在每轮新鲜随机性与稳定单轮隐私条件下可自适应。"
result: "使用树前缀和等原始实现轨迹级(ε,δ)-DP，隐私与延迟通过桶大小U显式关联。"
conclusion: 提供可审计的模块化配方，有效解决参与隐私下连续学习的隐私保障问题。
---

## 摘要
现代联邦学习和流式学习系统通常释放中间模型，因此在自适应交互下，整个轨迹必须满足隐私保护。受参与隐私的启发，我们研究单编辑相邻用户流，其中一次插入/删除会改变所有后续更新，并破坏了标准的汉明邻居连续发布分析。我们给出一个可审计的模块化方案。一个随机缓冲包装器产生大小为 $[U,2U]$ 的桶，将单编辑流简化为具有显式积压/延迟保证的每桶汉明风格更新流，其中 $U$ 由隐私参数 $(\varepsilon,δ)$ 校准。然后我们证明一个认证定理，识别何时连续原语的自适应输入的非自适应汉明邻居DP证明成立：该原语必须使用新鲜的每轮随机性，并在共同的自适应上下文中具有稳定的单轮隐私配置文件。这些要素共同为标准原语（例如树前缀和）产生单编辑流的轨迹级别 $(\varepsilon,δ)$-DP，并通过 $U$ 建立显式的隐私-延迟联系。

## Abstract
Modern federated and streaming learning systems often release intermediate models, so privacy must hold for the full trajectory under adaptive interaction. Motivated by participation privacy, we study single-edit neighboring user streams, where one insertion/deletion shifts all subsequent updates and defeats standard Hamming-neighbor continual-release analyses. We give an auditable modular recipe. A randomized buffering wrapper emits bins of size $[U,2U]$, reducing single-edit streams to a Hamming-style per-bin update stream with explicit backlog/delay guarantees, where $U$ is calibrated by the privacy parameters $(\varepsilon,δ)$. We then prove a certification theorem identifying when a non-adaptive Hamming-neighbor DP proof for a continual primitive lifts to adaptive inputs: the primitive must use fresh per-round randomness and have a stable one-round privacy profile under common adaptive context. Together, these ingredients yield trajectory-level $(\varepsilon,δ)$-DP for single-edit streams using standard primitives (e.g., tree prefix sums), with an explicit privacy--latency link via $U$.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现代联邦学习和流式学习系统通常会发布中间模型，攻击者可以观察整个模型轨迹，而不仅仅是最终输出。这带来了对**参与隐私**的需求——用户希望对自己是否参与系统享有可否认性。然而，标准的连续差分隐私分析基于汉明相邻流（仅改变单个时间步的值），但在参与隐私场景下，用户事件的插入或删除会导致后续所有时间步的偏移（单编辑相邻性），破坏了汉明相邻分析的基础。此外，学习过程具有交互性：当前发布的模型会影响未来用户事件，因此隐私须在自适应交互下成立。
- **整体含义**：解决这一问题的关键在于设计一种模块化的、可审计的方案，将单编辑相邻流转换为下游连续DP原语能处理的汉明相邻流，同时保证隐私在反馈回路下依然有效。

## 2. 方法论

- **核心思想**：采用**随机缓冲包装器** + **认证的连续DP原语** + **模块化组合**。
- **关键技术细节**：
  - **随机缓冲包装器 (RandBin)**：将输入的用户事件流（如损失、梯度）按随机时间点分桶，每个桶的大小控制在 $[U, 2U]$ 之间，$U = \Theta(\frac{1}{\varepsilon_b}\log\frac{1}{\delta_b})$ 由隐私预算决定。当桶被发射时，输出一个具体桶（内含事件），否则输出 $\perp$。该包装器满足三个接口属性：桶大小受控、积压有界、单编辑相邻流映射到汉明相邻的桶更新流。
  - **认证定理**：识别了连续原语从非自适应汉明邻居DP提升到自适应交互的条件——原语必须在每轮使用独立新鲜随机性，且其单轮隐私配置文件在共同自适应上下文中保持稳定（称为“定量可分解性”）。通过该认证，标准树前缀和、下三角矩阵机制等原语可在自适应输入下保持原有DP保证。
  - **模块化组合**：将上游的NPDP（邻居保持差分隐私）包装器与下游认证的连续DP原语通过模块组合定理（Theorem C.1）结合，得到端到端的轨迹级 $(\varepsilon,\delta)$-DP，其中 $\varepsilon = \varepsilon_b + \varepsilon_a$, $\delta = \delta_b + \delta_a$。
- **算法流程**（文字说明）：
  1. 接收事件流 $f_1, f_2, \dots$。
  2. 运行RandBin（参数 $U$ 由 $(\varepsilon_b,\delta_b)$ 定），输出 $B_t$（桶或 $\perp$）。
  3. 当 $B_t \neq \perp$ 时，对桶内梯度求平均并裁剪，形成每桶更新 $u_k$。
  4. 将 $u_k$ 输入认证的连续DP原语（如树前缀和），得到私有前缀和 $S_k$。
  5. 更新模型 $w^{(t)} = w^{(0)} - S_k$，并在非发射时间步保持模型不变；未来事件可依赖已发布模型（反馈）。

## 3. 实验设计

- 本文**未在正文中给出具体实验**。作者明确提到：“The relevant experiments are given in [CSZZ26]”，即实验部分放在另一篇引用论文中。因此，本论文主要贡献是理论框架、认证定理和模块化组合。
- 从方法论描述看，实验应涉及标准流式SGD任务，可能使用图像/文本数据集（如CIFAR-10、MNIST等），对比基线包括无隐私SGD、标准树机制直接用在单编辑流、固定批处理等方法。但具体细节需参考[CSZZ26]。

## 4. 资源与算力

- 本文**未提及任何具体的算力资源**（GPU型号、数量、训练时长等）。实验部分在其他论文中，因此本工作总结无法给出。

## 5. 实验数量与充分性

- 由于本文没有提供实验，无法评估实验的充分性与客观性。理论部分贡献明确，但应用效果需依赖后续实验验证。作者在结论中称“实验见[CSZZ26]”，暗示该工作已完成实验，但本文作为理论版本未包含。

## 6. 主要结论与发现

- **端到端隐私保证**：通过随机缓冲和模块组合，可以实现对单编辑相邻用户流在使用标准连续DP原语（如树前缀和）下的轨迹级 $(\varepsilon,\delta)$-DP，且延迟（积压）可由隐私预算显式控制。
- **认证定理**：对于满足新鲜随机性和稳定单轮隐私配置文件的机制，非自适应汉明邻居DP证明可直接提升到自适应交互场景，无需重写隐私分析。
- **隐私-延迟显式关联**：桶大小 $U=\Theta(\frac{1}{\varepsilon_b}\log\frac{1}{\delta_b})$ 直接决定了缓冲引起的延迟阶数 $O(\frac{1}{\varepsilon_b}\log t·(\log t+\log\frac{1}{\delta_b}))$。

## 7. 优点

- **模块化与可审计性**：将缓冲和聚合分离，每个组件都有明确的接口（RandBin的NPDP合同和原语的认证条件），便于独立验证和重用。
- **理论严谨性**：使用幂函数框架给出精确的隐私组成，认证定理给出了充分条件，并证明了新鲜随机性单独不足以实现自适应安全性（给出了反例）。
- **覆盖实际关切**：同时考虑了参与隐私（单编辑相邻）、自适应反馈和时序可见性（攻击者观察发射时间），较全面。
- 适用于多种连续DP原语（树、矩阵机制），灵活性高。

## 8. 不足与局限

- **缺乏实验验证**：本文未展示任何数据集上的精度、延迟与隐私预算的权衡曲线，无法直接判断实际效用。
- **延迟开销可能较大**：积压界具有 $\log t$ 因子，在长流上可能累计显著延迟。
- **假设较强**：认证定理要求每轮新鲜随机性且单轮隐私配置稳定，排除了一些常见优化（如复用噪声）。此外，缓冲器要求事件可识别（如唯一ID），否则需要额外假设。
- **边缘情况**：匿名流、重复值需要额外处理；纯DP（$\delta=0$）下无法获得有界延迟保证。
- **应用限制**：仅针对参与隐私（单编辑相邻），不直接覆盖其他相邻定义（如内容隐私）。需要用户事件具有固定敏感度（如梯度裁剪），且聚合器只处理标量或向量更新，不支持复杂状态（动量、自适应学习率）的自适应安全证明。

（完）
