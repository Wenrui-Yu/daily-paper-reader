---
title: "MOSAIC-FL, a micro-service based privacy-preserving framework with application to genomics"
title_zh: MOSAIC-FL：一个基于微服务的隐私保护框架及其在基因组学中的应用
authors: "Paul Largillier, Karl Paygambar, Cédric Gouy-Pailler, Vincent Meyer, Mallek Mziou, Oana Stan"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.25107v1"
tags: ["query:daily"]
score: 10.0
evidence: 基于微服务的隐私保护联邦学习框架
tldr: 联邦学习在医疗和基因组学中面临安全与隐私挑战。提出MOSAIC-FL框架，采用微服务架构、gRPC通信和有限状态机实现稳健同步与威胁检测。通过阈值CKKS同态加密实现容错安全聚合，利用噪声泛滥确保安全性并轮换密钥抵御攻击。在EMNIST图像识别和TCGA乳腺癌分型任务中验证了效果，分析了不同阈值和模型规模下的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25107v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 736, \"height\": 453, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25107v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 715, \"height\": 866, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25107v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 536, \"height\": 800, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25107v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 783, \"height\": 433, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25107v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1655, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25107v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1647, \"height\": 265, \"label\": \"Table\"}]"
motivation: 联邦学习在敏感数据（如医疗、基因组学）应用中需平衡模型性能与隐私保护，现有框架缺乏模块化、灵活性和对特定攻击（如密钥恢复）的防御。
method: 提出MOSAIC-FL微服务框架，集成gRPC通信和有限状态机，采用阈值CKKS同态加密实现t-out-of-N客户端容错安全聚合，通过噪声淹没和每轮密钥更新增强安全性。
result: 在EMNIST和TCGA乳腺癌分型任务中验证了框架有效性，分析了不同阈值和模型规模对系统性能的影响，证明了低通信开销和高安全性。
conclusion: MOSAIC-FL为联邦学习提供了模块化、隐私保护且抗攻击的解决方案，尤其适用于基因组学等敏感领域，未来可扩展至更多应用场景。
---

## 摘要
安全性和隐私性是联邦学习（FL）的基本要求，特别是在医疗和基因组学等需要分析敏感信息的领域。我们的联邦学习框架旨在解决这些挑战，同时提出了一种模块化、灵活且基于微服务的架构。更具体地说，它集成了高效的gRPC通信层和有限状态机，以确保稳健的组件同步和威胁检测，同时依赖于一种基于CKKS同态密码系统的阈值变体的容错安全聚合协议。这使得编排服务器能够进行盲模型聚合，需要至少t-out-of-N个活跃客户端进行解密，同时通过密码学和网络协议最小化通信开销。我们通过噪声泛滥确保IND-CPA-D安全，并通过每轮更新集体密钥材料来缓解最近针对同步解密器的密钥恢复攻击。我们通过多种用例展示了该框架的有效性，从标准图像识别（EMNIST）到复杂的基因组分类，包括在TCGA上的乳腺癌亚型分类，并在不同阈值和模型规模下评估了系统性能。

## Abstract
Security and privacy are primordial requirements for Federated Learning (FL), especially in fields such as healthcare and genomics where sensitive information has to be analyzed. Our FL framework is designed to address these challenges while proposing a modular, flexible and micro-service architecture. More precisely, it integrates an efficient gRPC communication layer and a Finite State Machine to ensure robust component synchronization and threat detection, while relying on a fault-tolerant secure aggregation protocol using a Threshold variant of the CKKS homomorphic cryptosystem. This allows blind model aggregation by an orchestration server, requiring a minimum of $t$-out-of-$N$ active clients for decryption while minimizing communication overhead thanks to both cryptographic and network protocols. We ensure IND-CPA-D security through noise flooding and mitigate the recent key-recovery attack on synchronized decryptors by renewing the collective key material at every round. We demonstrate the framework's effectiveness through diverse use cases, ranging from standard image recognition (EMNIST) to complex genomic classification including breast cancer subtyping on TCGA, evaluating system performance across different threshold values and model scales.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

联邦学习（FL）在医疗、基因组学等敏感数据领域面临着严重的安全与隐私威胁（如模型反演、成员推断等）。现有 FL 框架在模块化、可扩展性、语言无关性以及对强安全协议（如阈值全同态加密 ThHE）的原生支持方面存在不足，且大多依赖单一可信聚合器，存在单点故障风险。此外，针对同步解密器的最新密钥恢复攻击也给 FHE 部署带来了新的挑战。为此，本文提出 MOSAIC‑FL，一个基于微服务架构的隐私保护联邦学习框架，旨在同时满足安全性、模型多样性、部署异构性和稳健监控等实际需求。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：采用**微服务架构**实现关注点分离——每个节点包含 Orchestrator（编排器）、ML Engine（机器学习引擎）和 Crypto Provider（密码提供者）三个独立容器，通过 gRPC 和 Protocol Buffers 进行高性能、语言无关的通信。
- **关键技术细节**：
  - 使用**有限状态机（FSM）** 同步各组件生命周期，检测故障和恶意行为。
  - 基于 **Mouchet 等人（2023）的 RLWE 构造实现 t-out-of-N 阈值 CKKS 同态加密方案**，允许 N 个客户端联合生成全局公钥，并由至少 t 个客户端协作解密聚合后的密文，而服务器在密文域上盲聚合。
  - **安全性增强**：通过**噪声淹没（noise flooding）** 确保 IND‑CPA‑D 安全；针对跨轮收集部分解密份额的密钥恢复攻击，每轮重新执行 Setup、DistKeyGen 和 ShareReshare，更新集体密钥材料（开销可忽略）。
- **流程简述**：
  1. **密码学设置**：各客户端分布式运行 Setup、DistKeyGen、ShareReshare，生成集体公钥和 Shamir 共享的私有份额。
  2. **每轮训练**：服务器选择 K 个活跃客户端；每个客户端本地训练模型，用公钥加密本地权重后发送给服务器；服务器在密文上计算加权平均（FedAvg）；然后广播聚合密文。
  3. **协作解密**：至少 t 个客户端计算部分解密份额，交换后任意节点可组合恢复明文模型。解密完成后，服务器开始下一轮注册，实现流水线。

### 3. 实验设计：数据集、场景、基准和对比方法

- **数据集与场景**：
  - **EMNIST**：80 万张 28×28 灰度手写字母/数字图像（62 类），用于图像分类，使用 CNN（42.8 万参数）。
  - **TCGA 乳腺癌基因表达数据**：用于 BRCA 亚型分类（PAM50 亚型），使用 Transformer 模型（67.9 万参数），基因表达表经缩放和随机过采样处理，模拟非 IID 分布。
- **基准**：以无加密的明文 FL（Baseline）作为基准，对比启用阈值 CKKS 加密（ThHE）的模式。
- **对比方法**：未与其他 FL 框架直接对比，仅在表 1 中与 Flower、FATE、PySyft 进行了定性比较（架构、密码支持、扩展性等）。

### 4. 资源与算力

文中明确说明：
- **硬件**：双路 AMD EPYC 7302 CPU（共 64 核 128 线程），125GB RAM，以及 **NVIDIA A100 GPU（40GB VRAM）**。
- **软件**：Docker 容器部署每个节点，使用 TensorFlow v2.19、Lattigo 密码库（CKKS 参数：多项式阶 N=2¹⁴，缩放因子 Δ=2⁴⁵，PQ 总位宽 438 bits，128-bit 安全等级）。
- **训练时长**：未给出总训练时间，但提供了每轮时间分解（表 2），例如 CNN‑ThHE 每轮约 17.30 秒，Transformer‑ThHE 约 167 秒。

### 5. 实验数量与充分性

- **实验数量**：主要包含两组实验（EMNIST CNN 和 TCGA Transformer），每组都进行了 Baseline 和 ThHE 两种模式对比，并在不同客户端数 N（如 4、8、16）和阈值 t 下测量了通信开销（图 3）和每轮时间分解（表 2）。还给出了 100 轮的准确率变化曲线（图 4）。
- **充分性**：实验覆盖了图像和基因组两种数据模态，验证了不同模型规模下的加密开销比例。但缺少与现有 FL 框架（如 Flower 或 PySyft）的横向性能对比，也缺少对大客户端数（如 100 以上）或不同网络拓扑的测试。消融实验方面，仅比较了有无 ThHE，未隔离其他模块（如 FSM 或 gRPC 优化）的贡献。总体而言，实验较为充分但仍有扩展空间。

### 6. 论文的主要结论与发现

1. MOSAIC‑FL 的微服务架构提供了高模块化、语言无关性和易于集成新安全/ML 模块的能力，优于现有主流框架。
2. 采用的 t-out-of-N 阈值 CKKS 方案实现了容错安全聚合：服务器无法解密，客户端协作解密需要至少 t 个活跃节点。
3. 在 EMNIST 和 TCGA 任务上，**加密 FL 的准确率与明文 FL 完全一致**，证明 CKKS 噪声被适当管理，没有精度损失。
4. **通信和计算开销可控**：对于简单 CNN，ThHE 模式每轮时间增加约 64%（10.56s 到 17.30s）；对于计算密集的 Transformer，加密开销仅占 5%（159s 到 167s），几乎可忽略。
5. 通过每轮密钥更新可以缓解同步解密器下的密钥恢复攻击，且开销很小。

### 7. 优点：方法或实验设计上的亮点

- **微服务+状态机**：实现了严格的关注点分离和容器级隔离，增强了安全性和可维护性；FSM 使得同步和威胁检测易于形式化验证和扩展。
- **阈值 FHE 的实用化**：基于成熟的 Lattigo 库，通过加噪声淹没和密钥轮换实现了强安全（IND‑CPA‑D）且防御了新型攻击。
- **基因组学真实应用**：在 TCGA 乳腺癌亚型任务上验证，展示了框架在现实场景中的可行性，填补了多密钥 FHE 用于组学 FL 的空白。
- **性能分解详细**：表 2 清晰列出了训练、评估、加密、解密、聚合、同步等各个环节的时间，有助于识别瓶颈。
- **开源友好**：选用 gRPC/Protobuf 和容器化，降低了多语言集成门槛。

### 8. 不足与局限

- **安全模型**：假设服务器为诚实但好奇、客户端半诚实且不共谋，未考虑恶意攻击者（如投毒或拒绝协作），也没有集成差分隐私等其他防御。
- **实验覆盖**：仅测试了两个数据集和两种模型，缺少更复杂模型（如大语言模型）或更大规模客户端数（>16）的实验；未与其他 FL 框架（Flower、PySyft）进行直接性能对比。
- **通信开销**：当客户端数 N=16、t=9 时，每轮通信时间达 14.2 秒，主要是 P2P 解密份额交换；在大规模场景下可能成为瓶颈。
- **计算开销**：当前方案需每轮重新执行密钥生成和重共享，虽然声称开销小，但未给出具体数值；对于训练轮数极多的场景，密钥管理成本可能累积。
- **实验公平性**：未提及随机种子、多次重复实验的统计变异（图 4 图中显示了标准差但未说明重复次数），且所有实验在同一硬件上运行，未评估异构客户端环境。

（完）
