---
title: "Not Your Usual Type(s): Data contracts as types across languages and engines"
title_zh: 并非你惯用的类型：跨语言和引擎的数据合同作为类型
authors: "Aldrin Montana, Colin Marc, Luca Bigon, Jacopo Tagliabue"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13339v1"
tags: ["query:daily"]
score: 9.0
evidence: 数据契约作为可组合多语言湖仓的类型
tldr: "当前组合数据系统在管道节点边界处规范薄弱，模式检查滞后且跨语言不一致。基于Bauplan运营百万级作业的经验，提出将数据合约作为类型的SDK设计，通过schema对象编码类型、约束、文档和血缘，在不同执行阶段解释这些注解。该方法有效解决了常见生产故障，支持跨语言和引擎的数据流确定性及非确定性推理。贡献在于以\"一切即代码\"哲学实现了多语言湖仓一体中数据合约的类型化治理，提升了系统可组合性与用户一致性体验。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13339v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13339v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 836, \"height\": 353, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13339v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 822, \"height\": 630, \"label\": \"Figure\"}]"
motivation: 组合数据系统节点边界规范薄弱，模式检查滞后且跨语言不一致。
method: 通过schema对象注释表，编码类型、约束、文档和血缘，并在执行生命周期中解释。
result: 解决了常见生产故障，支持跨语言和引擎的确定性及非确定性推理。
conclusion: "\"一切即代码\"哲学实现了多语言湖仓一体中数据合约的类型化治理。"
---

## 摘要
可组合数据系统有望让开发人员在不牺牲一致用户体验的情况下组合语言、引擎和目录。然而，在实践中，管道节点边界仍然定义薄弱：转换通过通常延迟检查、跨语言执行不一致且与业务用户关心的语义脱节的模式来交换表。基于在Bauplan运营数百万作业超过一年的经验，我们分享了新SDK背后的设计原则，该SDK将数据合同视为可组合、多语言湖仓的类型。用户（无论是人类还是代理）使用编码列类型、约束、文档和数据沿袭的模式对象来注释输入和输出表；然后，Bauplan在执行生命周期的不同点解释这些注释。我们展示了这种设计如何解决常见的生产故障，以及“一切皆代码”理念如何实现对跨语言和引擎的数据流进行确定性和非确定性推理。

## Abstract
Composable data systems promise to let developers combine languages, engines, and catalogs without sacrificing a coherent user experience. In practice, however, pipeline-node boundaries remain weakly specified: transformations exchange tables through schemas that are often checked late, enforced unevenly across languages, and disconnected from the semantics business users care about. Based on over a year of operating millions of jobs in Bauplan, we share the design principles behind our new SDK, which treats data contracts as types for a composable, multi-language lakehouse. Users, whether humans or agents, annotate input and output tables with schema objects that encode column types, constraints, documentation, and lineage; Bauplan then interprets these annotations at different points in the execution lifecycle. We show how this design addresses common production failures, and how an ''everything-as-code'' philosophy enables both deterministic and non-deterministic reasoning over data flows across languages and engines.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **背景**：可组合数据系统（如湖仓一体）允许开发者跨语言、引擎和目录构建数据管道，但实践中管道节点间的边界规范薄弱。表模式（schema）检查延迟、跨语言执行不一致、且与业务语义脱节，导致大量生产故障（如列丢失、类型变更、空值未处理、语义偏移）。  
- **动机**：基于 Bauplan 平台运营超过一年的数百万生产作业经验，作者发现大多数管道错误源于节点间的模式不匹配（“可避免的运行时错误”）。随着 AI Agent 参与数据工程，这种问题加剧。论文提出将数据合同（data contracts）作为类型（types）来设计 SDK，使模式、约束、文档和血缘成为可跨语言、跨引擎、跨生命周期阶段的便携契约。  
- **整体含义**：推动数据工程向软件工程式的“尽早失败”与“正确性保证”演进，实现多语言湖仓一体中数据合同的类型化治理。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：借鉴动态语言（如 Python）的类型注解思想，为数据管道的输入输出表定义轻量级 schema 对象，编码列类型、约束、文档和血缘。Bauplan 在执行生命周期的三个阶段解释这些注解，实现逐步验证和尽早失败。  
- **关键技术细节**：
  - **Schema 对象语法**：基于 Python 的 `typing.Annotated` 和 Pydantic 风格。定义 `TableSchema` 子类，每个字段使用 `Annotated[type, constraint, Doc(...)]` 声明。例如：`pid: Annotated[Int64, Required, Doc('Unique passenger ID')]`。支持列级约束（如 `Required`、`Enum`）和文档。
  - **血缘声明**：直接引用上游 schema 的字段（如 `WomenByClass['pid']`）实现显式列级血缘。
  - **转换注解**：
    - Python 节点：函数参数使用 `Annotated[Table[Schema], Model(...)]` 指定输入表及 schema，返回值使用 `-> Table[Schema]`。
    - SQL 节点：通过结构化注释（`-- bauplan: output_schema=WomenByClass`）关联。
  - **三阶段 Enforcement**：
    1. **Step 1（本地代码环境）**：本地类型检查器（如 mypy）直接在代码层面发现明显不匹配（仅 Python 节点）。
    2. **Step 2（控制平面计划）**：解析 DAG 元数据，验证节点间的合同组合性（如子节点投影是否匹配父节点输出），并关联目录中源表信息，进行确定性类型检查。
    3. **Step 3（运行时 Worker）**：在数据持久化前后，对物理 Arrow 表执行输入/输出 schema 验证（包括 null 约束、类型转换等）。失败时抛出明确错误并中止，防止脏数据写入存储。

#### 3. 实验设计
- **数据集 / 场景**：论文未设计独立的实验 benchmark。它基于 Bauplan 生产环境中的真实故障模式（列丢失、null 值、类型变化、语义偏移）作为驱动，并使用 Kaggle 提供的 **Titanic 数据集**作为示例贯穿全文，展示合同如何解决这些故障。
- **对比方法**：未进行定量对比。仅在相关工作部分提及现有工具（Dagster、dbt、Pandera、Patito、Great Expectations）的局限性，并指出本文方法的不同（如跨语言管线、三阶段验证、与 Iceberg/Arrow 原生集成等），但没有实验对比。
- **结论依据**：主要依靠逻辑论证和代码示例（如 Listing 8 的错误消息），以及作者在 Bauplan 平台运营百万级作业的实践经验。

#### 4. 资源与算力
- **未提及**：论文不涉及机器学习训练或大规模性能测试，没有说明 GPU 型号、数量、训练时长等算力资源。因此该部分在本文中不适用。

#### 5. 实验数量与充分性
- **实验数量**：无传统对照实验。论文仅通过一个线性三元 DAG（f → g → h）的多次变异（列丢失、null、类型变化、语义偏移）来定性说明设计有效性。
- **充分性评估**：
  - **主观性强**：论证基于作者在 Bauplan 的实际经验，缺乏第三方复现或公开 benchmark。
  - **覆盖范围有限**：仅展示了 Titanic 示例，未涉及更复杂拓扑（如分支、合并、动态表命名等）或大规模/性能场景。
  - **公平性欠缺**：未与现有工具进行系统化定量比较（如错误检测延迟、误报率、运行时开销等）。

#### 6. 主要结论与发现
- 通过在三阶段执行生命周期中逐步验证数据合同，能够将常见管道节点边界错误（列丢失、类型不匹配、null 值未处理、语义模糊）从运行时延迟到本地或计划阶段捕获，减少生产故障。
- “一切即代码”的设计（将 schema、约束、文档、血缘与转换代码共存）使得数据语义可持久化到 Iceberg 表元数据，供下游 Agent 和系统读取，实现跨工具、跨引擎的语义复用。
- 合同基于 Arrow 和 Iceberg 标准类型，保证了引擎间的可组合性（替换运行时仍可执行相同验证）。
- 未来方向：开发 Bauplan Language Server Protocol (LSP) 将更多验证移到 IDE 阶段，实现更快的反馈循环。

#### 7. 优点
- **方法创新**：将传统动态语言的类型注解思路迁移到数据管道合同，语义直观且与现有 Python 生态（Pydantic, mypy）兼容。
- **三阶段验证机制**：实现了“尽早失败（shift left）”原则，覆盖代码编辑、CI/CD 和运行时，每个阶段职责清晰。
- **声明式血缘与文档**：通过 schema 字段引用即可显式记录列来源，无需额外 ETL 或代码注解；文档随代码版本化并持久化，便于 Agent 和人类维护。
- **引擎无关性**：合同基于 Arrow 类型和 Iceberg 元数据，支持异构运行时（如替代专有沙箱）而不丧失合同保证。
- **源自生产实践**：基于数百外生产作业的失败模式归纳，针对性强。

#### 8. 不足与局限
- **缺乏定量实验证据**：未提供任何比较实验（如与 dbt contracts、Pandera、Great Expectations 的假阳性率、检测速度、用户效率等对比），仅依靠定性和案例论证。
- **验证覆盖有限**：当前仅支持 Python 和 SQL 节点，且 SQL 节点只能在 Step 2/3 检查，暂无本地 IDE 检查；不支持其他语言（如 Java、Scala）或自定义 UDF。
- **潜在性能开销**：未评估三阶段验证对系统延迟和资源消耗的影响（特别是 Step 3 的 Arrow 缓冲内检查）。
- **供应商偏见**：作者均为 Bauplan 成员，可能夸大自身设计的优异性；论文未在独立环境中复现。
- **泛化性未验证**：虽然声称设计可移植，但实际实现与 Bauplan 平台深度绑定（如控制平面、FaaS worker），外部系统集成需适配。
- **未讨论复杂场景**：如循环依赖、动态表创建、跨项目合同共享等未涉及。

（完）
