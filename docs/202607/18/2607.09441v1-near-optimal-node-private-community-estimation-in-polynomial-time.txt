Title: Near-optimal node-private community estimation in polynomial-time

URL Source: https://arxiv.org/pdf/2607.09441v1

Published Time: Mon, 13 Jul 2026 01:13:44 GMT

Number of Pages: 40

Markdown Content:
# Near-optimal node-private community estimation in polynomial-time 

Laurentiu Marchis Olga Klopp Univesrity of Cambridge ESSEC Business School 

lam223@cam.ac.uk b00732676@essec.edu 

Po-Ling Loh Ilias Zadik University of Cambridge Yale University 

pll28@cam.ac.uk ilias.zadik@yale.edu 

Abstract 

In this paper, we resolve an open question of Klopp & Zadik (2026) by providing a high-probability polynomial-time, node-private algorithm which nearly matches the performance of their exponential-time node-private algorithm for exact recovery in stochastic block models. Our result involves an explicitly constructed Lipschitz surrogate for the penalized likelihood function, as well as a carefully devised accept-reject algorithm that samples community labels from the corresponding exponential mechanism in polynomial-time. We rigorously analyze the privacy, runtime, and utility of our proposed algorithm, showing that even when the number of communities K grows logarithmically with the number of nodes n, we can achieve the minimax rates for exact recovery with the privacy parameter ε growing as log( n), thus matching known lower bounds on the cost of privacy for this setting. 

# 1 Introduction 

Since its inception 20 years ago [18, 20, 19], differential privacy has drawn a great deal of atten-tion in both theory and practice. In addition to devising new algorithms with minimal privacy leakage [1, 35, 51] and auditable guarantees [16, 28, 42, 47], researchers have been interested in tightly characterizing tradeoffs between privacy, accuracy, and sometimes computational aspects of differentially private algorithms [7, 33, 17, 29, 8, 30]. Despite the large volume of existing theoretical work on privacy, one area that has received relatively less attention is private estimation from graph inputs. Most previous work focuses on privacy with respect to edge perturbations [44, 23, 31, 22, 4], while [32, 4] developed general strategies for turning edge-private algorithms into node-private algorithms (i.e., two graphs are neighbors if one can be transformed to the other by altering connections to a single node). In this paper, we study the problem of community estimation under a random graph model known as the stochastic block model (SBM) [26]. The literature on (non-private) community estimation in stochastic block models is vast; we refer the reader to [2] for an excellent survey. Notably, most existing work on private SBM estimation has focused on edge-private algorithms [24, 41, 11, 10, 43, 36], while the focus in our paper will be on node-private algorithms which are minimax optimal in terms of statistical error. 

1.1 Related work 

Node-private estimation in SBMs was previously studied by [12], who focused on estimating the K ×

K matrix of connection probabilities, rather than recovering communities. Our work is most directly related to [34], who developed the first algorithm for private community estimation. However, the 1arXiv:2607.09441v1 [math.ST] 10 Jul 2026 runtime of their proposed algorithm is exponential in the size of the graph. Recently, [38] developed node-private community estimation algorithms which are computationally feasible; however, they focused on a weaker notion of consistent recovery. Their algorithmic approaches are completely separate from ours, as they are based on private spectral algorithms and smooth projections to the space of bounded-degree graphs, rather than direct sampling from a Gibbs distribution. 

1.2 Contributions 

Our main contribution is to analyze a novel algorithm for node-private community estimation in SBMs, which runs in time polynomial in the number of nodes n and the number of communities 

K. The runtime is with high probability over the input adjacency matrix drawn from the SBM: We provide separate results with a polynomial expected runtime guarantee (Theorem 1), uniformly over the internal randomness of the algorithm, and a worst-case runtime guarantee (Corollary 1), at the cost of a larger privacy parameter. Importantly, the output of our algorithm achieves the minimax risk of the exponential time algorithm in [34]. Our analysis allows for settings where K

grows logarithmically with n. By sharpening the utility analysis of [34], we show that a privacy level of ε = Θ(log( n)) is both necessary and sufficient for the success of our algorithm. Our private algorithm builds upon the exponential mechanism [39], which samples a candidate with probability proportional to the exponential of a suitably scaled utility score. We choose the score so that near-maximizers retain utility guarantees which nearly match [34]. The main chal-lenges are to show that: (1) we can define an efficiently computable score function which has low sensitivity on in-community bounded-degree graphs (implying privacy) and is sufficiently close to the penalized likelihood score (implying utility); and (2) we can sample from the corresponding exponential mechanism in a computationally tractable manner. We analyze these aspects in Sec-tions 3 and 4, respectively, and derive the consequences in terms of high-probability runtime and utility in Section 5. Finally, we demonstrate how a truncated version of the rejection sampler can be used to obtain approximate samples from the desired Gibbs distribution with a polynomial runtime guarantee that is worst- rather than average-case (cf. Section 6 and Appendix B). As we are aware of little work in the privacy literature which uses approximate sampling (cf. Remark 5), we believe this also constitutes an important conceptual contribution which may be of independent interest. A key algorithmic ingredient is to decompose the Gibbs measure into pieces which are easy to sample from: we devise a proposal distribution expressed as a product measure over partitions, with an easy-to-implement rejection step, leveraging a carefully defined semidefinite program (SDP). We note that this technique is employed in many fields of sampling theory; see [21] for a standard scheme for sampling one-dimensional Gibbs measures. In high dimensions, as in the present work, this approach is significantly more challenging. Another related example is how the Hubbard– Stratonovich decomposition [27, 48] has been used to sample from complicated Gibbs measures of high-temperature Ising models, by decomposing the measure as a strongly log-concave mixture of product measures. This idea has been key for a series of striking recent algorithmic advances (see e.g., [14] and references therein). However, we are not aware of any similar decompositions used in differential privacy, and we hope the present paper will inspire further algorithmic work on efficient sampling from utility-optimal exponential mechanisms. As our polynomial-time estimator achieves the minimax exact-recovery error with privacy bud-get matching the lower bound of ε = Ω(log( n)) [34], it is natural to wonder whether an alternative privacy notion could avoid the need for a privacy parameter that grows with n. For example, [5] re-cently studied one-pass gradient descent for private high-dimensional linear regression, and showed that it is possible to obtain non-trivial risk guarantees with a constant zero-concentrated differential privacy (zCDP) budget [5]. Their motivation to study zCDP is because standard gradient clipping 2Input  

> A
> Likelihood surrogate ˜TA,D
> Candidate SDP labeling σ⋆
> Equation (6)
> SDP certification Equation (7)
> Rejection sampler Algorithm 2 or 3
> Brute-force enumeration
> Output ˆσ
> Yes
> No

Figure 1: High-level structure of the proposed node-private estimator (cf. Algorithm 1). would require a diverging privacy budget under the usual privacy paradigm. In Appendix C, we show that the SBM setting is fundamentally different: information-theoretically, any estimator with polynomially small exact-recovery failure must have privacy parameter ρ = Ω(log( n)), even under zCDP. Similar results are derived for approximate differential privacy in Appendix B.2. Thus, the need for a diverging privacy budget is not a result of focusing solely on pure differential privacy. It is an interesting open question whether analogous lower bounds hold under other notions of privacy. 

# 2 Background and problem setup 

We begin by defining the notion of differential privacy for undirected graph inputs which will be used in this paper. We also provide definitions, notation, and assumptions regarding SBMs. Additional notation is contained in Appendix A. 

2.1 Differential privacy 

We begin with the classical notion of ( ε, δ )-differential privacy, which will be denoted by ( ε, δ )-DP: 

Definition 1. A randomized algorithm A satisfies (ε, δ )-DP, for ε, δ ≥ 0, if for all pairs of datasets 

X and X′ differing in one element and for all S in the range of A, we have P(A(X) ∈ S) ≤

eεP(A(X′) ∈ S) + δ. If A satisfies (ε, 0) -DP, we also say it satisfies pure ε-DP. 

Let Gn be the set of undirected graphs on the vertex set [ n], represented by symmetric, zero-diagonal adjacency matrices A ∈ { 0, 1}n×n. Two graphs A, A ′ ∈ G n are node-adjacent , written 

A ∼v A′, if they differ only on edges incident to a single vertex. Let dv(A, A ′) be the shortest-path metric generated by this adjacency relation; equivalently, dv(A, A ′) is the minimum number of vertex neighborhoods that must be rewired to transform A into A′.

Definition 2. We say a randomized algorithm A, evaluated on graph inputs in Gn, is (ε, δ )-node DP if for any A ∼v A′ and measurable set S, we have P(A(A) ∈ S) ≤ eεP(A(A′) ∈ S) + δ.

2.2 Stochastic block models 

We now formally define the stochastic block model. Let σ0 : [ n] → [K] denote the (unobserved) community assignments, where the nodes are partitioned into equal-sized communities of size s = 

> nK

, so σ0 ∈ Σ := σ : [ n] → [K] : σ−1(k) = s, ∀k ∈ [K] . Conditioned on σ0, the upper-triangular entries of the adjacency matrix A are generated independently according to 

P(Aij = 1 | σ0) = 

(

a/n, σ0(i) = σ0(j),b/n, σ0(i)̸ = σ0(j),

3for parameters a, b ∈ R. This is the family of SBMs Θ( n, K, a, b, 1) in the notation of [34]. For each σ ∈ Σ, define the cluster matrix Y σij = 1{σi = σj }, for all i, j ∈ [n]. Note that Y σ is invariant under relabeling of the communities. We will be interested in measuring the accuracy of a community estimation algorithm with respect to the best community labeling. For σ, τ : [ n] → [K], we define the Hamming distance 

h(σ, τ ) := |{ i ∈ [n] : σi̸ = τi}| and the permutation-invariant loss function r(σ, τ ) := dorb ([ σ],[τ ])  

> n

,where dorb ([ σ], [τ ]) := min π∈SK h(π ◦ σ, τ ). We state the following assumptions on the parameters of the SBM, which are required for our analysis of the utility and runtime of our algorithms (but not the privacy guarantees): 

Assumption 1. There exists an absolute constant Cmg > 0 such that K log( K) ≤ Cmg log( n).

Assumption 2. There exist absolute constants 0 < ρ − ≤ ρ+ < 1 and A0 > 0 such that aK ≥

A0 log( nK ), a, b = o(n), and 0 < ρ − ≤ b/a ≤ ρ+ < 1.

If I denotes the order-1 /2 Renyi divergence between Ber( a/n ) and Ber( b/n ), then under As-sumptions 1 and 2, we have inf ˆσ sup σ0∈Σ Er(σ0, bσ) = e−(1+ o(1))Signal , where Signal := nI K ≍ aK

and the expectation is taken with respect to the randomness in ˆ σ and the SBM generated with 

σ0 [50, 34]. The minimax rate can be attained by penalized likelihood methods [50]. 

# 3 Score function 

We introduce the critical choice of a score function for the exponential mechanism. One natural choice is the penalized likelihood score TA(σ) := P 

> i<j

(Aij − λ)1{σ(i) = σ(j)}, which achieves the non-private minimax rate [50] and was also utilized by [34] to obtain the private minimax rate. Note that for σ ∈ Σ, the penalty term λ P 

> i<j

1{σ(i) = σ(j)} = λK  n/K 

> 2

 is constant; however, we retain it to remain consistent with [50, 34]. For completeness, λ is chosen as in [34, Section 3.1]. Towards building an efficient algorithm, our first step is to introduce a linear programming (LP) proxy of TA(σ). For D > 0 and Y ∈ [0 , 1] n×n, we define 

eHA,D (Y ) := min 

> q,z

X

> ij ∈E(A)

zij Yij + D

> n

X

> i=1

qi

 , (1) subject to 0 ≤ qi ≤ 1 ∀i, 0 ≤ zij ≤ 1 ∀ij ∈ E(A), zij ≥ 1 − qi − qj ∀ij ∈ E(A). (2) We also define eΨA,D (Y ) := eHA,D (Y ) − λ P 

> i<j

Yij , and finally, the LP proxy eTA,D (σ) := eΨA,D (Y σ). 

Remark 1. For fixed A, D , and Y , we can compute eHA,D (Y ) using a linear program with n+|E(A)|

variables and O(n + |E(A)|) constraints, so eTA,D (σ) can be evaluated in polynomial time. 

Importantly, for “typical” graph inputs A, we will show that the score function eTA,D is close to 

TA for near-maximizers. Define the in-community bounded degree event, for a fixed D > 0, as 

ED := 

max 

> i∈[n]

X 

> j̸=i

Aij 1{σ0(j) = σ0(i)} ≤ D

 , (3) which we will later show (Lemma 8) occurs w.h.p. when A is sampled from an SBM. The proof of the following result is contained in Appendix D.1: 4Lemma 1 (Near-optimizer proximity) . For every graph A and every σ ∈ Σ, we have 

eTA,D (σ) ≤ TA(σ). (4) 

Furthermore, on ED, we have eTA,D (σ0) = TA(σ0); in particular, if eTA,D (σ) ≥ max τ ∈Σ eTA,D (τ ) − u,we have TA(σ) ≥ TA(σ0) − u.

Note that the conclusion of Lemma 1 is not that eTA,D is simply an “extension” of TA; the lemma merely states that eTA,D (σ0) = TA(σ0) and guarantees closeness of the two functions at near-optimizers. For a more complete discussion of the connection to Lipschitz extensions, see Remark 4 below. Nonetheless, we show that eTA,D has bounded sensitivity, leading to a private Gibbs sampling mechanism. The proof of the following result is contained in Appendix D.2: 

Lemma 2. For every pair of graphs (A, A ′), we have | eTA,D (σ) − eTA′,D (σ)| ≤ D d v(A, A ′), for all 

σ ∈ Σ. Thus, the mechanism which samples from the distribution 

πA(σ) := exp {η eTA,D (σ)}

P  

> τ∈Σ

exp {η eTA,D (τ )} , η := ε

2D . (5) 

is pure ε-node-DP. 

The output of our private algorithm is a sample from πA. In what follows, we describe a polynomial-time sampler and analyze its runtime and utility. Our key algorithmic idea is to de-compose πA as a mixture of a product measure proposal step and an SDP-based acceptance filter. 

# 4 Efficient sampling 

Whereas sampling directly from the Gibbs distribution (5) may purportedly take exponential time, we show that it is possible to construct a high-probability polynomial-time algorithm based on sequential rejection sampling: First, we use an SDP to compute a candidate labeling σ⋆, which with high probability (cf. Lemma 9) is equal to σ0. We then run another SDP to certify that σ⋆

maximizes the penalized likelihood (cf. Lemma 3), after which we can sample from πA using an acceptance filter. Details are provided in Algorithm 1 (see also Figure 1). Theorem 1 below shows that Algorithm 2 runs in polynomial time when the certification condi-tion (7) is satisfied. The algorithm also involves checking if σ is contained in a set Rσ⋆ of canonical permutations, the definition of which will be provided in Section 4.1. Only if the certification condition does not hold (which we show in Lemma 9 to be a low-probability event), do we sample exactly from πA by brute-force enumeration, which takes exponential time. 

Remark 2. Algorithm 2 also involves sampling a uniform permutation of K labels from the set 

SK . Although |SK | = K!, selecting a random permutation can be done in O(K2) time, assuming it is possible to generate Unif [0 , 1] random variables in constant time. 

Before proceeding, we prove that the certification step can be executed in polynomial time. The proof of the following result is contained in Appendix E.1: 

Lemma 3. If Z is a cluster matrix, then Vθ(Z) is the value of a polynomial-size SDP. 

In the subsections that follow, we analyze each step in turn. The main result is that, for an appropriate choice of θ, success of the certification condition ensures that the output of Algorithm 2 samples from the target distribution, and can be executed in (expected) polynomial time. 5Algorithm 1 Main algorithm  

> 1:

Input graph A, parameters K, D, θ, ε , η = ε 

> 2D

, and κ = 2 ηθ/K so that κ ≥ log(4 n(K − 1)).  

> 2:

Solve the candidate SDP max  

> X

⟨A, X ⟩ s.t. X ⪰ 0, X1 ≤ 1, Tr( X) = K, X ≥ 0. (6)  

> 3:

If s bX is an exact-size cluster matrix, read off some σ⋆ ∈ Σ such that Y σ⋆

= s bX. Otherwise, choose an arbitrary σ⋆ ∈ Σ. Set Y ⋆ = Y σ⋆

. 

> 4:

For a cluster matrix Z, define Vθ(Z) := max Y ∈C 

n eΨA,D (Y ) + θn ∥Y − Z∥1

o

, where C := 

{Y ⪰ 0, Y ii = 1 ∀i, Y ij ≥ 0 ∀i, j, Y 1 = s1}. 

> 5:

Check if 

Vθ(Y ⋆) = eΨA,D (Y ⋆). (7)  

> 6:

If condition (7) holds, run the certified rejection sampler (Algorithm 2) with parameter κ. 

> 7:

Otherwise, sample exactly from πA by brute-force enumeration of Σ. 

Algorithm 2 Certified rejection sampler  

> 1:

For each vertex i, independently propose a label σi by 

P(σi = σ⋆i ) = 11 + ( K − 1) e−κ , P(σi = ℓ) = e−κ

1 + ( K − 1) e−κ (ℓ̸ = σ⋆i ). 

> 2:

If σ / ∈ Σ or σ / ∈ Rσ⋆ , reject and restart.  

> 3:

Accept σ with probability bA(σ) := exp 

n

η[ eTA,D (σ) − eTA,D (σ⋆)] + κh (σ, σ ⋆)

o

. If rejected, restart.  

> 4:

If σ is accepted, draw Π ∼ Unif( SK ) and return Π ◦ σ.

Remark 3. For numerical implementation, the exact equality of SDP optima in condition (7) can be delicate. One could instead allow a slack ρ > 0 and require Vθ(Y ⋆) ≤ eΨA,D (Y ⋆)+ ρ. The analysis with such a slack is analogous to what follows. In fact, with high probability, condition (7) can be satisfied exactly. Since our focus is improving computational complexity rather than the mechanics of numerical optimization, we use the exact condition (7) .

4.1 Canonical permutations 

For bA(σ) to be a valid acceptance probability, we need η[ eTA,D (σ) − eTA,D (σ⋆)] + κh (σ, σ ⋆) ≤ 0. However, this inequality cannot hold over all of Σ, since permuting the community labels of σ⋆

leaves eTA,D unchanged, but may produce a labeling with a large Hamming distance from σ⋆. To remedy this issue, Algorithm 2 restricts attention to a set of permutations Rσ⋆ , which we now define. For σ ∈ Σ, define the set of canonical representatives Can σ⋆ (σ) := the lexicographically smallest element of arg min 

> π∈SK

h(π ◦ σ, σ ⋆),

where lexicographic order <lex means that for τ, τ ′ ∈ [K]n, we have τ <lex τ ′ if, at the first coordinate i for which τi̸ = τ ′ 

> i

, one has τi < τ ′ 

> i

. Define Rσ⋆ := {σ ∈ Σ : Can σ⋆ (σ) = σ}.6The following result, proved in Appendix E.2, shows that it is possible to check if a labeling is contained in Rσ⋆ in polynomial-time. It involves an application of the Hungarian algorithm [45]. 

Lemma 4. If σ ∈ Rσ⋆ , we have h(σ, σ ⋆) = dorb ([ σ], [σ⋆]) . For any σ ∈ Σ, the representative 

Can σ⋆ (σ), and hence membership in Rσ⋆ , can be computed in polynomial time. 

4.2 Certified rejection sampler 

We now turn to the analysis of Algorithm 2. Assume a candidate σ⋆ ∈ Σ satisfies the certificate (7) with Y ⋆ = Y σ⋆

. We begin with a technical lemma, proved in Appendix E.3, concerning the geometry of the space of cluster matrices: 

Lemma 5. For every σ, τ ∈ Σ, we have ∥Y σ − Y τ ∥1 ≥ 2nK dorb ([ σ], [τ ]) .

Using Lemma 5, we show that success of the certification condition implies a score margin, leading to a valid acceptance probability. The following result is proved in Appendix E.4: 

Lemma 6 (SDP certification implies stable LP likelihood maximization) . Suppose σ⋆ ∈ Σ satisfies 

Vθ(Y σ⋆

) = eΨA,D (Y σ⋆

). Then for every σ ∈ Σ, we have 

eTA,D (σ⋆) − eTA,D (σ) ≥ 2θK dorb ([ σ], [σ⋆]) . (8) 

In particular, if κ = 2ηθ K , we have bA(σ) ≤ 1 for every σ ∈ Σ ∩ Rσ⋆ .

Next, we show that the output of Algorithm 2 matches the desired Gibbs distribution, with a constant bound on the expected number of steps. The following result is proved in Appendix E.5: 

Lemma 7. Under the hypotheses of Lemma 6, Algorithm 2 outputs an exact sample from the Gibbs law (5) , and, if κ ≥ log(4 n(K − 1)) , the expected number of proposal trials is at most e1/4.

# 5 Analysis of SBMs 

In this section, we will show that with high probability over the distribution of adjacency matrices from an SBM, it is possible to produce a candidate labeling σ⋆ for which the certification step succeeds. The analysis is based on a result from [46] for exact recovery for SBMs via the SDP (6). This leads to an overall guarantee that, with high probability over the inputs, Algorithm 1 has polynomial expected runtime. We also show that the output of Algorithm 1 retains the minimax optimal private misclassification rate achieved by the exponential-time mechanism in [34]. 

5.1 High-probability expected runtime 

The following result, proved in Appendix F.1, lower-bounds the probability of ED (cf. equation (3)): 

Lemma 8. There are constants Cdeg , c deg > 0 such that if D = Cdeg aK and Assumption 2 holds with A0 sufficiently large, we have Pσ0 (EcD) ≤ (nK )−cdeg A0 .

The following result, proved in Appendix F.2, shows that the certification condition (7) is satisfied with high probability: 

Lemma 9. Let D = Cdeg aK and θ = χa , where Cdeg is sufficiently large and χ > 0 is chosen small enough that a−b−4θ ≥ cρa, for some constant cρ > 0 depending only on ρ+. Under Assumptions 1 and 2, with A0 sufficiently large, the true cluster matrix Y σ0 satisfies the certification condition (7) ,with probability at least 1 − n−c − (nK )−cA 0 .

7The main result of this section, proved in Appendix F.3, summarizes the privacy and high-probability runtime guarantees of Algorithm 1 for SBM inputs: 

Theorem 1 (Privacy and runtime guarantees) . Consider Assumptions 1 and 2. Choose D = Cdeg aK

and θ = χa , where Cdeg is sufficiently large and χ > 0 is sufficiently small depending only on ρ+.There exist constants L, c > 0 such that if ε ≥ L log( nK ), Algorithm 1 has the following properties: (i) For every input graph, it outputs an exact sample from πA in (5) .(ii) For every input graph, it is pure ε-node-DP. (iii) With probability at least 1 − n−c − (nK )−cA 0 over the SBM input, the certified branch is used and the conditional expected runtime, over the internal randomness of the sampler, is poly( n).

5.2 Utility analysis 

We now turn to analyzing the utility of the output of Algorithm 1. The proof of the following result is provided in Appendix F.4. It requires carefully adapting the proofs of Lemmas A.1 and A.5 in [34] (cf. Lemmas 18 and 19), because our exponential mechanism is run with the modified score eTA,D , not with the raw score TA. On the event ED, by Lemma 1, if σ is within a slack u

of maximizing eTA,D , i.e., eTA,D (σ) ≥ max τ ∈Σ eTA,D (τ ) − u, then TA(σ) ≥ TA(σ0) − u. However, this does not imply that TA(σ) ≥ max τ ∈Σ TA(τ ) − u, which is required by [34]. Hence, the utility analysis requires estimates for the set {σ ∈ Σ : TA(σ) ≥ TA(σ0) − u}, rather than only for the near-maximum set {σ ∈ Σ : TA(σ) ≥ max τ ∈Σ TA(τ ) − u}.

Theorem 2 (Risk guarantees) . Suppose Assumptions 1 and 2 hold and K ≥ 2. Let D = Cdeg aK

and θ = χa , where Cdeg is sufficiently large and χ > 0 is sufficiently small, depending on ρ+. There exist constants L, c 1, c 2, c 3 > 0 such that if ε ≥ L log( nK ), the output bσ of Algorithm 1 satisfies 

sup 

> σ0∈Σ

Er(σ0, bσ) ≤ exp 



−c1

nI K



+ 1

nK e−c2ε + ( nK )−c3A0 . (9) Combined with the lower bound in [34, Theorem 4.1], Theorem 2 shows that, for a ≍ K log( n), for any fixed c > 0 and A0 sufficiently large, a privacy level of ε = Θ(log( n)) is necessary and suf-ficient to drive the expected misclassification minimax risk inf  

> bσ:ε-node-DP

sup 

> σ0∈Σ

Er(σ0, bσ) below n−(1+ c).Theorem 2 significantly sharpens the results of [34], in which the proposed private mechanism is exponential-time and requires ε ≿ K log( n). 

Remark 4. The score eTA,D should not be viewed as a Lipschitz extension of TA, previously in-troduced in the literature, in the usual sense of agreeing with TA on all candidate labelings on a high-probability domain. The reason is that, in the growing-K regime, the relevant sensitivity scale is aK , while the total degree scale is a. A uniform agreement statement eTA,D (σ) = TA(σ), for all 

σ ∈ Σ, would require controlling the number of incident edges from a vertex to vertices placed in the same σ-community. Uniformly over all σ, this is controlled by the total degree and is therefore of order a, not aK . Using this larger scale would introduce an additional factor of K in the lower bound on ε, resulting in ε = Ω( K log( nK )) , since we would need to take D ≍ a, so η = ε 

> 2D

≍ εa .Since the proof of Theorem 2 requires η ≳ K log( nK )/a , this would in turn require ε ≳ K log( nK ).Instead, on the event ED, Lemma 1 gives eTA,D (σ) ≤ TA(σ), for all σ ∈ Σ, with eTA,D (σ0) = 

TA(σ0). This is sufficient for the utility analysis: If eTA,D (σ) ≥ max τ ∈Σ eTA,D (τ ) − u, we have 

eTA,D (σ) ≥ eTA,D (σ0) − u = TA(σ0) − u. 

8Since TA(σ) ≥ eTA,D (σ), it follows that TA(σ) ≥ TA(σ0) − u. Thus, every near-maximizer of the modified score is a near-maximizer of the raw score. The peeling argument in Lemma 19 shows that the Gibbs sampler outputs a near-maximizer of eTA,D with high probability, and Lemma 20 converts this into the desired risk bound. 

# 6 Truncated rejection sampler 

As noted in Theorem 1, the expected runtime of Algorithm 1, assuming the certification condition is met (which occurs w.h.p. over SBM inputs) is polynomial in n. In fact, we can derive a stronger algorithmic guarantee, turning the runtime under the certification condition into worst-case poly-nomial time uniformly over the internal randomness of the algorithm. The analysis of the truncated sampler (Algorithm 3) is inspired by [9, Lemma 1], which derives a total variation (TV) distance approximation guarantee on the output of the truncated sampler. In order to preserve pure dif-ferential privacy, we require a more careful construction of the fallback sampling distribution (in case no proposal is accepted) and the behavior of the output distribution. For completeness, in Appendix B.1, we include the analysis of a truncated sampler based on TV distance, resulting in an approximate-DP guarantee. 

Remark 5. [49, 40] show that a mechanism whose output law is close, uniformly over input datasets, in TV distance to that of a DP mechanism, satisfies approximate DP. Our analysis in Appendix B.1 produces similar (ε, δ ) terms, where δ = (1 + eε)ζ0, and ζ0 is a bound on the TV distance. Truncated rejection samplers for approximate DP have also been analyzed recently in [3]. 

Algorithm 3 Certified truncated rejection sampler  

> 1:

Input graph A, candidate σ⋆ ∈ Σ, truncation level M ≥ 1.  

> 2:

for t = 1 , . . . , M do  

> 3:

For each vertex i, independently propose a label σi by 

P(σi = σ⋆i ) = 11 + ( K − 1) e−κ , P(σi = ℓ) = e−κ

1 + ( K − 1) e−κ (ℓ̸ = σ⋆i ). 

> 4:

If σ / ∈ Σ or σ / ∈ Rσ⋆ , reject this proposal and continue to the next iteration.  

> 5:

Accept σ with probability bA(σ) := exp 

n

η[ eTA,D (σ) − eTA,D (σ⋆)] + κh (σ, σ ⋆)

o

. If rejected, continue to the next iteration.  

> 6:

If σ is accepted, draw Π ∼ Unif( SK ) and return Π ◦ σ. 

> 7:

end for  

> 8:

If no proposal is accepted, draw Π ∼ Unif( SK ) and output Π ◦ σ⋆.Let Q(M ) 

> A

denote the output law of the truncated sampler (Algorithm 1, with Algorithm 3 used in place of Algorithm 2). Define rM :=  1−e−1/4M and ξM := max − log(1 − rM ), log  1 + ( e1/4 − 1) rM

 .The first result, proved in Appendix G.1, shows that the truncated rejection sampler leads to an additive increase in the privacy parameter by at most 2 ξM :

Lemma 10. Assume that κ = 2 ηθ/K and κ ≥ log(4 n(K − 1)) . For every input graph A and every 

σ ∈ Σ, we have e−ξM πA(σ) ≤ Q(M ) 

> A

(σ) ≤ eξM πA(σ). In particular, if η = ε 

> 2D

, the mechanism which samples from Q(M ) 

> A

is pure (ε + 2 ξM )-node-DP. 

9Importantly, a utility guarantee for the truncated sampler follows immediately from the utility guarantee of Theorem 2, with an extra factor of eξM . The following result is proved in Appendix G.2: 

Theorem 3. Suppose Assumptions 1 and 2 hold, and K ≥ 2. Let D = Cdeg aK and θ = χa ,where Cdeg is sufficiently large and χ > 0 is sufficiently small depending only on ρ+. Let bσ(M ) ∼

Q(M ) 

> A

be the output of the truncated node-private mechanism with η = ε 

> 2D

. There exist constants 

L, c 1, c 2, c 3 > 0 such that if ε ≥ L log( nK ), the output bσ = bσ(M ) of Algorithm 1, with Algorithm 3 used in place of Algorithm 2, satisfies 

sup 

> σ0∈Σ

Eσ0 r(σ0, bσ(M )) ≤ eξM



exp 



−c1

nI K



+ 1

nK e−c2ε + ( nK )−c3A0



.

The following corollary, proved in Appendix G.3, shows that taking M = O(poly( n)) leads to similar utility guarantees as the non-truncated mechanism, with only an e−Θ( M ) increase in privacy. 

Corollary 1. Let M = O(poly( n)) .1. For η = ε 

> 2D

, the mechanism which samples from Q(M ) 

> A

is pure O(ε + e−Θ( M ))-node-private. 2. For the parameter choices of Theorem 1, there exist constants c, c ′ > 0 such that, under the SBM input, with probability at least 1 − n−c − (nK )−c′A0 , the runtime of the truncated mechanism is polynomial in n, uniformly over its internal randomness. 3. If, in addition, the hypotheses of Theorem 2 hold, then for K ≥ 2, we have 

sup 

> σ0∈Σ

Eσ0 r(σ0, bσ(M )) ≤ exp 



−c4

nI K



+ 1

nK e−c5ε + ( nK )−c6A0 .

Thus, taking M ≍ log( n), the truncated sampler has worst-case polynomial runtime, privacy parameter ε + o(1), and the same minimax risk as in Theorem 2. Consequently, combined with the lower bound [34, Theorem 4.1], Corollary 1 shows that for a ≍ K log( n), any fixed c > 0, and 

A0 sufficiently large depending on c, a privacy level of ε = Θ(log( n)) is necessary and sufficient to drive the expected misclassification minimax risk inf  

> bσ:ε-node-DP

sup 

> σ0∈Σ

Er(σ0, bσ) below n−(1+ c).

# 7 Discussion 

We have presented polynomial-time algorithms for node-private community estimation in SBMs that achieve the minimax rate of [34]. Our results are applicable when K grows as eO(log( n)), in which case we show that ε ≍ log( n) is necessary and sufficient for a misclassification risk of 1poly( n) .Interesting open directions include developing and analyzing node-private algorithms (either polynomial- or exponential-time) for exact recovery when K grows more rapidly as a function of 

n, or developing polynomial-time algorithms that are more widely applicable to stochastic block models with approximately equal-sized communities. Another important question is to exactly characterize upper and lower bounds for ε in terms of n and K order for the risk to scale as a more general function f (n). Similar questions could be posed concerning necessary and sufficient scaling of the joint parameters ( ε, δ ) in the setting of approximate differential privacy. 10 8 AI declaration 

This paper was written with the help of ChatGPT 5.5 Plus. Although ChatGPT played a critical role in brainstorming the initial idea for a polynomial-time algorithm and providing proof outlines for the main results, the authors were heavily involved in interpreting, correcting, and rigorizing all the arguments in the paper. 

# A Notation 

For K ∈ N, we write [ K] to denote the set {1, . . . , K }. Let SK denote the permutation group of [K], and define the orbit [ σ] := {π ◦ σ : π ∈ SK }.For a matrix A, we write ∥A∥1 to denote the entrywise ℓ1-norm. For square matrices A and B,we denote their inner product by ⟨A, B ⟩ = Tr( AB T ). We write E(A) to denote the set of unordered edges of an adjacency matrix A, and we use both ij and {i, j } to denote elements of E(A). For functions f (n) and g(n), we write f (n) ≾ g(n) and f (n) = O(g(n)) to mean that f (n) ≤

c1g(n), if n ≥ c2, for universal constants c1, c 2 ∈ (0 , ∞), and define f (n) ≿ g(n) and f (n) = Ω( g(n)) analogously. We write f (n) ≍ g(n) and f (n) = Θ( g(n)) when both f (n) ≾ g(n) and f (n) ≿ g(n)hold simultaneously. We write f (n) = poly( n) when f (n) = Θ( nα) for some constant α > 0. We use the notation c, C, c i, etc., to denote absolute positive constants, whose values may change between results in our paper. 

# B Approximate differential privacy 

In this appendix, we derive privacy, runtime, and utility results for approximate Gibbs sampling based on a slightly simpler truncated rejection sampler. The results should be viewed in comparison to Section 6. The algorithm and analysis are easier, but the result is approximate rather than pure differential privacy. For completeness, we then derive lower bounds for exact recovery rates under approximate DP in Appendix B.2. 

B.1 Total variation-based truncated rejection sampling 

We present a variant of the analysis in Section 6, which results in an approximate DP guarantee. At a high level, this analysis shows that approximate sampling in terms of TV distance translates directly into an approximate differential privacy guarantee. This may be interesting in its own right: Note that in the analysis of Lemma 11, the distribution of the fallback sampling distribution 

RA is irrelevant, in contrast to the proof of Lemma 21. We begin with a privacy guarantee: 

Lemma 11. For every input graph A, we have ∥Q(M ) 

> A

− πA∥TV ≤ rM . In particular, for η = ε 

> 2D

,the truncated mechanism which samples from Q(M ) 

> A

is (ε, (1 + eε)rM )-node-DP. Proof. If C(A) = 1, by the same argument as in Lemma 21 (with RA arbitrary), we have 

Q(M ) 

> A

= (1 − rA

> acc

)πA + rA

> acc

RA, (10) with rA 

> acc

≤ rM . If C(A) = 0, then Q(M ) 

> A

= πA, so equation (10) still holds. Hence, we have 

∥Q(M ) 

> A

− πA∥TV = rA

> acc

∥RA − πA∥TV ≤ rA 

> acc

≤ rM .

11 This proves the TV bound. Now let A ∼v A′, and let S ⊆ Σ. Since η = ε/ (2 D), by Lemma 2, we have πA(S) ≤ eεπA′ (S). By the TV bound, we have 

Q(M ) 

> A

(S) ≤ πA(S) + rM ≤ eεπA′ (S) + rM ≤ eεQ(M ) 

> A′

(S) + (1 + eε)rM .

This proves the privacy. A utility guarantee for the truncated sampler follows immediately from the utility guarantee of Theorem 2, with an extra additive rM term: 

Theorem 4. Suppose Assumptions 1 and 2 hold, and K ≥ 2. Let D = Cdeg aK and θ = χa , where 

Cdeg is sufficiently large and χ > 0 is sufficiently small depending only on ρ+. Let bσ(M ) ∼ Q(M )

> A

be the output of the truncated node-private mechanism with η = ε/ (2 D). There exist constants 

L, c 1, c 2, c 3 > 0 such that, if ε ≥ L log( nK ), the output bσ = bσ(M ) of Algorithm 1, with Algorithm 3 used in place of Algorithm 2, satisfies 

sup 

> σ0∈Σ

Eσ0 r(σ0, bσ(M )) ≤ exp 



−c1

nI K



+ 1

nK e−c2ε + ( nK )−c3A0 + rM .

Proof. Fix an input graph A and a true labeling σ0 ∈ Σ. Combining the fact that r(σ0, σ ) ∈ [0 , 1] with equation (10), we have 

EQ(M )

> A

r(σ0, σ ) = (1 − rA

> acc

)EπA r(σ0, σ ) + rA

> acc

ERA r(σ0, σ ) ≤ EπA r(σ0, σ ) + rA 

> acc

≤ EπA r(σ0, σ ) + rM .

Taking an expectation over the SBM graph A, conditional on σ0, yields 

Eσ0 r(σ0, bσ(M )) ≤ Eσ0 EπA r(σ0, σ ) + rM .

Taking the supremum over σ0 ∈ Σ and applying Theorem 2 to the Gibbs law πA gives the desired bound. Finally, the following corollary shows that taking M = O(poly( n)) leads to similar utility guarantees as the non-truncated mechanism, with an approximate DP guarantee instead of pure DP: 

Corollary 2. Let M = log 

 (1+ eε)/δ 

  

> −log(1 −e−1/4)

, with 1+ eε 

> epoly( n)

≤ δ < 1.1. For η = ε 

> 2D

, the truncated mechanism which samples from Q(M ) 

> A

is (ε, δ )-node-DP. 2. For the parameter choices of Theorem 1, there exist constants c, c ′ > 0 such that, under the SBM input, with probability at least 1 − n−c − (nK )−c′A0 , the runtime of the truncated mechanism is polynomial in n, uniformly over its internal randomness. 3. If, in addition, the hypotheses of Theorem 2 hold, then for K ≥ 2, we have 

sup 

> σ0∈Σ

Eσ0 r(σ0, bσ(M )) ≤ exp 



−c1

nI K



+ 1

nK e−c2ε + ( nK )−c3A0 + δe −ε.

Proof. The choice of M gives (1 + eε)rM = δ, so the first claim follows directly from Lemma 11. Next, since 1+ eε 

> epoly( n)

≤ δ < 1, we have M = O(poly( n)), so the second claim follows as in the proof of Corollary 1. For the final claim, since we assume ε ≥ L log( nK ), we have sup 

> σ0∈Σ

Eσ0 r(σ0, bσ(M )) ≤ exp 



−c1

nI K



+ 1

nK e−c2ε + ( nK )−c3A0 + rM ,

by Theorem 4. Noting that (1 + eε)rM = δ completes the proof. 12 B.2 Lower bound 

In this section, we derive an extension of the lower bound argument in [34], adapted to approximate differential privacy. The key modification is the application of group privacy. First, we describe the aspect of group privacy of size two in the lemma below: 

Lemma 12. Let M : Gn → [K]n be (ε, δ )-node differentially private. If A, A ′ ∈ G n satisfy 

dv(A, A ′) ≤ 2, then for every measurable S ⊆ [K]n, we have 

P(M(A) ∈ S) ≤ e2εP(M(A′) ∈ S) + (1 + eε)δ. 

Proof. Since dv(A, A ′) ≤ 2, there exists H ∈ G n such that A ∼v H and H ∼v A′. Applying (ε, δ )-node DP twice, we obtain 

P(M(A) ∈ S) ≤ eεP(M(H) ∈ S) + δ

and 

P(M(H) ∈ S) ≤ eεP(M(A′) ∈ S) + δ. 

Substituting the second inequality into the first gives 

P(M(A) ∈ S) ≤ e2εP(M(A′) ∈ S) + (1 + eε)δ, 

as required. 

Theorem 5. Fix K ≥ 2 and assume nK ≥ 2. Let Θ = Θ( n, K, a, b ) be the homogeneous bal-anced SBM parameter class with K communities, within-community edge probability a/n , across-community edge probability b/n , and balanced labelings in Σ.For each θ ∈ Θ, let σθ ∈ Σ be a representative ground-truth labeling, and let Pθ and Eθ denote the probability and expectation under the SBM law corresponding to θ. Then 

inf  

> M:( ε,δ )-node-DP

sup 

> θ∈Θ

Pθ

 r(σθ, M(A)) > 0 ≥

 1 − (1 + eε)δ

1 + e2ε



> +

and 

inf  

> M:( ε,δ )-node-DP

sup 

> θ∈Θ

Eθr(σθ, M(A)) ≥ 1

n

 1 − (1 + eε)δ

1 + e2ε



> +

,

where [x]+ := max {x, 0}.Proof. Let M : Gn → [K]n be any ( ε, δ )-node differentially private mechanism. Fix an arbitrary 

θ ∈ Θ, and let σ := σθ ∈ Σ be a representative ground-truth labeling. Since K ≥ 2 and every community has size at least n/K ≥ 2, we may choose two vertices u̸ = v in different communities. Write 

σ(u) = k, σ(v) = ℓ, k̸ = ℓ. 

Define a new labeling σ′ : [ n] → [K] by swapping the labels of u and v:

σ′(u) = ℓ, σ′(v) = k, σ′(i) = σ(i) for all i / ∈ { u, v }.

Then σ′ ∈ Σ. Let θ′ ∈ Θ be the SBM parameter corresponding to the labeling σ′. Define the two exact-recovery target events 

Eσ := {M (A) ∈ [σ]}, Eσ′ := {M (A) ∈ [σ′]},

13 where [ σ] := {π ◦ σ : π ∈ SK }.We first show that the two target orbits are disjoint. Suppose, for contradiction, that there exists π ∈ SK such that σ′ = π ◦ σ. Since the community k contains at least two vertices, there exists w̸ = u such that σ(w) = k. Since w / ∈ { u, v }, we also have σ′(w) = σ(w) = k. Therefore, 

k = σ′(w) = π(σ(w)) = π(k).

Similarly, because the community ℓ contains at least two vertices, there exists w′̸ = v such that 

σ(w′) = ℓ. Since w′ /∈ { u, v }, we have σ′(w′) = σ(w′) = ℓ, so 

ℓ = σ′(w′) = π(σ(w′)) = π(ℓ).

Thus, we have π(k) = k and π(ℓ) = ℓ. But then σ′(u) = π(σ(u)) = π(k) = k, whereas by construction, σ′(u) = ℓ̸ = k. This is a contradiction. This implies that [ σ] ∩ [σ′] = ∅, so Eσ ∩ Eσ′ =

∅.We now couple the two SBM graph distributions. Let μσ and μσ′ denote the respective laws of the SBM adjacency matrices with true community assignments σ and σ′. Let A ∼ μσ and A′ ∼ μσ′ .Construct the coupling ( A, A ′), as follows: For every unordered pair {i, j } such that 

{i, j } ∩ { u, v } = ∅,

the labels of both endpoints are the same under σ and σ′. Hence, the corresponding edge prob-abilities under the two SBM laws are equal. For these edges, sample Aij once from this common Bernoulli law and set A′ 

> ij

= Aij . For edges incident to u or v, sample the entries of A according to their correct SBM probabilities under σ, and sample the entries of A′ according to their correct SBM probabilities under σ′. This construction has marginals A ∼ μσ and A′ ∼ μσ′ . Note that A

and A′ can differ only on edges incident to u or v, so dv(A, A ′) ≤ 2 under this coupling. Let S ⊆ [K]n be any measurable event. By Lemma 12, conditionally on any coupled pair ( A, A ′)with dv(A, A ′) ≤ 2, we have 

P(M(A) ∈ S | A, A ′) ≤ e2εP(M(A′) ∈ S | A, A ′) + (1 + eε)δ. 

Taking an expectation over the coupling gives 

Pθ(M(A) ∈ S) ≤ e2εPθ′ (M(A) ∈ S) + (1 + eε)δ, 

where on the right-hand side, A denotes a freshly drawn adjacency matrix from the SBM law under 

θ′. Apply this inequality with S = [ σ]. Then 

Pθ(Eσ) ≤ e2εPθ′ (Eσ) + (1 + eε)δ. 

Since Eσ and Eσ′ are disjoint, we have Pθ′ (Eσ) ≤ 1 − Pθ′ (Eσ′ ). Now define ∆σ := Pθ

 r(σ, M(A)) > 0 = 1 − Pθ(Eσ), ∆σ′ := Pθ′

 r(σ′, M(A)) > 0 = 1 − Pθ′ (Eσ′ ).

The preceding inequalities imply that 1 − ∆σ ≤ e2ε∆σ′ + (1 + eε)δ. Let ∆ := max {∆σ, ∆σ′ }. Since ∆σ ≤ ∆ and ∆ σ′ ≤ ∆, we have 1 − (1 + eε)δ ≤ (1 + e2ε)∆. Hence, ∆ ≥ 1 − (1 + eε)δ

1 + e2ε .

Since ∆ ≥ 0, this yields ∆ ≥

 1 − (1 + eε)δ

1 + e2ε



> +

.

14 Since θ, θ ′ ∈ Θ, we have sup θ∈Θ Pθ

 r(σθ, M(A)) > 0 ≥ ∆. Thus, we have sup 

> θ∈Θ

Pθ

 r(σθ, M(A)) > 0 ≥

 1 − (1 + eε)δ

1 + e2ε



> +

.

It remains to prove the expected-mismatch lower bound. For any two labelings σ, τ : [ n] → [K], if r(σ, τ ) > 0, at least one vertex is misclassified after the best permutation of labels, implying that 

r(σ, τ ) ≥ 1

n 1{r(σ, τ ) > 0}.

Applying this bound under θ and θ′ yields Eθr(σ, M(A)) ≥ 1 

> n

∆σ and Eθ′ r(σ′, M(A)) ≥ 1 

> n

∆σ′ .Taking the maximum of the two inequalities, we obtain max Eθr(σ, M(A)) , Eθ′ r(σ′, M(A)) ≥ 1

n ∆.

Since both θ and θ′ belong to Θ, we have sup 

> θ∈Θ

Eθr(σθ, M(A)) ≥ 1

n

 1 − (1 + eε)δ

1 + e2ε



> +

.

Taking the infimum over all ( ε, δ )-node-DP mechanisms proves the corresponding minimax lower bounds. The following corollary shows that if both δ and the target expected misclassification risk are polynomially small, the privacy parameter must satisfy ε = Ω(log( n)). 

Corollary 3. Let γn be such that 1 − nγ n − δ > 0. Suppose an (ε, δ )-node-DP mechanism M :

Gn → [K]n satisfies 

sup 

> θ∈Θ

Eθr(σθ, M(A)) ≤ γn.

Then 

ε ≥ 12 log 

 1 − nγ n − δnγ n + δ



.

In particular, if γn ≾ 1  

> nf (n)

and δ ≾ n−c, where c > 0 and f (n) → ∞ , then for all sufficiently large 

n, we have 

ε ≿ min {log( f (n)) , c log( n)}.

Proof. By Theorem 5, we have 

γn ≥ 1

n

 1 − (1 + eε)δ

1 + e2ε



> +

,

implying that nγ n(1 + e2ε) + (1 + eε)δ ≥ 1. Since eε ≤ e2ε, we obtain nγ ne2ε + δe ε ≤ (nγ n + δ)e2ε,so (nγ n + δ)e2ε ≥ 1 − nγ n − δ. 

If 1 − nγ n − δ > 0, then 

ε ≥ 12 log 

 1 − nγ n − δnγ n + δ



.

If γn ≾ 1/(nf (n)) and δ ≾ n−c, then nγ n +δ ≤ Cf (n)−1 +Cn −c. Since f (n) → ∞ and n−c → 0, we also have 1 − nγ n − δ ≥ 1/2, for all sufficiently large n. This implies that 

ε ≿ log 

 1

f (n)−1 + n−c



≍ min {log( f (n)) , c log( n)}.

15 Combining Corollaries 2 and 3 allows us to draw some useful conclusions about the minimax risk. Applying c > 0 and setting δ ≍ n−c, Corollary 3 with f (n) = nc, implies that any estimator whose expected misclassification minimax risk inf  

> bσ:( ε,δ )-node-DP

sup 

> σ0∈Σ

Er(σ0, bσ) is less than n−(1+ c) must satisfy 

ε ≿ log( n). Conversely, Corollary 2 shows that for a ≍ K log( n), for A0 sufficiently large depending on c, and for ε = L log( n) (so that 1+ eε 

> epoly( n)

≤ δ), for sufficiently large L, the truncated sampler (1) is ( ε, δ )-node-DP, (2) runs in worst-case polynomial time, uniformly over the internal randomness of the sampler (with high-probability over SBM inputs), and (3) has expected misclassification minimax risk at most n−(1+ c). Since Assumption 1 gives log( nK ) ≍ log( n), a privacy level ε ≍

log( n) is therefore both necessary and sufficient for the value of the expected misclassification minimax risk to fall below n−(1+ c).

# C Zero-concentrated differential privacy 

In this appendix, we prove a zCDP analog of the private lower bound (Theorem 5) for stochastic block model community recovery. The conclusion is that using the notion of zCDP also leads to a lower bound of ρ = Ω(log( n)) for polynomially small exact-recovery failure, meaning the privacy parameter ρ still needs to grow with n.

C.1 Preliminaries 

For probability distributions P and Q defined on a finite output space, recall that the α-Renyi divergence is defined by 

Dα(P ∥Q) = 1

α − 1 log X

> y

P (y)αQ(y)1−α

!

,

where α > 1. Also recall the KL divergence: 

DKL 

 P ∥ Q = X

> y

P (y) log 

 P (y)

Q(y)



.

We begin by defining the notion of a node-zCDP mechanisms on graph inputs, adapted from the standard definitions of zCDP in [6]. 

Definition 3. A randomized mechanism M : Gn → [K]n is ρ-node-zCDP if, for all A ∼v A′ and all α > 1, we have 

Dα

 L(M(A)) ∥ L (M(A′))  ≤ αρ, 

where L(·) denotes the probability law. 

Since zCDP satisfies group privacy properties [6], so does node-zCDP: 

Lemma 13. Let M : Gn → [K]n be ρ-node-zCDP. If dv(A, A ′) ≤ 2, then for every α > 1, we have 

Dα

 L(M(A)) ∥ L (M(A′))  ≤ 4αρ. 

Moreover, DKL 

 L(M(A)) ∥ L (M(A′))  ≤ 4ρ.Proof. The group privacy of size two claim follows from [6, Proposition 5.3]. Since the output space [K]n is finite, we have DKL (P0∥P2) = lim α→1 Dα(P0∥P2) ≤ 4ρ, as required. 16 We will use the fact that the KL divergence is convex: 

Lemma 14 (Theorem 2.7.2 of [15]) . Let ν be a probability measure on an index space Z, and let 

{Pz }z∈Z , {Qz }z∈Z be probability kernels on a common finite space. Define 

P := 

Z

Pz ν(dz ), Q := 

Z

Qz ν(dz ).

Then 

DKL (P ∥Q) ≤

Z

DKL (Pz ∥Qz ) ν(dz ).

We will also use the Bretagnolle–Huber testing inequality: 

Lemma 15 (Theorem 14.2 of [37]) . For any two probability measures P and Q on a common finite space and any event B, we have 

P (Bc) + Q(B) ≥ 12 exp {− DKL (P ∥Q)}.

C.2 Lower bound 

We begin with the statement of the lower bound on the failure probability of ρ-node-zCDP algo-rithms for exact recovery, followed by a corollary which translates the result into a lower bound on 

ρ.

Theorem 6. Fix K ≥ 2 and assume nK ≥ 2. Let Θ = Θ( n, K, a, b ) be the homogeneous bal-anced SBM parameter class with K communities, within-community edge probability a/n , across-community edge probability b/n , and balanced labelings in Σ. For each θ ∈ Θ, let σθ ∈ Σ be a representative ground-truth labeling, and let Pθ and Eθ denote the corresponding SBM probability and expectation. Then 

inf   

> M:ρ-node-zCDP

sup 

> θ∈Θ

Pθ

 r(σθ, M(A)) > 0 ≥ 14 e−4ρ,

and 

inf   

> M:ρ-node-zCDP

sup 

> θ∈Θ

Eθ

r(σθ, M(A))  ≥ 14n e−4ρ.

Proof. Let M : Gn → [K]n be any ρ-node-zCDP mechanism. Taking the infimum over M at the end will give the desired minimax lower bounds. First, we construct two disjoint exact-recovery targets. Fix any θ ∈ Θ, and let σ := σθ ∈ Σ be a representative ground-truth labeling. Since K ≥ 2 and each community has at least two vertices, choose vertices u, v ∈ [n] in different communities. Write 

σ(u) = k, σ(v) = ℓ, k̸ = ℓ. 

Define σ′ ∈ [K]n by swapping the labels of u and v:

σ′(u) = ℓ, σ′(v) = k, σ′(i) = σ(i) for i / ∈ { u, v }.

The community sizes of σ′ are the same as those of σ, so σ′ ∈ Σ. Let θ′ ∈ Θ be the SBM parameter corresponding to the labeling σ′. We claim that the two label orbits are disjoint: [ σ] ∩ [σ′] = ∅.Indeed, suppose not. Then σ′ ∈ [σ], so σ′ = π ◦ σ for some π ∈ SK . Since community k has at 17 least two vertices, choose w̸ = u with σ(w) = k. Then w̸ = v, so σ′(w) = σ(w) = k. Therefore, we have 

k = σ′(w) = π(σ(w)) = π(k),

so π(k) = k. Similarly, because community ℓ has at least two vertices, choose w′̸ = v with σ(w′) = ℓ.Then σ′(w′) = σ(w′) = ℓ, so π(ℓ) = ℓ. But then 

σ′(u) = π(σ(u)) = π(k) = k, 

contradicting the fact that σ′(u) = ℓ. Thus, the orbits are disjoint. Let E := [ σ] and E′ := [ σ′]. These are the exact-recovery events in output space for σ and σ′, respectively, and E ∩ E′ = ∅.Next, let μσ and μσ′ be the respective laws of the SBM adjacency matrices with true community assignments σ and σ′. We construct a coupling ν of A ∼ μσ and A′ ∼ μσ′ : For every unordered pair {i, j } with {i, j } ∩ { u, v } = ∅, the labels of both endpoints are unchanged by the swap. Hence, the edge probability for {i, j } is the same under σ and σ′. Sample one Bernoulli variable with this common probability and set Aij = A′ 

> ij

. For all edges incident to u or v, sample the entries of A and 

A′ independently over edges, with their correct SBM marginal laws under σ and σ′, respectively. By construction, we have A ∼ μσ and A′ ∼ μσ′ , and A and A′ can differ only on edges incident to 

u or v. Therefore, we have dv(A, A ′) ≤ 2. Using the node-zCDP property of M, we now bound the KL divergence between the output laws Lσ(M(A)) and Lσ′ (M(A)). For a fixed graph G, let KG := L(M(G)), where the law is over the internal randomness of M. Let 

P := Lσ(M(A)) , Q := Lσ′ (M(A)) .

Equivalently, under the coupling ν, we have 

P =

Z

KA ν(dA, dA ′), Q =

Z

KA′ ν(dA, dA ′).

For every coupled pair ( A, A ′), we have dv(A, A ′) ≤ 2. Hence, by Lemma 13, we have DKL (KA∥KA′ ) ≤

4ρ. Using Lemma 14, we have 

DKL (P ∥Q) ≤

Z

DKL (KA∥KA′ ) ν(dA, dA ′) ≤ 4ρ. 

We now test between the two exact-recovery targets Pθ

 r(σ, M(A)) > 0 and Pθ′

 r(σ′, M(A)) >

0. Define 

δσ := P (Ec) = Pθ

 r(σ, M(A)) > 0,

and 

δσ′ := Q(( E′)c) = Pθ′

 r(σ′, M(A)) > 0.

Since E ∩ E′ = ∅, we have Q(E) ≤ Q(( E′)c) = δσ′ . Apply Lemma 15 with the event B = E. Then 

δσ + Q(E) = P (Ec) + Q(E) ≥ 12 exp {− DKL (P ∥Q)} ≥ 12 e−4ρ.

Since Q(E) ≤ δσ′ , this implies δσ + δσ′ ≥ 12 e−4ρ, so max {δσ, δ σ′ } ≥ 14 e−4ρ. Both σ and σ′ belong to Σ, so sup 

> θ∈Θ

Pθ

 r(σθ, M(A)) > 0 ≥ 14 e−4ρ. (11) 18 It remains to prove the expected-mismatch lower bound. For every τ ∈ Σ, we have 

r(τ, M(A)) ≥ 1

n 1{r(τ, M(A)) > 0}.

Taking an expectation and then a supremum over θ ∈ Θ, and using inequality (11), we obtain sup 

> θ∈Θ

Eθr(σθ, M(A)) ≥ 1

n sup 

> θ∈Θ

Pθ(r(σθ, M(A)) > 0) ≥ 14n e−4ρ.

Taking the infimum over all ρ-node-zCDP mechanisms then proves the corresponding minimax lower bounds. The following corollary shows that polynomially small exact-recovery failure or expected mis-classification risk requires ρ = Ω(log( n)): 

Corollary 4. Fix c > 0. Under the assumptions of Theorem 6: 1. If a ρ-node-zCDP mechanism M : Gn → [K]n satisfies sup θ∈Θ Pθ(r(σθ, M(A)) > 0) ≤ n−c,then 

ρ ≥ c

4 log( n) − 14 log(4) .

2. If a ρ-node-zCDP mechanism M : Gn → [K]n satisfies sup θ∈Θ Eθr(σθ, M(A)) ≤ n−(1+ c), then 

ρ ≥ c

4 log( n) − 14 log(4) .

Proof. For exact recovery, Theorem 6 gives e−4ρ ≤ 4n−c. Taking logarithms, we obtain −4ρ ≤

log 4 − c log( n), from which the first claim follows. For expected misclassification, Theorem 6 gives n−(1+ c) ≥ 14n e−4ρ. Multiplying by n, we obtain 

n−c ≥ 14 e−4ρ, and the same calculation yields the same lower bound on ρ.

# D Proofs for Section 3 

D.1 Proof of Lemma 1 

First, choose qi = 0 for all i and zij = 1 for all ij ∈ E(A). This is in the feasible set (2); hence, for every Y , we have 

eHA,D (Y ) ≤ X

> ij ∈E(A)

Yij . (12) Taking Y = Y σ and subtracting the same penalty gives inequality (4). Now suppose ED holds and take any feasible ( q, z ) in the set (2). Since zij ≥ 1 − qi − qj and 

Y σ0 

> ij

≥ 0, we have 

X

> ij ∈E(A)

zij Y σ0 

> ij

+ D X

> i

qi ≥ X

> ij ∈E(A)

(1 − qi − qj )Y σ0 

> ij

+ D X

> i

qi

= X

> ij ∈E(A)

Y σ0 

> ij

+ X

> i

qi

D − X 

> j̸=i

Aij 1{σ0(j) = σ0(i)}

 ≥ X

> ij ∈E(A)

Y σ0 

> ij

.

19 Taking the minimum over feasible ( q, z ) gives eHA,D (Y σ0 ) ≥ P 

> ij ∈E(A)

Y σ0 

> ij

. Combining with inequal-ity (12), this is actually an equality. Subtracting the penalty gives eTA,D (σ0) = TA(σ0). Finally, note that if eTA,D (σ) ≥ max τ ∈Σ eTA,D (τ ) − u, we have 

TA(σ) ≥ eTA,D (σ) ≥ max 

> τ

eTA,D (τ ) − u ≥ eTA,D (σ0) − u = TA(σ0) − u. 

D.2 Proof of Lemma 2 

It suffices to prove the claim for node-adjacent graphs. Suppose A and A′ differ only in edges incident to a vertex v. Fix Y ∈ [0 , 1] n×n and let ( q, z ) be feasible for eHA,D (Y ). Construct ( q′, z ′)for A′, as follows: Set q′ 

> v

= 1 and q′ 

> i

= qi for i̸ = v. For every edge ij ∈ E(A′) not incident to v, set 

z′ 

> ij

= zij ; such an edge also belongs to E(A). For every edge vj ∈ E(A′) incident to v, set z′ 

> vj

= 0. The constraints are feasible. Nonincident constraints are inherited. If vj ∈ E(A′), then 1 − q′ 

> v

− q′ 

> j

= 1 − 1 − q′ 

> j

= −q′ 

> j

≤ 0 = z′ 

> vj

.

Thus, ( q′, z ′) is feasible for eHA′,D (Y ). The edge terms corresponding to new incident edges have 

z′ = 0, nonincident terms are unchanged, and the only possible increase in the deletion penalty is 

D(q′ 

> v

− qv) = D(1 − qv) ≤ D. 

Hence, we have eHA′,D (Y ) ≤ eHA,D (Y )+ D, implying that | eHA,D (Y )− eHA′,D (Y )| ≤ D, by symmetry. The penalty −λ P 

> i<j

Yij is independent of A. Plugging in Y = Y σ proves the node-adjacent case, and telescoping over a shortest node path proves the claim. The privacy claim follows immediately from privacy of the exponential mechanism. 

# E Proofs for Section 4 

E.1 Proof of Lemma 3 

Note that for Y ∈ C , we have 0 ≤ Yij ≤ pYii Yjj = 1 , ∀i, j. 

Since Zij ∈ { 0, 1}, we can write 

∥Y − Z∥1 = 2 X 

> i<j :Zij =1

(1 − Yij ) + 2 X 

> i<j :Zij =0

Yij , (13) which is affine in Y over C.It remains to represent the hypograph of eHA,D . For fixed Y , the LP (1)–(2) is feasible and bounded. Introduce nonnegative dual variables αi ≥ 0, βij ≥ 0, and ζij ≥ 0 for the constraints 

qi ≤ 1, zij ≤ 1, 1 − qi − qj − zij ≤ 0.

The Lagrangian is 

L(q, z ; α, β, ζ ) = X

> ij ∈E(A)

zij Yij + D X

> i

qi + X

> i

αi(qi − 1) + X

> ij ∈E(A)

βij (zij − 1) + X

> ij ∈E(A)

ζij (1 − qi − qj − zij )= X

> i

D + αi − X

> j:ij ∈E(A)

ζij 

 qi + X

> ij ∈E(A)

(Yij + βij − ζij )zij 

+ X

> ij ∈E(A)

ζij − X

> i

αi − X

> ij ∈E(A)

βij .

20 Taking the infimum over q, z ≥ 0 is finite if and only if 

D + αi − X

> j:ij ∈E(A)

ζij ≥ 0, Yij + βij − ζij ≥ 0. (14) Thus, strong duality gives 

eHA,D (Y ) = max 

> α,β,ζ ≥0

X

> ij ∈E(A)

ζij − X

> i

αi − X

> ij ∈E(A)

βij 

 ,

subject to the constraints (14). Consequently, t ≤ eHA,D (Y ) if and only if there exist nonnegative 

α, β, ζ satisfying the constraints (14) and 

t ≤ X

> ij ∈E(A)

ζij − X

> i

αi − X

> ij ∈E(A)

βij . (15) Therefore, using equation (13), we see that Vθ(Z) is the value of the SDP that maximizes 

t − λ X

> i<j

Yij + θn ∥Y − Z∥1

over variables ( Y, t, α, β, ζ ), subject to Y ∈ C , inequalities (14) and (15), and nonnegativity of the dual variables. This formulation has polynomially many variables and constraints, and the only nonlinear constraint is Y ⪰ 0. 

E.2 Proof of Lemma 4 

We first recall a standard formulation of the linear sum assignment problem: Given a weight matrix W = ( Wab ) ∈ RK×K , the maximum-weight assignment problem is max π∈SK

PKa=1 Wa,π (a).Equivalently, writing xab ∈ { 0, 1} for whether row a is assigned to column b, this is the integer program max 

> x∈{ 0,1}K×K
> K

X

> a=1
> K

X

> b=1

Wab xab 

subject to 

> K

X

> b=1

xab = 1 , a = 1 , . . . , K, (16) 

> K

X

> a=1

xab = 1 , b = 1 , . . . , K. 

The feasible binary matrices are exactly the permutation matrices, so the two formulations are identical. The Hungarian algorithm, or Kuhn–Munkres algorithm, solves the linear sum assignment problem in O(K3) arithmetic operations [45, Chapter 11.2]. Now note that if ρ ∈ SK , we have 

{π ◦ (ρ ◦ σ) : π ∈ SK } = {π ◦ σ : π ∈ SK }.

Hence, Can σ⋆ (ρ ◦ σ) = Can σ⋆ (σ). The canonical representative depends only on the orbit, not on the particular labeling used to describe it. Therefore, every orbit contains exactly one element of 21 Rσ⋆ , namely its canonical representative. If σ ∈ Rσ⋆ , then σ is the selected closest element of its orbit to σ⋆. Hence, we have 

h(σ, σ ⋆) = min 

> π∈SK

h(π ◦ σ, σ ⋆) = dorb ([ σ], [σ⋆]) .

It remains to prove polynomial-time computability. Define the confusion matrix 

Nab (σ, σ ⋆) := |{ i : σi = a, σ ⋆i = b}| , a, b ∈ [K].

For a permutation π, the number of agreements between π ◦ σ and σ⋆ is 

A(π) := 

> K

X

> a=1

Na,π (a)(σ, σ ⋆).

Indeed, vertices with σi = a receive label π(a) after relabeling, and exactly Na,π (a)(σ, σ ⋆) of them have σ⋆i = π(a). Therefore, we have 

h(π ◦ σ, σ ⋆) = n − A(π),

so closest relabelings are exactly the maximizers of A(π). We now encode the lexicographic tie-breaking into the same assignment problem. Set 

B := 2 K + 1 , La(σ) := X

> i:σi=a

Bn−i,

and define 

Wab := Bn+1 Nab (σ, σ ⋆) − bL a(σ).

For a permutation π, let 

F (π) := 

> K

X

> a=1

Wa,π (a) = Bn+1 A(π) − R(π), R(π) := 

> K

X

> a=1

π(a)La(σ).

We claim that a maximizer of F gives exactly Can σ⋆ (σ). We first prove that every maximizer of F maximizes A. For every π ∈ SK , since B = 2 K + 1, we have 0 ≤ R(π) = 

> n

X

> i=1

(π ◦ σ)iBn−i ≤ K

> n−1

X

> r=0

Br < B n,

where the identity follows from 

> K

X

> a=1

π(a)La(σ) = 

> K

X

> a=1

π(a) X

> i:σi=a

Bn−i =

> n

X

> i=1

(π ◦ σ)iBn−i.

If A(π) ≥ A(ρ) + 1, then 

F (π) − F (ρ) = Bn+1 {A(π) − A(ρ)} − { R(π) − R(ρ)} ≥ Bn+1 − R(π) + R(ρ) > B n+1 − Bn > 0.

Hence, A(π) > A (ρ) implies F (π) > F (ρ). Therefore, any maximizer of F must belong to arg max π A(π). Now restrict to M := arg max π∈SK A(π). For π ∈ M , note that A(π) is con-stant. Hence, maximizing F (π) over M is equivalent to minimizing R(π) over M:arg max  

> π∈M

F (π) = arg min  

> π∈M

R(π).

22 Finally, we have 

R(π) = 

> n

X

> i=1

(π ◦ σ)iBn−i.

Since B > K , minimizing this base-B encoding selects the lexicographically smallest relabeled vector π ◦ σ. Hence, a maximizer of F gives exactly Can σ⋆ (σ). Regarding exact computation, maximizing F (π) = PKa=1 Wa,π (a) is precisely the standard maximum-weight assignment problem (16) on the K × K matrix W = ( Wab ). This can be solved by the Hungarian algorithm in O(K3) arithmetic operations. Since B = 2 K + 1, 0 ≤ Nab ≤ n, and 0 ≤ La(σ) < B n, the integer weights Wab have polynomial encoding length in n. After computing the maximizing permutation πH, we have Can σ⋆ (σ) = πH ◦ σ. 

Thus, membership in Rσ⋆ is tested by computing πH ◦ σ, and checking whether it equals σ, which takes O(n) additional time. We conclude that membership in Rσ⋆ is testable in polynomial time using one K × K assignment problem. 

E.3 Proof of Lemma 5 

Relabel σ so that h(σ, τ ) = dorb ([ σ], [τ ]). Let 

Cx := {i : τi = x}, Dy := {i : σi = y},

and define the normalized confusion matrix Mxy := |Cx∩Dy | 

> n

. Every row and column of M has sum 1/K . Optimal alignment gives 

X

> x

Mxx ≥ X

> x

Mx,π (x), ∀π ∈ SK , (17) because we have relabeled σ to minimize the Hamming distance to τ . Now, KM is doubly stochas-tic. By the Birkhoff–von Neumann theorem [13, Theorem 1.1], we have 

KM = X

> π∈SK

wπPπ, wπ ≥ 0, X

> π

wπ = 1 ,

where Pπ is the permutation matrix. Using inequality (17) and multiplying by K, we have 

⟨KM, P π⟩ ≤ ⟨ KM, I ⟩ = Tr( KM ), ∀π, 

implying that 

∥KM ∥2 

> F

= ⟨KM, KM ⟩ = X

> π

wπ⟨KM, P π⟩ ≤ Tr( KM ).

Equivalently, we have P 

> x,y

M 2 

> xy

≤ 1

> K

P 

> x

Mxx . The number of ordered pairs that are in the same cluster under τ is Ks 2 = n2/K , and similarly for σ. The number of ordered pairs in the same cluster under both labelings is X

> x,y

|Cx ∩ Dy|2 = n2 X

> x,y

M 2

> xy

.

Thus, we have 1

n2 ∥Y σ − Y τ ∥1 = 2

K − 2 X

> x,y

M 2 

> xy

≥ 2

K 1 − X

> x

Mxx 

!

.

Finally, we have 1 − P 

> x

Mxx = h(σ,τ ) 

> n

= dorb ([ σ],[τ ])  

> n

, establishing the claim. 23 E.4 Proof of Lemma 6 

The certificate says that, for every Y ∈ C , we have 

eΨA,D (Y ) + θn ∥Y − Y σ⋆

∥1 ≤ eΨA,D (Y σ⋆

).

Taking Y = Y σ gives 

eTA,D (σ⋆) − eTA,D (σ) ≥ θn ∥Y σ − Y σ⋆

∥1 ≥ 2θK dorb ([ σ], [σ⋆]) ,

as required, by Lemma 5. For σ ∈ Rσ⋆ , Lemma 4 gives h(σ, σ ⋆) = dorb ([ σ], [σ⋆]). Thus, inequality (8) gives 

eTA,D (σ) − eTA,D (σ⋆) ≤ − 2θK h(σ, σ ⋆).

Multiplying by η and using κ = 2 ηθ/K , yields 

η[ eTA,D (σ) − eTA,D (σ⋆)] + κh (σ, σ ⋆) ≤ 0.

Hence, bA(σ) ≤ 1. 

E.5 Proof of Lemma 7 

By Lemma 6, we have bA(σ) ≤ 1 for every σ ∈ Σ ∩ Rσ⋆ . We now compute the law of the accepted canonical representative. Let G :=  1 + ( K − 1) e−κn. For every σ ∈ [K]n, the product proposal probability is 

νA(σ) = G−1e−κh (σ,σ ⋆). (18) Define the one-trial acceptance function 

aA(σ) := 

(exp 

n

η  eTA,D (σ) − eTA,D (σ⋆) + κh (σ, σ ⋆)

o

, σ ∈ Σ ∩ Rσ⋆ ,

0, otherwise .

For σ ∈ Σ ∩ Rσ⋆ , we have bA(σ) ≤ 1, so 0 ≤ aA(σ) ≤ 1. The probability that a single proposal trial is accepted is 

pA 

> acc

:= X

> σ∈[K]n

νA(σ)aA(σ).

Since aA(σ) = 0 outside Σ ∩ Rσ⋆ , using equation (18) gives 

pA 

> acc

= X

> σ∈Σ∩Rσ⋆

G−1e−κh (σ,σ ⋆) exp 

n

η  eTA,D (σ) − eTA,D (σ⋆) + κh (σ, σ ⋆)

o

= G−1e−η eTA,D (σ⋆) X

> σ∈Σ∩Rσ⋆

eη eTA,D (σ). (19) In particular, we have pA 

> acc

> 0, since σ⋆ ∈ Σ ∩ Rσ⋆ . Let T be the first accepted trial, and let bσR

denote the accepted canonical representative before the final random relabeling. Fix σ ∈ Σ ∩ Rσ⋆ .Then 24 P(bσR = σ) = 

> ∞

X

> t=1

P(T = t, bσR = σ) = 

> ∞

X

> t=1

(1 − pA

> acc

)t−1νA(σ)aA(σ) = νA(σ)aA(σ)

pA

> acc

. (20) Substituting equation (18), the definition of aA(σ), and equation (19) into equation (20), the factors involving G, κh (σ, σ ⋆), and eTA,D (σ⋆) cancel. We obtain 

P(bσR = σ) = eη eTA,D (σ)

P  

> τ∈Σ∩Rσ⋆

eη eTA,D (τ ) , σ ∈ Σ ∩ Rσ⋆ . (21) It remains to pass from canonical representatives to all labeled assignments in Σ. Since Σ is the exact-size labeling space, each label is used exactly s = n/K times. Therefore, no non-identity permutation in SK fixes a labeling σ ∈ Σ. Hence, every orbit [σ] = {π ◦ σ : π ∈ SK }

has exactly K! distinct elements. By Lemma 4, the set Rσ⋆ contains exactly one representative from each orbit. Consequently, the map SK × Rσ⋆ → Σ, such that ( π, r ) 7 → π ◦ r, is a bijection. Fix an arbitrary σ ∈ Σ. Let rσ ∈ Rσ⋆ be the unique canonical representative of the orbit [ σ]. By the bijection, there is a unique permutation πσ ∈ SK such that σ = πσ ◦ rσ. The final output is 

bσ = Π ◦ bσR, where Π ∼ Unif( SK ) is independent of bσR. Thus, we have 

P(bσ = σ) = X

> π∈SK

P Π = π, π ◦ bσR = σ = X

> π∈SK

P(Π = π) P bσR = π−1 ◦ σ. (22) For π̸ = πσ, the labeling π−1 ◦σ is not the canonical representative rσ. Hence, we have π−1 ◦σ / ∈ Rσ⋆ ,and since bσR ∈ Rσ⋆ almost surely, we have P bσR = π−1 ◦ σ = 0. Therefore, only the term π = πσ

contributes to equation (22), and P(bσ = σ) = 1 

> K!

P(bσR = rσ). Using equation (21), we have 

P(bσ = σ) = 1

K!

eη eTA,D (rσ )

P 

> r∈Σ∩Rσ⋆

eη eTA,D (r) . (23) Finally, note that eTA,D is invariant under global relabeling, because it depends on σ only through the cluster matrix Y σ. Hence, we have eTA,D (rσ) = eTA,D (σ). Moreover, since each orbit has exactly 

K! elements and eTA,D is constant on each orbit, we have 

X 

> τ∈Σ

eη eTA,D (τ ) = K! X

> r∈Σ∩Rσ⋆

eη eTA,D (r).

Combining this with equation (23) gives 

P(bσ = σ) = eη eTA,D (σ)

P  

> τ∈Σ

eη eTA,D (τ ) ,

which is the desired Gibbs distribution. Turning to the expected number of proposal trials, note that since ( K − 1) e−κ ≤ (4 n)−1, we have 

G = (1 + ( K − 1) e−κ)n ≤



1 + 14n

n

≤ e1/4.

25 Let 

ZA := X

> σ∈Σ∩Rσ⋆

exp {η[ eTA,D (σ) − eTA,D (σ⋆)] }.

The labeling σ⋆ is canonical and contributes 1, so ZA ≥ 1. From the equation (19), we have 

pacc = G−1ZA ≥ G−1. Thus, we have 

E[trials] = 1 /p acc ≤ G ≤ e1/4.

# F Proofs for Section 5 

F.1 Proof of Lemma 8 

First, we have a result based on a Chernoff bound for independent Bernoulli variables. It is a special case of [25, Theorem 1]. 

Lemma 16 (Theorem 1 in [25]) . Let Xrindep. 

∼ Bern( pr), for r ∈ [m]. Set X = Pmr=1 Xr and 

μ = EX. For every t > μ , we have 

P(X ≥ t) ≤

 eμ t

t

.

For a fixed vertex i, define Xi := P  

> j̸=i

Aij 1{σ0(j) = σ0(i)}. Then Xi is a sum of s − 1independent Bernoulli variables with parameter a/n , so 

μi := EXi ≤ sa n = aK .

For D = Cdeg a/K with Cdeg large enough, Lemma 16 with t = D implies P(Xi ≥ D) ≤ e−cD . A union bound gives 

P(EcD) ≤ ne −cC deg a/K ≤ exp {log( n) − cC deg A0 log( nK )}.

Increasing Cdeg and absorbing constants yields the desired result. 

F.2 Proof of Lemma 9 

We will use the following result, applicable to weighted adjacency matrices. It follows directly from Theorem 3 of [46], specialized to the case of balanced communities with no outlier nodes: 

Lemma 17. There is a universal constant CSDP > 0 with the following property: Consider a complete weighted graph with independent upper-triangular weights 0 ≤ Wij ≤ 1, whose distribution depends only on the labeling σ0 of nodes into K equal-sized communities. Let 

μin := EWij (i, j in the same communities ),μout := EWij (i, j in different communities ),

let γdist := μin − μout , and suppose all variances are bounded by σ2. If 

γdist s ≥ CSDP max 

n√σ2n, pσ2s log( n), log( n)

o

, (24) 

then, with probability at least 1 − n−c, the matrix X0 := 1 

> s

Y σ0 is the unique optimizer of 

max  

> X

⟨W, X ⟩ subject to X ⪰ 0, X1 ≤ 1, Tr( X) = K, X ≥ 0. (25) 26 For Y ∈ C , we have P 

> i<j

Yij = ns −n 

> 2

, so the penalty λ P 

> i<j

Yij is constant over C. Let 

Y 0 := Y σ0 . By Lemma 1, on ED, we have 

eΨA,D (Y 0) = X

> ij ∈E(A)

Y 0 

> ij

− λ X

> i<j

Y 0 

> ij

.

By inequality (12) in the proof of Lemma 1, we have 

eΨA,D (Y ) ≤ X

> ij ∈E(A)

Yij − λ X

> i<j

Yij ,

for all Y ∈ C . Since the penalty is constant over C, it suffices to show that 

X

> i<j

Aij Yij + θn ∥Y − Y 0∥1 ≤ X

> i<j

Aij Y 0 

> ij

, ∀Y ∈ C . (26) Using equation (13) with Z = Y 0, define δn := 2θn and the shifted weights 

Bij := 

(

Aij − δn, σ0(i) = σ0(j),Aij + δn, σ0(i)̸ = σ0(j). (27) Then inequality (26) is equivalent to saying that Y 0 maximizes Y 7 → P 

> i<j

Bij Yij over C. More explicitly, X

> i<j

Aij Yij + θn ∥Y − Y 0∥1 = C′ 

> 0

+ X

> i<j

Bij Yij ,

where C′ 

> 0

is independent of Y , and equality at Y = Y 0 is exact. It remains to prove that Y 0 is the unique maximizer of the shifted objective with high proba-bility. Since Bij may be slightly negative or slightly larger than one, define 

Wij := Bij + δn

1 + 2 δn

. (28) Adding δn to every off-diagonal weight and multiplying by the positive constant (1 + 2 δn)−1 does not change the maximizer over C, because P 

> i<j

Yij is constant over C. Moreover, 0 ≤ Wij ≤ 1. Conditional on σ0, the weights are independent and 

μin = a/n 

1 + 2 δn

, μout = b/n + 2 δn

1 + 2 δn

.

Thus, 

γdist := μin − μout = a − b − 4θn(1 + 2 δn) ≥ c · an ,

since a − b − 4θ ≥ cρa and a = o(n). Also, all variances are bounded by Ca/n , implying that 

γdist s ≥ c aK . On the other hand, we have 

√σ2n ≤ C√a, pσ2s log( n) ≤ C

r a log( n)

K .

Write S := a/K . The high-signal condition gives S ≥ A0 log( nK ) ≥ A0 log( n). Assumption 1 implies K ≲ log( n)/ log(log( n)), so for A0 large and all sufficiently large n, we have 

S ≥ C√KS = C√a, S ≥ CpS log( n), S ≥ C log( n).

27 Hence, condition (24) holds for the shifted weights W . Lemma 17 implies that X0 = Y 0/s is the unique optimizer of (25), on an event Ω P A , with P(Ω P A ) ≥ 1 − n−c. Since every Y ∈ C gives 

X = Y /s feasible for (25), Y 0 is the unique maximizer of the shifted objective over C. This proves inequality (26), and hence also the certification condition (7), on ED ∩ ΩP A . The probability bound follows from Pσ0 (EcD) ≤ (nK )−cdeg A0 , using Lemma 8. 

F.3 Proof of Theorem 1 

The first statement holds by construction. If the certificate branch is used, exactness follows from Lemma 7; if not, exactness follows from brute-force enumeration. Since the output law is πA for every input graph, privacy follows from Lemma 2. It remains to prove the runtime statement. By Lemma 17 and the same calculation as in Lemma 9, but without the shift, the candidate SDP recovers X0 = Y 0/s with probability at least 1 − n−c. Indeed, for the unshifted weights Wij = Aij , the mean gap is ( a − b)/n ≍ a/n and the variance is at most a/n , so condition (24) follows from Assumptions 1 and 2, with A0 large. Let Ωcand := {s bX = Y 0}

denote the candidate recovery event. The preceding argument shows that P(Ω cand ) ≥ 1 − n−c. By the proof of Lemma 9, on ED ∩ ΩP A , where 

Pσ0 (ED ∩ ΩP A ) ≥ 1 − n−c − (nK )−cA 0 ,

the eroded certificate succeeds at Y 0. Define Ω poly := Ω cand ∩ E D ∩ ΩP A . By a union bound, after adjusting constants, we have 

Pσ0 (Ω poly ) ≥ 1 − n−c − (nK )−cA 0 .

On Ω poly , the candidate satisfies Y σ⋆

= Y 0, and the eroded certificate succeeds at Y 0. Hence, we have 

Vθ(Y σ⋆

) = Vθ(Y 0) = eΨA,D (Y 0) = eΨA,D (Y σ⋆

).

Moreover, κ = η 2θK = ε 

> 2D
> 2θK

= εθ DK . Thus, choosing L sufficiently large, the assumption ε ≥

L log( nK ) implies κ ≥ log(4 n(K − 1)). On Ω poly , condition (7) holds, so the algorithm enters the certified branch. Lemma 7 gives an expected number of proposal trials at most e1/4, and each proposal can be processed in polynomial time. Indeed, each trial requires sampling n labels, and checking membership in Σ takes polynomial time. Next, checking canonicity can be done by solving one 

K × K assignment problem, and this can be executed in polynomial time by Lemma 4. Evaluating 

eTA,D is polynomial, by Remark 1. The candidate SDP and eroded certificate SDP are polynomial-size convex programs under the exact-oracle convention, by Lemma 3. Moreover, sampling a uniform permutation in Algorithm 2 is polynomial-time computable, by Remark 2. Let U denote the internal randomness of the mechanism. For a deterministic input graph H, let Runtime( H, U) denote the runtime of the full mechanism on input H. By the preceding argument, for every ω ∈ Ωpoly , the full mechanism run on A(ω) enters the certified rejection sampler branch. Lemma 7 gives an expected number of proposal trials at most e1/4, and each proposal is processed in polynomial time. The candidate recovery program is an SDP, and by Lemma 3, the eroded certificate check is also a polynomial-size SDP. Hence, since the candidate SDP and the eroded certificate SDP are polynomial-size convex programs, there exists a polynomial p(n) such that 

EU [Runtime( A(ω), U)] ≤ p(n), ∀ω ∈ Ωpoly .

28 Equivalently, we have Ωpoly ⊆ { ω : EU [Runtime( A(ω), U)] ≤ p(n)} .

Since 

Pσ0 (Ω poly ) ≥ 1 − n−c − (nK )−cA 0 ,

we obtain 

Pσ0 (EU [Runtime( A, U)] ≤ p(n)) ≥ 1 − n−c − (nK )−cA 0 .

This proves the high-probability polynomial expected runtime claim and completes the proof. 

F.4 Proof of Theorem 2 

F.4.1 Supporting lemmas 

The first lemma records the entropy estimate needed for the peeling argument. It is obtained from the proof of Lemma A.5 in [34], specialized to our equal-sized community setting. 

Lemma 18. Under Assumptions 1 and 2, there are constants C0, C 1, c 0 > 0 such that, with prob-ability at least 1 − e−c0nI/K , we have 

sup 

> u≥0

{log ( |{ σ ∈ Σ : TA(σ) ≥ TA(σ0) − u}| ) − Bu } ≤ C1 log( nK ), (29) 

where B := C0 K log( nK ) 

> nI

.Proof. The proof of this result follows from the proof of Lemma A.5 in [34]. Although that lemma is stated for the near-maximum set 

Su(A) = {σ : TA(σ) ≥ max   

> τ∈Σ

TA(τ ) − u},

their proof introduces eSu(A) = {σ : TA(σ) ≥ TA(σ0) − u}

and proves the displayed entropy bound for eSu(A), using Su(A) ⊆ eSu(A). The next result is the analog of the “peeling result,” Lemma A.6, in [34]. 

Lemma 19. Suppose Assumptions 1 and 2 hold. Let bσ ∼ πA. Let B = C0 K log( nK ) 

> nI

, and suppose 

γ0 := η − 2B > 0. For 0 < α < 1/2, set 

u⋆(α) := C2 log( nK ) + log(4 /α )

γ0

, (30) 

where C2 is a sufficiently large absolute constant. Then 

P



ED ∩

eTA,D (bσ) < max  

> τ∈Σ

eTA,D (τ ) − u⋆(α)

 

≤ α + e−c0nI/K .

Proof. Condition on a graph A for which ED and the event (29) both hold. For u ≥ 0, define 

Stilde  

> u

(A) := 

n

σ ∈ Σ : eTA,D (σ) ≥ max 

> τ

eTA,D (τ ) − u

o

.

By Lemma 1, on ED, we have 

Stilde  

> u

(A) ⊆ { σ ∈ Σ : TA(σ) ≥ TA(σ0) − u} .

29 By Lemma 18, we have log( |S tilde  

> u

(A)|) ≤ Bu + C1 log( nK ). Let M := max τ eTA,D (τ ). A standard exponential-mechanism peeling bound implies, for any s > 0, that 

P(M − eTA,D (bσ) > s | A) ≤ X

> ℓ≥1

Stilde (ℓ+1) s(A) exp {− ηℓs } ≤ (nK )C1

X

> ℓ≥1

exp {B(ℓ + 1) s − ηℓs }

= ( nK )C1 exp {− (η − 2B)s}

1 − exp {− (η − B)s} . (31) Indeed, for ℓ ≥ 1, set Aℓ := 

n

σ ∈ Σ : ℓs < M − eTA,D (σ) ≤ (ℓ + 1) s

o

. Then 

{M − eTA,D (bσ) > s } ⊆ [

> ℓ≥1

{bσ ∈ A ℓ}.

For σ ∈ A ℓ, we have 

πA(σ) = exp {η eTA,D (σ)}

P  

> τ∈Σ

exp {η eTA,D (τ )} ≤ exp {η(M − ℓs )}

exp {ηM } = e−ηℓs .

We also have Aℓ ⊆ S tilde (ℓ+1) s(A). This proves the first inequality in (31). The second inequality of (31) follows from the entropy bound on Stilde  

> u

(A) with u = ( ℓ+1) s. Taking s = u⋆(α) and increasing 

C2 if necessary, we have (nK )C1 exp {− (η − 2B)s}

1 − exp {− (η − B)s} ≤ α, 

since η − B ≥ η − 2B = γ0, and α < 1/2. Averaging over A and adding the probability e−c0nI/K 

that inequality (29) fails proves the claim. The next result gives a bound on Er(σ0, bσ), provided bσ satisfies TA(bσ) ≥ TA(σ0) − u. This is similar in spirit to Lemma A.1 of [34], where they prove their result under the condition TA(bσ) ≥

max τ ∈Σ TA(τ ) − u.

Lemma 20. Suppose Assumptions 1 and 2 hold, with Cmg sufficiently small, and A0 sufficiently large. Let 

t⋆ := 12 log a  1 − bn



b  1 − an

!

.

Let bσ ∈ Σ be a (randomized) estimator. Suppose that for some deterministic u ≥ 0, we have 

TA(bσ) ≥ TA(σ0) − u, almost surely. Then 

Er(σ0, bσ) ≤ exp 



−(1 + o(1)) nI K + t⋆u



.

Proof. All probabilities and expectations are under the SBM with ground truth σ0, together with any auxiliary randomness used by bσ. For m = 1 , . . . , n , define 

Em(u) := {∃ σ ∈ Σ : dorb ([ σ], [σ0]) = m and TA(σ) ≥ TA(σ0) − u} .

Since TA(bσ) ≥ TA(σ0) − u almost surely and bσ ∈ Σ, we have 

{dorb ([ bσ], [σ0]) = m} ⊆ E m(u), m = 1 , . . . , n. 

30 Therefore, we have 

Er(σ0, bσ) ≤ 1

n

> n

X

> m=1

m P(Em(u)) . (32) Both TA(σ) and dorb ([ σ], [σ0]) are constant on [ σ]. Let 

Gm := {[σ] : σ ∈ Σ, d orb ([ σ], [σ0]) = m}.

For each Γ ∈ G m, choose one representative σΓ ∈ Γ. Then 

Em(u) = [

> Γ∈G m

{TA(σΓ) ≥ TA(σ0) − u}.

Fix σ ∈ Σ, and note that P 

> i<j

1{σ(i) = σ(j)} = K n/K 

> 2

 is independent of σ. Hence the λ-penalty cancels in TA(σ) − TA(σ0). Define 

S(σ; σ0) := {i < j : σ0(i) = σ0(j), σ (i)̸ = σ(j)},M (σ; σ0) := {i < j : σ0(i)̸ = σ0(j), σ (i) = σ(j)},

and 

α(σ; σ0) := |S(σ; σ0)|, γ(σ; σ0) := |M (σ; σ0)|.

Exact balance gives α(σ; σ0) = γ(σ; σ0). Indeed, the total number of within-community pairs is the same under σ and σ0, so the number of true within-pairs lost must equal the number of true between-pairs gained. Under σ0, we have 

TA(σ) − TA(σ0) d

=

> γ(σ;σ0)

X

> ℓ=1

Xℓ −

> α(σ;σ0)

X

> ℓ=1

Yℓ,

where Xℓ iid 

∼ Ber( b/n ) and Yℓ iid 

∼ Ber( a/n ) are independent. By Markov’s inequality, we have 

Pσ0 (TA(σ) − TA(σ0) ≥ − u) ≤ et⋆uEσ0 exp t⋆ TA(σ) − TA(σ0) .

Using α = γ and the definition of the order-1 /2 R´ enyi divergence I, we have 

Eσ0 exp t⋆ TA(σ) − TA(σ0) =

h

Eet⋆X1 Ee−t⋆Y1

iα(σ;σ0)

= e−Iα (σ;σ0).

Thus, with Q(σ, σ 0) := e−Iα (σ;σ0), we have 

Pσ0 (TA(σ) − TA(σ0) ≥ − u) ≤ et⋆uQ(σ, σ 0).

Taking a union bound over orbits gives 

P(Em(u)) ≤ et⋆u X

> Γ∈G m

Q(σΓ, σ 0).

Substituting this into inequality (32), we obtain 

Er(σ0, bσ) ≤ et⋆u 1

n

> n

X

> m=1

m X

> Γ∈G m

Q(σΓ, σ 0). (33) 31 It remains to prove that 1

n

> n

X

> m=1

m X

> Γ∈G m

Q(σΓ, σ 0) ≤ exp 



−(1 + o(1)) nI K



. (34) Write SI := Signal = nI K . Under the sparse constant-SNR regime (Assumption 2), we have I = o(1) and nI ≍ a. By Assumption 2, after increasing its absolute constant if necessary, we may assume that SI ≥ A1 log( nK ) for an arbitrarily large fixed constant A1. Also, Assumption 1 implies 

K = o(n) and log( K) = o(SI ). First consider K = 2. Since distance is computed modulo a global relabeling, we have Gm = ∅

for m > n/ 2. For each Γ ∈ G m, choose σΓ so that h(σΓ, σ 0) = m. The two-community split/merge identity gives 

α(σΓ; σ0) + γ(σΓ; σ0) = m(n − m).

Since exact balance gives α = γ, we have α(σΓ; σ0) = m(n−m)2 . Thus, we have 

Q(σΓ, σ 0) = exp 



− I

2 m(n − m)



.

Moreover, |G m| ≤   nm

, implying that 1

n

> n

X

> m=1

m X

> Γ∈G m

Q(σΓ, σ 0) ≤ 1

n

> ⌊n/ 2⌋

X

> m=1

m

 nm



exp 



− I

2 m(n − m)



,

where SI = nI/ 2. The m = 1 term is at most exp 



− I

2 (n − 1) 



= exp {− (1 + o(1)) SI }.

For 2 ≤ m ≤ n/ 4, we have I 

> 2

m(n − m) ≥ 34 SI , while mn

  nm

 ≤   en m

m. Taking A1 large enough gives 

 en m

m

e−3mS I /4 ≤ e−5mS I /8,

uniformly over 2 ≤ m ≤ n/ 4. Therefore, the contribution of 2 ≤ m ≤ n/ 4 is o(e−SI ). For 

n/ 4 < m ≤ n/ 2, using m/n ≤ 1, the crude bounds P

> n/ 4<m ≤n/ 2

  nm

 ≤ 2n and m(n − m) ≥ n2/8give a total contribution bounded by exp {n log(2) − nS I /8} = o(e−SI ).

Thus, inequality (34) holds for K = 2. Now assume K ≥ 3. By Lemma B.5 in [34], specialized to exact balance, we have for every 

σ ∈ Σ with dorb ([ σ], [σ0]) = m, that 

α(σ; σ0) ≥



nm K − m2, m ≤ n

2K ,c⋆

nm K , m > n

2K ,

where c⋆ > 0 is an absolute constant. This follows from the usual bound on α ∧ γ, because exact balance gives α = γ. We also use the elementary bound |G m| ≤   enK m

m. Indeed, an orbit at distance m has a representative obtained by choosing the m moved vertices and assigning each of them one of at most K labels. Split the sum into three ranges: 32 1. If 1 ≤ m ≤ n/ (4 K), then α(σΓ; σ0) ≥ 3nm  

> 4K

, and hence, 

mn |G m|e−Iα (σΓ;σ0) ≤ mn

 enK m

m

e−3mS I /4.

Since SI ≥ A1 log( nK ) with A1 sufficiently large, we have 

 enK m

m

e−3mS I /4 ≤ e−5mS I /8.

The m = 1 term is at most Ke −3SI /4. This is not sharp enough by itself, so for m = 1, we use the sharper bound α ≥ n/K − 1, which gives 1

n |G 1|e−Iα ≤ K exp {− I(n/K − 1) } = exp {− (1 + o(1)) SI + log( K)} = exp {− (1 + o(1)) SI },

because log( K) = o(SI ). The remaining terms m ≥ 2 in this range are o(e−SI ). 2. If n/ (4 K) < m ≤ n/ (2 K), then α(σΓ; σ0) ≥ nm  

> 2K

, so 

mn |G m|e−Iα ≤

 enK m

m

e−mS I /2.

Again, for A1 sufficiently large, we have 

 enK m

m

e−mS I /2 ≤ e−mS I /4.

Since m > n/ (4 K) and K = o(n), the total contribution of this range is o(e−SI ). 3. Finally, if m > n/ (2 K), then α(σΓ; σ0) ≥ c⋆ nm K and enK m ≤ 2eK 2, Therefore, we have 

mn |G m|e−Iα ≤ exp {m log(2 eK 2) − c⋆mS I }.

Since log( K) = o(SI ), for all sufficiently large n, we have log(2 eK 2) ≤ c⋆ 

> 2

SI . Thus, each term in this range is at most e−c⋆mS I /2. Since m > n/ (2 K) and K = o(n), the whole contribution is o(e−SI ). Combining the three ranges gives inequality (34) for K ≥ 3, implying the desired bound. Finally, substituting inequality (34) into inequality (33) yields 

Er(σ0, bσ) ≤ exp 



−(1 + o(1)) nI K + t⋆u



.

Taking the supremum over σ0 ∈ Σ proves the claim. 

F.4.2 Proof of theorem 

By Theorem 1, the output law of Algorithm 1 is exactly πA, so Lemma 19 applies. Since D =

Cdeg a/K and nI ≍ a, we have 

B = C0

K log( nK )

nI ≍ K log( nK )

a .

33 Also, η = ε 

> 2D

≍ εK a . Thus, for L sufficiently large, ε ≥ L log( nK ) implies η ≥ 4B, so γ0 = η − 2B ≍

η ≍ εD . Choose α = ( nK )−1e−cε , with c > 0 small. Then equation (30) gives 

u⋆(α) ≲ D log( nK ) + cε ε ≍ aK

 log( nK )

ε + c



.

Since nI/K ≍ a/K and t⋆ = Θ(1), choosing L large and then c small gives 

t⋆u⋆(α) ≤ c′ nI K , (35) for a sufficiently small constant c′ > 0. Let 

U := ED ∩

n eTA,D (bσ) ≥ max 

> τ

eTA,D (τ ) − u⋆(α)

o

.

By Lemmas 8 and 19, we have P(U c) ≤ α + e−c0nI/K + ( nK )−cdeg A0 . On U , Lemma 1 gives 

TA(bσ) ≥ TA(σ0) − u⋆(α). Now define a hybrid estimator 

eσ(A) := 

(bσ(A), A ∈ U, σraw (A), A / ∈ U, 

where σraw (A) ∈ arg max σ∈Σ TA(σ). Then TA(eσ) ≥ TA(σ0) − u⋆(α) always. Lemma 20, and (35) yield 

Er(σ0, eσ) ≤ exp 



−c1

nI K



,

for some c1 > 0. Since r ≤ 1, we have Er(σ0, bσ) ≤ Er(σ0, eσ) + P(U c). Finally, we have 

α = ( nK )−1e−cε , e−c0nI/K ≤ exp 



−c nI K



,

and the within-degree error is bounded by Lemma 8. Absorbing constants proves the bound (9). 

# G Proofs for Section 6 

G.1 Proof of Lemma 10 

Given an input graph A, Algorithm 1 produces a candidate σ⋆(A) ∈ Σ. Define 

C(A) := 1

n

Vθ(Y σ⋆(A)) = eΨA,D (Y σ⋆(A)) and κ ≥ log(4 n(K − 1)) 

o

.

When C(A) = 1, the modified algorithm runs the truncated certified rejection sampler (Algorithm 3) with candidate σ⋆(A). When C(A) = 0, the modified algorithm samples exactly from πA by brute-force enumeration. Thus, we have 

Q(M ) 

> A

=

(law of Algorithm 3 with candidate σ⋆(A), C(A) = 1 ,πA, C(A) = 0 .

We begin with the following result: 34 Lemma 21. Suppose C(A) = 1 , and let RA denote the uniform distribution on the orbit [σ⋆(A)] .Then RA(σ) ≤ e1/4πA(σ), for all σ ∈ Σ, and 

Q(M ) 

> A

= (1 − rA

> acc

)πA + rA

> acc

RA, (36) 

where rA 

> acc

≤ rM .Proof. Suppose C(A) = 1, and abbreviate σ⋆ = σ⋆(A). Let G =  1 + ( K − 1) e−κn. The proposal law in one trial is 

νA(σ) = G−1e−κh (σ,σ ⋆), ∀σ ∈ [K]n,

and the probability of acceptance is 

aA(σ) := 



exp 

n

η eTA,D (σ) − eTA,D (σ⋆) + κh (σ, σ ⋆)

o

, σ ∈ Σ ∩ Rσ⋆ ,

0, otherwise .

Since C(A) = 1, Lemma 6 implies 0 ≤ aA(σ) ≤ 1. Hence, the one-trial acceptance probability is 

pA 

> acc

= P 

> σ∈[K]n

νA(σ)aA(σ). For σ ∈ Σ ∩ Rσ⋆ , we have 

νA(σ)aA(σ) = G−1 exp 

n

η eTA,D (σ) − eTA,D (σ⋆)o

,

implying that pA 

> acc

= G−1ZA, where 

ZA := X

> σ∈Σ∩Rσ⋆

exp 

n

η eTA,D (σ) − eTA,D (σ⋆)o

.

Since σ⋆ ∈ Σ ∩ Rσ⋆ , we clearly have ZA ≥ 1. Let BM denote the event that at least one of the first M proposal trials is accepted. Then 

P(BcM ) = (1 − pA

> acc

)M := rA

> acc

.

We next compute the law conditional on BM . Let bσR denote the accepted canonical representative before the final random relabeling. For σ ∈ Σ ∩ Rσ⋆ , we have 

P(bσR = σ | BM ) = 

PMt=1 (1 − pA

> acc

)t−1νA(σ)aA(σ)1 − (1 − pA

> acc

)M = νA(σ)aA(σ)

pA

> acc

.

Substituting the formulas above gives 

P(bσR = σ | BM ) = exp {η eTA,D (σ)}

P  

> τ∈Σ∩Rσ⋆

exp {η eTA,D (τ )} , ∀σ ∈ Σ ∩ Rσ⋆ .

The uniform random relabeling maps this canonical Gibbs law to the full Gibbs law πA, exactly as in Lemma 7. Hence, conditional on BM , the output has law πA. Now, recall that RA is the uniform distribution on the orbit [ σ⋆], which has cardinality K! since σ⋆ ∈ Σ is exact-balanced. Conditional on BcM , the fallback output is Π ◦ σ⋆ with Π ∼ Unif( SK ), whose law is RA. Thus, we have equality (36). It remains to prove the two bounds. Since C(A) = 1 implies κ ≥ log(4 n(K − 1)), we have 

G =  1 + ( K − 1) e−κn ≤



1 + 14n

n

≤ e1/4.

35 Since pA 

> acc

= G−1ZA and ZA ≥ 1, we obtain pA 

> acc

≥ G−1 ≥ e−1/4, so 

rA 

> acc

= (1 − pA

> acc

)M ≤  1 − e−1/4M = rM .

Finally, note that 

X 

> τ∈Σ

exp {η eTA,D (τ )} = K! X 

> τ∈Σ∩Rσ⋆

exp {η eTA,D (τ )}.

Thus, for σ ∈ [σ⋆], we have πA(σ) = 1 

> K!ZA

. Since RA(σ) = 1 

> K!

on [ σ⋆], we have 

RA(σ)

πA(σ) = ZA.

Also, pA 

> acc

= G−1ZA ≤ 1, so ZA ≤ G ≤ e1/4. Therefore, RA(σ) ≤ e1/4πA(σ) on [ σ⋆]. Outside [ σ⋆], we have RA(σ) = 0, so the same inequality is trivial. This completes the proof. If C(A) = 0, then Q(M ) 

> A

= πA, and the claim is immediate. Suppose C(A) = 1. By Lemma 21, we have 

Q(M ) 

> A

= (1 − rA

> acc

)πA + rA

> acc

RA, rA 

> acc

≤ rM , RA ≤ e1/4πA.

Since RA ≥ 0, we have 

Q(M ) 

> A

(σ) ≥ (1 − rA

> acc

)πA(σ) ≥ (1 − rM )πA(σ).

Also, 

Q(M ) 

> A

(σ) ≤ (1 − rA

> acc

)πA(σ) + rA

> acc

e1/4πA(σ)=  1 + ( e1/4 − 1) rA

> acc

πA(σ)

≤  1 + ( e1/4 − 1) rM

πA(σ).

The definition of ξM gives the two-sided likelihood-ratio bound. Now let A ∼v A′ and let S ⊆ Σ. We have Q(M ) 

> A

(S) ≤ eξM πA(S) and πA′ (S) ≤ eξM Q(M ) 

> A′

(S). Since η = ε/ (2 D), by Lemma 2, we have πA(S) ≤ eεπA′ (S). Combining the three inequalities proves the claim. 

G.2 Proof of Theorem 3 

Fix an input graph A and a true labeling σ0 ∈ Σ. Since the loss r(σ0, σ ) is nonnegative, Lemma 10 gives 

EQ(M )

> A

r(σ0, σ ) = X

> σ∈Σ

r(σ0, σ )Q(M ) 

> A

(σ) ≤ eξM

X

> σ∈Σ

r(σ0, σ )πA(σ) = eξM EπA r(σ0, σ ).

Taking an expectation over A, conditioned on σ0, yields Eσ0 r(σ0, bσ(M )) ≤ eξM Eσ0 EπA r(σ0, σ ). Taking the supremum over σ0 ∈ Σ and applying Theorem 2 to the Gibbs law πA gives the desired bound. 36 G.3 Proof of Corollary 1 

First note that since rM ≤ 1/2, we have − log(1 − rM ) ≤ 2rM , and log  1 + ( e1/4 − 1) rM

 ≤ (e1/4 − 1) rM ≤ 2rM . (37) So, ξM ≤ 2rM ≍ rM = e−Θ( M ). We look at each claim in turn: The first claim follows from Lemma 10 directly. For the second claim, note that by the lines of argument in the proof of Theorem 1, we have Ωpoly , with Pσ0 (Ω poly ) ≥ 1 − n−c − (nK )−c′A0 , and C(A) = 1 on Ω poly . On this event, Algorithm 1 enters the truncated certified branch, and Algorithm 3 is used. Since M = O(poly( n)), the runtime of the algorithm is polynomial-time, by construction. For the final claim, since we assume ε ≥ L log( nK ), Theorem 3 implies sup 

> σ0∈Σ

Eσ0 r(σ0, bσ(M )) ≤ eξM



exp 



−c1

nI K



+ 1

nK e−c2ε + ( nK )−c3A0



.

Since ξM = e−Θ( M ) ≤ 1 and nI K = Ω(log( nK )), we have −c1 nI K + ξM ≤ − c4 nI K , for some c4 > 0. Since ε = Ω(log( n)), we have −c2ε + ξM ≤ − c5ε, for some c5 > 0. Lastly, there exists c6 > 0 such that ( nK )−c3A0 eξM ≤ (nK )−c6A0 .

# References 

[1] M. Abadi, A. Chu, I. Goodfellow, H. B. McMahan, I. Mironov, K. Talwar, and L. Zhang. Deep learning with differential privacy. In Proceedings of the 2016 ACM SIGSAC Conference on Computer and Communications Security , pages 308–318, 2016. [2] E. Abbe. Community detection and stochastic block models: Recent developments. Journal of Machine Learning Research , 18(177):1–86, 2018. [3] J. Awan and V. Rao. Privacy-aware rejection sampling. Journal of Machine Learning Research ,24(74):1–32, 2023. [4] J. Blocki, A. Blum, A. Datta, and O. Sheffet. The Johnson-Lindenstrauss transform itself pre-serves differential privacy. In 2012 IEEE 53rd Annual Symposium on Foundations of Computer Science , pages 410–419. IEEE, 2012. [5] S. Bombari, I. Seroussi, and M. Mondelli. Better rates for private linear regression in the proportional regime via aggressive clipping. arXiv preprint arXiv:2505.16329 , 2025. [6] M. Bun and T. Steinke. Concentrated differential privacy: Simplifications, extensions, and lower bounds. In Theory of Cryptography Conference , pages 635–658. Springer, 2016. [7] M. Bun, J. Ullman, and S. Vadhan. Fingerprinting codes and the price of approximate dif-ferential privacy. In Proceedings of the Forty-Sixth Annual ACM Symposium on Theory of Computing , pages 1–10, 2014. [8] T. T. Cai, Y. Wang, and L. Zhang. The cost of privacy: Optimal rates of convergence for parameter estimation with differential privacy. The Annals of Statistics , 49(5):2825–2850, 2021. 37 [9] M. X. Cao, N. Ramakrishnan, M. Berta, and M. Tomamichel. Channel simulation: Finite blocklengths and broadcast channels. IEEE Transactions on Information Theory , 70(10):6780– 6808, 2024. [10] A. Chakraborty, S. Chatterjee, and S. Nandy. PriME: Privacy-aware membership profile estimation in networks. arXiv preprint arXiv:2406.02794 , 2024. [11] H. Chen, V. Cohen-Addad, T. d’Orsi, A. Epasto, J. Imola, D. Steurer, and S. Tiegel. Private estimation algorithms for stochastic block models and mixture models. Advances in Neural Information Processing Systems , 36:68134–68183, 2023. [12] H. Chen, J. Ding, T. d’Orsi, Y. Hua, C. Liu, and D. Steurer. Private graphon estimation via sum-of-squares. In Proceedings of the 56th Annual ACM Symposium on Theory of Computing ,pages 172–182, 2024. [13] H. Chen, L. Qi, L. Caccetta, and G. Zhou. Birkhoff-von Neumann theorem and decomposition for doubly stochastic tensors. Linear Algebra and its Applications , 583:119–133, 2019. [14] Y. Chen and R. Eldan. Localization schemes: A framework for proving mixing bounds for Markov chains. In 2022 IEEE 63rd Annual Symposium on Foundations of Computer Science (FOCS) , pages 110–122. IEEE, 2022. [15] T. M. Cover. Elements of Information Theory . John Wiley & Sons, 1999. [16] Z. Ding, Y. Wang, G. Wang, D. Zhang, and D. Kifer. Detecting violations of differential pri-vacy. In Proceedings of the 2018 ACM SIGSAC Conference on Computer and Communications Security , pages 475–489, 2018. [17] J. C. Duchi, M. I. Jordan, and M. J. Wainwright. Minimax optimal procedures for locally private estimation. Journal of the American Statistical Association , 113(521):182–201, 2018. [18] C. Dwork. Differential privacy. In M. Bugliesi, B. Preneel, V. Sassone, and I. Wegener, editors, 

Automata, Languages and Programming , pages 1–12, Berlin, Heidelberg, 2006. Springer Berlin Heidelberg. [19] C. Dwork. Differential privacy: A survey of results. In International Conference on Theory and Applications of Models of Computation , pages 1–19. Springer, 2008. [20] C. Dwork, F. McSherry, K. Nissim, and A. Smith. Calibrating noise to sensitivity in private data analysis. In Theory of Cryptography Conference , pages 265–284. Springer, 2006. [21] W. R. Gilks and P. Wild. Adaptive rejection sampling for Gibbs sampling. Journal of the Royal Statistical Society: Series C (Applied Statistics) , 41(2):337–348, 1992. [22] A. Gupta, A. Roth, and J. Ullman. Iterative constructions and private data release. In Theory of Cryptography Conference , pages 339–356. Springer, 2012. [23] M. Hay, C. Li, G. Miklau, and D. Jensen. Accurate estimation of the degree distribution of private networks. In 2009 Ninth IEEE International Conference on Data Mining , pages 169–178. IEEE, 2009. [24] J. Hehir, A. Slavkovi´ c, and X. Niu. Consistent spectral clustering of network block models under local differential privacy. The Journal of Privacy and Confidentiality , 12(2), 2022. 38 [25] W. Hoeffding. Probability inequalities for sums of bounded random variables. Journal of the American statistical association , 58(301):13–30, 1963. [26] P. W. Holland, K. B. Laskey, and S. Leinhardt. Stochastic blockmodels: First steps. Social Networks , 5(2):109–137, 1983. [27] J. Hubbard. Calculation of partition functions. Physical Review Letters , 3:77–78, 1959. [28] M. Jagielski, J. Ullman, and A. Oprea. Auditing differentially private machine learning: How private is private SGD? Advances in Neural Information Processing Systems , 33:22205–22216, 2020. [29] G. Kamath, J. Li, V. Singhal, and J. Ullman. Privately learning high-dimensional distributions. In Conference on Learning Theory , pages 1853–1902. PMLR, 2019. [30] G. Kamath, A. Mouzakis, M. Regehr, V. Singhal, T. Steinke, and J. Ullman. A bias-accuracy-privacy trilemma for statistical estimation. Journal of the American Statistical Association ,120(552):2338–2349, 2025. [31] V. Karwa, S. Raskhodnikova, A. Smith, and G. Yaroslavtsev. Private analysis of graph struc-ture. Proceedings of the VLDB Endowment , 4(11):1146–1157, 2011. [32] V. Karwa and A. B. Slavkovi´ c. Differentially private graphical degree sequences and syn-thetic graphs. In International Conference on Privacy in Statistical Databases , pages 273–285. Springer, 2012. [33] V. Karwa and S. Vadhan. Finite sample differentially private confidence intervals. arXiv preprint arXiv:1711.03908 , 2017. [34] O. Klopp and I. Zadik. Node-private community detection in stochastic block models. arXiv preprint arXiv:2604.09078 , 2026. [35] A. Koskela, J. J¨ alk¨ o, and A. Honkela. Computing tight differential privacy guarantees using FFT. In International Conference on Artificial Intelligence and Statistics , pages 2560–2569. PMLR, 2020. [36] A. Koskela, M. Seif, and A. J. Goldsmith. On the price of differential privacy for spectral clus-tering over stochastic block models. IEEE Transactions on Network Science and Engineering ,13:5176–5191, 2025. [37] T. Lattimore and C. Szepesv´ ari. Bandit Algorithms . Cambridge University Press, 2020. [38] L. Marchis, E. D’souza, T. Fl´ ıdr, and P. Loh. Node-private community estimation in stochastic block models: Tractable algorithms and lower bounds. arXiv preprint arXiv:2605.15943 , 2026. [39] F. McSherry and K. Talwar. Mechanism design via differential privacy. In 48th Annual IEEE Symposium on Foundations of Computer Science (FOCS’07) , pages 94–103. IEEE, 2007. [40] K. Minami, H. Arai, I. Sato, and H. Nakagawa. Differential privacy without sensitivity. Ad-vances in Neural Information Processing Systems , 29, 2016. [41] M. S. Mohamed, D. Nguyen, A. Vullikanti, and R. Tandon. Differentially private community detection for stochastic block models. In International Conference on Machine Learning , pages 15858–15894. PMLR, 2022. 39 [42] M. Nasr, J. Hayes, T. Steinke, B. Balle, F. Tram` er, M. Jagielski, N. Carlini, and A. Terzis. Tight auditing of differentially private machine learning. In 32nd USENIX Security Symposium (USENIX Security 23) , pages 1631–1648, 2023. [43] D. Nguyen and A. K. Vullikanti. Differentially private exact recovery for stochastic block models. In International Conference on Machine Learning , pages 37798–37839. PMLR, 2024. [44] K. Nissim, S. Raskhodnikova, and A. Smith. Smooth sensitivity and sampling in private data analysis. In Proceedings of the Thirty-Ninth Annual ACM Symposium on Theory of Computing ,pages 75–84, 2007. [45] C. H. Papadimitriou and K. Steiglitz. Combinatorial Optimization: Algorithms and Complex-ity . Courier Corporation, 1998. [46] A. Pirinen and B. Ames. Exact clustering of weighted graphs via semidefinite programming. 

Journal of Machine Learning Research , 20(30):1–34, 2019. [47] T. Steinke, M. Nasr, and M. Jagielski. Privacy auditing with one (1) training run. Advances in Neural Information Processing Systems , 36:49268–49280, 2023. [48] R. L. Stratonovich. On a method of calculating quantum distribution functions. Soviet Physics Doklady , 2:416, 1958. [49] Y.-X. Wang, S. Fienberg, and A. Smola. Privacy for free: Posterior sampling and stochastic gradient Monte Carlo. In International Conference on Machine Learning , pages 2493–2502. PMLR, 2015. [50] A. Y. Zhang and H. H. Zhou. Minimax rates of community detection in stochastic block models. The Annals of Statistics , pages 2252–2280, 2016. [51] Y. Zhu, J. Dong, and Y.-X. Wang. Optimal accounting of differential privacy via characteristic function. In International Conference on Artificial Intelligence and Statistics , pages 4782–4817. PMLR, 2022. 40