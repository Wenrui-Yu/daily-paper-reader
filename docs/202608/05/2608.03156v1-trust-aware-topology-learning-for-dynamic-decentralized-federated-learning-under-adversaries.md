---
title: Trust-Aware Topology Learning for Dynamic Decentralized Federated Learning under Adversaries
title_zh: 对抗环境下动态去中心化联邦学习的信任感知拓扑学习
authors: "Shubham Vaishnav, Murtaza Rangwala, Ali Beikmohammadi, Sindri Magnússon, Rajkumar Buyya"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03156v1"
tags: ["query:daily"]
score: 8.0
evidence: 对抗投毒下动态去中心化联邦学习的信任感知拓扑学习
tldr: 针对动态移动去中心化联邦学习中的恶意攻击，本文提出DMTT协议，在MURMURA基础上结合证据深度学习与拓扑信任筛选。设备通过链路可靠性估计、签名拓扑声明、见证佐证及Beta信任模型构建局部视图，仅聚合信任筛选后的协作方。证明筛选矩阵可将拜占庭影响限制在可消失残差内，并在UCI HAR和PAMAP2上优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03156v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1843, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03156v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 124, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03156v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1874, \"height\": 1650, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03156v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 614, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03156v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03156v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 782, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03156v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 1071, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03156v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 933, \"height\": 324, \"label\": \"Table\"}]"
motivation: 动态去中心化联邦学习中，攻击者可同时污染模型更新和拓扑信息，现有鲁棒聚合器在拓扑操纵下失效。
method: 基于MURMURA扩展，结合证据深度学习、拓扑信任模型与复合评分筛选协作方，实现无协调器的分布式协议。
result: "在100客户端、10-80%攻击比例下，准确率保持0.862/0.829以上，静态与动态FedAvg及鲁棒聚合器均失败。"
conclusion: DMTT是唯一在全部攻击比例下超越本地基线的方案，且拜占庭聚合权重为零，验证了理论界。
---

## 摘要
在动态移动去中心化联邦学习（DFL）中，对抗者可以同时毒化模型更新以及设备用于选择合作者的拓扑信息。我们提出了DMTT（带可信拓扑的动态MURMURA），一种基于MURMURA构建的去中心化个性化联邦学习协议，MURMURA使用证据深度学习来降低分布不匹配对等体的权重，此处将其扩展到拓扑操纵攻击下的时变图。每个设备根据链路可靠性估计、签名拓扑声明、见证者佐证以及Beta分布的源信任模型，维护一个置信度加权的本地拓扑视图，然后仅通过融合模型兼容性、拓扑信任和链路可靠性的复合分数，在经信任筛选的协作者集合上进行聚合。我们证明了筛选后的混合矩阵将拜占庭影响限制在有界残差δ_max内，该残差在完美筛选下消失，并将DMTT实现为无协调者的分布式系统，每个客户端作为独立的ZeroMQ进程运行，由共享的挂钟周期同步。在UCI HAR和PAMAP2上，每个数据集均按Dirichlet异质性划分到100个移动客户端，DMTT在所有测试的对抗者比例（10%至80%）下将诚实节点准确率保持在0.862（UCI HAR）和0.829（PAMAP2）以上，在低比例时几乎与无攻击准确率相当，在极端情况下优雅地退化为仅本地性能；静态和动态FedAvg在每个比例下均崩溃为随机猜测，而鲁棒聚合器（Krum、BALANCE、UBAR）无法持续击败仅本地基线，DMTT是唯一在两个数据集的所有比例下均超过该基线的方法，其幸存的拜占庭聚合权重在整个过程中经验上为零，与δ_max=0一致。该协议通过墨尔本研究云上的无协调者ZeroMQ后端在真实节点上端到端运行。

## Abstract
In dynamic mobile decentralized federated learning (DFL), adversaries can poison both model updates and the topology information devices use to choose collaborators. We present DMTT (Dynamic MURMURA with Trusted Topology), a decentralized personalized FL protocol built on MURMURA, which uses evidential deep learning to down-weight distribution-mismatched peers, extended here to time-varying graphs under topology-manipulation attacks. Each device maintains a confidence-weighted local topology view from link-reliability estimates, signed topology claims, witness corroboration, and a Beta-distributed source-trust model, then aggregates only over a trust-screened collaborator set using a composite score fusing model compatibility, topology trust, and link reliability. We prove the screened mixing matrices confine Byzantine influence to a bounded residual $δ_{max}$ that vanishes under perfect screening, and implement DMTT as a coordinator-free distributed system with each client running as an independent ZeroMQ process synchronized by a shared wall-clock epoch. On UCI HAR and PAMAP2, each partitioned across 100 mobile clients with Dirichlet heterogeneity, DMTT sustains honest-node accuracy above 0.862 (UCI HAR) and 0.829 (PAMAP2) across all tested adversary fractions (10 to 80%), nearly matching no-attack accuracy at low fractions and degrading gracefully toward local-only performance at extremes; static and dynamic FedAvg collapse to chance at every fraction, and robust aggregators (Krum, BALANCE, UBAR) fail to consistently beat a local-only baseline, while DMTT is the only method that clears this bar across both datasets at all fractions, with surviving Byzantine aggregation weight empirically zero throughout, consistent with $δ_{max}$=0. The protocol runs end-to-end on real nodes via a coordinator-free ZeroMQ backend on the Melbourne Research Cloud.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- 在**去中心化联邦学习（DFL）**中，没有中心服务器，设备直接与对等节点交换和聚合模型更新，因此每个设备必须自行判断“该信任哪些对等节点”。同时，真实移动场景下数据呈**非IID分布**，单一全局模型难以适配每个设备，需要个性化建模。
- 现有工作（如 MURMURA）通过证据深度学习区分“分布不匹配”和“数据固有难度”，实现了模型层面的信任评估；但在**动态拓扑**下，攻击者不仅会**毒化模型更新**，还会**伪造、隐瞒或篡改拓扑信息**（如谎报邻居关系、重放过期声明、泛洪虚假声明），影响设备“向谁学习”的决策。
- 论文的核心问题是：在**时变移动网络**中，能否设计一种协议，使设备在同时面对“模型中毒攻击”和“拓扑操纵攻击”时，仍能可靠地筛选出诚实且有价值的协作者，完成个性化联邦学习。
- 论文给出了肯定回答，提出 **DMTT（Dynamic MURMURA with Trusted Topology）**，将 MURMURA 扩展到动态拓扑场景，把“拓扑信任”纳入协作决策，并提供了收敛性分析与系统实现。

## 2. 方法论

**核心思想**：将网络信息分为三层，分别处理——直接可达性（能否通信）、拓扑信任（信息是否可信）、协作图（哪些节点值得学习）。DMTT 在 MURMURA 的模型兼容性信任基础上，引入基于**签名声明、见证佐证、Beta 源信任模型**的拓扑信任层，再用复合评分筛选协作者。

**关键技术细节**：

- **链路可靠性估计**（式 1）：对直接邻居维护指数滑动平均：  
  `p̂_{ij}^{t+1} = (1-ρ) p̂_{ij}^t + ρ·ack_{ij}^t`
- **签名拓扑声明**：当节点观察到边添加/删除时，广播带签名、版本、时间戳等信息的声明 `c`；接收方缓存为证据，不立即采信。
- **声明处理与边置信度**（式 2–5）：计算见证权重 `ψ_{ij}^t(e) = T_top^{t}_{ij} exp(-ξ h_{ij}^t(e))`（信任越高、跳数越少、签名有效、版本新鲜则权重越大），汇总支持/反对总量 `S+`、`S-`，得到边置信度 `χ_i^t(e) = S+/(S++S-+ε)`，只有满足阈值和多样性条件才接受边添加/删除。
- **源信任更新**（式 6–10）：Beta 式证据追踪，正事件（握手确认、佐证、一致性）更新 `α`，负事件（矛盾、泛洪、失配）更新 `β`；由 Beta 均值得出声誉 `R`，由 Beta 方差得到认知不确定性 `U`，最终信任分数 `T_top = R·exp(-η max{0, U-τ_U})`，对不确定性高的来源进行惩罚。
- **受控转发**：仅转发签名有效、版本够新、来源信任高于阈值、且未超出转发预算和跳数上限的声明，抑制泛洪。
- **本地训练与协作选择**：每个节点进行 E 步本地 SGD；对收到的候选模型计算 MURMURA 风格兼容性得分 `smodel`（基于本地验证准确率与认知不确定性）；最终协作分数为：  
  `
