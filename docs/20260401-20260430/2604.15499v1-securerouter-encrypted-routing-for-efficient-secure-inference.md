---
title: "SecureRouter: Encrypted Routing for Efficient Secure Inference"
title_zh: SecureRouter：用于高效安全推理的加密路由
authors: "Yukuan Zhang, Mengxin Zheng, Qian Lou"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15499v1"
tags: ["query:daily"]
score: 8.5
evidence: 使用安全多方计算的隐私保护推理系统
tldr: 针对安全多方计算（MPC）在隐私保护推理中因使用固定模型导致的高延迟和高成本问题，本文提出了SecureRouter框架。该框架通过输入自适应的加密路由机制，在保持数据和模型机密性的前提下，根据加密特征动态选择最优模型。它结合了感知MPC成本的安全路由器和经过优化的模型池，实现了推理效率与准确性的平衡，为可扩展的安全AI推理提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15499v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of (a) a current MPC framework utilizing a single, fixedmodel, with (b) our proposed framework, which introduces a Secure Router to dynamically select an appropriatemodel from anMPCModel Pool.\", \"page\": 1, \"index\": 1, \"width\": 502, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.15499v1/fig-002.webp\", \"caption\": \"Figure 2: An illustration of our proposed secure router framework, divided into an offline training phase and an online inference phase. The diagram simplifies the architecture to focus on the User and the End-to-End Privacy Inference Service Provider.\", \"page\": 3, \"index\": 2, \"width\": 504, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.15499v1/fig-003.webp\", \"caption\": \"Figure 3: The online inference protocol, illustrating the 2-Party Computation (2PC) flow between the Client and the Server (Party 0 and Party 1).\", \"page\": 3, \"index\": 3, \"width\": 503, \"height\": 352}]"
motivation: 现有的隐私保护推理系统对所有输入使用单一固定模型，忽略了不同输入对模型规模的需求差异，导致计算效率低下。
method: 提出了一种端到端加密路由框架，包含一个预测模型效用与成本的安全路由器，以及一个经过MPC协同优化的多规模模型池。
result: 实验结果显示，SecureRouter在几乎不损失准确率的情况下，将推理延迟降低了1.95倍。
conclusion: 该研究证明了通过输入自适应的加密路由可以显著提升安全推理的效率，是实现大规模隐私保护AI应用的关键一步。
---

## 摘要
加密安全神经网络推理通常依赖于安全多方计算（MPC）等安全计算技术，使云服务器能够在不解密的情况下处理客户端输入。尽管之前的隐私保护推理系统将网络优化与 MPC 进行了协同设计，但它们仍然缓慢且昂贵，限制了实际部署。一个主要的瓶颈是它们对所有加密输入都使用单一、固定的 Transformer 模型，忽略了不同的输入需要不同的模型大小来平衡效率和准确性。我们提出了 SecureRouter，这是一个端到端的加密路由和推理框架，通过在加密状态下进行输入自适应的模型选择来加速安全 Transformer 推理。SecureRouter 建立了一个统一的加密流水线，将安全路由器与 MPC 优化的模型池集成在一起，在保持数据和模型完全机密性的同时，实现了协调的路由、推理和协议执行。该框架包括训练阶段和推理阶段的组件：一个感知 MPC 成本的安全路由器，用于根据加密特征预测每个模型的效用和成本；以及一个 MPC 优化的模型池，其架构和量化方案经过协同训练，以最小化 MPC 通信和计算开销。与之前的工作相比，SecureRouter 在准确率损失微乎其微的情况下，实现了 1.95 倍的延迟降低，为可扩展且高效的安全 AI 推理提供了一条切实可行的路径。我们的开源实现可在以下网址获得：https://github.com/UCF-ML-Research/SecureRouter

## Abstract
Cryptographically secure neural network inference typically relies on secure computing techniques such as Secure Multi-Party Computation (MPC), enabling cloud servers to process client inputs without decrypting them. Although prior privacy-preserving inference systems co-design network optimizations with MPC, they remain slow and costly, limiting real-world deployment. A major bottleneck is their use of a single, fixed transformer model for all encrypted inputs, ignoring that different inputs require different model sizes to balance efficiency and accuracy. We present SecureRouter, an end-to-end encrypted routing and inference framework that accelerates secure transformer inference through input-adaptive model selection under encryption. SecureRouter establishes a unified encrypted pipeline that integrates a secure router with an MPC-optimized model pool, enabling coordinated routing, inference, and protocol execution while preserving full data and model confidentiality. The framework includes training-phase and inference-phase components: an MPC-cost-aware secure router that predicts per-model utility and cost from encrypted features, and an MPC-optimized model pool whose architectures and quantization schemes are co-trained to minimize MPC communication and computation overhead. Compared to prior work, SecureRouter achieves a latency reduction by 1.95x with negligible accuracy loss, offering a practical path toward scalable and efficient secure AI inference. Our open-source implementation is available at: https://github.com/UCF-ML-Research/SecureRouter